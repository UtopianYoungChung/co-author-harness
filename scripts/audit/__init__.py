"""Deterministic manuscript audit suite (v0.15.0-pre, PR-2).

Anything regex-expressible over manuscript prose lives here as a script and emits
a canonical findings.json. The LLM consumes findings, never re-counts.
"""
