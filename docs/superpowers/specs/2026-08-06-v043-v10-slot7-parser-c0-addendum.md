# v0.43 V10 slot-7 parser failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v10-nulltxn-stop-c0-addendum.md`,
committed alone at `2739713419804c6b6395b68792ef63d800ff6287`, tree
`71ce5755982fd9f412c7f1243e361ca5cca26640`, parent
`56017fe1e72cb273f984ca353ef5e3125e528c85`, 7,480 bytes, SHA-256
`da548106559575df2c17fa5cb21ff4ab1edc0075496601702c8835b4974d2ccc`,
with independent exact B0/M0/m0 approval.

Exact V10 assembly now exists at
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1.assembly.tmp`,
405,909 bytes, SHA-256
`fd9562174f3fced07f69e09b30c2bb3fff5e764d602ebe61f86e349d6356f076`,
4,770 LF, zero CR, terminal LF, no BOM, 63,471 tokens/zero errors at both
parsers. Final V10 is absent. C2 is 30 files, zero directories, 27,164,402
bytes. Main is clean. Retry-26 remains protected clean/detached at
`91ad31ae250dd9600ef85f330e8d4acaef36da8e`, tree
`4c0c00527b4dba2bd3edd56d6d3f58ab5a4fb358`.

Static attempt 11 is consumed. Slots 1-6 each launched in a separate strict
Windows PowerShell 5.1 process and passed. During caller-side source preparation
for slot 7, the compressed loop header `foreach($m in$mutators)` failed parsing:
without required token-separating whitespace PowerShell did not recognize the
`in` keyword. No slot-7 child, slot 8, slot 9, move, post-move action,
operational mode, cleanup, or qualification action occurred. Slots 1-6 are
partial evidence only and are not reusable. This is static orchestration
evidence-incomplete, not a V10 rejection, operational RED, qualification result,
or success.

All protected evidence and plane separation remain. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact static attempt 12

After exact B0/M0/m0 approval and an isolated commit, authorize one fresh ordered
nine-slot attempt 12 over immutable V10; no script main and no operational mode.
Every slot is a separate strict-mode Windows PowerShell 5.1 process with a
600,000-ms ceiling and fail-closed consumption. Parse the complete sources for
slots 1-8 in caller memory before launching slot 1; any parse error stops before
any slot. Slot 9 is excluded because its exact source cannot exist until slot 8
returns the observed digest.

Repeat the complete attempt-11 obligations from zero. Slot 7 changes only the
malformed header to exact ordinary source `foreach ($m in $mutators)`. Require
the old compressed header exactly once in source preparation, zero after repair,
the corrected header exactly once after repair, a zero-error full-source parse,
and exact reverse equality. Do not compress PowerShell language keywords against
variables anywhere in the nine slot sources.

Slot 7 must still report exactly fifteen retry-26 and five corrected retry-24
PASS rejections. The retry-26 cases are the nine path/byte/hash tuple leaves plus
verdict, the ordered two-entry `error_codes` vector, spawn count, preflight-read
count, target canonical SHA, and normalized-publication canonical SHA. Each
mutation must preserve type, clone pristine state, invoke
`Assert-PreservedRetry26FailureShape`, and match its expected SCRIPT, TARGET,
PUBLICATION, or VALUES rejection group. The five retry-24 cases remain target
error detail, publication two-error order swap, target `ended_utc`, one appended
ASCII space in canonical bytes, and raw target terminal-LF removal.

Slots 8 and 9 each repeat the complete protected state binding, require scalar
`[int]$state.events_seen -eq 0`, normalize only label and capture timestamp, and
compute the entire-state canonical SHA-256. Slot 9 must equal slot 8 and bind
V2-V9, base29, same volume, and final V10 absence. Slot 9 must be authored
directly as one literal here-string only after slot 8 reports its digest; prior
source extraction/replay, replacement injection, concatenated assertion source,
or generated quote boundaries remain forbidden. Parse that exact literal source
after slot 8 and before slot-9 launch; the digest appears once as a quoted data literal and its
`Assert-Capture` is ordinary source.

Only after slot 9 PASS, with no intervening command except result assertions,
publish by non-overwriting two-argument move to final V10. Repeat complete
post-move validation. Then obtain three independent exact-byte B0/M0/m0 final
V10 reviews and perform a new complete protected rebind before operational mode.
Inherited retry-26/admin removal, retry-30/31, and attempts-045/046 remain
review-gated. C2 gates remain 30 post-review, 38 pre-cleanup, and 34 final.

## Review and ledger

This exact candidate requires independent exact-byte B0/M0/m0 review and an
isolated one-file commit directly on main. Reviewers must bind the failure,
V10/protected state, exact parser repair, all nine slots, downstream sequencing,
prospective population, claim ceiling, and Gate 6 closure. Do not push or tag.
Prospective population is 772 tracked, 765 included, zero `.plugin`/`.zip`, six
`scripts/analysis/`, and one fixture manifest; its 40,290-byte
Python-Unicode-sorted UTF-8 included-path payload without terminal LF has
SHA-256 `fa1bb5f885122aa668688491a9f5515e261538e3d4cae583eb8db1ddba34d740`.

Writable paths are only this candidate/isolated commit, then the existing V10
pair through the authorized move, then review-gated inherited ledgers. Preserve
every prior artifact and every other plane. Every failure stops. Gate 6 remains
closed absent independent v0.43 consumer evidence.
