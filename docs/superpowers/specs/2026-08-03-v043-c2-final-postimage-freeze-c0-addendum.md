# v0.43 C2 registered-worktree observation final postimage C0 addendum

**Status:** approved package-maintainer final freeze of the completed
registered-worktree observation repair and authority to commit only that exact
five-file postimage before the already-authorized attempt 013/014 transactions.
This addendum grants no package clearance, shipment, cache mutation, consumer
re-attestation, host qualification, activation, research mutation, acceptance,
promotion, release, or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-03-v043-c2-registered-worktree-observation-c0-addendum.md`
and
`docs/superpowers/specs/2026-08-03-v043-c2-controller-review-bytecode-cleanup-c0-addendum.md`.
All parent prohibitions remain binding.

## Frozen repository and residue state

- Repository: `B:\Agents\platform\co-author-harness`; branch: `main` only.
- HEAD: `2a456bf89f2549c9c0faddd04928f7681cfd6b9c`.
- HEAD tree: `a9bf431f6eabff7471635ca218f93728d10e1711`.
- Exactly the five parent-authorized repair/binding paths are modified.
- The fixture manifest remains 54,250 bytes, SHA-256
  `d40f27708a307b9b5fc84994b7c3e386728f03767054de2bd1f4198621682739`.
- The primary bytecode inventory exactly equals the attempt-011 governed
  baseline: 180 files and four `__pycache__` directories. Both review-emitted
  bytecode files authorized for removal are absent. Detached-retry-2 is clean
  at `e379644562ad72372bcbb51739af0c164bf20b91` with zero bytecode files and
  zero `__pycache__` directories.
- `.git/index.lock` is absent and no matching controller smoke or qualification
  process is live.

## Exact final postimage

| Path | Bytes | SHA-256 |
|---|---:|---|
| `scripts/release_qualification_controller.py` | 105,020 | `c9aa44f7ec82f945b4e3aaedcc07cbad1a198579f1be9c58a039721b4f6d7cdc` |
| `scripts/release_qualification_controller_smoketest.py` | 158,923 | `0d582a2033b8ed80e7f5fe471909404fb540d47cc241ac77e728b3d9e96a6879` |
| `references/contract_kernel.v1.json` | 37,242 | `d3ca3dbf3b9e67445ac78ff393d3db5caf1c940a95c959f22dbe5bb866053d84` |
| `references/compatibility/shipment-v2/contract_kernel_projection.json` | 2,907 | `28026ca439d31d20f7fbb2a1f44b63dd65b1d672d523b12bd13759ba3e2fd2ee` |
| `references/compatibility/shipment-v2/compatibility_profile.json` | 4,510 | `19c5fd05d703ca49ab7f1d25106c9458d1c3d78efe7e99556bf04ab1aece632b` |

The kernel component binds the controller exactly. The projection and profile
bind kernel SHA-256
`d3ca3dbf3b9e67445ac78ff393d3db5caf1c940a95c959f22dbe5bb866053d84`.
The profile binds the canonical six-file compatibility-kit aggregate
`c71906bb509cd6b9bb8976c5141ceb302593bbfe73303aefdd9f9c8dc92f3162`.

## Final proof and preserved red

- Windows controller smoke: PASS, 46 behavioral cases and nine declared
  POSIX-only skips.
- WSL/POSIX controller smoke on the exact final controller/smoke postimage:
  PASS, 51 behavioral cases and zero skips.
- Schema-runtime, version, distribution-rights, catalog, command-surface,
  path-hygiene, snippet, output-economy, version-planes,
  commitment-interactions, phase-engagement, retirement-sweep,
  destination-coverage, focused fixture-infrastructure, subprocess-text,
  kernel-coherence, contract-kernel, and all shell-syntax checks passed with
  explicit bytecode suppression.
- During an earlier intermediate postimage, one POSIX run produced a missing
  `lost.counter` assertion and failed. It was not converted into a pass. It did
  not recur in two consecutive reruns of that intermediate postimage or the
  final-postimage run, and no matching process residue remained.

The final regression proves exact registered-root/common-directory equality,
`DEST-PROTECTED`-only observation recovery, strict allowed-output authority,
raw reparse refusal before resolution, candidate-level ignored-file authority,
the exact shared-lock-only protected ignore exception, authority validation at
the final pre-spawn point, watch-root and ignored-file validation on both sides
of postimage inventory, typed inventory/topology races, and mutation detection
in either watched root.

No further tracked edit is authorized before the five-file postimage above is
committed. Any changed byte, new finding, warning, residue, or failed postflight
is a stop and requires another committed addendum. After commit, only the
parent-authorized attempt-013 path may be created; attempt 014 remains closed
until the exact successful attempt-013 manifest is committed.
