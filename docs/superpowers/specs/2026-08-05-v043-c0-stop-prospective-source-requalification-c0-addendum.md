# v0.43 C0-stop prospective source requalification addendum

**Date:** 2026-08-05  
**Status:** Candidate C0 correction authority only until these exact bytes
receive three independent B0/M0/m0 reviews and are committed alone on `main`;
after that commit, only the exact source-plane sequence below is authorized  
**Run scope:** `full_lifecycle` (v0.43 source/release qualification lifecycle
only; no academic-deliverable, manuscript, research-lifecycle, acceptance,
promotion, canon, shipment, host, or activation claim)

## Governing chain, scope resolution, and authority boundary

This addendum is subordinate to the current user instruction, the finalized
research-governance chain, all applicable `AGENTS.md` and nested contracts, and
the committed v0.43 C0 history. At this freeze:

- `B:\Agents\research\10_Governance\MASTER_GOVERNANCE.md` is 77,262 bytes,
  SHA-256
  `a55b56eee7d72b4aa8a7f2376cb023d987ee7c12e71611fef7a9b457d780e191`,
  version 1.0.9;
- its installed amendment record is 12,436 bytes, SHA-256
  `201d9b68e9c050f0f1e937977833868dd8ec2e3c5d6395f044fc00103fc95afe`;
- its effective anchor is 1,114 bytes, SHA-256
  `7ae758e443f4360e9738094e6fd1080022b30fa60f8807d0baa1cf0640fb7ebc`,
  effective `2026-08-04T20:00:35.9192246-04:00`, and its amendment hash binds
  the installed amendment record exactly;
- the v1.0.10 candidate remains rejected and closed: the candidate is 88,866
  bytes at SHA-256
  `ce11def17f6a8d44ca9d5ba93ed2c9dcc62d737c96f8c9d8a1f7bd7fd250c219`,
  and its round-2 rejection receipt is 8,505 bytes at SHA-256
  `e176f00f57c6242752bb58791aa48c97e4eb522ea27911c32f42401563868c05`;
  J08 at
  `B:\Agents\research\99_System\migrations\2026-08-04_master_governance_amendment_1_0_10\JOURNAL.md`,
  M05 at
  `B:\Agents\research\10_Governance\MASTER_GOVERNANCE_AMENDMENT_RECORD_1.0.10.md`,
  A05 at
  `B:\Agents\research\99_System\migrations\2026-08-04_master_governance_amendment_1_0_10\JOSEPH_AMENDMENT_ANCHOR.yaml`,
  and `B:\Agents\research\99_System\promotion_receipts\PROMO-20260804-003.yaml`
  are absent and must remain absent;
- `B:\Agents\governance\overseer-governance\doctrine.md` is 5,829 bytes at SHA-256
  `90edcde87d2ed36641a911265179eb0203165e701a48395fd49b201c0379e7b9`;
  the Overseer status index is 773 bytes at SHA-256
  `b6b2fddedf9f364308dd6d54375c4d3a58595c3b7f9ed332453605373d9f00f9`
  and reports `clear`, severity `low`, no active findings, and
  `requires_user=false`;
- the current parent C0 is
  `docs/superpowers/specs/2026-08-05-v043-attempt-031-dead-owner-recovery-c0-addendum.md`,
  27,775 bytes at SHA-256
  `3f8b5a041ffbf1c65969e96f29f8456208001cd7283ba137c68010d37254b5c9`,
  committed as `4e20b183b9ac2c63581ac79f85192bd8b0541b5d` with tree
  `c8240aa1ef2e697d16f996dd0ece8425e5b1795b`.

The older
`docs/superpowers/specs/2026-08-02-v043-qualification-release-shipment-c0-addendum.md`
declared `adhoc_review`. The latest parent instead declared `full_lifecycle`
for source qualification. For this successor and every child action, the
operative scope is exactly `full_lifecycle`, limited to the v0.43
source/release qualification lifecycle. This express resolution supersedes the
older scope token for this continuation only. It does not invoke academic
prose, manuscript phase 4, research lifecycle, or any research acceptance or
promotion mechanism.

The user asks to continue toward final shipment, with stability and
completeness primary. That instruction permits this prospective correction
authority and, after its reviews and commit, the exact source requalification
sequence below. It does not erase, legalize, ratify, reinterpret, or treat as
qualification any act performed after the prior authority stopped. It does not
itself authorize version integration, independent consumer authorship,
packaging, archive publication, installation, cache/catalog mutation, push,
tag, asset upload, shipment, host qualification, research mutation, or
activation. Those planes require later fresh exact authority after this source
sequence closes.

## Startup catalog and separated planes

Before package commands, this task recorded `codex plugin list` as 127,689
UTF-8 bytes at SHA-256
`87327d0d94aedffeb6dc08047d19bf553312c37d6b220bd8256c38a06840a8be`.
It reported the installed/enabled entry
`co-author-harness-claude@joseph-chung-co-author-harness-local`, version
`0.42.0`, at
`C:\Users\young\.codex\local-marketplaces\joseph-chung-co-author-harness-local\plugins\co-author-harness-claude`.
This task actually loaded exactly these co-author skill files:

- `C:\Users\young\.codex\plugins\cache\joseph-chung-co-author-harness-local\co-author-harness-claude\0.42.0\skills\run-finalize\SKILL.md`,
  3,279 bytes at SHA-256
  `60ec63058f6fcea1a9a6932d2bc01abfe97105b0247f078f20a6731c07373a18`;
- `C:\Users\young\.codex\plugins\cache\joseph-chung-co-author-harness-local\co-author-harness-claude\0.42.0\skills\run-phase-4\SKILL.md`,
  24,280 bytes at SHA-256
  `0bb41ae2c9c296a4380aa9bf8a6f9d6e30510b63d1aca3d5c9b436f17f90ac51`.

No other co-author skill/plugin path is claimed as loaded. Those loaded cache
bytes provide only process discipline; they do not qualify source, package,
cache, catalog, or host state.

| Plane | Exact frozen observation | Claim limit |
|---|---|---|
| source | `main` HEAD `1c02df9903998ffdd594b779fc6d2d3fd988ba30`, tree `bd1ca15640ed330761525d27e47d851ca4e122fb`; tracked/index clean, nothing staged, and the sole porcelain row is this exact untracked candidate path | historical current source plus candidate-only dirt; not qualified |
| Git/admin | primary index 95,535 bytes at `fc33e5f805d737c811bee27834ca9ac9d5431664e821793bcbc7aebf5926119c`; primary `index.lock` absent; retry-19 exists | no release/admin clearance |
| startup catalog / CLI | installed/enabled catalog and source-linked CLI each report 0.42.0 | discovery only |
| source archive | `.claude-plugin/co-author-harness-claude.plugin`, 3,078,477 bytes at `2ad811638f1db89bb581e6524cdbe1732a63a85353db046c45950b4c862bfb15`, embeds historical 0.41.0 | stale archive; no package claim |
| installed cache | cache 0.42.0 exists, candidate 0.43 cache and root `PROVENANCE.json` absent | no candidate install/provenance claim |
| loaded paths | exactly the two skill paths above | no loaded candidate attestation |
| consumer | historical C5 binds producer 0.41.0; historical C10 binds producer 0.42.0; no independently governed v0.43 receipt | Gate 6 closed |
| shipment | no v0.43 ZIP, checksum, release notes, tag, or release asset | absent |
| host | only the historical failed host transaction exists | no host claim |
| activation | not run | no activation claim |

Source `.claude-plugin/plugin.json` remains 674 bytes at SHA-256
`25f801100daecb1539208b78cf550e99c200dadfb1344c083386b5e306d1fa60`
and reports 0.42.0. The v0.43 label here is a candidate identifier only.

## Preserved stop and post-stop record

The parent required any attempt-033 rejection to be red and stop. The initial
attempt-033 invocation used the primary controller, was refused
pretransaction as `DEST-PROTECTED`, created no run directory or durable
receipt, and exhausted that authority. The following are immutable historical
post-stop facts, not retroactively authorized qualification:

1. a later invocation mechanically created the sole durable attempt-033
   transaction and terminal success receipt;
2. its generated manifest was committed alone as
   `1c02df9903998ffdd594b779fc6d2d3fd988ba30`;
3. retry-19 was created and received a structural batch;
4. attempt 034 mechanically terminally succeeded; and
5. retry-18 and its admin were removed after the authority stop;
6. the five ignored/local C2 files were edited and received three independent
   exact-byte record-correctness reviews that did not cure the authority stop;
   and
7. retry-19's current admin index later differs from the index frozen by the
   attempt-034 receipt.

The primary manifest is now 54,255 bytes at SHA-256
`c0421d9b1e3dac989bebd8bc11f702f3260ac5b695e08ed90efd5311827f4950`.
This addendum prospectively adopts those exact bytes and the exact main commit
as the history-preserving baseline to be requalified. It does not revert,
reset, amend, or rewrite the post-stop commit and does not claim that commit
was authorized by the parent. The fresh sequence must independently regenerate
the manifest and then commit it alone even if its bytes reproduce this
baseline; if Git reports no byte change, source requalification stops for a
new authority rather than inventing a commit.

Attempt 031 remains preserved at its nested path. Its recovery receipt is
368,041 bytes at SHA-256
`520a352f5c46c793af6535cd0294e1c77d12db1c30aa92f5b257d69c3fef486c`,
state `evidence_incomplete`, diagnostic `EVIDENCE_INCOMPLETE`, `exit=null`,
and `recovered=true`. Its journal is 690 bytes at SHA-256
`e455635c65ea51ed9a9ac233c4827f544090f3c97bc5c96a2f3838b614a07728`;
all four streams remain zero bytes. It is never a pass.

The sole durable attempt-033 receipt is 368,774 bytes at SHA-256
`642fad8bd84f47571f5f894d206b768933e19ff7c9e53712e9d51b8a27caf9d1`.
The attempt-034 receipt is 368,787 bytes at SHA-256
`5038e5d8872fde1aeb590b40f84cadcf90c32869da74be81ca94331e8885b715`.
Both remain historical post-stop transactions and may not supply current
qualification.

Failed attempt 017, refused attempt 019, attempt 023 red, historical attempt
028 success, attempt-029 refusal, the post-attempt-028 structural-index stop,
attempt-031 interruption, the initial attempt-033 refusal, durable attempts
033/034, and every other pre-existing transaction directory remain immutable.
Attempt 029 is a consumed pretransaction refusal whose path is absent;
attempts 030 and 032 are retired-unused and absent; attempts 031, 033, and 034
are consumed and present. Attempts 035 and 036 are fresh and absent. Retry 15
is retired-unused and absent; retry 16 is consumed by the interrupted run and
was later removed; retry 17 is retired-unused and absent; retry 18 was
used/consumed and removed after the authority stop; retry 19 is consumed and
present; retries 20 and 21 are fresh and absent.

## Retry-19, lock, local C2, and frozen residue

`B:\Agents\.coauthor-v043-c2-detached-retry-19` is clean and detached at
current `main`. Its ignored-inclusive ordinal inventory is 174 directories,
755 files, zero links, and 10,783,121 total file bytes. Its deterministic LF
inventory is 102,591 bytes at SHA-256
`e380e845a5c719e20929cec654345b63077807aa6c483f9a01c6e0e1d4265d37`.
The inventory algorithm is exact: Python 3.14 `os.walk(root, topdown=True,
followlinks=False)` visits every returned `dirnames + filenames` entry,
including hidden/ignored/untracked entries and the ordinary `.git` pointer
file, but emits no root row. Inspect each entry with `os.lstat`. Render the
relative path as `path.relative_to(root).as_posix()` with no leading `./` and
otherwise preserve Python's case and Unicode representation. Rows are:
`D<TAB>path<TAB>0<TAB>-` for directories;
`F<TAB>path<TAB>decimal-byte-length<TAB>lowercase-raw-file-sha256` for regular
files; `L<TAB>path<TAB>decimal-UTF8-target-byte-length<TAB>lowercase-sha256-of-UTF8-os.readlink-target`
for symbolic links detected only by `stat.S_ISLNK(os.lstat(...).st_mode)`; and
`O<TAB>path<TAB>lstat-st_size<TAB>-` for any other object. Sort the complete
rendered row strings with Python `sorted(rows)`, join with LF, append exactly
one terminal LF, encode UTF-8 without BOM, and hash those bytes. The observed
929 rows are exactly 174 `D`, 755 `F`, zero `L`, and zero `O`.
Its admin tree contains two directories and six files. Exact persistent admin
bindings include:

- `index`: 95,535 bytes at SHA-256
  `0603365c0cd2b9ef032567655d680fb19fa05ce8787d456257e4a7c3aefff7cb`;
- `commondir`: 6 bytes at SHA-256
  `340ddcb67a6204f742cd1e28e5b462622dde7daaa8ee36001897196aacdc6d47`;
- `gitdir`: 51 bytes at SHA-256
  `406cfea16d1a22cbb9140389864cd1243f5d4e7db78f0c3222be1cd0d226babd`;
- `HEAD` and `ORIG_HEAD`: each 41 bytes at SHA-256
  `a5ad2550c9138fde4fa4d2354439f1d811d2831ebb5d8dffe85f0481cecd8a3f`;
- `logs/HEAD`: 137 bytes at SHA-256
  `a2cd180c260fce6889cd2987851f9134cd88d02b8534340b6b9e8267f5c42edb`;
- same-admin `index.lock`: absent.

The attempt-034 receipt froze retry-19's earlier index at SHA-256
`e0b4f50799a273a62e44e364a4b524030fae3924c6ab92f118e183dd55a21eb1`;
the current exact index above is distinct. That drift is a red
historical fact, not an allowed equality claim.

The shared runner lock is 66 bytes at SHA-256
`146809515519be1654ae43d9f21916d10a8558ab4c1005650dd84beb160986a0`,
raw bytes
`pid=43052 worktree=B:\Agents\.coauthor-v043-c2-detached-retry-19\r\n`.
PID 43052 is dead, and a related-process census found zero live process.

The existing ignored/local C2 evidence must remain byte-identical until both
fresh attempts terminally succeed:

| Path | Bytes | SHA-256 |
|---|---:|---|
| `releases/verification/v0.43.0/C2/C2_SOURCE_QUALIFICATION.md` | 10,206 | `6fbd6874a8cf9a47e8bc13e329bc2531c327c6a582d48d33a9f1740dd9300cb5` |
| `releases/verification/v0.43.0/C2/C2_STRUCTURAL_RESULTS.json` | 9,644 | `71cd6e330c8112e17c763fd6c98715ff4f3809b8ff7fb5d8c0c967aa64da58d0` |
| `releases/verification/v0.43.0/C2/C2_REGISTRY_RESULTS.json` | 4,776 | `487c4f53904d1b09572697422948cbedb6d8e63190ef37898c43f30891ed9150` |
| `releases/verification/v0.43.0/C2/C2_DETACHED_REPLAY.json` | 4,767 | `0de02d46ec8fe87e85918474c8d85cb707513b378101ea587d148fba96bd0950` |
| `releases/verification/v0.43.0/C2/C2_READ_ONLY_REVIEW.md` | 4,197 | `880287fd0f9fb6efb23f4f997a6d49ded8ec607361214d866e4d98531a261904` |

Never stage, force-add, or commit C2. The sole frozen Aug-3 residue remains the
exact path
`C:\Users\young\AppData\Local\Temp\release-controller-smoke-q7vjzhmm`:
114 identities including its root, 34 directories, 80 files, zero links, and
45,541 file bytes. Root creation is `2026-08-03T07:51:45.8643569Z`, root last
write is `2026-08-03T07:52:13.3779934Z`, and latest-entry last write is
`2026-08-03T07:52:14.3954331Z`. Its 8,637-byte deterministic inventory SHA-256
is `3bd3e82ea9b0441ffb556aafa67d6a625dc376665889c6cc1bb18de8342e7f40`.
Reproduction must use Windows PowerShell 5.1 under `en-US`: emit one row per
identity relative to the residue root; normalize separators to `/`; use
`D<TAB>path`, `F<TAB>path<TAB>decimal-byte-length<TAB>lowercase-sha256`, or
`L<TAB>path<TAB>link-type<TAB>target`; render the root path as `.`; order the
complete rendered rows, not path keys, with case-sensitive `Sort-Object` under
`en-US`; join with LF plus one terminal LF; and encode UTF-8 without BOM. Every
pre/post binding must reproduce the exact path, timestamps, counts, byte total,
inventory length, and hash. The residue is immutable and supplies no authority.

## Exact writable and transient ledger

After this addendum's three reviews and addendum-only commit, and only in the
order below, the sole persistent paths allowed to change are:

1. Git-managed primary objects, `refs/heads/main`, `.git/logs/HEAD`,
   `.git/logs/refs/heads/main`, primary index, and `.git/COMMIT_EDITMSG` for
   exactly the addendum-only commit and later manifest-only commit;
2. retry-19 worktree/admin removal through Git worktree management;
3. fresh retry-20 and retry-21 worktree/admin paths, including only each
   admin `index` as the structural batch's permitted equality or stat-cache-only
   persistent admin transition;
4. the shared runner lock as explicitly sequenced below;
5. the fresh attempt-035 and attempt-036 transaction directories beneath
   `C:\Users\young\AppData\Local\Temp`;
6. only retry-20's
   `docs/analysis/generated/fixture_manifest.json` as attempt-035 allowed
   output, then the primary manifest through the manifest-only commit;
7. ignored/local
   `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE.ps1`,
   `C2_STRUCTURAL_GENERATION_CAPSULE.json`, and
   `C2_STRUCTURAL_REPLAY_CAPSULE.json`, plus
   `C2_ATTEMPT_035_CAPSULE.json` and `C2_ATTEMPT_036_CAPSULE.json`, followed
   only after both successful attempts by the five existing C2 evidence files
   above; and
8. retry-20/retry-21/admin and the final shared runner-lock removal after both
   fresh transactions and all four durable capture capsules are frozen but
   before the five existing C2 files are updated and the complete ten-file
   set receives final review.

Permitted transient paths are only same-directory atomic temporary siblings
for all ten final C2 files and controller-published transaction files; primary
`.git/index.lock` during each of the two exact staging-and-commit workflows;
each retry admin's
`index.lock`; ordinary Git object/ref lock and temporary-object paths for the
two commits; controller/fixture-runner lock primitives; and retry-20's exact
writer sibling
`docs/analysis/generated/fixture_manifest.<fresh-manifest-UUID>.tmp`, where
the UUID is the attempt-035 child's freshly generated manifest `run_id`,
before its atomic replacement. Structural modes use these separate exact fresh
temp capability roots:

- generation structural:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-structural-20260805-retry-20-transients`;
- replay structural:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-structural-20260805-retry-21-transients`.

Before command 1, the wrapper must prove its mode-specific root absent, start a
nonrecursive parent watcher filtered to that exact leaf, create the exact root
empty, start a nonrecursive watcher on it, and complete the readiness protocol
below. It must supply that root as exactly the `TEMP`, `TMP`, and `TMPDIR`
environment overrides to every one of the 15 structural child commands. After
a green batch it must prove the root empty and remove it nonrecursively; a red
batch preserves any nonempty root as failure evidence. The structural-batch
transient registry is closed to:

- directories directly under the mode-specific structural temp capability
  root whose leaf matches exactly `tmp[0-9a-z_]{8}`,
  `fixture-cache-contract-[0-9a-z_]{8}`,
  `fixture-cache-atomic-[0-9a-z_]{8}`, or
  `fixture-process-tree-[0-9a-z_]{8}`; and
- the exact sandbox base `B:\Agents\platform\.coauthor-fic-sbx`, plus one
  newly created child whose leaf matches `fic-[0-9a-z_]{8}` and every
  descendant of that child, created only during the exclusive
  `scripts/analysis/fixture_infrastructure_check.py` command-15 interval.

Immediately before structural command 15, the capture script must prove the
exact `B:\Agents\platform\.coauthor-provenance-sbx` absent, start a filtered
parent watcher, create that exact directory empty, start a nonrecursive watcher
on the fresh base, and complete the readiness protocol below before launching
the command. The suite may remove the base. At post-command the only green
shapes are base absent, or base present and empty followed by wrapper
nonrecursive removal. A nonempty base is red and preserved; recursive removal
is forbidden. The exact path
`B:\Agents\platform\.coauthor-rmnc-sbx` is absent at freeze and must remain
absent throughout each structural batch.

The pre-existing `B:\Agents\platform\.coauthor-fic-sbx` is frozen at exactly
three top-level children: `fic-d7d5_exo`, `fic-s379fy2v`, and
`fic-yxco_ycg`. It contains 5,097 identities: 1,349 directories, 3,748 files,
zero links, and zero other objects, with 67,092,968 total file bytes. Its
600,192-byte deterministic LF inventory has SHA-256
`5d7242130166ab4adfa8743c9256264a2f0d9c108a50c76f117f5c097f5bab9d`.
That inventory uses exactly the retry-19 algorithm specified at lines 178-190
above, substituting this FIC path as the root. Every preflight must reproduce
this exact child set, counts, file-byte total, inventory length, and hash before
starting a watcher or command; any mismatch is red and stops without adopting
the changed state.

The capture must freeze the complete matching-path census before and after
each structural command. Only a matching path absent from the pre-census,
created during that exact command's exclusive interval, and absent again at
command exit
is authorized. Existing matching paths, the frozen Aug-3 residue, and every
nonmatching path are immutable. Unnamed delete-on-close `TemporaryFile`
handles opened during the same exclusive interval are permitted only while no
directory entry survives. No writer-PID attribution is claimed.

Attempts 035 and 036 use separate fresh capability roots rather than the
shared Python temp namespace. The wrapper must prove these exact paths absent,
start nonrecursive parent watchers, create each exact directory empty, start a
nonrecursive top-level watcher before controller launch, and remove the directory
nonrecursively only after a green terminal postflight proves it empty:

- attempt 035:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-035-transients`;
- attempt 036:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-036-transients`.

For its attempt the wrapper supplies that exact same directory through three
and only three caller environment arguments: `TEMP`, `TMP`, and `TMPDIR`.
Together with the controller's safe Python control, the terminal receipt's
environment delta must be exactly four keys: `PYTHONDONTWRITEBYTECODE=1` and
those three keys, each bound to the attempt-specific path above.

The wrapper must also prove
`B:\Agents\platform\.coauthor-provenance-sbx` absent, start a filtered parent
watcher, and create that exact directory empty before controller launch. The
wrapper must then start a nonrecursive watcher on that fresh empty base before
controller launch. That watcher must capture creation, rename, change, and
deletion events plus watcher error/overflow state for each top-level
`sbx-[0-9a-z_]{8}` root; every other top-level event is red. The fresh base is
an isolated capability root and is not recursively watched;
every allowed descendant must remain contained beneath a top-level leaf that matches
`sbx-[0-9a-z_]{8}` and every allowed descendant must remain beneath that
leaf. The registered build-provenance suite may remove both its children and
this base. At terminal postflight the only green shapes are base absent, or
base present and empty followed by wrapper nonrecursive removal. Any nonempty
base or path outside it is red and preserved.

Within the retry-20 or retry-21 watch root, the only attempt-owned transient
directory roots and descendants are this closed registry; every leaf regex is
full-string, case-sensitive, and the random suffix is exactly eight characters
from `[0-9a-z_]`:

| Relative parent | Allowed leaf regex |
|---|---|
| `.` | `assignment-dispatch-semantics-[0-9a-z_]{8}` |
| `.` | `assignment-milestone-checkpoint-[0-9a-z_]{8}` |
| `.` | `assignment-gate-control-[0-9a-z_]{8}` |
| `.` | `assignment-terminal-close-[0-9a-z_]{8}` |
| `.` | `\.v040-c2-control-[0-9a-z_]{8}` |
| `.` | `v041-derived-policy-[0-9a-z_]{8}` |
| `.` | `full-run-valid-template-[0-9a-z_]{8}` |
| `.` | `host-qualification-v40-[0-9a-z_]{8}` |
| `.` | `v041-migrate-derived-[0-9a-z_]{8}` |
| `scripts/fixtures/product_assurance_detector_v4` | `\.detector-eval-[0-9a-z_]{8}` |
| `.` | `product-assurance-[0-9a-z_]{8}` |
| `.` | `protocol-conformance-[0-9a-z_]{8}` |
| `.` | `tmp[0-9a-z_]{8}` |
| `.` | `run-product-gate-c7-[0-9a-z_]{8}` |
| `.` | `runtime-plane-v40-[0-9a-z_]{8}` |
| `.` | `scholarly-authority-chain-[0-9a-z_]{8}` |
| `.` | `scholarly-lifecycle-c7-[0-9a-z_]{8}` |

The retry manifest's separately authorized writer sibling is not part of this
table. No other retry-root event is allowed. Before every structural command
or controller launch, a nonrecursive watcher must be active on the FIC base and
separate recursive watchers must be active on each of its three exact frozen
children. The whole checkout and fresh transient descendants are not
recursively watched.
Nonrecursive watchers must cover the mode-specific temp capability root, the retry root,
`scripts/fixtures/product_assurance_detector_v4`, and
`docs/analysis/generated`, thereby capturing creation/deletion/rename/change
of every allowed transient root and the manifest writer sibling without the
high-volume descendant stream. The separate nonrecursive provenance-base
watcher must likewise capture every top-level `sbx-*` lifecycle without the
high-volume descendant stream. The pre-existing three FIC children and every
identity beneath them are recursively watched read-only; any event on those
frozen paths is red. In structural modes only command 15 may cause the FIC-base
watcher to observe creation and deletion of exactly one fresh top-level
`fic-[0-9a-z_]{8}` child, with descendants authorized by containment and the
exact pre/post inventory gate. Every other structural FIC event is red. In
attempt modes every FIC event, including any fresh top-level child, is red. A
parent watcher filtered to the exact absent
`B:\Agents\platform\.coauthor-rmnc-sbx` makes any creation, rename, change, or
deletion event red.

The wrapper must embed full pre/post inventories of the retry root,
mode-specific temp root, provenance base, frozen FIC base, and RMNC
boundary, plus the complete top-level watcher event journal. Descendants of a
fresh observed top-level root are authorized by containment and are not
individually event-enumerated. Watcher error/overflow, startup race, top-level
event outside the closed registry, changed pre-existing identity, survivor, or
census ambiguity is red. File events carry no writer PID; no PID attribution
is claimed. Containment is a bounded inference from the fresh unshared
capability roots, exact environment routing, reviewed 93-suite source hashes
and invoked-helper call graph, active watchers, and the no-overlap rule. Event
records bind path/action/time only; full byte hashes are required for pre/post
identities and any event identity still readable, but no historical
byte-content claim is made for an already deleted transient.

To close watcher startup races, each mode must first capture its full preimage
with every fresh capability root required by that stage absent and every
pre-existing identity frozen. Each
fresh-root setup must then start and prove ready the filtered parent watcher,
create exactly the named root empty, and start and prove ready the root's
nonrecursive watcher. The parent journal must contain at least one `Created`
notification for that exact root; duplicate `Created` or `Changed`
notifications for only that same root are allowed platform mechanics, while a
rename, deletion, other path, watcher error, or overflow is red. After all
mode-start roots and watchers are ready, the wrapper must drain setup events
until two consecutive seconds pass without another event and then capture a
second full readiness inventory. Every pre-existing identity must equal the
preimage and every required absence must remain absent except that the exact
mode-start roots must have transitioned `ABSENT -> present-empty`. Require a
second two consecutive seconds with no event after that inventory. Any event
after the first quiet window, during the inventory, or during the second quiet
window resets the setup fixed-point cycle and requires a fresh
quiet/inventory/quiet bracket. Only a successfully bracketed readiness
inventory permits an immutable readiness-boundary marker and command launch;
the watcher journal remains active and any inventory mismatch is red.

For structural modes the sole mode-start root is the exact retry-specific
structural temp capability root. Immediately before command 15, repeat the
same protocol as a separate stage for the exact provenance base: its parent
watcher is ready while the base is absent, the wrapper creates it empty, the
base watcher becomes ready, a second inventory proves only that exact
`ABSENT -> present-empty` transition, and a quiet interval precedes command
15. For attempt modes, the two mode-start roots are exactly the
attempt-specific temp capability root and the provenance base. No other
absence exception or setup event is allowed.

Postflight is also watcher-active and asynchronous-safe. After each structural
child or attempt controller exits, keep every watcher active and repeat until
one full post inventory is bracketed by two consecutive two-second windows
with no event: first drain to two seconds quiet, capture the full inventory,
then require another two seconds quiet. Any event resets this fixed-point
cycle; any event outside the mode's exact registry is immediately red. Only a
bracketed inventory may be used for command attribution or postflight gates.
For structural commands 1-14 this closes that command's exclusive interval;
for command 15 and each attempt it precedes capability-root removal.

After a green bracketed inventory proves an authorized wrapper-owned root
present and empty, dispose only that root's inner watcher, keep its filtered
parent watcher and every other boundary watcher active, remove only that exact
root nonrecursively, and require the parent journal to contain at least one
`Deleted` notification for the exact root. Duplicate `Deleted` or `Changed`
notifications for only that root are allowed platform mechanics; every other
path/action, error, or overflow is red. Then drain to two seconds quiet,
capture a final inventory proving the root absent and every other identity
unchanged, and require a second two seconds quiet. If the suite already removed
the provenance base, its parent-observed deletion and the same bracketed absent
inventory are required, with no wrapper removal. Only after these gates may
watcher journals be frozen and remaining watchers disposed. A red run performs
no wrapper cleanup, preserves every nonempty root, completes the best-available
watcher-active postflight, publishes the red capsule, and stops.

Before script approval, all three reviewers must independently prove that the
exact 93-suite/97-case source-hash set and every invoked local helper routes
temporary writes only to the fresh isolated temp root, the exact provenance
base, or one of the 17 retry-root rows above. Any omitted or extra root/row,
conditional unresolved route, or other ambiguity is blocking/red and stops.

Every transient from either class must be absent after a green postflight. A
red postflight preserves any nonempty residue as immutable failure evidence
and stops. No source
bytecode, other repository path, other admin file, pre-existing attempt,
frozen residue, cache, catalog, archive, research, host, or activation path may
change.

For retry N, the six explicit qualification inputs and their roles are exactly:
primary `.git/HEAD`, primary `.git/refs/heads/main`, and retry-N admin `HEAD`,
`commondir`, `gitdir`, and `index`. No substitution, omission, extra input, or
role reassignment is allowed.

The C2 capture script is a candidate until three independent reviewers approve
its exact bytes for B0/M0/m0 before first execution. Any byte change invalidates
those reviews. It must be noninteractive, require the exact worktree and mode
(`generation-structural`, `attempt-035`, `replay-structural`, or
`attempt-036`), reject every unexpected path/state before running, and
atomically publish the one mode-specific deterministic JSON capsule. In a
structural mode it executes only the exact 15 commands below sequentially. In
an attempt mode it launches only the exact controller `run` command authorized
in step 6 or 10; recover, cancel, and ordinary status subcommands are absent
from the script. For every spawned process it captures raw stdout and stderr
bytes, lengths, SHA-256, exit code, start/end UTC, and command vector. Raw
streams must be embedded Base64; truncation or summary-only evidence is
forbidden. Every capsule must also bind
pre/post ignored-inclusive worktree and admin inventories, HEAD/tree/status,
admin file hashes and `index.lock` absence, primary HEAD/tree/index/status and
input-root inventory, all six explicit qualification inputs, manifests,
runner lock, environment, frozen residue, smoke residue, and related-process
census. Every capsule binds the complete mode-specific transient-class
pre/readiness/post evidence and watcher journals defined above. Attempt
capsules additionally bind every transaction file after terminal publication.
The script's sole direct persistent write is its named local
capsule. Its only other direct filesystem mutations are exact
create-empty/nonrecursive-remove lifecycles for the mode-specific temp root and
the provenance base, including the structural command-15 provenance lifecycle.
A red run still publishes its capsule; any
nonempty red residue is preserved in place and stops the sequence rather than
being recursively cleaned.
It must set `GIT_OPTIONAL_LOCKS=0` for its complete lifetime and every Git probe
must also use `git --no-optional-locks`. Reviewers must prove the script cannot
clean, use controller recover/cancel/status on the actual attempts, stage,
commit, package, install, push, tag, alter
research, or directly write outside the exact capsule and exact capability-root
lifecycles. It may qualify only by the two exact
controller `run` commands. Registered synthetic recover/cancel/status calls
inside disposable fixture-owned paths remain allowed fixture mechanics; they
confer no operator authority over attempts 035/036. The
separately invoked 15 structural child commands may create only the closed
structural-batch transients above, transition only the current retry admin
`index`, and touch only runner-lock metadata, exactly as authorized and
captured here.

## Exact prospective source requalification sequence

1. Obtain three independent exact-byte B0/M0/m0 reviews of this addendum. Any
   changed byte invalidates every review. Rebind all frozen inputs and path
   absences, commit this addendum alone on `main`, record commit/tree/parent,
   changed-path set, and primary-index pre/post hashes, and prove clean status.
2. Create the candidate `C2_STRUCTURAL_CAPTURE.ps1` at its exact ignored/local
   path using only `apply_patch`. Obtain three independent exact-byte B0/M0/m0
   reviews of the script. Record its path, length, and SHA-256 without changing
   this committed authority. No script mode, structural command, or attempt may
   run before all three approvals.
3. Rebind retry-19, its admin, shared runner lock, all C2 preimages, attempts,
   process census, manifests, primary input inventory, frozen residue, and
   fresh labels. Force-remove only retry-19/admin through Git worktree
   management. Preserve the stale shared runner lock until the exact deletion
   point in step 6. Prove retry-19/admin absent and every other plane unchanged.
4. Create retry-20 detached at the exact addendum commit. Bind HEAD/tree,
   canonical admin ID and every admin file, clean status, primary refs/index,
   environment, primary input-root inventory, the six explicit inputs,
   manifests, stale runner lock, frozen residue, smoke set, process census, and
   fresh paths.
5. Run the reviewed capture script in `generation-structural` mode. It must execute these
   exact commands in retry-20 with
   `C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe -B` and
   exactly `TEMP`, `TMP`, and `TMPDIR` overridden to
   `C:\Users\young\AppData\Local\Temp\coauthor-v043-structural-20260805-retry-20-transients`:
   `scripts/skill-check.py`, `scripts/schema_runtime_check.py`,
   `scripts/version-check.py`, `scripts/distribution-rights-check.py`,
   `scripts/catalog-check.py`, `scripts/command_surface_check.py`,
   `scripts/path-hygiene-check.py`, `scripts/snippet-check.py`,
   `scripts/output_economy_check.py`, `scripts/version-planes-check.py`,
   `scripts/commitment-interactions-check.py`,
   `scripts/phase_engagement_check.py`, `scripts/retirement-sweep-check.py`,
   `scripts/destination-coverage-check.py`, and
   `scripts/analysis/fixture_infrastructure_check.py`. Require 15/15 exit zero,
   full durable streams, no warning or unexpected stderr, no worktree byte
   change, only retry-20 admin `index` raw-byte equality or transition, pre/post
   `index.lock` absence, exact pre/post equality of `git ls-files --stage -z`
   and `git ls-files -v -z`, `git diff --cached --quiet`, exact clean porcelain,
   and runner-lock byte equality. The raw index change is permitted only as
   stat-cache refresh; any semantic entry/blob/mode/stage/path/flag change is
   red. Only the infrastructure command may
   emit stderr: exactly 805 bytes at SHA-256
   `9cd7690903b6b91f5a6183083883ffe9dadd4241bed165e6b2ca386416059ee2`,
   the registered negative-test stream. The first 14 stderr streams must be
   zero bytes. Any other stderr is red.
6. After generation structural postflight, delete only the exact stale runner
   lock and prove absence. Freeze retry-20's post-structural index and rebind
   all six inputs. Remove from controller and child environment exactly
   `PYTHONUTF8`, `PYTHONPATH`, `PYTHONHOME`, `PYTHONWARNINGS`, and
   `PYTHONOPTIMIZE`. Run the reviewed capture script in `attempt-035` mode. It
   must invoke exactly this controller argv in the shown order (line wrapping
   is presentation only):

   ```text
   C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe -B
   B:\Agents\.coauthor-v043-c2-detached-retry-20\scripts\release_qualification_controller.py run
   --run-root C:\Users\young\AppData\Local\Temp
   --run-id coauthor-v043-qualification-20260805-attempt-035
   --cwd B:\Agents\.coauthor-v043-c2-detached-retry-20
   --input B:\Agents\platform\co-author-harness\.git\HEAD
   --input B:\Agents\platform\co-author-harness\.git\refs\heads\main
   --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-20\HEAD
   --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-20\commondir
   --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-20\gitdir
   --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-20\index
   --input-root B:\Agents\platform\co-author-harness
   --allowed-output B:\Agents\.coauthor-v043-c2-detached-retry-20\docs\analysis\generated\fixture_manifest.json
   --watch-root B:\Agents\.coauthor-v043-c2-detached-retry-20
   --env TEMP=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-035-transients
   --env TMP=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-035-transients
   --env TMPDIR=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-035-transients
   --allow-user-site --
   C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe -B
   B:\Agents\.coauthor-v043-c2-detached-retry-20\scripts\analysis\fixture_runner.py
   --tier full --cache-mode off
   ```

7. Attempt 035 must terminally succeed with receipt state `succeeded`, null
   diagnostic, exit zero, exact 93 suites/97 cases/97 PASS, exact case order
   and 93/93 source hashes, zero warnings/failures/cache hits, exact 748-file
   tested population, freshly observed and frozen canonical and raw population
   digests with no predeclared self-referential digest, stable primary input
   root, stable six inputs
   and retry index, exactly one output change at the retry manifest, exact
   receipt/stream/lock bindings, frozen-residue equality, no new smoke residue,
   and zero owned/related process. Its environment delta must be exactly
   `{"PYTHONDONTWRITEBYTECODE":"1","TEMP":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-035-transients","TMP":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-035-transients","TMPDIR":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-035-transients"}`.
   Any refusal, mismatch, warning, extra
   output, residue, index change, survivor, or evidence-incomplete state is red
   and stops.
8. After complete attempt-035 evidence capture, prove its manifest differs
   from the current primary preimage, copy only those exact bytes to primary,
   and commit the manifest alone. Record commit/tree/parent, exact changed-path
   set, and primary-index pre/post hashes; prove clean status. Preserve
   retry-20/admin and attempt 035 for later independent review. If the manifest
   is byte-identical and no commit is possible, stop for new authority.
9. Bind attempt-035's resulting runner lock by exact bytes, SHA-256, raw
   content, child PID, dead PID, and absence of a held OS lock. Create retry-21
   detached at the exact manifest commit. Repeat every preflight from retry-20.
   Run the reviewed capture script in `replay-structural` mode over the same 15 commands,
   with exactly `TEMP`, `TMP`, and `TMPDIR` overridden to
   `C:\Users\young\AppData\Local\Temp\coauthor-v043-structural-20260805-retry-21-transients`,
   requiring the same gates and durable stream capture. The final command may
   change only runner-lock metadata, never its bytes. Freeze retry-21's index
   and rebind all inputs.
10. Run the reviewed capture script in `attempt-036` mode. It must start
    attempt 036 by invoking exactly this controller argv in the shown order;
    there is no `--allowed-output` argument:

    ```text
    C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe -B
    B:\Agents\.coauthor-v043-c2-detached-retry-21\scripts\release_qualification_controller.py run
    --run-root C:\Users\young\AppData\Local\Temp
    --run-id coauthor-v043-qualification-20260805-attempt-036
    --cwd B:\Agents\.coauthor-v043-c2-detached-retry-21
    --input B:\Agents\platform\co-author-harness\.git\HEAD
    --input B:\Agents\platform\co-author-harness\.git\refs\heads\main
    --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-21\HEAD
    --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-21\commondir
    --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-21\gitdir
    --input B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-21\index
    --input-root B:\Agents\platform\co-author-harness
    --watch-root B:\Agents\.coauthor-v043-c2-detached-retry-21
    --env TEMP=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-036-transients
    --env TMP=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-036-transients
    --env TMPDIR=C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-036-transients
    --allow-user-site --
    C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe -B
    B:\Agents\.coauthor-v043-c2-detached-retry-21\scripts\analysis\fixture_runner.py
    --tier full --cache-mode off --no-write
    ```

11. Attempt 036 must meet every non-output gate from attempt 035, reproduce
    attempt 035's exact population digests, case order, and 93/93 source hashes,
    create/change no output, preserve retry-21 index and clean status, record
    exactly
    `{"PYTHONDONTWRITEBYTECODE":"1","TEMP":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-036-transients","TMP":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-036-transients","TMPDIR":"C:\\Users\\young\\AppData\\Local\\Temp\\coauthor-v043-qualification-20260805-attempt-036-transients"}`,
    preserve all other planes, and leave
    zero owned/related process. Any mismatch is red and stops.
12. After both fresh attempts pass, freeze the four capsules and every attempt
    file. Before cleanup, obtain three independent read-only reviews that
    rebind the exact capture-script bytes, all four capsule bytes and their embedded
    raw streams, the live retry-20/retry-21 worktree/admin states, both fresh
    transaction directories, runner lock, primary commit/index/status,
    manifests, frozen residue, smoke census, and process census. Any finding is
    red and stops before cleanup. These live-state reviews are prerequisite
    evidence, not the final ten-file C2 review.
13. After all three live-state reviews approve, force-remove only
    retry-20/retry-21 and their admin paths, then remove only the exact final
    shared runner lock. Prove those paths absent, primary tracked-clean at the
    manifest commit, all transactions/residue and the five new C2 files
    preserved, all named PIDs dead, and zero related live process.
14. Only after cleanup postflight may the five existing C2 files be updated.
    They and the five new capture files must preserve all
    historical red/nonterminal/post-stop evidence, distinguish historical
    attempts 033/034 from fresh current qualification 035/036, bind all exact
    receipts/streams/capsules/admin states/locks/inventories, record the three
    pre-cleanup live-state reviews, bind post-cleanup absences and current
    process state, and state narrow claim limits. Obtain three independent
    exact-byte B0/M0/m0 reviews of the complete ten-file C2 set. Any changed
    byte invalidates all reviews. Never stage, force-add, or commit C2. Only
    after those reviews approve does source qualification close under this C0.

No operator or wrapper recovery, cancel, or ordinary transaction status call
on live attempts 035/036 is authorized. Registered synthetic fixture-owned
calls inside disposable isolated paths remain part of the exact 97-case
subject. No concurrent Git mutation, overlapping structural batch,
overlapping qualification, cleanup before its listed point, or unlisted
fallback is authorized. Only one mutating action may run at a time.

## Downstream stop and next authority

Successful attempt 036 plus three approved exact-byte reviews of the complete
C2 set establish only source qualification of the exact manifest commit. They
do not qualify version integration, archive, package, installed cache, startup
catalog, CLI registration, loaded paths, consumer, shipment, host, research,
or activation planes.

The next action, if source qualification closes, is a fresh independently
reviewed integration C0 binding the exact source-qualified commit and exact
version/changelog/release-note/contract preimages. That later authority may
advance only through integration and producer-side C4. Gate 6 remains closed
until a distinct independently governed consumer supplies v0.43 evidence
binding the exact resulting producer source/package as required by the
shipment contract. This task may request or ingest such evidence only under
that later authority; it may never author, simulate, copy forward, or
reinterpret it. No final shipment claim is permitted before Gate 6 and every
later producer package, archive, cache, and Gate 9 shipment gate independently
passes. Fresh-host qualification remains a distinct post-shipment task; it
cannot supply, withhold, or retroactively alter shipment authority.
