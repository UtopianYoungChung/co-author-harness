# v0.43 retry-26 static-validator rejection - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority and precedence

This instrument is subordinate to the finalized research-governance chain and
narrows only the stopped v0.43 source-qualification recovery:

- `B:\Agents\research\10_Governance\MASTER_GOVERNANCE.md`, 77,262 bytes,
  SHA-256
  `a55b56eee7d72b4aa8a7f2376cb023d987ee7c12e71611fef7a9b457d780e191`;
- `B:\Agents\research\10_Governance\MASTER_GOVERNANCE_AMENDMENT_RECORD_1.0.9.md`,
  12,436 bytes, SHA-256
  `201d9b68e9c050f0f1e937977833868dd8ec2e3c5d6395f044fc00103fc95afe`;
- A02 anchor
  `B:\Agents\research\99_System\migrations\2026-08-04_master_governance_amendment_1_0_9\JOSEPH_AMENDMENT_ANCHOR.yaml`,
  1,114 bytes, SHA-256
  `7ae758e443f4360e9738094e6fd1080022b30fa60f8807d0baa1cf0640fb7ebc`,
  effective `2026-08-04T20:00:35.9192246-04:00`;
- Overseer doctrine, 5,829 bytes, SHA-256
  `90edcde87d2ed36641a911265179eb0203165e701a48395fd49b201c0379e7b9`;
- Overseer status `clear`, 773 bytes, SHA-256
  `b6b2fddedf9f364308dd6d54375c4d3a58595c3b7f9ed332453605373d9f00f9`;
  and
- parent C0
  `docs/superpowers/specs/2026-08-06-v043-retry26-canonical-rejection-c0-addendum.md`,
  committed alone at `1cfb31042c47371d2967d2359cdf6fff6a95353f`,
  30,857 bytes, SHA-256
  `52686cb9e6f0d4149736b2510b000bfa56502be14abb131303b59309f177ddb1`.

The parent C0 exact bytes received:

- B0 approval from `/root/retry26_authority_audit`, message
  `amsg_019fd8ca-c042-7bc1-a02c-5499ac460c72`;
- M0 approval from `/root/retry26_mechanics_audit`, message
  `amsg_019fd8c9-eb20-72c3-b4d5-a35011964e85`; and
- m0 approval from `/root/retry26_ledger_audit`, message
  `amsg_019fd8ce-1a0a-7723-9f6c-552b9334c0d5`.

All three reviews bound 30,857 bytes, 530 LF, zero CRLF, terminal LF, no
BOM, and the SHA-256 above. Parent requirements continue except where this
later instrument registers the new static rejection, preserves the published
script, retires unused labels, substitutes a V2 script path and fresh labels,
or updates exact cardinalities and writable paths. Any absent, draft,
ambiguous, inconsistent, superseded, or conflicting governing binding fails
closed.

## Plane separation and claim ceiling

Source, primary Git/admin, startup catalog, CLI registration, archive/package,
installed cache/provenance, loaded paths, consumer evidence, shipment, host
qualification, research, and activation remain separate planes. This grants
no push, tag, install, catalog/cache mutation, archive/package creation,
shipment, consumer attestation, research mutation, host qualification,
promotion, canon change, or activation.

Gate 6 remains closed. User authorization cannot replace independently
governed v0.43 consumer evidence or any exact review predicate below.

## Registered post-publication static rejection

The parent C0 assembly first rejected an in-memory replacement-count mismatch
before creating a temporary or destination file. That pre-write diagnostic
left no residue and consumed no operational label.

The corrected closed assembly then atomically published:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY.ps1`

at exactly 402,956 bytes, SHA-256
`9565c7c10524746c9c539d21122db34d0e54f32a78076ee908ee2e6ebd19a376`,
4,735 LF, zero CRLF, terminal LF, no BOM. Both Windows PowerShell 5.1 parser
entry points reported 63,252 tokens and zero errors. Its exact assembly sibling
is absent.

No mode review, retry creation, worktree removal, controller, supervised
structural target, qualification transaction, manifest write, or cleanup
followed. An isolated read-only invocation extracted only the candidate's
static helper functions and called its retry-26 validator against the immutable
retry-26 RED pair. It failed closed at `RETRY26-RED-IMMEDIATE-STATE`.

Cause: the candidate incorrectly requires `primary_git.head` and
`retry_git.head` inside retry-26's initialization-stage
`red_post_state.immediate_state`. Those properties are absent because retry-26
failed before pre-state capture. The exact immediate-state field order is:

`label`, `captured_utc`, `mode`, `retry_worktree`, `retry_admin`,
`primary_admin`, `primary_input_root`, `six_inputs`, `primary_manifest`,
`retry_manifest`, `runner_lock`, `mode_temp`, `ownership_temp`, `provenance`,
`fic`, `rmnc`, `frozen_residue`, `smoke_census`, `processes`,
`registered_transients`, `environment`, `attempt_transaction`, `events_seen`.

The failed candidate is now immutable evidence: never edit, delete, rename,
stage, force-add, commit, package, or execute it. This is a static rejection,
not a mode RED, qualification result, source pass, or terminal success.

## Frozen stopped state

At this drafting boundary:

- `main` is tracked-clean at commit
  `1cfb31042c47371d2967d2359cdf6fff6a95353f`, tree
  `32ab21efeef80600d02fc36f459ea147e5af938f`, parent
  `91ad31ae250dd9600ef85f330e8d4acaef36da8e`;
- that commit contains 758 tracked paths, zero `.plugin`/`.zip`, six
  `scripts/analysis/` paths, one fixture manifest, and 751 included paths;
  its Python-Unicode-sorted included-path payload without terminal LF is
  39,272 bytes at SHA-256
  `9d427133735ad746d57359fff31d4272026b5990c7dfd0acab1d8de921ab9eb6`;
- C2 contains exactly 21 files, zero directories, and 23,518,448 file bytes:
  the parent C0's exact 20-file table plus only the failed 402,956-byte script
  above;
- the candidate-excluded primary input-root preimage contains 1,885 identities
  (315 directories, 1,570 files, zero links/other), 126,732,485 file bytes,
  218,212 inventory bytes, and SHA-256
  `d15e698c380fd9d0a236582369be750c2f1900fc0e967a51ebb611117d6521da`;
- retry-26 remains clean/detached with the parent C0's exact worktree/admin
  bindings; the two retry-26 RED files, manifests, stale lock, FIC, and Aug-3
  residue retain their parent-C0 bindings;
- retry-28/29 worktrees/admins, attempts 043/044, all associated transients and
  fresh capsules, and related processes are absent or zero; and
- the V2 script, its assembly sibling, retry-30/31 worktrees/admins, attempts
  045/046, their capsules/preimages, transactions, and transients are verified
  absent.

The inventory tuple is a candidate-excluded preimage. Every review must bind
the live root as exactly that preimage plus this C0 candidate's reviewed row;
any other delta fails closed.

## Retired and fresh labels

Retry-28/29 and attempts 043/044 are retired unused and remain absent. They may
not be reused. Retry-26 remains consumed RED and preserved.

Fresh generation is retry-30 at
`B:\Agents\.coauthor-v043-c2-detached-retry-30`, capsule
`C2_STRUCTURAL_GENERATION_RETRY30_CAPSULE.json`. Qualification is attempt 045
at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260806-attempt-045`,
capsule `C2_ATTEMPT_045_CAPSULE.json`. Replay is retry-31 at
`B:\Agents\.coauthor-v043-c2-detached-retry-31`, capsule
`C2_STRUCTURAL_REPLAY_RETRY31_CAPSULE.json`. No-write qualification is attempt
046 at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260806-attempt-046`,
capsule `C2_ATTEMPT_046_CAPSULE.json`.

Both controllers pass only `C:\Users\young\AppData\Local\Temp` as
`--run-root`; the controller appends its run ID.

The six exact capability roots substitute retry-30/31 and attempts 045/046 in
the parent pattern, all under
`C:\Users\young\AppData\Local\Temp` and all absent at drafting.

## Exact V2 repair and assembly

The immutable source is the failed 402,956-byte script at SHA-256
`9565c7c10524746c9c539d21122db34d0e54f32a78076ee908ee2e6ebd19a376`.
The exact absent assembly sibling and destination are:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V2.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V2.ps1`.

Use the parent C0's exact create-new, strict-UTF-8, LF/no-BOM, sequential
prefix-write, on-disk-flush, read-back validation, and non-overwriting
same-volume two-argument `[IO.File]::Move` protocol. The parent initial,
zero-through-full exact-prefix construction, validated-staging, publication,
and RED failure states apply with only the V2 paths above. No alternate temp,
direct destination write, overwrite, incremental destination edit, cleanup,
resume, or republish is authorized.

The C2 closure profile changes exactly once. Before a complete assembly sibling
exists, construction may contain only a zero-through-full exact prefix at that
sibling; the closed-set helper is not invoked on an incomplete prefix. After
the complete sibling has been flushed, closed, and read back, the assembly
static profile is exactly the immutable 21-file base plus that sibling, with
the final V2 path absent. After the non-overwriting move, the runtime profile is
exactly the same immutable 21-file base plus the final V2 path, with the
assembly sibling absent. No validated state permits both V2 leaves or neither
V2 leaf.

Apply only these deterministic in-memory changes:

1. bind this C0's eventual isolated commit/path/bytes/hash and the V2 script
   path;
2. preserve every failed-script byte and check except replace the false
   retry-26 immediate-state assertion with an exact field-set assertion using
   the 23 fields above, explicit absence of `primary_git` and `retry_git`, and
   the existing exact retry worktree/admin, manifests, stale-lock, FIC,
   residue, process/transient, environment, and source-only bindings;
3. retain the exact `preserved_retry26_failure` schema and require canonical
   equality across all four new capsules and their publication preimages;
4. retire only active retry-28/29 and attempts 043/044 labels and substitute
   retry-30/31 and attempts 045/046 across configs, paths, capsules,
   transactions, transient ledgers, prior-capsule tables, sequence gates,
   manifests, and cleanup ledgers; historical labels remain only in immutable
   validators or retired assertions;
5. add the failed V1 script to the immutable base C2 set and make V2 the sole
   current script. The immutable base is exactly the parent C0's 20-row C2
   binding table plus
   `C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY.ps1`, hence 21 files and
   23,518,448 bytes. Change `Assert-C2ClosedSet` to the exact interface
   `param([Parameter(Mandatory = $true)]$CurrentScriptBinding,
   [switch]$AssemblyStatic)`. With `-AssemblyStatic`, require the current
   binding path to be the exact V2 assembly sibling, require the final V2 path
   absent, and compare the directory against exactly the base 21 plus that
   sibling. Without the switch, require the current binding path to be the
   exact final V2 path, require the assembly sibling absent, and compare
   against exactly the base 21 plus that final file and any phase-authorized
   capsule rows already required by the inherited state machine. Both forms
   require zero directories and reject every extra or missing entry. Main and
   every mode entry call only the no-switch runtime form; `-AssemblyStatic` is
   exclusively an extracted-helper pre-publication validation surface. Require
   22 files pre-generation, 30 files before cleanup, and 26 files after
   cleanup; and
6. update only bindings induced by this C0's isolated commit.

Static validation must extract and invoke the candidate's exact helper
functions without executing main; the retry-26 preservation validator and C2
closed-set validator must pass. Before the move, invoke the extracted
`Assert-C2ClosedSet` with `-AssemblyStatic` and a binding to the complete,
read-back assembly sibling while the final path is absent. Only after every
pre-Move check passes may the non-overwriting move occur. Then re-extract the
helper from the published final bytes, verify the assembly sibling absent, and
invoke its no-switch runtime form with a binding to the final V2 path before
any review freeze or mode. Static validation must also repeat both 0-error
parser entry points, retry-24/retry-26 canonical recomputation and adversarial
mutations, active-label closure, exact manifest UUID-v4 writer grammar,
protected-corpus hashing, exactly one guarded final capsule
`[IO.File]::Replace`, immediate return, and no catch-path republish.

Freeze V2 bytes only after all checks pass. Obtain three independent
exact-byte B0/M0/m0 reviews of that same candidate. Any edit invalidates all
three. No mode runs before unanimous approval.

## Writable ledger

Only these paths may change:

1. this C0 path and primary Git objects/ref/log/index/`COMMIT_EDITMSG` for its
   isolated commit, then the later manifest-only commit;
2. only the exact V2 assembly sibling/destination and their registered states;
3. retry-26 worktree/admin removal only after V2 reviews and full protected
   rebind;
4. retry-30/31 worktrees/admins, attempts 045/046, their exact six capability
   roots and registered children, exact four capsule targets/preimages,
   attempt transactions, runner-lock transitions, retry-30's manifest and one
   UUID-v4 writer sibling, and the primary manifest-only commit;
5. inherited exact provenance/FIC transient children under the parent C0; and
6. green-only reviewed cleanup followed by updates to the five summary files.

Every other path is read-only, especially the failed V1 script, all older RED
and nonterminal evidence, retry-26 RED pair, cache/catalog, archive/package,
shipment, research, consumer, host, and activation planes.

## Reviews, commit, and execution sequence

Before commit, this exact candidate requires three independent exact-byte
B0/M0/m0 reviews. Each binds the governance chain, stopped static failure,
published V1 bytes, corrected predicate, V2 assembly, labels, ledgers,
cardinalities, sequence, and claim ceiling.

After unanimous approval, commit only this file directly on `main`. The commit
must parent `1cfb31042c47371d2967d2359cdf6fff6a95353f`, contain 759 tracked and
752 included paths, zero plugin/ZIP, six `scripts/analysis/` paths, one
manifest, and a 39,351-byte included-path payload at SHA-256
`375f7633097994cb3e5f31719ead84e140d8c5d0a7561eb7a3729dc7835ff7d1`.
Do not push or tag.

Then:

1. rebind governance, C0 commit, clean main, all 21 C2 files, retry-26/admin,
   manifests, lock, FIC/residue, absences, events, and processes;
2. assemble V2, complete static/adversarial checks, and obtain three exact-byte
   B0/M0/m0 approvals;
3. rebind all 22 C2 files, then Git-remove only retry-26/admin;
4. run reviewed V2 generation on retry-30; require terminal PASS and exact 15
   supervised structural closures, otherwise preserve and stop;
5. only after generation PASS, remove the exact stale lock and run attempt 045
   with terminal succeeded controller evidence and 97 cache-off cases;
6. commit only attempt 045's generated manifest bytes on `main`;
7. create retry-31 at that manifest commit; run structural replay and attempt
   046 no-write, requiring exact population/manifest/case-order equality;
8. after four PASS results, obtain three independent pre-cleanup live-state
   reviews over all 30 C2 files and every live transaction/worktree;
9. only after approvals, remove four provisional preimages, retry-30/31 and
   admins, registered empty capabilities/provenance roots, and final lock;
10. update five summaries; final ignored/local C2 is exactly 26 files and
    receives three independent exact-byte reviews; never stage or commit C2;
11. stop at Gate 6 pending independent governed v0.43 consumer evidence.

Any RED, interruption, missing receipt, survivor, dirty state, hash/count
drift, unregistered path, failed review, or ambiguity stops and preserves all
evidence.
