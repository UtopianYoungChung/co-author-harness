# v0.43 C2 Windows executable-alias containment C0 addendum

**Status:** approved package-maintainer boundary for preserving attempt 016 as
governed red, containing its escaped transaction-owned process, removing only
its exact detached-worktree residue, repairing Windows executable identity
containment, and performing one fresh source transaction plus one fresh
detached transaction. This addendum grants no C2 qualification, version,
package clearance, shipment, cache mutation, consumer re-attestation, host
qualification, activation, research mutation, acceptance, promotion, release,
or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-03-v043-c2-ambient-bytecode-invocation-c0-addendum.md`
and
`docs/superpowers/specs/2026-08-02-v043-qualification-release-shipment-c0-addendum.md`.
All parent prohibitions and stop gates remain binding.

## Frozen attempt-016 red

- Repository `main` is clean at
  `9326e64f59e54d1185fe64143b3f5f9e3818d6da`, tree
  `8c4ea96ae91a81904e325a94f6db9c77dadd58f1`.
- Attempt 016 is preserved read-only at
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-016`.
  It is terminal `refused` with exit `1223`, diagnostic
  `RELEASE-CONTROLLER-OUTPUT-SCOPE`, 513 reported changed paths, empty stdout,
  and empty stderr. Its receipt is 89,619 bytes, SHA-256
  `230cb342a11a81f08ab4d4a18443e1cdb65193daf2cfaecce70323b95df23991`.
- The receipt binds `intent.json` 862,967 bytes / SHA-256
  `c835d334100e1f29528938839672165276c432bde7e829b16bd265e1661c0052`,
  `exit.json` 754 /
  `3b608bc1f723a590ae639bca1bf01af38cf47bfe327847632b9389b0f806ba7b`,
  `journal.json` 765 /
  `e400dc42d724cef1b9070cd2dbb4983d29ca0f9ae5ad6ee5229c41572a0e114e`,
  `owner.json` 113 /
  `3efaeabbbb2953be45c53bd6e7bc548d0441e2c09107e4538d7b8915b6f63b8e`,
  and both zero-byte streams at SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
- The invoked executable was the absolute zero-byte reparse/AppExecLink
  `C:\Users\young\AppData\Local\Microsoft\WindowsApps\python.exe`.
  The controller owned launcher PID 56800, but after cancellation the real
  interpreter
  `C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe`
  continued as PID 61236 running
  `scripts\assignment_milestone_checkpoint_smoketest.py`; its child PID 51584
  was also observed. PID 51584 exited before containment. PID 61236 was
  revalidated by exact executable/command identity, terminated, and proved
  absent to stop continuing unauthorized writes. This emergency containment
  is recorded as a failure response, never as qualification evidence.
- A concurrent read-only Git observation occurred after child start. That
  independently violates the required transaction quiescence; attempt 016 can
  never be upgraded or reused even apart from its terminal refusal.
- Retry-4 is detached at the exact commit above and has zero `.pyc` files and
  zero `__pycache__` directories. Its sole untracked root is
  `B:\Agents\.coauthor-v043-c2-detached-retry-4\assignment-milestone-checkpoint-iyorkfdl`.
  After process containment that root contains 469 files and 159 nested
  directories. A UTF-8 compact JSON array sorted by relative POSIX path and
  containing `{path,length,sha256}` records is 84,104 bytes with SHA-256
  `bfbbf26d7e9aa2ff9666922b762c02d202970400df025fa04b39128c255c684a`.
- The primary remains clean with the governed 180-file/four-directory bytecode
  baseline. The committed fixture manifest remains 54,247 bytes, SHA-256
  `7e25595be68fedbdc7759d6fd61f3e98116191c9ac672107f918c4acd88c01c0`.

## Exact cleanup and transient ledger

After this addendum is committed, the maintainer may:

1. Revalidate that no process command line names retry-4 and that the residue
   root has the exact count and inventory digest above.
2. Remove only that exact untracked residue root, recursively and without
   following reparse points. Any identity/count/hash mismatch is a stop.
3. Prove retry-4 tracked state clean, detached bytecode zero/zero, primary
   bytecode 180/four, and the attempt-016 directory unchanged.
4. Remove retry-4 only through `git worktree remove` and prove its worktree
   administration entry absent. Common Git administration may not be manually
   deleted.

Attempt 016 and its transaction artifacts remain immutable. The fresh paths,
all verified absent before this addendum, are:

- source attempt 017:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-017`;
- detached attempt 018:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-018`;
- detached retry-5: `B:\Agents\.coauthor-v043-c2-detached-retry-5`.

No label or path may be repurposed.

## Exact tracked writable ledger

| Action | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE | `docs/superpowers/specs/2026-08-03-v043-c2-windows-alias-containment-c0-addendum.md` | 0 | `ABSENT` |
| MODIFY | `scripts/release_qualification_controller.py` | 105,020 | `c9aa44f7ec82f945b4e3aaedcc07cbad1a198579f1be9c58a039721b4f6d7cdc` |
| MODIFY | `scripts/release_qualification_controller_smoketest.py` | 158,923 | `0d582a2033b8ed80e7f5fe471909404fb540d47cc241ac77e728b3d9e96a6879` |
| MODIFY | `references/contract_kernel.v1.json` | 37,242 | `d3ca3dbf3b9e67445ac78ff393d3db5caf1c940a95c959f22dbe5bb866053d84` |
| MODIFY | `references/compatibility/shipment-v2/contract_kernel_projection.json` | 2,907 | `28026ca439d31d20f7fbb2a1f44b63dd65b1d672d523b12bd13759ba3e2fd2ee` |
| MODIFY | `references/compatibility/shipment-v2/compatibility_profile.json` | 4,510 | `19c5fd05d703ca49ab7f1d25106c9458d1c3d78efe7e99556bf04ab1aece632b` |
| QUALIFICATION OUTPUT ONLY | `docs/analysis/generated/fixture_manifest.json` | 54,247 | `7e25595be68fedbdc7759d6fd61f3e98116191c9ac672107f918c4acd88c01c0` |

The existing authorized C1/C2 evidence paths may be updated. No other tracked,
package, cache, registration, research, or remote path is writable in this
slice.

## Required containment repair

1. On Windows, refuse before product creation unless `argv[0]` is an absolute,
   existing, regular, non-reparse executable path. In particular, a Windows
   App Execution Alias must never stand in for the process the Job is expected
   to own. Preserve existing POSIX command behavior.
2. Immediately after suspended process creation and before Job assignment or
   resume, query the created process image. Require exact normalized identity
   with the validated requested executable. Mismatch or unobservable identity
   is a typed `RELEASE-CONTROLLER-PROCESS` refusal; terminate and wait for the
   still-suspended process before closing every handle.
3. Preserve suspended-before-assignment ordering, proven worker escape from an
   enclosing permissive Job, no product breakaway flags, explicit Job handle
   lifetime, `KILL_ON_JOB_CLOSE`, cancellation/recovery receipt semantics, and
   POSIX supervisor behavior.
4. Add deterministic Windows regressions for reparse/AppExecLink refusal,
   requested-versus-created image mismatch, zero sentinel execution on either
   refusal, handle closure, and ordinary concrete-interpreter descendant
   cancellation. Run the complete Windows and WSL/POSIX controller suites.
5. Rebind only the controller component hash in the kernel, then the raw kernel
   hash in the projection, then the raw six-file kit aggregate in the profile.
   Freeze and commit exact repair postimages before corpus work.

The currently authorized concrete interpreter is
`C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe`, 105,464
bytes, SHA-256
`ab60df0d679ed8c1150a9fe8ea38b99a2b9838807127f17a166f993bc5403f62`.
Reobserve it before each transaction; any drift requires a new committed
addendum.

## Qualification gates

Before attempt 017, require focused Windows and POSIX controller passage,
structural/mechanical and binding checks, exact bytecode censuses, zero live
transaction identities, and independent B0/M0/m0 review. Attempt 017 must use
the committed repair, the concrete interpreter above, `--tier full
--cache-mode off`, the primary as its sole watch root, the manifest as its sole
allowed output, and only the exact shared runner lock as ignored output. It
must terminally succeed with 93 suites / 97 direct cases, zero failures and
cache activity, stable inputs, empty stderr, and exact residue/process closure.
Commit only its exact successful manifest.

Only then may retry-5 be created at that exact commit. Attempt 018 must use the
same concrete interpreter with `--tier full --no-write --cache-mode off`, watch
retry-5 and primary, allow no package output, and ignore only the exact shared
runner lock. Require primary 180/four and retry-5 zero/zero before and after,
stable Git administration, no concurrent Git/package/cache observation, 93/97
direct green execution, stable inputs, empty stderr, zero live identities,
terminal success, and independent B0/M0/m0 review.

Any warning, residue, reparse/mismatch ambiguity, failed or skipped required
case, cache hit, incomplete evidence, output drift, process residue, or review
finding is a stop. C2 and all later version, package, cache, clearance, remote,
shipment, fresh-host, startup-catalog, loaded-path, consumer, activation,
research, acceptance, promotion, release, and canon claims remain closed.
