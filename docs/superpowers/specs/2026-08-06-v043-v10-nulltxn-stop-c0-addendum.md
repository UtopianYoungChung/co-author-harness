# v0.43 V10 protected assertion corrections - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and stopped state

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v10-preopen-stop-c0-addendum.md`,
committed alone at `56017fe1e72cb273f984ca353ef5e3125e528c85`, tree
`ee2a74d87adbbc153899dfe7d463db5a60946674`, parent
`2b92b1f5d3a6ac035cfa90897a472b103c61caeb`, 6,981 bytes, SHA-256
`ac34eeba4d2a750607a66b32da43acc5348a042f437b58c77e2774df615073b2`,
with independent exact B0/M0/m0 approval.

Immutable V9 remains
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V9.ps1.assembly.tmp`,
405,706 bytes, SHA-256
`e66161e3333fa451ea1da1199ad817fe12234b1169d0405f1616be0ea466e05b`,
4,768 LF, zero CR, terminal LF, no BOM, and 63,464 tokens/zero errors at
both parsers. Final V9 and both V10 paths are absent. C2 remains 29 files,
zero directories, and 26,758,493 file bytes. Main is clean at the parent.

The parent C0's corrected transport launched exactly one Windows PowerShell 5.1
child from one pre-parsed complete source payload through one single-line
redirected-standard-input wrapper. The wrapper used one
`[ScriptBlock]::Create` and literal mode/worktree arguments. The child entered
the complete protected rebind. In structural mode, `Get-TransactionBinding`
contractually returns `$null`; strict mode therefore rejected the validator's
incorrect property access `$state.attempt_transaction.state` with `The property
'state' cannot be found on this object`. The child exited 1 before the V10
derivation or create-new section. No V10 write, move, final, operational mode,
cleanup, or qualification action occurred.

This is a pre-open validator rejection, not a V10 rejection, operational RED,
qualification result, or success. Static attempt 11 has not started and remains
fresh. Retry-26/admin, manifests, stale lock, FIC, frozen residue, V2-V9, all
historical RED/nonterminal evidence, and every plane label remain protected.
Retry-26 remains clean/detached at its frozen source commit
`91ad31ae250dd9600ef85f330e8d4acaef36da8e`, tree
`4c0c00527b4dba2bd3edd56d6d3f58ab5a4fb358`; that preserved identity is an
ancestor of the current C0 chain and is not to be checked out, advanced, or
otherwise mutated during validation.

All plane-separation and no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact assertion repair and V10 derivation

After this exact addendum is independently approved and committed alone, one
fresh complete protected rebind may authorize opening only the V10 assembly and
final paths named by the parent. Retain the parent's exact reviewed transport,
source preparation, literal arguments, complete binder set, 600,000-ms ceiling,
and fail-fast behavior. Replace exactly one complete structural
transaction-absence assertion call from:

`Assert-Capture ($state.attempt_transaction.state -eq 'absent') 'TRANSACTION-PREFLIGHT' ([string]$state.attempt_transaction.state)`

to:

`Assert-Capture ($null -eq $state.attempt_transaction) 'TRANSACTION-PREFLIGHT' 'structural mode requires null attempt transaction'`.

Require the old full call exactly once before replacement, zero afterward, and
the replacement full call exactly once afterward. Parse and reverse-check the
repaired validator source before launch. This correction removes both strict
property accesses: the condition and its diagnostic argument. It reflects the exact
`Get-TransactionBinding` structural-mode contract and grants no weakening for
attempt modes, where a transaction binding remains required by the script.

The parent validator also incorrectly expected retry-26 at the parent C0 commit
and tree even though retry-26 is protected at its exact frozen source identity.
Replace exactly one complete retry assertion call from:

`Assert-Capture ($state.retry_git.head -ceq '2b92b1f5d3a6ac035cfa90897a472b103c61caeb' -and $state.retry_git.tree -ceq '5ccea2052091b27ce0321636807317bb98c73ea3') 'RETRY-HEAD' ([string]$state.retry_git.head)`

to:

`Assert-Capture ($state.retry_git.head -ceq '91ad31ae250dd9600ef85f330e8d4acaef36da8e' -and $state.retry_git.tree -ceq '4c0c00527b4dba2bd3edd56d6d3f58ab5a4fb358') 'RETRY-HEAD' ([string]$state.retry_git.head)`.

Require the old retry call exactly once before replacement, zero afterward, and
the new call exactly once afterward; reverse-check it with the null-transaction
repair. This is an exact read-only binding correction, not authority to move,
reset, checkout, update, repair, or remove retry-26.

Before opening V10, repeat every parent protected assertion: exact clean main
and current C0; C2 29/0/26,758,493 and V2-V9; both V10 paths absent; finalized
governance; retry-26 worktree/admin; retry-24 and retry-26 RED binders;
manifests; stale lock; FIC; frozen residue; runtime and six-input bindings; all
retry-30/31, attempt-045/046, capsule, provenance, temporary, ownership, and
transaction paths absent; and scalar events, related processes, jobs, and
registered transients zero. Any mismatch stops without write.

Derive V10 only from exact V9 under the parent transform, binding this
same-width committed C0 path, isolated commit, four-digit byte length, and
SHA-256. V10 remains exactly 405,909 bytes, 4,770 LF, zero CR, terminal LF, no
BOM, 63,471 tokens/zero parser errors, three V10 self leaves, and two residual
V9 evidence rows. Require exact reverse byte equality, bounded sequential
create-new writes, write-through durable flush, close, readback, both parsers,
and final absence.

## Attempt 11 and downstream limits

Only exact V10 assembly creation reopens the still-fresh parent static attempt
11. Its nine-slot mechanics, direct literal slot-9 here-string, slot-8/9
whole-state equality, no-intervening-action rule, non-overwriting move, and
post-move validation remain unchanged. Three independent exact-byte B0/M0/m0
final-V10 reviews and a new complete protected rebind still precede operational
mode. Only then may inherited retry-26/admin removal and retry-30/31
attempts-045/046 proceed. C2 gates remain 30 post-review, 38 pre-cleanup, and
34 final. Every failure stops and is preserved.

## Review and ledger

This exact candidate requires independent exact-byte B0/M0/m0 review and an
isolated one-file commit directly on main. Reviewers must independently bind the
parent, failure, both exact full-call repairs, every remaining pre-open
assertion, same-width transform, protected ledgers, prospective commit
population, downstream sequencing, claim ceiling, and Gate 6 closure.
Prospective population: 771 tracked, 764 included, zero
`.plugin`/`.zip`, six `scripts/analysis/`, one fixture manifest; the 40,219-byte
Python-Unicode-sorted UTF-8 included-path payload without terminal LF has
SHA-256
`36f062757e68f4913b5adc985519b703a0fa858f0d5eff4873327a23e26fbcf2`.
Do not push or tag.

Writable paths are only this candidate/isolated commit, then the sole V10 pair,
then review-gated inherited removal/qualification ledgers. Preserve every prior
artifact and every other plane. Gate 6 remains closed absent independent v0.43
consumer evidence.
