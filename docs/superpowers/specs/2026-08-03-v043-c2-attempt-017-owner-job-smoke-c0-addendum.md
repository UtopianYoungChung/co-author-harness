# v0.43 C2 attempt-017 owner-Job smoke repair C0 addendum

**Status:** approved package-maintainer repair authority for the exact
attempt-017 controller-smoketest topology defect. This record must be committed
alone before any repair byte is edited or tested. It grants no package
clearance, shipment, cache mutation or equality, consumer re-attestation, host
qualification, activation, research mutation, acceptance, promotion, release,
or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-03-v043-c2-attempt-017-final-postimage-freeze-c0-addendum.md`.
All parent prohibitions remain binding except for the exact superseding repair
and retry ledger below.

## Preserved red transaction and root cause

Source attempt 017 is immutable failed evidence. Its durable receipt is
`child_failed`, its product exit is 1, and the authoritative runner reported 93
suites / 97 cases with exactly
`scripts/release_qualification_controller_smoketest.py::default` failed. The
runner correctly voided the prior fixture manifest before execution and wrote
no replacement; the tracked manifest deletion remains red transaction residue
and is excluded from repair commits.

The failure is a test-topology contract collision. The fixture supervisor
correctly owns every suite in a Windows Job with `KILL_ON_JOB_CLOSE` and no
breakaway permission. The controller smoketest's ordinary helper requested the
production default `detached=True`; the production controller correctly refused
because a detached worker cannot escape that enclosing non-breakaway Job. This
failure must not be converted into a pass by weakening either production
contract.

## Frozen preimage and exact writable ledger

- Repository: `B:\Agents\platform\co-author-harness`; branch: `main` only.
- Pre-repair HEAD: `7b14f624de7f01db82f3cb2039d7af5e17cff8c5`.
- Pre-repair HEAD tree: `acb2868f210252d4cf32a8bd1f85acc2f9f54708`.
- Governed bytecode baseline: 179 `.pyc` files and four `__pycache__`
  directories.
- Attempt 019, attempt 020, and detached retry-6 roots were freshly observed
  absent before this record.

| Authority | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| MODIFY | `scripts/release_qualification_controller_smoketest.py` | 190,829 | `efb0f9df8fa83c6bd4a372ffe851a11942672f22fbd486fea2d9c6d6f3658607` |
| MODIFY | `scripts/analysis/fixture_runner.py` | 40,818 | `01378dcbe8872c72d65b32219c581fd5786be1416714fb61db6953c6a0a25f5f` |
| MODIFY | `references/destination_coverage_registry.json` | 14,556 | `3ba87a9610eacb7e75d7dd02ec3f59faed4f58887c6628fa42aed643563d6c1a` |
| MODIFY | `references/contract_kernel.v1.json` | 37,588 | `64092ce49c6b8595937e94235e9dc010675d54da468e816d61dbbb2500caff38` |
| MODIFY | `references/compatibility/shipment-v2/contract_kernel_projection.json` | 2,907 | `798559bc56cba884ba7b7df7f554f1f7d482dc6beef2d6aa6269c77b50765058` |
| MODIFY | `references/compatibility/shipment-v2/compatibility_profile.json` | 4,510 | `6f15387da41a084ec577b1a2ef78fdd988791a739c1f8f53aced24591f7dfdba` |
| QUALIFICATION OUTPUT ONLY | `docs/analysis/generated/fixture_manifest.json` | absent after failed attempt 017 | not applicable |

No production controller, fixture supervisor, fixture-infrastructure, schema,
package, cache, catalog, startup, research, or shipment path is writable under
this repair authority.

## Required repair semantics

1. Register the controller smoke with one literal `--fixture-owner` argument;
   unknown arguments fail closed.
2. On Windows, that mode must prove current Job membership and exact enclosing
   `KILL_ON_JOB_CLOSE` / no-breakaway limits before substantive execution.
3. It must preserve a durable typed `RELEASE-CONTROLLER-PROCESS` refusal for the
   default detached launch, prove no fallback, no worker/child event, and no
   product sentinel.
4. Ordinary contained cases may then pass explicit `detached=False`, using the
   controller's existing private Job nested under the fixture owner's immutable
   outer Job. POSIX execution keeps its existing detached semantics.
5. Bare-host Windows execution remains the positive real-detachment and
   explicit-breakaway gate. Fixture-owner Windows execution substitutes a
   required nested-topology negative proving an inner permissive Job cannot
   punch through the non-breakaway outer owner.
6. Production controller and fixture supervisor bytes and behavior remain
   unchanged. No automatic fallback, filename carve-out, inherited outer Job
   handle, breakaway permission, or ambient marker is allowed.

## Verification and retry ledger

After repair and mechanical hash rebinding, run focused bare-host Windows,
fixture-owner Windows through the exact fixture supervisor, WSL/POSIX, native
and WSL text-policy, destination, kernel, projection, compatibility-kit,
schema/JSON/AST, LF, process-residue, and bytecode-baseline gates. Obtain three
independent exact-byte reviews and commit only the six repair/binding paths.

Only a clean reviewed repair commit may start fresh source attempt 019 at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-019`.
That transaction alone may regenerate the fixture manifest. Attempt 018 remains
closed and absent. Only a successful committed attempt-019 manifest may open a
detached `main` worktree at
`B:\Agents\.coauthor-v043-c2-detached-retry-6` and fresh detached attempt 020 at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-020`.
The detached worktree must be removed after evidence is copied and verified.

Any unlisted changed byte, warning, ambiguity, residue, failed focused gate, or
failed transaction is a stop requiring new explicit authority. Repair and
shipment must not overlap: all package, clearance, shipment, cache, catalog,
and fresh-host actions remain sequentially downstream of final exact-byte C2
qualification and independently governed consumer evidence.
