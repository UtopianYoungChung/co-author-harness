# v0.43 V9 slot-9 wrapper-construction failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and stopped state

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v8-slot8-checker-c0-addendum.md`,
committed alone at `a9acdee0e66d9cf714a26d97cb52dcbfb95a8d16`, tree
`2485e874cde58a227aebe196ebc829ffd32d7094`, parent
`e25f068e87758f6caec931d8d0f6678b4378030e`, 7,848 bytes, SHA-256
`43aa9a005e6851d986f124a8dfe34568231bff1b14f46318c36427f97a0a9cde`,
with independent exact B0/M0/m0 approval.

Immutable V9 is
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V9.ps1.assembly.tmp`,
405,706 bytes, SHA-256
`e66161e3333fa451ea1da1199ad817fe12234b1169d0405f1616be0ea466e05b`,
4,768 LF, zero CR, terminal LF, no BOM, and 63,464 tokens/zero errors at
both parsers. Final V9 is absent. C2 is 29 files, zero directories, and
26,758,493 file bytes.

The first V9 derivation wrapper stopped before create-new because compressed
`return$Text.Replace(...)` violated strict-mode function syntax. Both V9 paths
remained absent. Under the already reviewed pre-open grant rule, a fresh full
rebind and the exact whitespace repair left the create grant unused; the
corrected wrapper then created only exact V9 with durable create-new write,
readback, reverse equality, and parser closure.

Static attempt 10 is consumed. Slots 1-8 each passed in its own isolated
Windows PowerShell 5.1 process: exact V9 byte/format/parser/reverse/authority/
label/canonical/UUID/finalizer checks; 124 helpers, base28 at 26,352,787 bytes,
thirteen preimages and C2 29/26,758,493; all four preserved binders; all
15 retry-26 and five retry-24 adversarial rejections; and the complete first
protected binding with scalar event count zero. After normalizing only label
and capture timestamp, slot 8's whole-state canonical SHA-256 was
`d8cbec0b9a41f0b8629eeb35daf19f5d23c4011d3fce650040316eaa3bc40e3a`.

Before slot 9 launched, the outer wrapper builder attempted to inject that
digest assertion into prior source. Its generated source had malformed quote
boundaries; `[ScriptBlock]::Create` raised a parse exception at the digest.
No slot-9 PowerShell process launched, no slot-9 result exists, and no move or
post-move action occurred. Slots 1-8 are partial evidence only and are not
reusable. This is orchestration evidence-incomplete, not a V9 rejection,
operational RED, qualification result, or success.

V9 remains exact, final absent, main clean, retry-26 untouched, C2 unchanged,
and related survivors/transients zero. All older RED/nonterminal/static
evidence remains immutable.

All plane-separation and no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact V10 derivation

V2-V9 are immutable evidence. After this C0 is reviewed and committed alone,
repeat the complete protected rebind and create only V10 assembly/final:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1`.

Derive V10 only from exact V9: change V9 self leaves to V10; bind this
committed C0's exact path, commit, byte length, and SHA-256; add V9 assembly
once to `BaseC2Names` and its exact SHA above once to `C2Preimages`; and change
only the base assertion from 28 files/26,352,787 bytes to 29 files/26,758,493
bytes. The two V9 evidence rows add 134+66 bytes. The three V10 self leaves add
three bytes. Authority paths and four-digit byte literals are equal-width. V10
is therefore exactly 405,909 bytes, 4,770 LF, zero CR, terminal LF, no BOM,
and 63,471 tokens/zero parser errors. Prove exact counts,
two residual V9 evidence rows, reverse byte equality, create-new sequential
bounded writes, durable flush, close, and readback.

Before opening V10, require clean committed main, C2 29/0/26,758,493, exact
V2-V9, both V10 paths absent, exact governance/retry-26/admin/manifests/lock/
FIC/frozen residue/runtime bindings, all fresh paths absent, and process/event/
job/transient state zero. Any mismatch stops without write.

## Exact static attempt 11

Authorize one fresh ordered nine-slot attempt 11 over frozen V10; no script
main and no operational mode. Every slot is a separate strict-mode Windows
PowerShell 5.1 process with a 600,000-ms ceiling and fail-closed consumption.
Every slot has the full initializer. Slots 1-7 repeat the complete parent
checks, updated only for exact V10/base29/fourteen-preimage/total30 values.

Slots 8 and 9 each repeat the complete protected binding, require scalar
`[int]$state.events_seen -eq 0`, normalize only label and capture timestamp,
and compute the whole-state canonical SHA-256. Slot 9 must equal slot 8 and
also bind V2-V9, base29, same volume, and final absence.

The slot-9 wrapper must be authored directly as one literal here-string after
slot 8 reports its digest. Dynamic source extraction, prior-command replay,
`.Replace` injection, concatenated assertion source, or generated quote
boundaries are forbidden. The caller must parse that exact direct source in
memory before launching it; this is source preparation, not an additional
process slot. The direct source contains the observed digest once as a quoted
data literal and the `Assert-Capture` call as ordinary source text.

Only after slot 9 PASS, with no intervening command except result assertions,
publish by non-overwriting two-argument move to final V10. Post-move bind final
and V2-V9, prove sibling absent, repeat parsers/helpers/fresh state/scalar
events/`Assert-FrozenBindings`, and runtime base29+final closure with only
prospective `generation-structural` set.

Three independent exact-byte B0/M0/m0 final-V10 reviews and a complete rebind
precede mode. Then only inherited retry-26/admin removal and retry-30/31
attempts-045/046 may proceed. C2 gates are 30 post-review, 38 pre-cleanup, and
34 final.

## Review and ledger

This exact C0 requires independent exact-byte B0/M0/m0 review. Commit only it
with parent `a9acdee0e66d9cf714a26d97cb52dcbfb95a8d16`. Prospective population:
769 tracked, 762 included, zero plugin/ZIP, six `scripts/analysis/`, one
manifest; 40,077-byte included-path payload SHA-256
`c76a0ada55e52e398bdcdc9820faf9abfb766ad498dbf23be3c49383565914c6`.
Do not push/tag.

Writable paths are only this C0/isolated commit, then the sole V10 pair, then
review-gated inherited removal/qualification ledgers. Preserve every prior
artifact and every other plane. Any failure stops. Gate 6 remains closed.
