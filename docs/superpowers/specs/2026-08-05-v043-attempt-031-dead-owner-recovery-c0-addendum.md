# v0.43 attempt-031 dead-owner recovery C0 addendum

**Date:** 2026-08-05  
**Status:** Approved C0 correction authority only when these exact bytes receive
three independent B0/M0/m0 reviews and are committed alone on `main`; no
qualification, version, package, archive, cache, catalog, consumer, shipment,
host, research, activation, acceptance, promotion, release, or canon claim  
**Run scope:** `full_lifecycle` (source-qualification lifecycle only; no
academic-deliverable or terminal-paper claim)

## Governing authority and preserved stop

This correction is subordinate to the current user instruction, the finalized
research-governance chain, repository `AGENTS.md`, and the complete committed
v0.43 C0 chain. The governing bindings at freeze are:

- installed policy
  `B:\Agents\research\10_Governance\MASTER_GOVERNANCE.md`: 77,262 bytes,
  SHA-256
  `a55b56eee7d72b4aa8a7f2376cb023d987ee7c12e71611fef7a9b457d780e191`;
- installed amendment record
  `B:\Agents\research\10_Governance\MASTER_GOVERNANCE_AMENDMENT_RECORD_1.0.9.md`:
  12,436 bytes, SHA-256
  `201d9b68e9c050f0f1e937977833868dd8ec2e3c5d6395f044fc00103fc95afe`;
- current version/status: Master Governance v1.0.9, effective
  `2026-08-04T20:00:35.9192246-04:00`;
- the v1.0.10 amendment program is closed `REJECT / STOP`; its round-2
  rejection receipt is 8,505 bytes, SHA-256
  `e176f00f57c6242752bb58791aa48c97e4eb522ea27911c32f42401563868c05`;
- Overseer status index: 773 bytes, SHA-256
  `b6b2fddedf9f364308dd6d54375c4d3a58595c3b7f9ed332453605373d9f00f9`,
  status `clear`, risk `low`, `requires_user=false`;
- immediate parent C0
  `docs/superpowers/specs/2026-08-05-v043-attempt-029-env-delta-rejection-c0-addendum.md`:
  11,198 bytes, SHA-256
  `ea19fd86a9d750ba09c112f5b1b4ccd782f53c634d7a746491b958587e863ce1`,
  committed as `d818f74b291680f842aedd698ea459d87b3145c3` with tree
  `c6a378014af65a650c96d0bc0ad06f02e9229de0`;
- its parent C0 is 14,546 bytes, SHA-256
  `ad40e2edb8a1b60b6e28be973e656ab8b94bbdcf492f6e37ae74c88030309cd7`,
  committed as `82dffdd500b22caa5429abba93fd2dc104df5ee8`.

The precedence used here is: applicable external degree envelope, the current
user instruction, installed Master Governance v1.0.9, applicable
venue/advisor/project authority, D-STYLE, then instrument defaults. Plane-I
instrument evidence has no Plane-R acceptance or promotion authority. The
current instruction expressly requests a new narrow C0 for this recovery; it
does not authorize any downstream plane omitted from this document.

The parent authorized manifest replacement only after terminal attempt-031
success. That condition failed. Nothing in the parent authorizes recovery
publication, manifest restoration or copying, residue or lock deletion,
retry-16 removal, retry-17 creation, attempt-032 launch, or C2 modification.
This addendum supplies only the exact replacement authority below. Every other
parent invariant remains binding.

## Startup catalog and separated planes

Before any package command, this fresh task captured `codex plugin list`:
127,689 UTF-8 bytes, SHA-256
`87327d0d94aedffeb6dc08047d19bf553312c37d6b220bd8256c38a06840a8be`.
It reported 38 installed/enabled plugins. The co-author entry was
`co-author-harness-claude@joseph-chung-co-author-harness-local`, catalog version
`0.42.0`, with catalog path
`C:\Users\young\.codex\local-marketplaces\joseph-chung-co-author-harness-local\plugins\co-author-harness-claude`.
The cache skill root was
`C:\Users\young\.codex\plugins\cache\joseph-chung-co-author-harness-local\co-author-harness-claude\0.42.0\skills`,
containing 43 `SKILL.md` files. This task loaded zero co-author `SKILL.md`
files; no component skill path is claimed as loaded.

The planes remain independent:

| Plane | Frozen observation | Claim limit |
|---|---|---|
| source | clean saved `main` at `d818f74b291680f842aedd698ea459d87b3145c3` before this candidate | only source state |
| Git/admin | one `main` branch plus detached retry-16; exact admin state below | no release/admin clearance |
| startup catalog / CLI registration | installed/enabled catalog entry `0.42.0` at the paths above | discovery only |
| source archive | `.claude-plugin/co-author-harness-claude.plugin`, 3,078,477 bytes, SHA-256 `2ad811638f1db89bb581e6524cdbe1732a63a85353db046c45950b4c862bfb15`; embedded plugin manifest reports historical `0.41.0` | stale separate archive; no package claim |
| installed cache | cache root `...\0.42.0` exists; root `PROVENANCE.json` is absent | no provenance or cache-equality claim |
| loaded paths | no co-author skill was loaded by this task | no loaded-runtime attestation |
| consumer | v0.43 verification currently has only C0-C2; no v0.43 consumer receipt or research content hit binds this source | Gate 6 closed |
| host qualification | not run and forbidden in this task | no host claim |
| activation | not run and forbidden in this task | no activation claim |

Source `.claude-plugin/plugin.json` remains version `0.42.0`, 674 bytes,
SHA-256
`25f801100daecb1539208b78cf550e99c200dadfb1344c083386b5e306d1fa60`.
The v0.43 label in this authority is a candidate qualification identifier, not
a current-version, packaged-version, installed-version, or activated-version
claim.

## Preserved transaction history

Every existing transaction directory remains immutable except the three exact
attempt-031 recovery path changes and bounded atomic temporary siblings
authorized below. In particular:

| Attempt | Preserved classification | Receipt bytes | Receipt SHA-256 |
|---|---|---:|---|
| 017 | `child_failed`, child exit 1 | 2,426 | `527b0059b4343faafc817b275305d323b5bc8b208f3a1a0eee756683267e5eeb` |
| 019 | `refused`, `RELEASE-CONTROLLER-OUTPUT-SCOPE` | 3,498 | `94555709f8acba70fe917002badc9a54dd46bcaa85d1b928f2eba11545644d34` |
| 021 | historical `succeeded` | 367,297 | `813536afebedc56ea1195221051d81be98b7e516fa652f81ea5a0b6f2697195a` |
| 022 | historical `succeeded` | 367,535 | `3d323e25f4ff6f650a7845c0f4fcf19275798c05296dcc94aba882b810dc420a` |
| 023 | `child_failed`, child exit 1 | 367,551 | `e6e00b5612cb4a23d75f5d1fd3bb529cab0d0d35f77b3d70508b1ffc88764d2f` |
| 024 | historical focused `succeeded` | 367,781 | `5f74b479859804e89ef1249035e58148f1cba412c968dd91e20a06bad641c028` |
| 027 | historical generation `succeeded` | 368,031 | `537377d4574d3293243fbe035884dc35e90a6ba5a96929ee4dcb2fa7eedc1722` |
| 028 | historical clean replay `succeeded` | 368,044 | `bc9b24c30a718912f10366edeb55f36d1c75aeddf99e64e2350ade217fa04c5a` |
| 029 | consumed pretransaction `QUALIFICATION-ENV-DELTA` refusal; path absent | absent | not applicable |

Historical success is not current qualification. Attempt 029's absent-path
semantic refusal is not upgraded to a transaction. Attempts 030 and retry-15
remain retired and absent. All attempt 001-028 directories and receipts not
enumerated in the table also remain untouched historical evidence.

## Attempt-031 dead-owner freeze

The launcher supplied the attempt directory as `--run-root`; the controller
then appended `run_id`. The authorized outer path therefore contains an
unlisted same-name nested transaction:

`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-031\coauthor-v043-qualification-20260805-attempt-031`

The outer and nested directories are ordinary directories, not reparses. The
nested transaction has exactly these current files:

| File | Bytes | SHA-256 |
|---|---:|---|
| `intent.json` | 551,030 | `cce9178b3578062912b651ceaa44dffeed821fd8d8eeb442bc8fe4c0ac7346d4` |
| `journal.json` | 583 | `2ebe43ca43194f8d1b5a2950a6912ec0df082a25f3a6696f5b9ef98cba35e796` |
| `owner.json` | 113 | `3244fbcefc31ae1885b2d4d501b16b6578a707c01fc31d1ef04b0ab8c2fe289f` |
| `stdout.bin` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `stderr.bin` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `worker-stdout.bin` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `worker-stderr.bin` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

`receipt.json`, `exit.json`, `prechild-refusal.json`, `cancel.request.json`,
`worker-failure.json`, and `.controller.lock` are absent. The journal remains `running`;
its last event is `child_spawned` at `2026-08-05T15:01:41.205499Z`.
Worker PID 43232 and child PID 45128 are dead, and a process census excluding
the observation probe found zero live process whose command line names
attempt 031 or retry-16.

The canonical intent SHA-256 is
`480c46a0b1f20a3c7ca64135c5e2b3f5f477b54483086cfaebb64d8d49f8867a`;
request SHA-256 is
`e76ac90198bed7ca4c8a2b2f2c083c213c671016d55ea0e2ea6b44b04c35244c`;
the environment delta is exactly
`{"PYTHONDONTWRITEBYTECODE":"1"}`. The primary input root still matches its
1,863-entry binding at SHA-256
`7288650dbda46e50741bdcb6a6a0521f192bdecccc276c40799e0677cde03e49`.
All six explicit inputs match their recorded byte lengths and SHA-256 values.

The four zero-byte streams are placeholders, not terminally published stream
evidence. There is no exit status, terminal success, or qualification result.
The exact controller source proves that a dead-worker recovery with no exit
capsule can publish only `state=evidence_incomplete`, diagnostic code
`EVIDENCE_INCOMPLETE`, `exit=null`, and `recovered=true`. Recovery cannot turn
this transaction into a pass.

## Retry-16, lock, manifest, and residue freeze

Retry-16 is detached at the same HEAD/tree and has 491 porcelain rows under
`git --no-optional-locks status --porcelain=v1 --untracked-files=all`:

- one tracked deletion:
  `docs/analysis/generated/fixture_manifest.json`;
- 490 untracked regular files beneath
  `assignment-terminal-close-ce01lxnu`, totaling 1,106,077 bytes;
- no discovered reparse entry.

Their independently reproduced deterministic LF file inventory has 490 rows
and 89,452 UTF-8 bytes at SHA-256
`7b158b08f7875a710be862b78d86b7a2bc5e0648fe8653d979f491a441cacc80`.
The algorithm uses Python 3.14 `pathlib` enumeration of
`assignment-terminal-close-ce01lxnu.rglob('*')`, retains regular files, renders
each path relative to retry-16 with POSIX separators, sorts those rendered
paths by Python ordinal string order, and emits exactly
`path|byte_length|lowercase_sha256` per row. Rows are joined by LF with one
terminal LF and encoded as UTF-8 without a BOM. Culture-sensitive PowerShell
sorting or another delimiter is nonconforming.

For the exact current controller canonical-inventory algorithm
`_inventory([root], root / '.controller-excluded-impossible')` followed by
`_canonical()`:

- the launch output preimage in `intent.json` is 928 entries: 174 directories,
  754 files, zero reparses, 10,755,351 file bytes; canonical bytes SHA-256
  `0ff93e0163ae7f5e24a62ad2f2746bf5671c292c56cae47b4f3e9253f051fb23`;
- the current full retry-16 inventory is 1,581 entries: 338 directories, 1,243
  files, zero reparses, 11,807,168 file bytes; canonical bytes SHA-256
  `4ffdc793074e10b71e25ec90bd18375e2d2afbc9efc735fe7476419ddf730e01`;
- the current `assignment-terminal-close-ce01lxnu` descendant inventory is
  653 entries: 163 directories, 490 files, zero reparses, 1,106,077 file
  bytes; canonical bytes SHA-256
  `d026a294005779a26506b9e7a2ad6e6aec67a9c115f3f3fc5b2f274f91db1b13`.

The retry-16 admin `index` is 95,383 bytes, SHA-256
`eed3b932ee24f67f1c8ea5b5ba97efaf1e917eab7ff90a4e72e87f854004603e`;
`index.lock` is absent. Admin `HEAD`, `commondir`, and `gitdir` still match the
attempt intent. The primary manifest remains present and unchanged at 54,260
bytes, SHA-256
`1f6eb7fd26f1b0417c6af946a5ddfcf4800e268097ace6831ed4ad62c44f9f2a`.
The absent retry manifest must not be restored or copied.

The shared runner lock
`B:\Agents\platform\co-author-harness\.git\coauthor-fixture-runner.lock` is
66 bytes, SHA-256
`7a6aed96ac8eaca93f54eab26b17b427099991c96fc57b06a2c782c19c5c9183`,
containing exactly
`pid=45128 worktree=B:\Agents\.coauthor-v043-c2-detached-retry-16` plus
CRLF. PID 45128 is dead.

The sole frozen Aug-3 residue remains
`C:\Users\young\AppData\Local\Temp\release-controller-smoke-q7vjzhmm`:
114 identities including root, 34 directories, 80 files, zero links, 45,541
file bytes; its 8,637-byte en-US PowerShell 5.1 rendered-row LF inventory is
SHA-256
`3bd3e82ea9b0441ffb556aafa67d6a625dc376665889c6cc1bb18de8342e7f40`.
It remains the sole `release-controller-smoke-*` directory and is immutable.

The primary worktree is clean on `main`. Its index is 95,383 bytes, SHA-256
`f003822a98bdb315c6bae414a8ebc52e94e01fdbafc7acba10e9946dd2a66392`;
primary `index.lock` is absent. Retry-17/admin and attempt 032 were never
started. They are retired, must remain absent, and carry no intent, execution,
or evidence claim. Retry-16 and attempt 031 are consumed interruption labels
and must never be reused.

The four present C2 evidence files remain ignored/local at their exact parent
preimages:

| Path | Bytes | SHA-256 |
|---|---:|---|
| `releases/verification/v0.43.0/C2/C2_SOURCE_QUALIFICATION.md` | 2,034 | `cae9a2ef7bc88f0ee02e4c79c2b108888894b1a5f5d16def6a0afbd9e889d243` |
| `releases/verification/v0.43.0/C2/C2_STRUCTURAL_RESULTS.json` | 634 | `5556b373de0cfa7f209a0afa915620569e33515079f5bf5c00f1f8310f82998e` |
| `releases/verification/v0.43.0/C2/C2_REGISTRY_RESULTS.json` | 2,254 | `957a91428ef875097678d62590c58baae86988732d2c3a7be54b13c35b0ce9de` |
| `releases/verification/v0.43.0/C2/C2_DETACHED_REPLAY.json` | 2,590 | `38862b17278643de65e6784bb166f22998c6f6c9f5db35a3e74da38d0061cb49` |
| `releases/verification/v0.43.0/C2/C2_READ_ONLY_REVIEW.md` | absent | not applicable |

They are not writable until both new qualifications pass. They must never be
staged, force-added, or committed.

## Exact writable, cleanup, and transient ledger

Only these tracked or ignored/local evidence changes are authorized, in order:

| Action | Path | Preimage |
|---|---|---|
| CREATE AND COMMIT ALONE | `docs/superpowers/specs/2026-08-05-v043-attempt-031-dead-owner-recovery-c0-addendum.md` | absent |
| GENERATION OUTPUT; REPLACE AND COMMIT ALONE AFTER ATTEMPT 033 SUCCESS ONLY | `docs/analysis/generated/fixture_manifest.json` | 54,260 bytes; SHA-256 `1f6eb7fd26f1b0417c6af946a5ddfcf4800e268097ace6831ed4ad62c44f9f2a` |
| MODIFY AFTER ATTEMPTS 033 AND 034 SUCCESS ONLY; NEVER STAGE OR COMMIT | the four present C2 paths above | exact table preimages |
| CREATE AFTER ATTEMPTS 033 AND 034 SUCCESS ONLY; NEVER STAGE OR COMMIT | `releases/verification/v0.43.0/C2/C2_READ_ONLY_REVIEW.md` | absent |

After exact-byte review and the addendum-only commit, these attempt-031
transaction changes are authorized once:

| Action | Exact nested path | Preimage |
|---|---|---|
| MODIFY BY CONTROLLER RECOVERY ONLY | `journal.json` | 583 bytes; SHA-256 `2ebe43ca43194f8d1b5a2950a6912ec0df082a25f3a6696f5b9ef98cba35e796` |
| CREATE BY CONTROLLER RECOVERY ONLY | `receipt.json` | absent |
| CREATE AND RETAIN AS CONTROLLER LOCK FILE | `.controller.lock` | absent; recovery creates exactly one NUL byte, SHA-256 `6e340b9cffb37a989ca544e6bb780a2c78901d3fb33738768511a30617afa01d` |

Controller `_atomic()` may create only temporary siblings matching
`.journal.json.<decimal_controller_pid>.<32-lowercase-hex>.tmp` and
`.receipt.json.<decimal_controller_pid>.<32-lowercase-hex>.tmp` in the nested
transaction directory while publishing those two files. They must be absent
before recovery and leave zero survivors afterward. Every other attempt-031
byte must remain identical. Recovery may be declined only by stopping with the
transaction preserved: no cleanup or qualification step below is authorized
unless recovery publishes exactly
`evidence_incomplete / EVIDENCE_INCOMPLETE`. Any other result is red and stops.
The outer and nested transaction directories and all recovered evidence are
preserved permanently afterward.

Only after that recovery result may Git worktree management force-remove the
exact dirty retry-16 worktree and its exact canonical admin subtree while
preserving the stale runner-lock preimage byte-for-byte:

- `B:\Agents\.coauthor-v043-c2-detached-retry-16`;
- `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-16`.

The removal is authorized only while the full worktree, residue, admin index,
Git status, dead PIDs, manifest absence, primary manifest, stale runner lock,
and frozen Aug-3 residue match the bindings above. It intentionally deletes
the 490-file interrupted scratch residue; it does not delete or reinterpret
attempt 031. No manifest byte may be restored from retry-16. Prove both retry
paths absent, the stale runner-lock bytes unchanged, and main
HEAD/tree/index/status unchanged after removal.

Only these fresh paths may then be created:

- generation worktree `B:\Agents\.coauthor-v043-c2-detached-retry-18`;
- generation admin subtree
  `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-18`;
- generation transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-033`;
- clean-replay worktree `B:\Agents\.coauthor-v043-c2-detached-retry-19`;
- clean-replay admin subtree
  `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-19`;
- clean-replay transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-034`.

Every path is absent at freeze and must be proved absent immediately before
creation. The applicable admin `index` is the sole persistent admin byte
allowed to transition during a structural batch; only its same-admin
`index.lock` may exist transiently, and it must be absent before and after.
The final structural command may call `Path.touch()` on the already-present
shared runner lock, changing only filesystem metadata. On retry-18 its frozen
66 content bytes must remain identical. The stale lock is deleted only after
retry-18 structural postflight and immediately before attempt 033. Attempt 033
then creates a new dynamically sized lock binding whose exact bytes are frozen
after its child exits. Retry-19's final structural command may touch metadata
only while preserving those actual post-attempt-033 bytes; it does not inherit
the 66-byte retry-16 constraint. Shared runner-lock content may otherwise
transition only as the separately bound lock output of attempts 033 and 034.
No other admin or repository transient is allowed.

Ordinary Git objects, the primary index, the `main` reflog, and
`refs/heads/main` may change only as Git-managed consequences of exactly two
commits: this addendum alone and the later manifest alone. Record commit SHA,
tree, parent, exact changed-path set, and primary-index pre/post digest for
each. No branch creation, push, tag, package, archive, install, cache/catalog,
research, shipment, host, or activation mutation is authorized.

## Exact recovery and qualification sequence

1. Obtain three independent exact-byte B0/M0/m0 reviews of this addendum.
   Any changed byte invalidates every review.
2. Rebind governance, startup record, main, attempt 031, retry-16/admin,
   process census, all six inputs, primary inventory, runner lock, primary
   manifest, C2 preimages, frozen residue, retired paths, and fresh-path
   absences. Commit only this addendum on `main`; record exact Git effects and
   prove clean status.
3. To continue beyond preservation, recovery is required. Use the exact
   interpreter with `-B`, remove the five forbidden ambient Python controls,
   and call the controller `recover` with:

   ```text
   --run-root C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-031
   --run-id coauthor-v043-qualification-20260805-attempt-031
   ```

   This outer run root deliberately resolves the already-created nested
   transaction. Require recovered receipt state `evidence_incomplete`,
   diagnostic code `EVIDENCE_INCOMPLETE`, `exit=null`, `recovered=true`, dead
   owner/child, no live related process, and exact binding to the unchanged
   intent/owner/four streams. Bind the new journal and receipt paths, lengths,
   and SHA-256. Require the unlocked retained `.controller.lock` file to be
   exactly one NUL byte at SHA-256
   `6e340b9cffb37a989ca544e6bb780a2c78901d3fb33738768511a30617afa01d`.
   Require both permitted atomic temporary siblings to leave zero survivors.
   Never call this success or qualification.
4. Rebind the exact dirty retry-16 inventory and force-remove only
   retry-16/admin through Git worktree management while preserving the exact
   stale shared runner lock. Prove retry-16/admin absence and preservation of
   attempt 031, main, the runner-lock bytes, primary manifest, C2, and frozen
   residue.
5. Create retry-18 detached at the exact addendum commit. Bind HEAD/tree,
   canonical admin ID, admin `HEAD`, `index`, `commondir`, `gitdir`, clean
   status, primary refs/index, environment, primary input-root inventory, six
   explicit inputs, the exact stale runner-lock preimage, primary and retry
   manifests, frozen residue, smoke set, process census, and fresh-path set.
6. On retry-18 run these exact 15 commands sequentially with interpreter
   `C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe` and
   explicit `-B`, capturing each exit and stdout/stderr length and SHA-256 in
   memory:

   `scripts/skill-check.py`, `scripts/schema_runtime_check.py`,
   `scripts/version-check.py`, `scripts/distribution-rights-check.py`,
   `scripts/catalog-check.py`, `scripts/command_surface_check.py`,
   `scripts/path-hygiene-check.py`, `scripts/snippet-check.py`,
   `scripts/output_economy_check.py`, `scripts/version-planes-check.py`,
   `scripts/commitment-interactions-check.py`,
   `scripts/phase_engagement_check.py`, `scripts/retirement-sweep-check.py`,
   `scripts/destination-coverage-check.py`, and
   `scripts/analysis/fixture_infrastructure_check.py`.

   Require 15/15 exit zero. The first 14 stderr streams must be zero bytes.
   Only the fixture-infrastructure command may emit the registered 805-byte
   negative-test stderr at SHA-256
   `9cd7690903b6b91f5a6183083883ffe9dadd4241bed165e6b2ca386416059ee2`.
   Bind full ignored-inclusive worktree inventories, HEAD/tree, every admin
   input, primary planes, runner lock, manifest, frozen residue, smoke set,
   processes, and fresh paths before and after. Only retry-18 admin `index` may
   transition; same-admin `index.lock` must be absent pre/post. The final
   fixture-infrastructure command may perform only the registered metadata
   touch on the existing runner lock; its 66 bytes and SHA-256 must remain
   exact. Any other byte, warning, stderr, output, residue, or survivor is red
   and stops.
7. After structural postflight, rebind and delete only the exact stale shared
   runner lock, prove it absent, then freeze retry-18's index and rebind all six
   inputs. Start attempt 033 with the exact controller `--run-root`
   `C:\Users\young\AppData\Local\Temp` and the separate `--run-id`
   `coauthor-v043-qualification-20260805-attempt-033`. Controller and child use
   the exact interpreter with `-B`; `--allow-user-site` is present; no caller
   `--env` or ignored output is supplied; the five forbidden ambient Python
   controls are absent. Use primary as sole input root, retry-18 as sole watch
   root, retry-18 manifest as sole allowed output, and child:

   ```text
   <python> -B <retry-18>\scripts\analysis\fixture_runner.py
     --tier full --cache-mode off
   ```

8. Attempt 033 must terminally succeed with diagnostic null, exit zero, exact
   93/93 suites and 97/97 cases, exact case order and 93/93 source hashes, zero
   warning/failure rows and cache hits, stable 747-file tested population,
   stable canonical/raw population digests, stable primary input root, stable
   six inputs and frozen retry index, exactly one output change at the retry
   manifest, exact receipt/stream bindings, a separately bound runner-lock
   transition, frozen-residue equality, no new smoke residue, and zero owned
   processes. Its receipt must record exactly
   `{"PYTHONDONTWRITEBYTECODE":"1"}` and no other environment-delta key. Any
   rejection, evidence-incomplete state, warning, mismatch, ambiguity, extra
   output, residue, index change, or survivor is red and stops.
9. Only after complete attempt-033 evidence capture may its exact manifest
   replace primary. Prove byte equality, commit that manifest alone, record
   exact Git effects, then force-remove dirty retry-18/admin only after the
   manifest is safely committed. Preserve attempt 033.
10. Bind attempt 033's resulting shared runner lock by exact path, byte length,
    SHA-256, and raw content. It must contain
    `pid=<attempt-033-child-pid> worktree=B:\Agents\.coauthor-v043-c2-detached-retry-18`
    plus CRLF; the recorded PID must equal the attempt receipt's child PID,
    that PID must be dead, and no OS lock may remain held. Its length and
    SHA-256 are dynamic because PID width is not predetermined. Retain that
    exact file. Create retry-19 detached at the exact manifest commit. Repeat
    the complete preflight and 15-command structural batch, including
    index/index.lock, inventories, streams, residue, smoke, and process gates.
    The final fixture-infrastructure command may touch only runner-lock
    metadata; the dynamically bound post-attempt-033 content bytes, length,
    and SHA-256 must remain identical through the batch. Freeze the
    post-structural retry-19 index and rebind all six inputs. Retain the lock
    until attempt 034 acquires and rewrites it.
11. Start attempt 034 with controller `--run-root`
    `C:\Users\young\AppData\Local\Temp`, separate fresh run ID
    `coauthor-v043-qualification-20260805-attempt-034`, no allowed output,
    retry-19 as sole watch root, the same environment and inputs, and child:

    ```text
    <python> -B <retry-19>\scripts\analysis\fixture_runner.py
      --tier full --cache-mode off --no-write
    ```

12. Attempt 034 must meet every non-output gate from attempt 033, reproduce
    attempt 033's exact 747-file canonical/raw digests, manifest case order,
    and 93/93 source hashes, create or change no output, preserve retry-19
    index and clean status, record exactly the same one-key environment delta,
    preserve all other planes, and leave zero owned process. Any mismatch is
    red and stops.
13. Only after both attempts pass may the five C2 paths be updated to preserve
    every historical red, refusal, warning, interruption, and success,
    including attempt 017, refused attempt 019, attempt 023 red, historical
    attempt 028 success, attempt-029 invocation rejection, the structural-index
    stop, and attempt-031 `evidence_incomplete` interruption. Attempt 034 is
    the current clean replay. Obtain three independent exact-byte B0/M0/m0
    reviews of the complete five-file set; bind every pre/post length and
    SHA-256. Never stage, force-add, or commit C2. Then remove clean
    retry-19/admin and prove absence while preserving attempts 031, 033, and
    034.

## Downstream stop

Attempt-034 success and reviewed local C2 evidence, if reached, authorize only
completion of source qualification under this C0. They do not qualify the
existing archive, installed cache, startup catalog, CLI registration, loaded
paths, consumer, host, research, shipment, or activation planes.

Gate 6 remains closed because no independently governed v0.43 consumer
compatibility receipt binds the exact resulting source. This task may not
author, simulate, copy forward, or reinterpret such a receipt. Version change,
package/archive creation, cache or catalog mutation, install/uninstall,
package clearance, push, tag, assets, shipment, fresh-host qualification,
research mutation, and activation remain closed.
