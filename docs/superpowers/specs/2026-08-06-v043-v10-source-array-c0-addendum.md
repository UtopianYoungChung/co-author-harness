# v0.43 V10 attempt-12 source-array failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v10-slot7-parser-c0-addendum.md`,
committed alone at `7b882cfb683ea27172a310cc45e2796921953d6d`, tree
`2b2a190cdd116eb1671a3197af343c119b4c6069`, parent
`2739713419804c6b6395b68792ef63d800ff6287`, 5,845 bytes, SHA-256
`3694031c42187c1309207272f9932e5523edf5aa3db68d96fc7764fbaa2831fb`,
with independent exact B0/M0/m0 approval.

Exact V10 assembly remains
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1.assembly.tmp`,
405,909 bytes, SHA-256
`fd9562174f3fced07f69e09b30c2bb3fff5e764d602ebe61f86e349d6356f076`,
4,770 LF, zero CR, terminal LF, no BOM, and 63,471 tokens/zero errors at
both parsers. Final V10 remains absent. C2 remains 30 files, zero directories,
and 27,164,402 bytes. Main is clean. Retry-26 remains protected clean/detached
at `91ad31ae250dd9600ef85f330e8d4acaef36da8e`, tree
`4c0c00527b4dba2bd3edd56d6d3f58ab5a4fb358`.

Static attempt 12 is consumed before slot 1. Its caller constructed the eight
pre-slot sources with the unparenthesized PowerShell expression
`@($common+$b1,$common+$b2,...,$common+$b8)`. PowerShell applied the comma
expressions as array operands to string addition; the result was one scalar
string containing the eight intended values space-joined. The all-source
preparse loop parsed index zero and then threw `IndexOutOfRangeException` when
it requested index one. No slot child launched. No slot result, slot-9 source,
move, post-move action, operational mode, cleanup, or qualification action
occurred. A post-failure census found zero related live processes; V10 assembly
retained its exact binding, final V10 was absent, C2 remained 30/0/27,164,402,
and main remained clean.

This is caller-side static orchestration evidence-incomplete, not a V10
rejection, operational RED, qualification result, or success. Preserve it with
attempts 11 and all earlier RED/nonterminal evidence. All plane separation and
claim ceilings remain. No push, tag, install, cache/catalog/research mutation,
package, archive, shipment, consumer attestation, host qualification, or
activation is authorized. Gate 6 remains closed.

## Exact static attempt 13

After exact B0/M0/m0 approval and an isolated commit, authorize one fresh
ordered nine-slot attempt 13 over immutable V10; no script main and no
operational mode. Repeat every attempt-12 slot obligation from zero. Slots 1-8
must be complete sources in caller memory before slot 1; each must parse with
zero errors under Windows PowerShell 5.1. Each slot remains a separate strict
Windows PowerShell 5.1 process with a 600,000-ms ceiling. Any failure consumes
attempt 13 and stops.

Change only the caller's source collection mechanics. Do not use a comma inside
an addition expression. Create an empty typed
`System.Collections.Generic.List[string]`, append exactly `$common + $b1`
through `$common + $b8` in eight separate ordinary statements, require every
`Add` return to be suppressed, require exact list count eight, and bind the
UTF-8 byte length and SHA-256 of each complete source before parsing. Parse all
eight exact list entries before slot 1, retaining each zero-error parse result
and source binding. Require all eight launches to consume those same exact
bound entries in index order. This grants no source file, environment payload,
helper file, or durable static-result file.

The slot-7 caller repair remains exactly `foreach ($m in $mutators)`, with the
old compressed header once before repair, zero after, the corrected header once
after, a zero-error complete-source parse, and exact reverse equality. Slot 7
must report exactly fifteen retry-26 and five corrected retry-24 PASS
rejections. Slots 1-6 and 8 retain every parent requirement, including both
parsers, V9-to-V10 reverse derivation, finalizer/UUID checks, 124 helpers,
base29, fourteen preimages, total30, every preserved RED binder, full protected
state, scalar event zero, and normalized whole-state digest.

Slot 9 remains nonexistent until slot 8 returns its digest. Then author slot 9
directly as one literal here-string in a new command, parse that exact literal,
require the slot-8 digest exactly once as a quoted data literal in ordinary
`Assert-Capture` source, and launch it once. Source extraction/replay,
replacement injection, concatenated assertion source, and generated quote
boundaries remain forbidden. Slot 9 repeats the full protected state, equals
slot 8 after normalizing only label and capture timestamp, and additionally
binds V2-V9, base29, same volume, and final-V10 absence.

Only after slot 9 PASS, with no intervening command except result assertions,
publish by non-overwriting two-argument `[IO.File]::Move` from assembly to final
V10 and complete full post-move validation. Then obtain three independent
exact-byte B0/M0/m0 final-V10 reviews and perform a new complete protected
rebind before operational mode. Inherited retry-26/admin removal, retry-30/31,
and attempts-045/046 remain review-gated. C2 gates remain 30 post-review, 38
pre-cleanup, and 34 final.

## Review and ledger

This exact candidate requires independent exact-byte B0/M0/m0 review and an
isolated one-file commit directly on main. Reviewers must bind the parent,
attempt-12 failure and nonlaunch, immutable V10/protected state, exact typed-list
repair, all nine slots, downstream sequence, prospective population, claim
ceiling, and Gate 6 closure. Repair and repeat all three reviews on any finding.
Do not push or tag.

Prospective population is 773 tracked, 766 included, zero `.plugin`/`.zip`, six
`scripts/analysis/`, and one fixture manifest. Its 40,361-byte
Python-Unicode-sorted UTF-8 included-path payload without terminal LF has
SHA-256 `9384832a01e323b5536c3a79c8283640d9f47454bf53f090c3ac7f6d29e3a595`.

Writable paths are only this candidate and isolated commit, then the existing
V10 pair through the authorized move, then review-gated inherited ledgers.
Preserve every prior artifact and every other plane. Every failure stops. Gate
6 remains closed absent independent v0.43 consumer evidence.
