# v0.43 C2 cross-worktree output-scope closure C0 addendum

**Status:** approved package-maintainer boundary for closing the cross-worktree
output ambiguity exposed after detached replay 010, repairing one nested Python
launch surface, and running one final source transaction plus one final
multi-root detached transaction. This addendum grants no package clearance,
shipment, cache mutation, consumer re-attestation, host qualification,
activation, research mutation, acceptance, promotion, release, or canon
authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-02-v043-c2-source-bytecode-recurrence-c0-addendum.md`.
All parent prohibitions and earlier failed/refused evidence remain binding. Any
path not enumerated here or in the parent ledgers requires another committed
addendum before it is created or changed.

## Frozen state

- Repository: `B:\Agents\platform\co-author-harness`.
- Branch: `main`, the only permitted branch.
- Commit: `d816118614802e9c5291a2180a411cc8aaa860d8`.
- Tree: `6b69e8626601775d48cad3d808409ecca2ae3e6d`.
- Both registered tracked worktrees are clean at that exact commit.
- Source-root attempt 009 terminally succeeded: 93 suites / 97 direct cases,
  cache off, zero cache hits, stable 719-file canonical and raw tested-input
  digests, manifest publication, exit zero, empty stderr, and zero live
  identities. Its receipt is 2,419 bytes, SHA-256
  `7aa0436f2ae8997a0b6bba3fb87df77b22caed434617718bf2b2091ee0182b3c`.
- The committed manifest is 54,259 bytes, SHA-256
  `03d429334b1d8dd4aae4cdd676c31b24c04f52158e5ffc0c178da7a4fd304c77`.
- Detached attempt 010 terminally succeeded over its declared watched root:
  93 suites / 97 direct cases, `--no-write`, cache off, zero cache hits,
  stable tested inputs, zero changed entries among the detached root's 900
  preimage entries, zero detached bytecode, exit zero, empty stderr, and zero
  live identities. Its receipt is 2,365 bytes, SHA-256
  `8708446d77c3607c08573a1723db73a97ac49a613db72da2a4dd506cf269be06`.
- Attempt 010 watched only
  `B:\Agents\.coauthor-v043-c2-detached-retry`. During its child interval,
  the primary worktree acquired
  `scripts/__pycache__/graph_authority_gate.cpython-314.pyc`, 2,060 bytes,
  SHA-256
  `49c48d1df4cba88a8c87274ff200cd73b4c8ba16bd547517954cb6e09f4f5600`,
  created and last written at `2026-08-03T04:15:37.7874607Z`.
- Excluding only that exact recurrence restores the governed 180-file
  bytecode digest
  `08c6740aab85ac2b73162dd0bb7c792815e2ba871dacb0decc3a070363085f2b`.
  The detached worktree remains at zero bytecode files and zero
  `__pycache__` directories.

Attempt 010 proves its declared detached watch root did not change. It does
not prove package-wide no-output because the primary worktree was outside the
watch set. The residue timestamp falls inside the attempt-010 child interval
and near the registered graph-authority suite's execution window, but this
addendum does not assert an emitter that frozen evidence cannot uniquely prove.
C2 therefore remains failed closed.

## Exact writable ledger

Only these tracked paths may change in this closure slice:

| Action | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE | `docs/superpowers/specs/2026-08-03-v043-c2-cross-worktree-output-scope-c0-addendum.md` | 0 | `ABSENT` |
| MODIFY | `scripts/graph_authority_gate_smoketest.py` | 5,241 | `41e22fc51161eb32d2d2365aedb679ffa1faec59b19e01dde9576f75a903e489` |
| QUALIFICATION OUTPUT ONLY | `docs/analysis/generated/fixture_manifest.json` | 54,259 | `03d429334b1d8dd4aae4cdd676c31b24c04f52158e5ffc0c178da7a4fd304c77` |

The already-authorized ignored C1/C2 evidence paths may be updated to bind
attempts 009 through 012. No controller, fixture-runner, kernel, version,
package, cache, registration, research, or remote path is writable here.

## Guarded cleanup and repair proof

After this addendum is committed and zero attempt-010 worker/product processes
are re-observed, only the exact primary-worktree bytecode file frozen above
may be removed. Its path, size, and SHA-256 must match immediately before
removal. Its parent directory must remain. Broad, recursive, globbed, or
best-effort bytecode cleanup is forbidden.

The graph-authority smoketest repair must:

1. expose the nested script argv through a deterministic helper and first add
   a red assertion that rejects the current missing `-B` position;
2. add explicit `-B` to every nested `sys.executable` script launch made by
   that helper, without weakening functional assertions;
3. pass with every ambient `PYTHON*` variable stripped while the outer test is
   launched with `-B`; and
4. prove exact bytecode-file and `__pycache__` topology equality across every
   registered worktree before and after the focused run.

Run the focused controller Windows/POSIX, graph-authority, fixture-
infrastructure, subprocess-text, schema-runtime, shell-syntax, and mechanical
root checks with the restored 180-file source baseline. Obtain independent
B0/M0/m0 review before committing the repair.

## Final transaction paths and gates

The only new transaction paths are:

- source-root attempt 011:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-011`;
- detached replay attempt 012:
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-012`;
- detached retry worktree:
  `B:\Agents\.coauthor-v043-c2-detached-retry-2`.

The preserved attempt-010 worktree may be removed only after its clean commit,
zero-bytecode postimage, receipt, and B1 review are bound. Each new path must
be absent before creation. Attempt 011 must use the durable controller, the
complete authoritative cache-off registry, and the primary worktree as its
watched root. Its manifest may be committed only on terminal success.

Attempt 012 must run the exact committed tree with
`--tier full --no-write --cache-mode off` and must declare **both** the new
detached worktree and the primary worktree as output watch roots. It permits no
package output in either root. Before and after the transaction, the primary
root must remain exactly 180 bytecode files at the governed digest, while the
detached root must remain at zero bytecode files and zero `__pycache__`
directories.

Any warning, refusal, incomplete evidence, source drift, cache hit, output in
either root, failed case, missing process closure, or review finding is a stop.
C2, version, package, cache, clearance, remote, shipment, fresh-host,
startup-catalog, loaded-path, consumer, activation, research, acceptance,
promotion, and canon claims remain closed until their separate gates pass.
