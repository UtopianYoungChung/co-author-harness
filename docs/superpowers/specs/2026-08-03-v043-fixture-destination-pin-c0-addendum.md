# v0.43 fixture destination-pin refresh C0 addendum

**Status:** approved package-maintainer boundary for resolving the exact
`PIN-DRIFT` produced by the authorized fixture process-tree repair. This grants
no C2 qualification, version, package clearance, shipment, cache mutation,
consumer re-attestation, host qualification, activation, research mutation,
acceptance, promotion, release, or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-03-v043-fixture-suite-tree-ownership-c0-addendum.md`.

The non-corpus structural battery passed through `retirement-sweep-check.py`
on the current authorized repair bytes, then `destination-coverage-check.py`
stopped with exactly two findings:

- `scripts/analysis/fixture_infrastructure_check.py`: `test_only` pin drift;
- `scripts/analysis/fixture_runner.py`: `package_confined` pin drift.

The classifications and their evidence remain accurate. The infrastructure
check still writes only disposable temporary/cloned test sandboxes. The runner
still confines its persistent write to the package manifest constant, with
`--no-write` suppressing that write; the new suite supervisor uses OS-managed
temporary streams and introduces no caller-selected persistent destination.

## Exact writable ledger

| Action | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE | `docs/superpowers/specs/2026-08-03-v043-fixture-destination-pin-c0-addendum.md` | 0 | `ABSENT` |
| MODIFY | `references/destination_coverage_registry.json` | 14,472 | `0113fe84aa5aaf621b2e2e592a6922082a0c4ea57a4be80f3e7614c2b5194fcb` |

After this addendum is committed, the maintainer may run the registry's
supported `--pin` operation once. The only permitted semantic changes are the
two SHA-256 values above, bound to the exact reviewed postimages. Any class,
reason/evidence text, other pin, ordering, or writer-census change is a stop.
Re-run destination coverage and the complete remaining non-corpus battery.
All qualification and later shipment gates remain closed.
