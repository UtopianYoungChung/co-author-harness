# v0.43 V5 slot-3 initializer failure - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and an isolated one-file commit.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification.

## Authority and claim boundary

This instrument is subordinate to finalized Master Governance v1.0.9,
amendment/A02, Overseer doctrine/status, and exact parent C0
`docs/superpowers/specs/2026-08-06-v043-v4-slot1-checker-c0-addendum.md`,
committed alone at `d6c10512e2fe839eeb19407edb37815d50079e10`, tree
`70250d1f86a0fa6dfde3ddb6abe99e90af663101`, parent
`d170cf288c6e763896f984713350106fc151e4b9`, 9,531 bytes, SHA-256
`97988ec751dc8a741bc5a7694ab3ed3596bf5d6e989142933c5083504ff2f278`.
Its exact bytes received independent B0/M0/m0 approval from the three existing
review roles. All protected planes and no-claim boundaries continue unless
explicitly superseded below. No push, tag, install, cache/catalog/research
mutation, package, shipment, consumer attestation, host qualification, or
activation is authorized. Gate 6 remains closed absent independent v0.43
consumer evidence.

## Registered attempt-6 failure

The exact immutable V5 sibling is
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V5.ps1.assembly.tmp`,
404,906 bytes, SHA-256
`4e00ee2b73b486261931148747d6ca0567cede7ab203a026eebf90fa3d1c1cd2`,
4,760 LF, zero CR, terminal LF, no BOM, and 63,436 tokens/zero errors at both
parser entry points. Final V5 is absent. C2 is exactly 25 files, zero
directories, and 25,136,869 bytes. Immutable V2/V3/V4 remain exact at their
parent-bound full hashes.

Static attempt 6 returned PASS in slots 1 and 2. Slot 1 completed every exact
format/parser/transformation/authority/label/canonical/UUID/finalizer check.
Slot 2 completed strict-mode assembly-static closure with 124 functions, base24
at 24,731,963 bytes, nine protected preimages, total C2 above, and zero helper
errors. These are preserved partial evidence only and may not be reused.

Slot 3 freshly rebound V5/final absence, parsed helpers, set primary/C2/errors
and invoked only `Get-PreservedFailedCaptureBinding`. Its wrapper had not set
`$script:EmptySha256`. Strict mode raised `VariableIsUndefined` while the binder
checked an expected zero-byte stream. Slot 3 emitted no PASS. This is a static
wrapper failure/evidence-incomplete result, not a V5 rejection, operational
RED, qualification result, source pass, or terminal success.

Attempt 6 is consumed. Slots 4 through 9 did not start. No move, mode, retry,
controller, qualification, manifest write, cleanup, or publication occurred.
Immediate reconciliation found V5 unchanged, final absent, exact C2 above,
zero related survivor, and clean parent main.

## Exact V6 derivation

V2 through V5 are immutable parent-bound evidence: never edit, move, delete,
clean, or execute them. After this C0 is reviewed and committed alone, create
only:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V6.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V6.ps1`.

First repeat the complete protected rebind: clean main at this C0 commit; C2
25/0/25,136,869; exact V2/V3/V4/V5; V6 absent; retry-26/admin, manifests,
stale lock, FIC/residue, runtime/supervisor and governance exact; all fresh
paths absent; process/event/job/transient state zero.

Derive V6 only from exact V5 with these changes:

1. V5 self-leaf literals become V6;
2. four authority fields bind this committed C0 path/commit/bytes/SHA;
3. add V5 assembly once to `BaseC2Names` and its exact SHA above once to
   `C2Preimages`, yielding base25 and ten protected preimages;
4. change only the closed-set base assertion from 24 files/24,731,963 bytes to
   25 files/25,136,869 bytes.

The two exact LF-terminated rows add 134 and 66 bytes. Successor and parent
authority paths have equal byte length. This C0 must remain four decimal digits
in byte length, equal in digit width to V5's 9,531-byte authority literal; all
other replacements are length-neutral. V6 is exactly 405,106 bytes, 4,762 LF,
zero CR, terminal LF, no BOM, and both parsers must return exactly 63,443
tokens/zero errors. Freeze its SHA. Prove exact source/replacement counts, only
two residual V5 evidence rows, and reverse byte equality. Create-new with
bounded sequential writes, durable flush, close, and independent readback.

## Exact static attempt 7

Authorize exactly one fresh nine-slot attempt 7 over V6; no main or operational
mode. Each ordered slot is a separate strict-mode Windows PowerShell 5.1
process with the inherited 600,000-ms ceiling and fail-closed consumption.

Every slot begins by setting exactly:

- `$script:Primary` to exact `B:\Agents\platform\co-author-harness`;
- `$script:C2` to exact
  `B:\Agents\platform\co-author-harness\releases\verification\v0.43.0\C2`;
- `$script:EmptySha256` to exact
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- `$script:Errors` to a new `System.Collections.ArrayList`; and
- `$script:Phase` to its exact attempt-7 slot label.

Slots needing more state must set it explicitly before any helper call. No
single-letter helper or unseparated positional invocation is permitted.

Slot 1 repeats every inherited exact byte/format/parser/text check with named
`Get-LiteralCount -Value`. Slot 2 repeats 124-helper/base25/ten-preimage
assembly-static closure: base25 is 25,136,869 bytes and total C2 is exactly 26
files/25,541,975 bytes. Slots 3 through 6 separately run the original failed,
retry-22, retry-24, and retry-26 binders. Slot 6 also runs the retry-26 shape
validator. Slot 7 repeats fifteen retry-26 and five corrected retry-24
adversarial rejections. Slots 8 and 9 repeat the complete protected phase-5
binding; slot 9 equals slot 8 and additionally binds V2/V3/V4/V5, base25,
same-volume publication, and final absence. Any failure consumes attempt 7.

Only after slot 9 PASS, with no intervening command except result assertions,
move V6 sibling to final V6 using non-overwriting two-argument
`[IO.File]::Move`. Rebind final and V2-V5, prove sibling absence, repeat both
parsers, re-extract helpers under strict mode, capture fresh complete state,
set only prospective `generation-structural`, require `Assert-FrozenBindings`,
and require runtime base25 plus final V6 closure.

Obtain three independent exact-byte B0/M0/m0 reviews of final V6 before any
mode. After their full protected rebind, only Git-managed retry-26/admin removal
then the inherited retry-30/31 attempts-045/046 sequence may proceed. C2 gates
are superseded to 26 post-review/pre-generation, 34 pre-cleanup, and 30 final.
Four terminal capsule/preimage pairs add eight; cleanup removes only four
provisional preimages.

## Review, commit, and ledger

This C0 requires three independent exact-byte B0/M0/m0 reviews. Commit only
this file on `main`, parent `d6c10512e2fe839eeb19407edb37815d50079e10`.
The prospective commit has 765 tracked, 758 included, zero plugin/ZIP, six
`scripts/analysis/`, one manifest, and a 39,793-byte included-path payload at
SHA-256
`261d2b48933b5d893db8d665b641881e4f10b1ed91769da698d22a352c9777b0`.
Do not push or tag.

Writable paths are only this C0/Git commit state, sole V6 create/move, then the
inherited review-gated retry removal and qualification ledger. Preserve every
older RED/nonterminal/static artifact, all prior candidates, retry-26 RED pair,
manifests, lock, FIC/residue, package/cache/catalog/shipment/consumer/research/
host/activation planes. Any failure stops. Gate 6 remains closed.
