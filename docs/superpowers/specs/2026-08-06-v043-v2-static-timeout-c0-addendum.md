# v0.43 V2 static-validation timeout - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority, precedence, and claim ceiling

This instrument is subordinate to the finalized research-governance chain
bound by its parent:

- `B:\Agents\research\10_Governance\MASTER_GOVERNANCE.md`, 77,262 bytes,
  SHA-256
  `a55b56eee7d72b4aa8a7f2376cb023d987ee7c12e71611fef7a9b457d780e191`;
- `B:\Agents\research\10_Governance\MASTER_GOVERNANCE_AMENDMENT_RECORD_1.0.9.md`,
  12,436 bytes, SHA-256
  `201d9b68e9c050f0f1e937977833868dd8ec2e3c5d6395f044fc00103fc95afe`;
- A02 anchor
  `B:\Agents\research\99_System\migrations\2026-08-04_master_governance_amendment_1_0_9\JOSEPH_AMENDMENT_ANCHOR.yaml`,
  1,114 bytes, SHA-256
  `7ae758e443f4360e9738094e6fd1080022b30fa60f8807d0baa1cf0640fb7ebc`,
  effective `2026-08-04T20:00:35.9192246-04:00`;
- Overseer doctrine, 5,829 bytes, SHA-256
  `90edcde87d2ed36641a911265179eb0203165e701a48395fd49b201c0379e7b9`;
- Overseer status `clear`, 773 bytes, SHA-256
  `b6b2fddedf9f364308dd6d54375c4d3a58595c3b7f9ed332453605373d9f00f9`;
  and
- parent C0
  `docs/superpowers/specs/2026-08-06-v043-retry26-static-validator-c0-addendum.md`,
  committed alone at `c1b9e6094f5b69e5eb3f1d4b63193b71bd0ea62e`,
  tree `85e469733132bbb34b5d6b2b765c2a4186090948`, parent
  `1cfb31042c47371d2967d2359cdf6fff6a95353f`, 15,823 bytes, SHA-256
  `ec36e0245bd3f252155a66a1350055127044a116689e02934fc2f73370bd97f9`.

The parent exact bytes received independent B0, M0, and m0 approval from
`/root/retry26_authority_audit`, `/root/retry26_mechanics_audit`, and
`/root/retry26_ledger_audit`. All three bound 15,823 bytes, 298 LF, zero CRLF,
terminal LF, no BOM, and the parent SHA-256 above. Parent requirements continue
except where this instrument registers the interrupted static transaction and
narrows its recovery.

Source, primary Git/admin, startup catalog, CLI registration, archive/package,
installed cache/provenance, loaded paths, consumer evidence, shipment, host
qualification, research, and activation remain separate planes. This grants
no push, tag, install, catalog/cache mutation, archive/package creation,
shipment, consumer attestation, research mutation, host qualification,
promotion, canon change, or activation. Gate 6 remains closed absent
independently governed v0.43 consumer evidence.

Any absent, draft, ambiguous, inconsistent, superseded, or conflicting binding
fails closed.

## Registered complete-but-unvalidated staging state

The parent C0's first dry in-memory assembly check stopped before any write
because replacing the closed-set helper removed two old self-path literals and
the next exact-count guard correctly observed one remaining literal rather
than the source's original three. A second dry check stopped before any write
because Windows PowerShell 5.1 does not expose the two-argument
`String.Contains` overload used by that checker. A first create command then
stopped before opening a file because its one-letter SHA helper collided with
the `Get-History` alias. Each diagnostic was pre-write: neither V2 path existed,
no operational label was consumed, and no mode ran.

The corrected closed derivation then used only the parent-authorized immutable
source and deterministic mappings. It produced the in-memory V2 candidate at
exactly 404,288 bytes, SHA-256
`a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`,
4,754 LF, zero CR, terminal LF, no BOM. Both in-memory parser passes reported
63,408 tokens and zero errors. All fourteen superseded active-label forms were
absent; the new C0 commit, path, byte-length, and SHA bindings each occurred
exactly once; the failed V1 appeared exactly in the base-name and protected
preimage maps; and exactly one guarded final capsule `[IO.File]::Replace`
remained.

After re-proving the exact source, committed parent, clean main, 21-file C2
preimage, and absent V2 leaves, the assembly process opened only:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V2.ps1.assembly.tmp`

with `FileMode.CreateNew`, `FileAccess.Write`, and `FileShare.None`, wrote only
candidate prefixes sequentially from offset zero, requested `Flush(true)`,
closed, and read the complete sibling back. The readback is exactly 404,288
bytes at the SHA-256 above. The final path remains absent. C2 therefore contains
exactly the immutable 21-file base plus this sibling: 22 files, zero
directories, and 23,922,736 file bytes. Primary Git remains clean at
`c1b9e6094f5b69e5eb3f1d4b63193b71bd0ea62e` and tree
`85e469733132bbb34b5d6b2b765c2a4186090948`.

Static-validation attempt 1 then started one isolated read-only process over
the complete sibling. It was intended to run both parser entry points,
assembly-profile closure, all preserved RED/nonterminal binders, retry-24 and
retry-26 canonical/adversarial checks, label closure, manifest writer grammar,
protected-corpus checks, and finalizer uniqueness. The caller terminated it at
its 100,034-millisecond ceiling with exit 124 before it emitted any result.
No per-check completion record exists. No check from that process may be
claimed as PASS or reused. This is `evidence_incomplete`, not a candidate
rejection, mode RED, qualification result, source pass, or terminal success.
Static-validation attempt 1 is consumed and may not be resumed.

Immediate post-timeout reconciliation found the validation process dead and
zero related surviving process. The assembly sibling retained its exact
404,288-byte binding, final V2 remained absent, C2 retained 22 files and
23,922,736 bytes, and main remained clean. The candidate-excluded primary
input-root preimage contains 1,887 identities: 315 directories, 1,572 files,
zero links/other, 127,152,596 file bytes, 218,530 inventory bytes, and SHA-256
`3dc75f452577333d1d9b49b1f9c1013bb0a99c5d595fa42827bb9a835cb43181`.

No mode review, retry removal or creation, controller, supervised structural
target, qualification transaction, manifest write, cleanup, packaging, or
downstream action occurred.

## Exact recovery authority

The complete sibling is frozen evidence. Do not edit, append, truncate,
rewrite, rebuild, replace, rename except at the single successful publication
point below, delete, clean, resume attempt 1, or create an alternate V2/V3
candidate. The destination must remain absent until every fresh validation
phase passes. Any byte, topology, authority, process, or protected-evidence
drift stops without cleanup or publication.

Authorize exactly one fresh static-validation attempt 2 over the frozen
sibling. It executes no script main and no operational mode. To avoid another
all-or-nothing execution ceiling, run these fresh bounded read-only phases
sequentially in separate processes, rechecking the exact sibling and absent
destination before and after each phase:

Every separately launched validation process below has an exact caller
wall-clock ceiling of 600,000 milliseconds. Phase 3 launches one process for
each of its four binders; every other numbered phase launches exactly one
process. A ninth and final pre-publication rebind process is required below, so
attempt 2 has exactly nine ordered process slots. A process that has not
returned by its ceiling is terminated once by the caller's process-timeout
mechanism. The caller then performs only a read-only related-process census and
exact frozen-state rebind. Any survivor, missing ordinary exit, or ceiling
expiry consumes attempt 2 as `evidence_incomplete`; no retry, larger timeout,
additional process slot, cleanup, move, or downstream action is authorized.

1. strict UTF-8/LF/no-BOM, 404,288-byte/SHA binding, Windows PowerShell 5.1
   `ParseFile` and `ParseInput`, 63,408-token/zero-error equality, exact
   transformation-count closure, authority/label closure, exact two active
   `a5babe17...` and zero `e806448e...` literals, UUID-v4 manifest writer
   grammar, and the sole guarded finalizer with immediate return and no
   catch-path republish;
2. extract only candidate helper definitions, execute only the exact
   `BaseC2Names` and `C2Preimages` data assignments, keep mode/capsule null,
   and require `Assert-C2ClosedSet -AssemblyStatic` to accept exactly base21
   plus the complete sibling while final V2 is absent;
3. invoke each preserved-corpus binder separately for the older failed capture,
   retry-22, retry-24, and retry-26 evidence. Require every exact raw and
   canonical binding, including retry-24 target `083583d2...`, retry-24
   normalized/preimage `a5babe17...`, retry-26 target `db4b4e8...`, and
   retry-26 normalized/preimage `311823f6...`;
4. in fresh in-memory clones only, require rejection of retry-24 detail, error
   order, `ended_utc`, canonical serialization, and terminal-LF mutations, and
   rejection of every retry-26 preserved field mutation; and
5. rebind finalized governance, this C0 commit, clean main, the complete
   22-file assembly-static C2 set, retry-26/admin, manifests, stale lock,
   FIC/residue, runtime/supervisor, fresh label/path absences, event state, and
   zero related process.

Each process slot must emit an ordinary process exit and concise exact result
before the next begins. A timeout, crash, parser error, rejection, missing
result, survivor, or drift consumes attempt 2 as incomplete/failed and stops.
No partial phase verdict authorizes publication.

Only after all five phases pass, launch the ninth process immediately before
publication. It must freshly repeat the complete phase-5 protected-evidence
rebind and prove exact equality to every phase-5 binding: finalized governance,
this C0 commit, clean main, complete sibling, 22-file C2 set, retry-26/admin,
manifests, stale lock, FIC/residue, runtime/supervisor, every fresh path
absence, event state, and zero related process. It must also re-prove final-path
absence, same-volume relation, and base21 equality. Any delta or missing field
stops. No other command may intervene between the ninth process's ordinary
PASS exit and publication except the non-mutating caller assertions that its
result exists and matches phase 5.

Then atomically publish only with the non-overwriting two-argument
`[IO.File]::Move($assemblySibling, $finalV2)`. Re-read final V2, prove exact byte
equality to the validated sibling binding and sibling absence, repeat both
parser entry points, re-extract helpers from final bytes, set the prospective
mode only to `generation-structural`, and require the no-switch runtime
`Assert-C2ClosedSet` form to accept exactly base21 plus final V2. Any failure
after publication preserves final V2 and stops under new authority.

Freeze final V2 bytes only after all post-Move checks pass. Then obtain three
independent exact-byte B0/M0/m0 reviews of that final file. Any edit invalidates
all reviews. No operational mode runs before unanimous approval and a complete
protected-evidence rebind.

## Writable ledger and isolated commit

Before this C0 becomes operative it requires three independent exact-byte
B0/M0/m0 reviews. Each must bind the governance chain, parent C0, complete
unvalidated sibling, timeout semantics, frozen preimage, phased validator,
publication boundary, writable ledger, downstream sequence, and Gate 6 ceiling.
Any edit invalidates all reviews.

After unanimous approval, commit only this file directly on `main`. The commit
must parent `c1b9e6094f5b69e5eb3f1d4b63193b71bd0ea62e`, contain 760 tracked and
753 included paths, zero plugin/ZIP, six `scripts/analysis/` paths, one
manifest, and a 39,423-byte included-path payload at SHA-256
`40bcc250da98bfa460a42cc4b4f713abccfac9b92108bca5bc0af081df2f7a40`.
Do not push or tag.

Only these paths may change:

1. this C0 path and primary Git objects/ref/log/index/`COMMIT_EDITMSG` for its
   isolated commit;
2. the exact complete assembly sibling only through its successful atomic move
   to the exact final V2 path; its bytes may not change; and
3. after final V2 exact-byte reviews and the inherited full protected rebind,
   only the still-pending parent-C0 recovery and qualification ledger.

Every other path remains read-only. In particular, all older RED/nonterminal
evidence, the immutable V1 script, retry-26 RED pair and worktree/admin,
manifests, stale lock, FIC/residue, package/archive, cache/catalog, shipment,
consumer, research, host, and activation planes remain untouched during C0
review, static validation, publication, and final-V2 review.

If final V2 passes static validation and its three reviews, resume the parent
sequence only at its protected rebind and retry-26 Git-managed removal point,
using fresh retry-30/31 and attempts 045/046 exactly as registered. Preserve
every new RED, interruption, timeout, or nonterminal artifact and stop on the
first unmet predicate. Even a fully green source sequence stops at Gate 6
pending independently governed v0.43 consumer evidence.
