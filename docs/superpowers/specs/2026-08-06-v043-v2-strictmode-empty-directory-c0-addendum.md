# v0.43 V2 strict-mode empty-directory failure - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority and claim boundary

This instrument is subordinate to the finalized research-governance chain and
the exact parent C0
`docs/superpowers/specs/2026-08-06-v043-v2-adversary-index-c0-addendum.md`,
committed alone at `257cc59db8469186f9fc2b40e771bfe901049298`, tree
`56177acccc3e06965f117b92fd1fbae9fa8060a6`, parent
`6c7ec69678b6c921f2c115261db2b96f91512a71`, 9,632 bytes, SHA-256
`95447dae2a0d66ef9cd6436026dc413837d33f6d70f30eb6ffe3c54c28d3c89d`.
That parent received independent exact-byte B0/M0/m0 approval from
`/root/retry26_authority_audit`, `/root/retry26_mechanics_audit`, and
`/root/retry26_ledger_audit`, each binding 9,632 bytes, 182 LF, zero CRLF,
terminal LF, and no BOM.

The parent's finalized Master Governance v1.0.9, amendment, A02 anchor,
Overseer doctrine/status, plane separation, protected evidence, downstream
sequence, and Gate 6 ceiling continue unchanged except for the exact C2
cardinality supersession below. Any absent, draft, ambiguous, inconsistent,
superseded, or conflicting binding fails closed.

This grants no push, tag, install, catalog/cache mutation, archive/package
creation, shipment, consumer attestation, research mutation, host
qualification, promotion, canon change, or activation. Gate 6 remains closed
absent independently governed v0.43 consumer evidence.

## Registered static-validation attempt 3 failure

The frozen assembly sibling remained exactly:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V2.ps1.assembly.tmp`

at 404,288 bytes, SHA-256
`a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`,
4,754 LF, zero CR, terminal LF, and no BOM. Final V2 remained absent.

Static-validation attempt 3 ran under the parent's exact ordered nine-slot,
600,000-millisecond-per-slot contract. Slot 1 returned ordinary exit-zero PASS
for the exact V2 binding, format, both parser entry points at 63,408 tokens and
zero errors, retired-label closure, literal counts, UUID grammar, guarded
finalizer, absent final path, clean parent commit, and zero mutation.

Slot 2 freshly parsed 124 helper definitions, evaluated only the two registered
constant assignments, set the exact primary/C2 paths, kept mode and capsule
null, enabled `Set-StrictMode -Version Latest`, and invoked only
`Assert-C2ClosedSet -AssemblyStatic` over the exact V2 sibling. It returned
ordinary exit 1 after 3.7 seconds before emitting a PASS result:

`Assert-C2ClosedSet : The property 'Name' cannot be found on this object.`

The exception arose only while constructing the failure-detail argument for
the already-empty directory set. Under strict mode, the zero-length array does
not expose the synthetic `.Name` member used by:

`(($directories.Name -join ','))`

The directory count was zero, but PowerShell evaluates the detail argument
before `Assert-Capture`, so the valid zero-directory condition could not be
reported. This is a candidate helper defect exposed by the validator, not a
protected-evidence mismatch, operational-mode RED, qualification result,
source pass, or terminal success.

Attempt 3 is consumed and may not be resumed. Slot 1 is preserved partial
evidence only and may not substitute for a future result. Slots 3 through 9 did
not start. No mode, retry removal/creation, controller, supervised target,
qualification, manifest write, cleanup, publication, or downstream action
occurred.

Immediate post-failure reconciliation found zero related surviving process,
the sibling unchanged at the exact binding above, final V2 absent, C2 exactly
22 files/zero directories/23,922,736 bytes, and clean main at the parent
commit/tree. Retry-26 remains present and untouched.

## Exact V3 derivation authority

V2 is now an immutable failed-static-validation preimage at its existing path.
Do not edit, append, truncate, rewrite, rebuild, replace, delete, rename, move,
or execute it. Authorize creation of exactly one complete sibling and eventual
final path:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V3.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V3.ps1`.

Create V3 only from exact V2 bytes by a closed deterministic transformation.
Before opening the new sibling, rebind exact V2, absent V3 leaves, clean main
at this C0's isolated commit, C2 at 22 files/zero directories/23,922,736 bytes,
retry-26, its admin, all protected C2 preimages, manifests, stale lock,
FIC/residue, runtime/supervisor, label/path absences, process/event/job
transients, and every other inherited protected plane. Any mismatch stops
pre-write.

The transformation may make only these semantic edits:

1. change the recovery self-leaf literals from V2 to V3 for the assembly and
   final paths;
2. bind `AuthorityRel`, `AuthorityCommit`, `AuthoritySha256`, and
   `AuthorityBytes` to this committed C0's exact path, commit, bytes, and hash;
3. add the immutable V2 assembly sibling leaf exactly once to
   `BaseC2Names` and add its exact SHA-256
   `a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`
   exactly once to `C2Preimages`, making it both base evidence and the seventh
   individually hash-bound protected preimage;
4. change only the closed-set base assertions from 21 files/23,518,448 bytes
   to 22 files/23,922,736 bytes; and
5. replace only the strict-unsafe empty-directory diagnostic expression
   `($directories.Name -join ',')` with
   `(@($directories | ForEach-Object { $_.Name }) -join ',')`.

No other semantic or byte change is authorized. The assembler must prove every
source literal's exact expected count, every replacement count, the exact
post-transform literal counts, no V2 active self-leaf except its exact
`BaseC2Names` and `C2Preimages` evidence rows, and byte-for-byte equality
outside the registered replacement spans. Create the V3 sibling with
create-new semantics, sequential
bounded writes, durable flush, close, and independent readback. A partial or
mismatched sibling is immutable failure evidence and stops for successor
authority.

## Exact static-validation attempt 4 authority

After successful V3 creation, freeze its exact path/bytes/SHA/format/parser
binding. Authorize exactly one fresh static-validation attempt 4. It executes
no script main and no operational mode. It runs the inherited nine slots from
zero, in order, in separate Windows PowerShell 5.1 processes, each explicitly
using `Set-StrictMode -Version Latest` and the same exact 600,000-millisecond
caller wall-clock ceiling, timeout termination, survivor census, evidence
preservation, and attempt-consumption semantics.

Slots 1 through 6 repeat the inherited exact format/parser/text,
assembly-static closed-set, failed-capture, retry-22, retry-24, and retry-26
checks. Slot 2 must prove 124 isolated helper definitions, 22 base names, six
protected preimages plus V2 as the seventh exact hash-bound preimage, base22 at
23,922,736 bytes, C2 containing immutable V2 plus V3 sibling and no directory,
mode/capsule null, and zero helper errors under strict mode.

Slot 7 repeats the parent's corrected adversaries: fifteen closed retry-26
field-mutation rejections and five retry-24 binding/canonical/order/LF
rejections, with the target's one-error vector and publication preimage's
two-error vector validated before mutation. Any unexpected shape, exception,
missing rejection, or missing PASS consumes attempt 4 and stops.

Slot 8 repeats the complete protected phase-5 rebind, including V2 through the
seventh `C2Preimages` entry. Slot 9 freshly repeats the same complete binding
set, requires exact equality to slot 8, proves V3 final absence, same-volume
relation, immutable V2 path/bytes/SHA equality, and base22 equality, and permits
no intervening command except non-mutating result assertions. Only then may the
caller atomically publish with the non-overwriting two-argument
`[IO.File]::Move($assemblySiblingV3, $finalV3)`.

After a successful move, re-read final V3, prove exact equality to the
validated sibling and sibling absence, freshly rebind immutable V2 through the
seventh protected-preimage entry as part of a fresh complete protected state,
repeat both parsers, re-extract helpers from final bytes under strict mode, set
only prospective `generation-structural`, and require both
`Assert-FrozenBindings` over that fresh state and no-switch runtime closed-set
equality for base22 plus final V3. Any post-move failure preserves final V3 and
stops.

Then obtain three independent exact-byte B0/M0/m0 reviews of final V3. Any edit
invalidates them. No operational mode runs before unanimous approval and the
inherited full protected-evidence rebind, which must again prove the seventh
V2 protected-preimage binding.

## Superseded downstream C2 cardinalities

Because immutable V2 becomes one permanent base-evidence file, this instrument
expressly supersedes every inherited exact C2 cardinality of 22 pre-generation,
30 pre-cleanup, or 26 final. After V3 publication and its unanimous review, the
post-review protected rebind must find exactly 23 files: base22 plus final V3.

The inherited retry-30 generation, attempt-045, manifest-only commit, retry-31
replay, and attempt-046 no-write sequence remains ordered and otherwise
unchanged. Its four successful capsule/publication-preimage pairs add exactly
eight files. Therefore the pre-cleanup protected rebind and all three
pre-cleanup independent reviews must bind exactly 31 C2 files. Only after those
reviews may the inherited cleanup remove exactly the four provisional
publication-preimage files. The final protected rebind, summary, and all three
final independent reviews must bind exactly 27 C2 files. V2, final V3, every
older preserved RED/static artifact, and all four terminal capsule files remain.
No other C2 addition, removal, rename, or cardinality interpretation is
authorized.

## Review, commit, and writable ledger

Before this C0 becomes operative, three independent exact-byte B0/M0/m0 reviews
must bind its parent, attempt-3 evidence, strict-mode diagnosis, exact V3
derivation, attempt-4 slots, publication boundary, protected ledger,
downstream sequence, and Gate 6. Any edit invalidates all reviews.

After unanimous approval, commit only this file directly on `main`, with exact
parent `257cc59db8469186f9fc2b40e771bfe901049298`. The prospective commit
contains exactly 762 tracked paths, zero `.plugin`/`.zip`, six paths beneath
`scripts/analysis/`, one fixture manifest, and 755 governed included paths.
Their Python-Unicode-sorted UTF-8 LF payload without terminal LF is 39,580
bytes at SHA-256
`3922739c723258738770c9ee6b6f96847596dbed297ad3bdf8cde00c0b7fb0ca`.
Do not push or tag.

Only these paths may change:

1. this C0 path and primary Git objects/ref/log/index/`COMMIT_EDITMSG` for its
   isolated commit;
2. the exact V3 assembly sibling during its one create transaction and then
   only through a successful atomic move to exact final V3; its bytes may not
   change after close; and
3. after final-V3 unanimous review and the inherited full protected rebind,
   only the exact Git-managed removal of
   `B:\Agents\.coauthor-v043-c2-detached-retry-26` and its registered admin;
   use Git worktree management, never manual deletion, and prove both absent
   while every other protected binding remains unchanged; then only the
   still-pending inherited retry-30/31 and attempts 045/046
   recovery/qualification ledger.

Every other path remains read-only. Preserve all older RED/nonterminal/static
evidence, immutable V1, immutable V2, retry-26 RED pair permanently, retry-26
worktree/admin through the exact review-and-rebind gate above, manifests,
stale lock, FIC/residue, runtime/supervisor, package/archive, cache/catalog,
shipment, consumer, research, host, and activation planes.

Any new timeout, crash, rejection, survivor, RED, missing receipt, or drift
stops and preserves evidence. Even a fully green source sequence stops at Gate
6 pending independently governed v0.43 consumer evidence.
