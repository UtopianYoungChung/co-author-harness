# v0.43 C2 source-bytecode recurrence cleanup C0 addendum

**Status:** approved package-maintainer boundary for removing one exact
source-root bytecode residue discovered during independent review of the
detached-facade repair and for repeating the focused proof on the restored
governed baseline. This addendum grants no package clearance, shipment, cache
mutation, consumer re-attestation, host qualification, activation, research
mutation, acceptance, promotion, release, or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-02-v043-c2-detached-facade-bytecode-retry-c0-addendum.md`.
All parent prohibitions and transaction gates remain binding. Any path not
enumerated here or in the parent ledgers requires another committed addendum
before it is created or changed.

## Frozen review state

- Repository: `B:\Agents\platform\co-author-harness`.
- Branch: `main`, the only permitted branch.
- Commit: `01b9b3dd85923a61034ac3958f291005fa1b61c0`.
- Tree: `4b0adff3108da0bbb1b185065ddd6aec8cfb18b4`.
- The tracked worktree differs from that tree only at the five repair paths
  authorized by the parent addendum.
- Independent POSIX review reported B1/M0/m0 because the repository bytecode
  census contained 181 files rather than the governed 180-file baseline.
- The additional exact ignored file is
  `scripts/__pycache__/graph_authority_gate.cpython-314.pyc`, 2,060 bytes,
  SHA-256
  `49c48d1df4cba88a8c87274ff200cd73b4c8ba16bd547517954cb6e09f4f5600`,
  created and last written at `2026-08-02T22:59:22Z`.
- An earlier refused transaction produced the same logical path with different
  bytes and SHA-256
  `211c6c57430d316d222c4fd7a7fd0a6c48fc65c475e4e0bf6c3dd1586ffc0251`.
  Its guarded removal authority does not authorize this recurrence.
- The current file remained byte-for-byte stable during the latest focused
  Windows proof. Stability does not legitimize an unledgered output, and its
  precise emitter is not asserted by this addendum.

## Exact writable ledger

Only this tracked path is newly writable in this closure slice:

| Action | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE | `docs/superpowers/specs/2026-08-02-v043-c2-source-bytecode-recurrence-c0-addendum.md` | 0 | `ABSENT` |

After this addendum is committed and no qualification transaction or related
product process is live, only the exact ignored bytecode file frozen above may
be removed. The file must still match its full path, size, and SHA-256
immediately before removal. Its parent directory must not be removed. Broad,
recursive, globbed, or best-effort bytecode cleanup is forbidden.

## Required closure proof

1. Re-establish the governed repository bytecode baseline of exactly 180 files
   with inventory digest
   `08c6740aab85ac2b73162dd0bb7c792815e2ba871dacb0decc3a070363085f2b`.
   Any other count or digest is a stop.
2. Re-run the exact-postimage controller smoke on Windows and WSL/POSIX with
   `-B`, proving that the bytecode inventory and `__pycache__` topology are
   unchanged across each run.
3. Re-run the focused fixture-infrastructure, subprocess-text, schema-runtime,
   kernel-coherence, shell-syntax, and parent-required mechanical structural
   checks against the same stable baseline.
4. Obtain independent B0/M0/m0 review of the exact five-file repair postimage
   and its refreshed focused evidence before committing that repair.

Only after all four requirements pass may the parent-authorized source-root
attempt 009 begin. Attempts 005 and 008 remain immutable; labels 006 and 007
remain nontransactions and may not be reused. Any new output, mismatch,
warning, failed check, process ambiguity, or review finding is a stop requiring
new authority.
