# v0.43 C2 partial PID-handoff smoke repair C0 addendum

**Status:** approved package-maintainer repair authority for the exact
post-resume smoketest handoff race exposed by parallel focused execution. This
record must be committed alone before the affected test byte is edited. It
grants no full-corpus, package, shipment, cache, catalog, fresh-host, consumer,
research, acceptance, promotion, release, activation, or canon authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-03-v043-c2-attempt-017-owner-job-smoke-c0-addendum.md`.
Its six-path repair/binding ledger, attempt-017 red preservation, retry
identities, and all no-claim boundaries remain binding.

## Preserved focused red

Two focused Windows controller-smoke modes were deliberately exercised in
parallel after independent review corrections. One run failed in the
post-resume `BaseException` regression when the child had created its PID
handoff path but had not yet filled it:

`ValueError: invalid literal for int() with base 10: ''`

The exact failing read followed an existence-only wait for
`post-resume-descendant.ready`. This is a test-observation race, not evidence
of product success or failure. The run remains red and is not replaced by a
rerun. Its sibling fixture-owner process tree was allowed to close under its
existing supervisor; afterward, exact Python controller/smoketest processes
were zero. The governed bytecode inventory remained 179 `.pyc` files and four
`__pycache__` directories.

## Frozen preimage and authority

- Repository: `B:\Agents\platform\co-author-harness`; branch: `main` only.
- HEAD: `3e0ca9e5539aaa9a6ba28e802b01e7fceef9ab45`.
- HEAD tree: `e6acd049ceaada175c941ba32f19e6c7a99d7a0c`.
- Affected uncommitted preimage:
  `scripts/release_qualification_controller_smoketest.py`, 196,188 bytes,
  SHA-256
  `362b23b199af824a63a3a607265862337201e28136c122ef51f7a2ecca426cab`.

Authority is limited to adding one strict helper in that already-authorized
smoketest path and replacing the existence-only PID read with it. The helper
must wait for a complete canonical positive base-10 integer, tolerate only the
known partial writer handoff until a fixed monotonic deadline, reject zero,
negative, whitespace-padded, signed, leading-zero, non-ASCII, or non-integer
content, and surface the last observation on timeout. No production controller,
supervisor, runner, registry, kernel, projection, profile, schema, manifest,
package, or cache byte may change under this addendum.

After the repair, rerun AST/LF/diff checks; bare-host and fixture-owner Windows
focused gates sequentially; a parallel stress pair; WSL/POSIX controller smoke;
native and WSL text policy; the structural/binding matrix; bytecode and process
residue checks; and independent exact-byte review. Any further red or residue
is a stop requiring new authority. Source attempt 019 remains closed until all
focused evidence is green and the reviewed six-path repair is committed.
