# v0.43 post-receipt index-observer C0 addendum

**Status:** approved package-maintainer correction for the exact detached-index
observer effect found after attempt 021 reached its immutable terminal receipt.
Commit this record alone before removing retry-7 or creating retry-8. It grants
no product-source repair, package,
cache, catalog, consumer, shipment, host, activation, research, acceptance,
promotion, release, or canon authority.

**Run scope:** `adhoc_review`.

**Parent authorities:**

- `docs/superpowers/specs/2026-08-05-v043-attempt-019-git-plane-isolation-c0-addendum.md`;
- `docs/superpowers/specs/2026-08-05-v043-worktree-admin-id-correction-c0-addendum.md`.

All parent red-preservation, path, exact-byte, single-branch, sequencing, and
no-claim boundaries remain binding except for the exact observer correction
below.

## Preserved successful transaction and later observer effect

Attempt 021 at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-021`
has an immutable terminal `succeeded` receipt with child exit 0:

- receipt: 367,297 bytes, SHA-256
  `813536afebedc56ea1195221051d81be98b7e516fa652f81ea5a0b6f2697195a`;
- finalized at `2026-08-05T06:51:55.588122Z`;
- product: 93/93 suites and 97/97 cases, zero failed/warning rows;
- manifest: 54,261 bytes, SHA-256
  `1448990c4599879aed679e3a7fa5e62154d8e13479a6200c9b3fa9e443a07de1`,
  run ID `02443322-e93d-4f1e-891e-3dd08013ba9e`;
- receipt-bound stdout: SHA-256
  `eefba557709d097636299bf42a1da36b890ec23aa12d0cf97b6776c76b9969d7`;
- 97/97 stdout rows match the manifest and 93/93 suite hashes match the
  detached source;
- tested-inputs remained stable across execution: 741 files, canonical
  `d49e82542255dcd9bb560726a11ede091f4d969f0a3335923ad39fe63f2cbd78`,
  raw `e3aa01e7042961e8eb6da5853ff4b688929a9a9b192d586352a4a7a1c3643f4d`;
- no attempt-021 controller/runner process survives.

The terminal receipt proves all six explicit inputs, including retry-7's
detached index, were unchanged when the controller finalized. The bound index
was 94,623 bytes, SHA-256
`48b12423b52df4e3e9c075b678e16daa6ee1334efb19b3e83014f2180228733d`.

At `2026-08-05T06:52:39.1594478Z`, more than 43 seconds after receipt
finalization, the required postflight invocation of ordinary
`git status --porcelain=v1 --untracked-files=all` atomically refreshed the
detached index stat cache. The live index remains 94,623 bytes but now has
SHA-256
`ab2a01a40e5aa3cc01bceffefcf815bb1de2d01828814cb3979b007675ed0143`.
The status reported exactly one worktree change, the authorized detached
fixture manifest. All other five explicit inputs remain byte-identical to the
receipt. The primary attempt-019 manifest residue remains 54,258 bytes,
SHA-256
`ae32f0d52f6d3fd0acfd7c20463ac93edd0059ba41560114accced81aed697a9`.
No primary product byte was copied or changed, and attempt 022 remains absent.

This post-receipt administrative observer effect does not rewrite the earlier
terminal receipt or upgrade any other evidence. It is preserved as a new
finding and is not called harmless merely because worktree bytes did not
change.

Attempt 021 qualifies only the exact 741-file tested population at detached
commit `db7c4a7b6fee7091c1379d0cefb2c107cb2851b8`. This addendum was absent from
that detached HEAD and from its primary input-root inventory. Because this
document is itself a shipped/tested package byte, committing it changes the
tested population and both canonical and raw digests. Therefore the
attempt-021 manifest is historical successful evidence only and may not replace
the primary manifest or qualify the post-addendum source.

## Superseding transient authority

Retry-8 and attempt 022 remain absent and are reassigned from clean replay to a
fresh manifest-generating qualification at the committed addendum HEAD:

- `B:\Agents\.coauthor-v043-c2-detached-retry-8`;
- `.git/worktrees/-coauthor-v043-c2-detached-retry-8/**`;
- `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-022`.

The final clean replay moves to these freshly observed-absent paths:

- `B:\Agents\.coauthor-v043-c2-detached-retry-9`;
- `.git/worktrees/-coauthor-v043-c2-detached-retry-9/**`;
- `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-023`.

Only Git-managed creation/removal of the two exact admin subtrees is
authorized. A different canonical admin ID is a stop.

## Exact correction and remaining sequence

1. Before commit, revalidate attempt-021 receipt/stdout/manifest bindings, the
   five non-index explicit inputs against their receipt bytes, the exact current
   observer-refreshed index, primary manifest residue, runner-lock transition,
   zero surviving processes, and that the sole primary input-root addition
   after receipt finalization is this untracked addendum. Then create and commit
   only this addendum on `main`. Ordinary Git object, index, reflog, and
   `refs/heads/main` changes are authorized only as Git-managed consequences of
   this correction commit. Record the expected main-ref and primary-input-root
   authority transitions; do not call them receipt-identical.
2. The parent requirement for ordinary detached `git status` is superseded.
   Every remaining retry-7/retry-8/retry-9 status observation must use
   `git --no-optional-locks -C <exact-worktree> status --porcelain=v1
   --untracked-files=all`, with detached-index SHA-256 and byte length recorded
   immediately before and after. Any further index change is a stop.
3. Attempt 021 remains `succeeded` under its immutable receipt because its
   exact index was stable through controller finalization. It need not be
   rerun, but its manifest may not be copied. Keep retry-7 and its exact
   historical manifest/index postimages unchanged until the fresh attempt-022
   manifest is captured and verified.
4. Create retry-8 detached at the committed addendum HEAD. Record its exact
   hyphenated Git-dir and bind fresh `HEAD`, `index`, `commondir`, and `gitdir`
   bytes. Attempt 022 repeats the parent attempt-021 topology with retry-8 as
   sole watch root, its manifest as sole allowed output, primary as the sole
   input root, exact primary HEAD/main and retry-8 Git-dir inputs, no ignored
   output, the same concrete Python with `-B`, `--allow-user-site`, cache off,
   and manifest-writing full tier. Use only no-optional-locks status checks.
5. Require attempt 022 to produce an immutable terminal `succeeded` receipt,
   93/93 suites, 97/97 cases, stable post-addendum tested-input digests, stable
   explicit inputs and primary input root through finalization, only its exact
   manifest changed, a separately bound runner-lock transition, and zero
   surviving owned processes. Any other result is red and stops.
6. Immediately bind attempt 022's receipt, stdout, manifest bytes/hash/length,
   97 rows, 93 suite hashes, run ID, Git inputs, no-optional-locks status and
   unchanged retry-8 index. Only then copy exactly that new manifest to primary,
   prove equality, and commit only the primary manifest on `main`.
7. After all attempt-021 and attempt-022 bindings are captured, force-remove
   dirty retry-7 and retry-8, including their exact hyphenated admin subtrees,
   and prove all four paths absent. Attempt 021's immutable transaction root
   and receipt remain historical evidence.
8. Create retry-9 detached at the manifest commit. Record and bind its fresh
   Git-dir inputs and use only no-optional-locks status checks. Attempt 023 is
   the required cache-off `--no-write` clean exact-byte replay with no allowed
   output and otherwise identical source/ref/input-root bindings.
9. Record the observer effect, attempt-021 historical boundary, and exact
   attempt-022/023 evidence in the registered C2 paths before three independent
   exact-byte reviews.

Any further index rewrite, source drift, warning, receipt ambiguity, additional
worktree change, process residue, or unlisted path is a stop requiring new
explicit authority.
