# v0.43 V8 slot-8 event-scalar failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and consumed attempt

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v7-slot5-checker-c0-addendum.md`,
committed alone at `e25f068e87758f6caec931d8d0f6678b4378030e`, tree
`823bb3097d81e40b7d6943e4f494a1f4df0a1720`, parent
`4f77f064fdb35a1db5b91c02ba8c762b9e41eea1`, 8,419 bytes, SHA-256
`996ea85c58dcea9f9bf14d0ff9425661cca5736cd7a2d07a92f2d50c1451e369`,
with independent exact B0/M0/m0 approval.

Immutable V8 is
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V8.ps1.assembly.tmp`,
405,506 bytes, SHA-256
`39b2b1f17c2ca7bf6d15b4e0d09437b22f60556bb6b0bab03af609e8bb9b5e32`,
4,766 LF, zero CR, terminal LF, no BOM, and 63,457 tokens/zero errors at
both parsers. Final V8 is absent. C2 is 28 files, zero directories, and
26,352,787 file bytes.

The first V8 derivation wrapper stopped before opening either destination when
one-letter `H(...)` resolved to the `Get-History` alias. The complete prewrite
state remained exact and both paths absent. Three independent B0/M0/m0
authority audits held that the path-state create-new grant remained unused.
After a fresh complete rebind, the corrected non-colliding wrapper created only
V8 assembly, used create-new sequential bounded writes, durable flush, close,
readback, reverse equality, and both parsers. No operational label was used.

Static attempt 9 is consumed. Ordered slots 1-7 each passed in its own Windows
PowerShell 5.1 process: exact byte/format/parser/reverse/authority/label/
canonical/UUID/finalizer closure; 124 helpers, base27 at 25,947,281 bytes,
twelve preimages and C2 28/26,352,787; all four preserved binders including
the retry-24 six-key return schema and retry-26 shape; and all 15 retry-26 plus
five corrected retry-24 adversarial rejections.

Slot 8 completed the expensive protected inventory and its assertions until
the reporting wrapper required `@($state.events_seen).Count -eq 0`.
`Collect-WatcherEvents` returns the scalar integer `$script:Events.Count`, so
the exact no-event value is integer `0`; array-wrapping that scalar produces a
one-element array. `Assert-Capture` therefore raised `SLOT8-EVENTS` even though
the governed event count was zero. Slot 8 emitted no PASS result; slot 9 did
not start. All slots 1-7 are partial evidence only and are not reusable.

This is wrapper evidence-incomplete, not a V8 rejection, operational RED,
qualification result, or success. No move, mode, retry, manifest, cleanup, or
publication occurred. V8 remains exact, final absent, main clean, retry-26
untouched, C2 unchanged, and related survivors/transients zero.

All parent plane-separation and no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact V9 derivation

V2-V8 are immutable evidence. After this C0 is reviewed and committed alone,
repeat the complete protected rebind and create only these V9 paths:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V9.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V9.ps1`.

Derive V9 only from exact V8: change V8 self leaves to V9; bind this committed
C0's exact path, commit, byte length, and SHA-256; add V8 assembly once to
`BaseC2Names` and its exact SHA above once to `C2Preimages`; and change only the
base assertion from 27 files/25,947,281 bytes to 28 files/26,352,787 bytes.
The inserted evidence rows add 134+66 bytes; authority paths and four-digit
byte-length literals are equal-width. V9 must therefore be exactly 405,706
bytes, 4,768 LF, zero CR, terminal LF, no BOM, and 63,464 tokens/zero parser
errors. Prove exact transformation counts, two residual V8 evidence rows,
reverse byte equality, create-new sequential bounded writes, durable flush,
close, and readback. Freeze its SHA-256 before validation.

Before opening V9, require clean committed main, C2 28/0/26,352,787, exact
V2-V8, V9 absent, exact governance/retry-26/admin/manifests/lock/FIC/frozen
residue/runtime bindings, all fresh paths absent, and process/event/job/
transient state zero. Any mismatch stops without write.

## Exact static attempt 10

Authorize one fresh ordered nine-slot attempt 10 over frozen V9; no script main
and no operational mode. Every slot is a separate strict-mode Windows
PowerShell 5.1 process with a 600,000-ms ceiling and fail-closed consumption.
Every slot first sets exact Primary root, exact C2 directory, exact empty SHA,
fresh ArrayList Errors, and its unique slot Phase; additional state precedes
helper calls.

Slot 1 uses the parent C0's exact four-line `Get-LiteralCount` declaration and
named calls, and repeats all byte/format/parser/reverse/authority/label/
canonical/UUID/finalizer checks. Slot 2 repeats 124 helpers, base28 at
26,352,787 bytes, thirteen exact preimages, and total C2 29 files/26,758,493
bytes. Slots 3-6 separately run the four preserved binders; slot 5 uses only
the retry-24 six-key return schema and internal canonical assertions; slot 6
also validates retry-26 shape. Slot 7 repeats all fifteen retry-26 and five
corrected retry-24 adversarial rejections.

Slots 8 and 9 each repeat the complete protected binding. The no-event
predicate is exactly `[int]$state.events_seen -eq 0`; array cardinality is
forbidden. After normalizing only the state label and captured timestamp, each
slot computes the canonical SHA-256 of the entire state; slot 9 must equal the
exact slot-8 digest. Slot 9 additionally binds V2-V8, base28, same-volume
publication, and final V9 absence.

Only after slot 9 PASS, with no intervening command except result assertions,
publish by non-overwriting two-argument move from V9 assembly to final V9.
Post-move bind final and V2-V8, prove sibling absent, repeat both parsers, all
helpers, fresh complete state, scalar-zero events, `Assert-FrozenBindings`, and
runtime base28+final closure with only prospective `generation-structural` set.

Three independent exact-byte B0/M0/m0 final-V9 reviews and one complete rebind
must precede any operational mode. Only then may inherited Git-managed
retry-26/admin removal and retry-30/31 attempts-045/046 proceed. C2 gates are
29 files post-review, 37 pre-cleanup, and 33 final; four fresh pairs add eight
files and cleanup removes four preimages.

## Review, commit, and writable ledger

This exact C0 requires independent exact-byte B0/M0/m0 review. Commit only it
with parent `e25f068e87758f6caec931d8d0f6678b4378030e`. The prospective commit
population is 768 tracked, 761 included, zero plugin/ZIP, six
`scripts/analysis/`, and one fixture manifest. Its 40,006-byte
Python-Unicode-sorted UTF-8 included-path payload without terminal LF has
SHA-256 `58e220bf96860e1aaa2ad65962209bc0045d62cd23aabd7ee3053cd11464ea3d`.
Do not push or tag.

Before V9 creation, writable paths are only this C0 and its isolated Git
commit. After commit and full rebind, only the sole V9 assembly/final pair is
writable. After final-V9 reviews and another full rebind, only the inherited
exact retry-26/admin Git removal and reviewed retry-30/31 generation/manifest/
replay ledgers are writable. Preserve every RED, nonterminal, and static
artifact; V1-V8; retry-26 RED pair; manifests; stale lock; FIC/frozen residue;
and every source/Git/admin/startup/catalog/CLI/archive/cache/provenance/
consumer/host/activation plane except an exact later-authorized path. Any
failure stops. Gate 6 remains closed.
