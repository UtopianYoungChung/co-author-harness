# v0.43 V7 slot-5 reporting-schema failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and consumed failure

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v6-slot1-checker-c0-addendum.md`,
committed alone at `4f77f064fdb35a1db5b91c02ba8c762b9e41eea1`, tree
`64d38c62b7bf2a7deee6d5f3bedd5ff7c383e475`, parent
`7872d74884ff54a8521a05add774b9b192a63f64`, 5,779 bytes, SHA-256
`5fdcbafb0958e140fc5487611a7ff7b67450656d9d8a0048cff6baf7e83c0d24`,
with independent exact B0/M0/m0 approval.

Immutable V7 is
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V7.ps1.assembly.tmp`,
405,306 bytes, SHA-256
`3fd77ce6eb4a7ef3c97b47f534c63342db79a71a8c8a1704748abb6124f77ce7`,
4,764 LF, zero CR, terminal LF, no BOM, and 63,450 tokens/zero errors at
both parsers. Final V7 is absent. C2 is 27 files, zero directories, and
25,947,281 file bytes.

Static attempt 8 is consumed. Ordered slots 1-4 each passed their own process:
slot 1 bound V7 bytes/format/parsers/transformation/authority/labels and exact
literal counts; slot 2 bound 124 functions, base26 at 25,541,975 bytes, eleven
preimages, and C2 27/25,947,281; slot 3 bound the preserved retry-20 failed
preimage;
slot 4 bound retry-22 target 2,696,077 bytes at SHA-256
`90ef9d091afbe738e97608f16faef4179d153af7baf1b7ca03f827df1dd2b404`
and publication 2,696,245 bytes at SHA-256
`6117146b60dc9952e8549db8e54cc2022f528a8da505a1a1b83ce79df8f05cb2`.
Those partial results are evidence only and are not reusable.

In slot 5, `Get-PreservedRetry24FailureBinding` completed its internal exact
checks, including target canonical SHA-256
`083583d2264ed2d9da0a58551ea0d038a31f168bb1dfa3b7bb8efad8ef4c9962`
and normalized/publication canonical SHA-256
`a5babe17da8b26c2d72ce8a3f179858c26ea3f45db8318f6ab6c84362efad048`.
The reporting wrapper then accessed nonexistent top-level properties
`target_canonical_sha256` and
`normalized_publication_canonical_sha256`; strict mode raised
`PropertyNotFoundStrict` before a PASS result. The binder's actual top-level
schema is exactly `failed_script`, `target`, `publication_preimage`, `verdict`,
`spawn_count`, and `preflight_read_count`. Slots 6-9 did not start.

This is reporting-wrapper evidence-incomplete, not a V7 rejection, operational
RED, qualification result, or success. No move, mode, retry, manifest, cleanup,
or publication occurred. V7 remains exact, final absent, main clean, retry-26
untouched, and related survivors/transients zero. All prior RED/nonterminal and
static evidence remains immutable.

All parent plane separation/no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact V8 derivation

V2-V7 are immutable evidence. After this C0 is reviewed and committed alone,
repeat the complete protected rebind and create only these V8 paths:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V8.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V8.ps1`.

Derive V8 only from exact V7: change V7 self leaves to V8; bind this committed
C0's exact path, commit, byte length, and SHA-256; add the V7 assembly once to
`BaseC2Names` and its exact SHA above once to `C2Preimages`; and change only the
base assertion from 26 files/25,541,975 bytes to 27 files/25,947,281 bytes.
The inserted evidence rows add 134+66 bytes; authority paths and four-digit
byte-length literals are equal-width. V8 must therefore be exactly 405,506
bytes, 4,766 LF, zero CR, terminal LF, no BOM, and 63,457 tokens/zero parser
errors. Prove exact transformation counts, two residual V7 evidence rows,
reverse byte equality, create-new sequential bounded writes, durable flush,
close, and readback. Freeze its SHA-256 before validation.

Before opening V8, require clean committed main, C2 27/0/25,947,281, exact
V2-V7, V8 absent, exact governance/retry-26/admin/manifests/lock/FIC/frozen
residue/runtime bindings, all fresh paths absent, and process/event/job/
transient state zero. Any mismatch stops without write.

## Exact static attempt 9

Authorize one fresh ordered nine-slot attempt 9 over frozen V8; no script main
and no operational mode. Every slot is a separate strict-mode Windows
PowerShell 5.1 process with a 600,000-ms ceiling and fail-closed consumption.
Every slot first sets exact Primary root, exact C2 directory, exact empty SHA,
fresh ArrayList Errors, and its unique slot Phase; additional state must precede
helper calls.

Slot 1 must copy this declaration line-for-line, without compression:

```powershell
function Get-LiteralCount {
    param([string]$Value)
    return [regex]::Matches($text, [regex]::Escape($Value)).Count
}
```

Every call is exactly `Get-LiteralCount -Value '<literal>'`. Slot 1 repeats all
byte/format/parser/transformation/authority/label/canonical/UUID/finalizer
checks. Slot 2 repeats 124 helpers, base27 at 25,947,281 bytes, twelve exact
preimages, and total C2 28 files/26,352,787 bytes. Slots 3-6 separately run the
four preserved binders with the complete initializer; slot 6 additionally
validates retry-26 shape.

Slot 5 may consume only the actual retry-24 return schema. It must require zero
Errors; exact returned keys `failed_script`, `target`, `publication_preimage`,
`verdict`, `spawn_count`, `preflight_read_count`; exact returned failure path,
target path, publication-preimage path, verdict `RED`, spawn count 16, and
preflight-read count 9. It must report the two canonical values above solely as
internal binder assertions and must not access nonexistent top-level canonical
properties.

Slot 7 repeats all adversarial rejections: fifteen retry-26 field mutations;
retry-24 target errors-count-1 `UNHANDLED`; publication errors-count-2
`UNHANDLED` plus `CAPSULE-PUBLICATION-PENDING`; target detail mutation;
publication error-order swap; target `ended_utc`; canonical serialization with
one appended space; and target terminal-LF removal. The expected result is
15+5 PASS rejections. Slots 8/9 repeat the full protected binding; slot 9 must
equal slot 8 and additionally bind V2-V7, base27, same volume, and final V8
absence.

Only after slot 9 PASS, with no intervening command except result assertions,
publish by non-overwriting two-argument move from V8 assembly to final V8.
Post-move bind final and V2-V7, prove sibling absent, repeat both parsers, all
helpers, fresh complete state, `Assert-FrozenBindings`, and runtime
base27+final closure with only prospective `generation-structural` set.

Three independent exact-byte B0/M0/m0 final-V8 reviews and one complete rebind
must precede any operational mode. Only then may the inherited Git-managed
retry-26/admin removal and retry-30/31 attempts-045/046 sequence proceed. C2
gates are 28 files post-review, 36 pre-cleanup, and 32 final; four new
generation/replay pairs add eight files and cleanup removes four preimages.

## Review, commit, and writable ledger

This exact C0 requires independent exact-byte B0/M0/m0 review. Commit only it
with parent `4f77f064fdb35a1db5b91c02ba8c762b9e41eea1`. The prospective commit
population is 767 tracked, 760 included, zero plugin/ZIP, six
`scripts/analysis/`, and one fixture manifest. Its 39,935-byte
Python-Unicode-sorted UTF-8 included-path payload without terminal LF has
SHA-256 `7e212759b70d9b77b8ee4e6ad4f691952c761e07bca5a0fc2479d52614334bc8`.
Do not push or tag.

Before V8 creation, writable paths are only this C0 and its isolated Git
commit. After commit and full rebind, the only writable paths are the sole V8
assembly/final pair. After final-V8 reviews and another full rebind, only the
inherited exact retry-26/admin Git removal and reviewed retry-30/31
generation/manifest/replay ledgers are writable. Preserve every RED,
nonterminal, and static artifact; V1-V7; retry-26 RED pair; manifests; stale
lock; FIC/frozen residue; and all source/Git/admin/startup/catalog/CLI/archive/
cache/provenance/consumer/host/activation planes except where an exact later
step explicitly authorizes a path. Any failure stops. Gate 6 remains closed.
