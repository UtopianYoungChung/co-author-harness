# v0.43 V10 pre-open transport failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and stopped state

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v9-slot9-checker-c0-addendum.md`,
committed alone at `2b92b1f5d3a6ac035cfa90897a472b103c61caeb`, tree
`5ccea2052091b27ce0321636807317bb98c73ea3`, parent
`a9acdee0e66d9cf714a26d97cb52dcbfb95a8d16`, 6,889 bytes, SHA-256
`d855ee18b984d473f4eb820779aa0e2ffd3a031a04cb62dfda943f33c0f7db8f`,
with independent exact B0/M0/m0 approval.

Immutable V9 remains
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V9.ps1.assembly.tmp`,
405,706 bytes, SHA-256
`e66161e3333fa451ea1da1199ad817fe12234b1169d0405f1616be0ea466e05b`,
4,768 LF, zero CR, terminal LF, no BOM, and 63,464 tokens/zero errors at
both parsers. Final V9 is absent. Both V10 paths are absent. C2 remains 29
files, zero directories, and 26,758,493 file bytes.

The parent C0's V10 create grant is consumed without opening either V10 path.
The first wrapper encoded the complete in-memory validation source as a command
line; Windows refused process creation with `The filename or extension is too
long`. No child launched. The second wrapper delivered source through redirected
standard input, but caller interpolation removed the literal variable names from
the two-line preamble, producing `='generation-structural'` and
`='B:\Agents\.coauthor-v043-c2-detached-retry-26'`. Windows PowerShell 5.1
launched and rejected both expressions, then protected-state assertions failed
because `$Worktree` and `$state` were unset. It exited 1. No V10 create-new,
move, final, operational mode, cleanup, or qualification action occurred.

These are pre-open orchestration failures, not a V10 rejection, operational RED,
qualification result, or success. Static attempt 11 has not started and remains
fresh. Main remains clean at the parent commit; retry-26/admin, manifests, stale
lock, FIC, frozen residue, V2-V9, all historical RED/nonterminal evidence, and
all plane labels remain protected and unchanged.

All plane-separation and no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, archive, shipment, consumer
attestation, host qualification, or activation is authorized. Gate 6 remains
closed.

## Exact corrected V10 derivation

After this exact addendum is independently approved and committed alone, one
fresh complete protected rebind may authorize opening only:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V10.ps1`.

The caller shall build the complete validation source in memory from exact V9
plus a literal validation/derivation tail. Retain V9's parameter block. Parse the
complete source in memory before launch. Encode those exact source bytes as a
data payload and deliver one single-line wrapper through redirected standard
input to one strict-mode Windows PowerShell 5.1 process. That wrapper shall only
decode the payload, call `[ScriptBlock]::Create` once, and invoke it with literal
arguments `-Mode generation-structural -Worktree
B:\Agents\.coauthor-v043-c2-detached-retry-26`. The caller must not interpolate
`$Mode`, `$Worktree`, or any validation-source variable. No helper file,
environment payload, encoded command-line payload, or line-by-line validation
execution is permitted. Capture stdout/stderr, require exit 0, and enforce the
600,000-ms ceiling.

Inside that single invocation, before opening V10, require clean committed main;
C2 29/0/26,758,493; exact V2-V9; both V10 paths absent; exact finalized
governance, current C0, retry-26 worktree/admin, retry-24 and retry-26 RED
binders, manifests, stale lock, FIC, frozen residue, runtime and six-input
bindings; all retry-30/31, attempt-045/046, capsule, transaction, provenance,
temporary, and ownership paths absent; and scalar events, related processes,
jobs, and registered transients zero. Any mismatch stops without write.

Derive V10 only from exact V9: change the three V9 self leaves to V10; bind this
committed C0's exact same-width path, isolated commit, four-digit byte length,
and SHA-256; add V9 assembly once to `BaseC2Names` and its exact SHA once to
`C2Preimages`; and change only the base assertion from 28 files/26,352,787
bytes to 29 files/26,758,493 bytes. The two V9 evidence rows add 134+66 bytes
and the three V10 self leaves add three bytes. V10 therefore remains exactly
405,909 bytes, 4,770 LF, zero CR, terminal LF, no BOM, and 63,471 tokens/zero
parser errors. Prove exact occurrence counts, two residual V9 evidence rows,
reverse byte equality, create-new sequential writes bounded at 65,536 bytes,
write-through durable flush, close, readback, both parsers, and final absence.

## Static attempt 11 and downstream sequence

Only after exact V10 assembly exists may the still-fresh ordered nine-slot
attempt 11 run under the parent C0 without changing its mechanics: nine separate
strict-mode Windows PowerShell 5.1 processes, 600,000-ms ceilings, slots 1-7
updated for V10/base29/fourteen-preimage/total30, slots 8-9 complete protected
bindings with scalar event zero, and slot 9 authored directly as one literal
here-string after slot 8 reports its normalized whole-state digest. Dynamic
slot-9 extraction, replay, replacement injection, concatenated assertion source,
or generated quote boundaries remain forbidden.

Only after slot 9 PASS, with no intervening command except result assertions,
publish V10 by non-overwriting two-argument move and complete the parent C0's
post-move validation. Three independent exact-byte B0/M0/m0 final-V10 reviews
and a new complete protected rebind still precede operational mode. Only then
may inherited retry-26/admin removal and retry-30/31 attempts-045/046 proceed.
C2 gates remain 30 post-review, 38 pre-cleanup, and 34 final. Every failure
stops and must be preserved.

## Review and ledger

This exact candidate requires independent exact-byte B0/M0/m0 review and an
isolated one-file commit directly on main. The commit population and included
path payload must be independently reproduced before commit: 770 tracked, 763
included, zero `.plugin`/`.zip`, six `scripts/analysis/`, one fixture manifest,
and a 40,148-byte Python-Unicode-sorted UTF-8 included-path payload without
terminal LF at SHA-256
`7e184d482f424a71c2224986bb3686df721af1dc1a2c3c432676f7c764812373`.
Do not push or tag.

Writable paths are only this candidate/isolated commit, then the sole V10 pair,
then review-gated inherited removal/qualification ledgers. Preserve every prior
artifact and every other plane. Gate 6 remains closed absent independent v0.43
consumer evidence.
