# v0.43 V4 slot-1 invocation failure - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority and claim boundary

This instrument is subordinate to the finalized research-governance chain and
the exact parent C0
`docs/superpowers/specs/2026-08-06-v043-v3-slot1-checker-c0-addendum.md`,
committed alone at `d170cf288c6e763896f984713350106fc151e4b9`, tree
`f6755541e79c1f660917e0f8af4f0ce525cfddb2`, parent
`62e66c4ad9d9228f6dd8a0a88fcc83afc2227886`, 12,282 bytes, SHA-256
`ebbfc178ceaea8da1f876a1aa71b43b3f945442648602f5e4f43f1a631adb7a2`.
That parent received independent exact-byte B0/M0/m0 approval from
`/root/retry26_authority_audit`, `/root/retry26_mechanics_audit`, and
`/root/retry26_ledger_audit`, each binding 12,282 bytes, 226 LF, zero CRLF,
terminal LF, and no BOM.

The finalized Master Governance v1.0.9 chain, Overseer doctrine/status,
protected evidence, plane separation, inherited downstream sequence, and Gate
6 ceiling continue unchanged except for the exact V5 derivation and 25/33/29
C2 cardinality supersession below. Any absent, draft, ambiguous, inconsistent,
superseded, or conflicting binding fails closed.

This grants no push, tag, install, cache/catalog or research mutation,
archive/package creation, shipment, consumer attestation, host qualification,
promotion, canon change, or activation. Gate 6 remains closed absent
independently governed v0.43 consumer evidence.

## Registered V4 assembly and attempt-5 failure

The parent-authorized V4 derivation completed with exact source/replacement and
outside-span equality, create-new bounded sequential writes, durable flush,
close, and independent readback. The immutable sibling is:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V4.ps1.assembly.tmp`

at 404,707 bytes, SHA-256
`2fa7baa3551e97c238ba8e8728fc82e0cad20b2145e4c651ef26f7ef8864dfd0`,
4,758 LF, zero CR, terminal LF, and no BOM. Both parser entry points reported
63,429 tokens and zero errors. Final V4 remains absent. Immutable V2 remains
404,288 bytes at SHA-256
`a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`;
immutable V3 remains 404,520 bytes at SHA-256
`ceb7c25389fdcab0bceabbeff475071902daf059bee5a1d20e3ad8f0dfbac704`.
C2 is exactly 24 files, zero directories, and 24,731,963 bytes.

Static-validation attempt 5 launched only slot 1 in a fresh Windows PowerShell
5.1 process under strict mode. It freshly rebound V4, passed strict UTF-8,
format, both parsers, authority/self/base/evidence literal checks, and retired
label closure. The wrapper then assigned the first canonical-literal count
without whitespace between function name and string argument:

`$a5=LC'a5babe17...'`

Windows PowerShell treated the concatenated token as command name
`LCa5babe17...` and returned ordinary exit 1 with
`CommandNotFoundException`. Slot 1 emitted no PASS result. This is a static
validator wrapper failure/evidence-incomplete result, not a V4 rejection,
operational RED, qualification result, source pass, or terminal success.

Attempt 5 is consumed and may not be resumed. Slots 2 through 9 did not start.
No move, mode, retry removal/creation, controller, supervised target,
qualification, manifest write, cleanup, publication, or downstream action
occurred. Immediate reconciliation found zero related survivor, immutable
V2/V3/V4 unchanged, final V4 absent, exact C2 above, and clean main at the
parent commit/tree.

## Exact V5 derivation authority

V2, V3, and V4 are immutable parent-bound evidence and may not be edited,
moved, deleted, cleaned, or executed. After this exact C0 is reviewed and
committed alone, authorize creation of exactly:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V5.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V5.ps1`.

Before opening V5, repeat the complete inherited protected rebind, including
clean main at this C0 commit, C2 exactly 24 files/zero directories/24,731,963
bytes, exact V2/V3/V4, absent V5 leaves, retry-26/admin, manifests, stale lock,
FIC/residue, runtime/supervisor, fresh paths, and zero process/event/job state.

Create V5 only from exact V4 by a closed deterministic transformation:

1. change recovery self-leaf literals from V4 to V5;
2. bind the four authority fields to this committed C0's exact
   path/commit/bytes/SHA;
3. add immutable V4's assembly leaf once to `BaseC2Names` and its exact SHA-256
   above once to `C2Preimages`, yielding base24 and nine protected preimages;
4. change only the closed-set base assertion from 23 files/24,327,256 bytes to
   24 files/24,731,963 bytes.

The two inserted LF-terminated evidence rows add exactly 134 and 66 bytes. The
successor authority-relative path has the same byte length as V4's authority
path. This C0's exact byte length has four decimal digits while V4's authority
byte-length literal has five, contributing minus one byte; every other
replacement is length-neutral. V5 is therefore exactly 404,906 bytes, 4,760
LF, zero CR, terminal LF, and no BOM. Both parser entry
points must return exactly 63,436 tokens and zero errors. Freeze its computed
SHA before validation. Prove source/replacement counts, exact post-transform
counts, only the two allowed residual V4 self-leaf evidence rows, and
byte-for-byte outside-span equality. Use create-new bounded sequential writes,
durable flush, close, and independent readback. Any failure preserves evidence
and stops.

## Exact static-validation attempt 6 authority

Authorize exactly one fresh nine-slot attempt 6 over frozen V5. It executes no
script main and no operational mode. Each ordered slot runs in a separate
Windows PowerShell 5.1 process under `Set-StrictMode -Version Latest`, with the
inherited exact 600,000-millisecond ceiling, timeout termination, survivor
census, evidence preservation, and attempt-consumption semantics.

Slot 1 replaces the ambiguous one-letter wrapper with exactly:

`function Get-LiteralCount { param([string]$Value) return [regex]::Matches($text,[regex]::Escape($Value)).Count }`

Every invocation uses separated named syntax
`Get-LiteralCount -Value '<literal>'`. It freshly performs the full inherited
byte/format/parser, transformation, authority, label, canonical-literal,
manifest UUID-v4, sole-finalizer, immediate-return, and no-catch-republish
checks and must emit one PASS result.

Slot 2 extracts exactly 124 helpers, evaluates only `BaseC2Names` and
`C2Preimages`, keeps mode/capsule null, and proves assembly-static base24 at
24,731,963 bytes, nine individually hash-bound preimages including V2/V3/V4,
total C2 exactly 25 files/25,136,869 bytes, final absence, zero directories,
and zero errors. Slots 3 through 6 separately run the four inherited preserved
binders. Slot 7 repeats fifteen retry-26 and five corrected retry-24 adversarial
rejections. Slots 8 and 9 repeat the full protected binding; slot 9 must equal
slot 8 and additionally prove exact V2/V3/V4, final absence, same-volume
relation, and base24 equality. Any failure consumes attempt 6 and stops.

Only after slot 9 PASS, with no intervening command except non-mutating result
assertions, atomically move V5 sibling to final V5 using non-overwriting
two-argument `[IO.File]::Move`. Post-move rebind final V5 and V2/V3/V4, prove
sibling absence, repeat both parsers, re-extract helpers under strict mode,
capture a fresh complete protected state, set only prospective
`generation-structural`, require `Assert-FrozenBindings`, and require runtime
base24 plus final V5 closure.

Then obtain three independent exact-byte B0/M0/m0 reviews of final V5. No mode
runs before unanimous approval and another full protected rebind. The
post-review/pre-generation C2 gate is exactly 25 files. Four downstream
capsule/preimage pairs yield 33 files before cleanup. Removing exactly four
provisional preimages yields 29 final files. These 25/33/29 gates supersede all
older C2 cardinalities and bind the corresponding rebind/review/summary gates.

## Review, commit, and writable ledger

This exact C0 requires three independent exact-byte B0/M0/m0 reviews before it
becomes operative. Any edit invalidates them. Commit only this file on `main`
with parent `d170cf288c6e763896f984713350106fc151e4b9`. The prospective
commit has exactly 764 tracked, 757 governed included, zero plugin/ZIP, six
`scripts/analysis/`, and one manifest path. Its Python-Unicode-sorted UTF-8 LF
included-path payload without terminal LF is 39,722 bytes at SHA-256
`a90af4169241b1896bc0b326560584022af83e14946fa7cf54e93bb83273f9de`.
Do not push or tag.

Only this C0/Git commit state, the sole V5 create-new sibling and atomic move,
then after final-V5 review/rebind the exact Git-managed retry-26 worktree/admin
removal, and then the inherited retry-30/31 attempts-045/046 ledger may change.
Every other path is read-only. Preserve all RED/nonterminal/static evidence,
V1/V2/V3/V4, retry-26 RED pair permanently, manifests, stale lock,
FIC/residue, package/archive, cache/catalog, shipment, consumer, research,
host, and activation planes.

Any new timeout, crash, rejection, survivor, RED, missing receipt, or drift
stops and preserves evidence. Even a fully green source sequence stops at Gate
6 pending independently governed v0.43 consumer evidence.
