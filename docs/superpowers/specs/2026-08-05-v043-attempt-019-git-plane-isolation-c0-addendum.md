# v0.43 attempt-019 Git-plane isolation C0 addendum

**Status:** approved package-maintainer recovery authority for the exact
attempt-019 output-scope refusal. This record must be committed alone before
any residue disposition, detached-worktree creation, retry transaction, or
evidence update. It grants no package clearance, archive construction,
shipment, cache or catalog mutation/equality, consumer re-attestation, host
qualification, activation, research mutation, acceptance, promotion, release,
or canon authority.

**Run scope:** `adhoc_review`.

**Parent authorities:**

- `docs/superpowers/specs/2026-08-02-v043-qualification-release-shipment-c0-addendum.md`;
- `docs/superpowers/specs/2026-08-03-v043-c2-attempt-017-owner-job-smoke-c0-addendum.md`;
- `docs/superpowers/specs/2026-08-03-v043-c2-pid-width-first-observation-c0-addendum.md`.

All parent sequencing, red-preservation, path, single-branch, exact-byte, and
no-claim boundaries remain binding except for the exact superseding retry
ledger below.

## Active governance binding

The current research-governance head is Master Governance 1.0.9 through the
validated A02 -> M02 -> J04 resolver, not by inference from the installed
policy header alone:

- installed policy: `B:\Agents\research\10_Governance\MASTER_GOVERNANCE.md`,
  77,262 bytes, SHA-256
  `a55b56eee7d72b4aa8a7f2376cb023d987ee7c12e71611fef7a9b457d780e191`;
- amendment record M02: SHA-256
  `201d9b68e9c050f0f1e937977833868dd8ec2e3c5d6395f044fc00103fc95afe`;
- frozen journal J04: SHA-256
  `cb4ba88728574829de378a323b4b6f5c53aeaac9a81b890d5a2221ab9e7c52f4`;
- activation anchor A02: SHA-256
  `7ae758e443f4360e9738094e6fd1080022b30fa60f8807d0baa1cf0640fb7ebc`,
  activated `2026-08-04T20:00:35.9192246-04:00`.

This is bounded Plane-I instrument maintenance under the user's direct scoped
commission. It has no Plane-R standing. The rejected Plane-I amendment
proposal for Master Governance 1.0.10 is not authority for this work.

## Preserved attempt-019 transaction

`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260803-attempt-019`
is immutable failed evidence under its original authority:

- terminal receipt SHA-256
  `94555709f8acba70fe917002badc9a54dd46bcaa85d1b928f2eba11545644d34`;
- state `refused`; diagnostic `RELEASE-CONTROLLER-OUTPUT-SCOPE`;
- product exit 0 with 93 suites / 97 cases matching expected exits;
- seven disallowed postimage paths: two loose Git tree objects and five paths
  below `.git\refs\codex\turn-diffs\`;
- source ref preimage resolves to commit
  `e10ea6b05002c4945359a83a29cc90c786d9d5e5`, tree
  `7121fccbe16b29260ad8dc3e5462f22e949ad61c`.

The path names are consistent with ambient Codex Git-administration activity,
but the receipt does not prove causation. They remain a real output-scope
violation. The green child product result does not upgrade the refused
transaction and grants no downstream gate.

The sole tracked worktree change is failed-transaction residue:
`docs/analysis/generated/fixture_manifest.json`, 54,258 bytes, SHA-256
`ae32f0d52f6d3fd0acfd7c20463ac93edd0059ba41560114accced81aed697a9`.
It must remain byte-identical until a fresh successful transaction authorizes
its exact replacement. Attempt 017 and every other red, warning, interruption,
or missing-marker record remain preserved.

## Frozen repository and exact writable ledger

- Repository: `B:\Agents\platform\co-author-harness`; branch: `main` only.
- Pre-addendum HEAD:
  `e10ea6b05002c4945359a83a29cc90c786d9d5e5`.
- Pre-addendum HEAD tree:
  `7121fccbe16b29260ad8dc3e5462f22e949ad61c`.
- Exactly one registered worktree exists before this record.
- Both retry worktrees and attempt-021/022 roots below were observed absent.
- The shared runner lock exists at
  `.git/coauthor-fixture-runner.lock`, 57 bytes, SHA-256
  `d7eb675d2c86f85db64d7d20237c79398be3e679f07997a8440f197229190b75`;
  its recorded PID 52264 is not live.

| Authority | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE AND COMMIT ALONE | `docs/superpowers/specs/2026-08-05-v043-attempt-019-git-plane-isolation-c0-addendum.md` | absent | not applicable |
| QUALIFICATION OUTPUT REPLACEMENT ONLY | `docs/analysis/generated/fixture_manifest.json` | 54,258 | `ae32f0d52f6d3fd0acfd7c20463ac93edd0059ba41560114accced81aed697a9` |
| MODIFY AFTER SUCCESS ONLY | `releases/verification/v0.43.0/C2/C2_SOURCE_QUALIFICATION.md` | 2,034 | `cae9a2ef7bc88f0ee02e4c79c2b108888894b1a5f5d16def6a0afbd9e889d243` |
| MODIFY AFTER SUCCESS ONLY | `releases/verification/v0.43.0/C2/C2_STRUCTURAL_RESULTS.json` | 634 | `5556b373de0cfa7f209a0afa915620569e33515079f5bf5c00f1f8310f82998e` |
| MODIFY AFTER SUCCESS ONLY | `releases/verification/v0.43.0/C2/C2_REGISTRY_RESULTS.json` | 2,254 | `957a91428ef875097678d62590c58baae86988732d2c3a7be54b13c35b0ce9de` |
| MODIFY AFTER SUCCESS ONLY | `releases/verification/v0.43.0/C2/C2_DETACHED_REPLAY.json` | 2,590 | `38862b17278643de65e6784bb166f22998c6f6c9f5db35a3e74da38d0061cb49` |
| CREATE AFTER SUCCESS ONLY | `releases/verification/v0.43.0/C2/C2_READ_ONLY_REVIEW.md` | absent | not applicable |

No controller, smoketest, fixture source, schema, compatibility profile,
kernel, package, version, cache, catalog, startup, host, consumer, research, or
shipment byte is writable under this authority. A product-source defect or any
need to widen this ledger is a stop requiring a new C0 record.

## Exact transient authority and isolation semantics

Only these fresh transient paths may be created:

- detached registered worktree
  `B:\Agents\.coauthor-v043-c2-detached-retry-7`;
- clean detached replay worktree
  `B:\Agents\.coauthor-v043-c2-detached-retry-8`;
- generation transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-021`;
- clean exact-byte replay transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-022`.

`git worktree add --detach` and removal may create and remove only the
corresponding Git-managed administrative subtrees
`.git/worktrees/.coauthor-v043-c2-detached-retry-7/**` and
`.git/worktrees/.coauthor-v043-c2-detached-retry-8/**`, plus the containing
`.git/worktrees` directory only if Git itself finds it absent. Their exact
resolved paths and bytes must be recorded after creation; no manual
common-Git-directory edit is authorized. Ordinary Git object, index, reflog,
and `refs/heads/main` changes are authorized only as Git-managed consequences
of the three enumerated repository commits: this C0 alone, the successful
manifest alone, and the registered C2 evidence alone. No other branch may be
created or moved.

The first detached worktree must be created from the committed addendum HEAD in
detached state. It is the sole output watch root for attempt 021; only its exact
fixture-manifest path is an allowed output. The primary worktree root is bound
as an input root so any durable cross-worktree source postimage is input drift.
This pre/post inventory does not claim to detect a write that is restored to
identical bytes before finalization. The primary
`.git/HEAD`, `.git/refs/heads/main`, and the exact detached-worktree Git-dir
`HEAD`, `index`, `commondir`, and `gitdir` files are exact inputs. The resolved
detached Git-dir and each input binding must be recorded before launch. This
isolates the ambient shared Git object store and `refs/codex/turn-diffs`
administration from the product output plane without ignoring any byte inside
the tested detached worktree or any durable primary source postimage.

This topology proves exact source bytes and authority-bearing commit refs; it
does not claim total common-Git-directory quiescence. Unrelated ambient Codex
object and turn-diff refs are outside the product plane, while primary HEAD,
main, and detached HEAD remain exact drift-checked inputs.

The controller receipt excludes the shared runner lock because the lock is in
the common Git directory outside the sole detached watch root; it must not be
passed as `--ignored-output`. The lock is the only authorized child/product
write outside that watch root. Registered C2 evidence must separately bind its
pre/post bytes, SHA-256, raw PID text, owner liveness, and exact owner transition;
any other transition is a failure. It has no source, package, or qualification
standing. Controller transaction evidence and the enumerated Git-managed
worktree/commit administration are separate authorized writes. No `.git` path
may otherwise be deleted, restored, or classified as harmless evidence.

## Frozen launcher contract

Both launches use exact interpreter
`C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe` with `-B`
for the controller copy inside the applicable detached worktree and for its
fixture-runner child. Before launch, `PYTHONUTF8`, `PYTHONPATH`, `PYTHONHOME`,
`PYTHONWARNINGS`, and `PYTHONOPTIMIZE` must all be absent. Both controller
starts pass `--allow-user-site`, no `--ignored-output`, the primary checkout as
the sole `--input-root`, the primary HEAD/main inputs, and the four resolved
detached Git-dir inputs above.

Attempt 021 is started with:

```text
<python> -B <retry-7>\scripts\release_qualification_controller.py start
  --run-root C:\Users\young\AppData\Local\Temp
  --run-id coauthor-v043-qualification-20260805-attempt-021
  --cwd <retry-7>
  --input-root B:\Agents\platform\co-author-harness
  --input <primary-.git-HEAD> --input <primary-main-ref>
  --input <retry-7-gitdir-HEAD> --input <retry-7-gitdir-index>
  --input <retry-7-gitdir-commondir> --input <retry-7-gitdir-gitdir>
  --allowed-output <retry-7>\docs\analysis\generated\fixture_manifest.json
  --watch-root <retry-7> --allow-user-site --
  <python> -B <retry-7>\scripts\analysis\fixture_runner.py
  --tier full --cache-mode off
```

Attempt 022 uses the identical ordering with retry-8/attempt-022 substitutions,
omits `--allowed-output`, and appends `--no-write` to the child command. Angle
brackets above are exact values resolved and recorded after Git creates the
worktree; they are not shell variables. Status/wait operations use the same
detached controller copy, run root, and run ID.

## Recovery and proof sequence

1. Commit only this addendum on `main`; leave the attempt-019 manifest residue
   unstaged and byte-identical.
2. Re-observe exact HEAD, tree, status, worktrees, transient-root absence,
   process residue, shared runner lock, resolved detached Git-dir, its four
   exact inputs, and clean detached
   `git status --porcelain=v1 --untracked-files=all`.
3. Create detached retry-7 and start attempt 021 through the durable
   qualification controller with cache off, the exact output/input topology
   above, and the authoritative fixture registry without `--no-write`.
4. Require an immutable terminal `succeeded` receipt, product exit 0, 93/93
   suites, 97/97 cases, zero warnings, stable primary input root and all exact
   Git inputs, no output outside the detached manifest, exact detached status
   containing only its modified manifest, and zero surviving owned processes.
   A refusal, warning, ambiguity, residue, or missing receipt is a stop and
   remains red evidence.
5. Only after step 4 may the exact detached manifest postimage replace the
   primary residue. Immediately after the terminal receipt and before any other
   action, capture its exact bytes, SHA-256, and byte length; verify its run
   identifier, 93 suite hashes, and 97 result rows against receipt-bound stdout.
   Copy exactly those bytes to primary, prove byte equality, then commit only
   the manifest on `main`.
6. After receipt and manifest bindings are copied and verified, record the
   exact retry-7 path and resolved admin subtree, then run
   `git worktree remove --force` for retry-7 because its authorized manifest
   postimage leaves it dirty. Prove both worktree and admin subtree absent.
   Create detached retry-8 at the manifest commit. Run
   attempt 022 with cache off and `fixture_runner.py --no-write`, no allowed
   product output, the detached root watched, and the primary root plus exact
   primary ref and detached Git-dir files bound as inputs. Require a clean immutable
   terminal `succeeded` receipt, byte-identical pre/post source state, identical
   detached index input, and clean detached status before and after.
7. Only after both transactions succeed may the registered C2 evidence paths
   be updated, exact-byte reviewed by three independent reviewers, and
   committed on `main`. Copy and verify needed receipts before ordinary
   `git worktree remove` removes clean retry-8; prove both worktree/admin subtree
   and transient process residue are gone. The immutable transient transaction
   roots remain evidence.
8. Continue to version integration and repeated qualification only through the
   pre-existing main v0.43 C0 sequence. Gate 6 remains closed absent a separately
   governed v0.43 consumer compatibility receipt. This task may not author or
   simulate that receipt. ZIP/cache work, clearance, push/tag/assets, shipment,
   fresh-host qualification, and activation remain closed.

Any unlisted changed byte, new finding, warning, ambiguous postimage, failed
transaction, or inability to bind the exact planes above is a stop requiring
new explicit authority.
