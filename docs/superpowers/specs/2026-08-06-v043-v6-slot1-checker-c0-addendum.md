# v0.43 V6 slot-1 compression failure - C0 successor addendum

Status: proposed successor C0; non-operative until exact B0/M0/m0 approval and
isolated one-file commit. Date: 2026-08-06 America/Toronto. Run scope:
`full_lifecycle`, limited to v0.43 source qualification.

## Authority and failure

This instrument is subordinate to finalized Master Governance v1.0.9 and exact
parent C0
`docs/superpowers/specs/2026-08-06-v043-v5-slot3-checker-c0-addendum.md`,
committed alone at `7872d74884ff54a8521a05add774b9b192a63f64`, tree
`c4a72060d69b557d29101a84f97438b91722ba9b`, parent
`d6c10512e2fe839eeb19407edb37815d50079e10`, 7,644 bytes, SHA-256
`211e51d63f0b908a1404531872e20c13c78f8a6ffc2c5c907426a4620baa1bdd`,
with independent exact B0/M0/m0 approval.

Immutable V6 is
`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V6.ps1.assembly.tmp`,
405,106 bytes, SHA-256
`22e7bc4d3d98ca28dd65235a325a29fb768cfe894dbe008850d53ab6c1cf78e6`,
4,762 LF, zero CR, terminal LF, no BOM, 63,443 tokens/zero errors at both
parsers. Final V6 is absent. C2 is 26 files/zero directories/25,541,975 bytes.

Attempt 7 launched only slot 1. Its full initializer and initial V6 byte/format/
parser binding passed. The wrapper then compressed the reviewed declaration to
`return[regex]::Matches`, violating the required whitespace and producing exit
1 `CommandNotFoundException` before any literal check or PASS result. Attempt 7
is consumed; slots 2-9 did not start. This is wrapper evidence-incomplete, not
a V6 rejection, operational RED, qualification result, or success. No move,
mode, retry, manifest, cleanup, or publication occurred; V6 remains exact,
final absent, main clean, and related survivors zero.

All parent plane separation/no-claim rules continue. No push, tag, install,
cache/catalog/research mutation, package, shipment, consumer attestation, host
qualification, or activation is authorized. Gate 6 remains closed.

## Exact V7 derivation

V2-V6 are immutable evidence. After this C0 is reviewed and committed alone,
repeat the complete protected rebind and create only V7 assembly/final paths:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V7.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V7.ps1`.

Derive V7 only from exact V6: change V6 self leaves to V7; bind this committed
C0's four authority fields; add V6 assembly once to `BaseC2Names` and its SHA
above once to `C2Preimages`; change only the base assertion from
25 files/25,136,869 bytes to 26 files/25,541,975 bytes. The exact inserted rows
add 134+66 bytes; authority paths and four-digit byte-length literals are
equal-width. V7 is exactly 405,306 bytes, 4,764 LF, zero CR, terminal LF, no
BOM, and 63,450 tokens/zero parser errors. Prove exact counts, two residual V6
evidence rows, reverse byte equality, create-new sequential bounded writes,
durable flush, close, and readback. Freeze its SHA. Before opening, require
clean committed main, C2 26/0/25,541,975, exact V2-V6, V7 absent, exact
governance/retry-26/admin/manifests/lock/FIC/residue/runtime, all fresh paths
absent, and process/event/job/transient state zero.

## Exact static attempt 8

Authorize one fresh nine-slot attempt 8 over V7; no script main or operational
mode. Each ordered slot is a separate strict-mode Windows PowerShell 5.1
process with 600,000-ms ceiling and fail-closed consumption. Each slot first
sets exact Primary root, exact C2 directory, exact empty SHA, fresh ArrayList
Errors, and slot Phase; any additional state precedes helper calls.

Slot 1 must copy this declaration line-for-line, without compression:

```powershell
function Get-LiteralCount {
    param([string]$Value)
    return [regex]::Matches($text, [regex]::Escape($Value)).Count
}
```

Every call is exactly `Get-LiteralCount -Value '<literal>'`. Slot 1 repeats all
byte/format/parser/transformation/authority/label/canonical/UUID/finalizer
checks. Slot 2 repeats 124 helpers, base26 at 25,541,975 bytes, eleven exact
preimages, and total C2 27 files/25,947,281 bytes. Slots 3-6 separately run the
four preserved binders with the full initializer; slot 6 also validates
retry-26 shape. Slot 7 repeats fifteen retry-26 and five retry-24 adversarial
rejections. Slots 8/9 repeat the full protected binding; slot 9 equals slot 8
and additionally binds V2-V6, base26, same volume, and final absence.

Only after slot 9 PASS and no intervening command except result assertions,
non-overwriting two-argument move publishes final V7. Post-move rebind final
and V2-V6, prove sibling absent, repeat parsers, helpers, fresh complete state,
`Assert-FrozenBindings`, and runtime base26+final closure with only prospective
`generation-structural` set.

Three independent exact-byte B0/M0/m0 final-V7 reviews and a full rebind precede
any mode. Then only Git-managed retry-26/admin removal and inherited retry-30/31
attempts-045/046 may proceed. C2 gates are 27 post-review, 35 pre-cleanup, and
31 final; four pairs add eight and cleanup removes four preimages.

## Review and ledger

This C0 needs exact B0/M0/m0 review. Commit only it with parent
`7872d74884ff54a8521a05add774b9b192a63f64`. Prospective population: 766
tracked, 759 included, zero plugin/ZIP, six `scripts/analysis/`, one manifest;
39,864-byte included-path payload SHA-256
`63436b70c5dee6d960a4323f03451b3c936de78fe90eceb172928b02b5d1996f`.
Do not push/tag.

Writable paths are only this C0/Git commit, sole V7 create/move, then inherited
review-gated removal/qualification paths. Preserve every RED/nonterminal/static
artifact, V1-V6, retry-26 RED pair, manifests, lock, FIC/residue, and all
package/cache/catalog/shipment/consumer/research/host/activation planes. Any
failure stops; Gate 6 remains closed.
