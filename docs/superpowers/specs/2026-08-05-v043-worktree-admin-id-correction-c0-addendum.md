# v0.43 detached-worktree admin-ID correction C0 addendum

**Status:** approved package-maintainer correction for the exact Git-managed
administrative ID observed when creating detached retry-7. Commit this record
alone before removing, recreating, or using that worktree. It grants no new
product-source, qualification-result, package, cache, catalog, consumer,
shipment, host, activation, research, acceptance, promotion, release, or canon
authority.

**Run scope:** `adhoc_review`.

**Parent authority:**
`docs/superpowers/specs/2026-08-05-v043-attempt-019-git-plane-isolation-c0-addendum.md`,
13,252 bytes, SHA-256
`e2ed3f5bf30ceb24ffd2be354e72ded4236e09c301caa3b9d0dd896c0be7b5ad`,
committed as `63113ed7ff098c23de38558db00424a5272e7000`.

All parent sequencing, path, red-preservation, exact-byte, single-branch, and
no-claim boundaries remain binding except for the exact administrative-ID and
cleanup correction below.

## Preserved stop and observed topology

The parent preflight created detached worktree
`B:\Agents\.coauthor-v043-c2-detached-retry-7` at commit
`63113ed7ff098c23de38558db00424a5272e7000`, then stopped before starting the
controller because Git canonicalized the basename into the administrative ID
`-coauthor-v043-c2-detached-retry-7`, not the parent record's predicted dotted
ID. This is preserved as an unlisted Git-administration path requiring this
correction; it is not silently treated as conforming.

No attempt-021 transaction root exists. The detached worktree is clean under
`git status --porcelain=v1 --untracked-files=all`. Apart from this untracked
correction record, the primary worktree's sole pre-existing change remains the
immutable attempt-019 manifest residue, 54,258 bytes, SHA-256
`ae32f0d52f6d3fd0acfd7c20463ac93edd0059ba41560114accced81aed697a9`.
No product or qualification command ran.

The actual administrative subtree is
`B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-7`:

| Entry | Bytes | SHA-256 |
|---|---:|---|
| `commondir` | 6 | `340ddcb67a6204f742cd1e28e5b462622dde7daaa8ee36001897196aacdc6d47` |
| `gitdir` | 50 | `e06aa3aacce0cd7a07b47a3091fd2b996056f5af978e44496604a0dd7d5c5ab7` |
| `HEAD` | 41 | `f5df866912bd022928856a7205a36c0e5e22c07104706944cbe32bb53805d6db` |
| `index` | 94,471 | `5b5b4f10e50e9ce04f3e3fa2afba0c1f7f0a9ad0898093066d4d1c7c946c2229` |
| `logs/HEAD` | 137 | `68a4d404720ed3ad6a81eb53ff7cf17c3186f2e2de15311a96a93fe94c9f73b5` |
| `ORIG_HEAD` | 41 | `f5df866912bd022928856a7205a36c0e5e22c07104706944cbe32bb53805d6db` |

The empty `refs/` directory is also present. No other entry exists in the
subtree.

## Exact correction and commit ledger

1. Create and commit only this correction on `main`. Ordinary Git object,
   index, reflog, and `refs/heads/main` changes are authorized only as the
   Git-managed consequence of this correction commit.
2. The parent references to
   `.git/worktrees/.coauthor-v043-c2-detached-retry-7/**` and
   `.git/worktrees/.coauthor-v043-c2-detached-retry-8/**` are superseded by the
   observed retry-7 Git ID
   `.git/worktrees/-coauthor-v043-c2-detached-retry-7/**` and the expected,
   fail-closed retry-8 ID
   `.git/worktrees/-coauthor-v043-c2-detached-retry-8/**` respectively.
3. After the correction commit, record retry-7's clean state and exact resolved
   path, then use ordinary `git worktree remove` to remove it and its exact
   hyphenated admin subtree. Prove both absent. No force removal is authorized
   at this pre-qualification point because the worktree is clean.
4. Recreate retry-7 with `git worktree add --detach` at the correction commit.
   Require the same hyphenated administrative ID, record its exact files and
   hashes, bind `HEAD`, `index`, `commondir`, and `gitdir` as the parent requires,
   and re-run the full parent preflight before attempt 021.
5. The parent force-removal authority after a successful dirty attempt 021 and
   ordinary-removal authority after clean attempt 022 apply only to the exact
   corrected hyphenated admin subtrees.

No dotted admin subtree may be created, deleted, or used as evidence. Any
different Git administrative ID, extra worktree entry, changed primary residue,
created attempt root, non-clean retry, warning, or ambiguity is a stop requiring
new explicit authority.
