# v0.43 V2 adversary-index failure - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority and claim boundary

This instrument is subordinate to the finalized research-governance chain and
the exact parent C0
`docs/superpowers/specs/2026-08-06-v043-v2-static-timeout-c0-addendum.md`,
committed alone at `6c7ec69678b6c921f2c115261db2b96f91512a71`, tree
`68c8646e1e36081c609c1adfa20e3a7556fb298f`, parent
`c1b9e6094f5b69e5eb3f1d4b63193b71bd0ea62e`, 13,094 bytes, SHA-256
`f2f439f7d18cdd9af091b6807e88bfc1961a9407146e8724bebd95514602df61`.
That parent received independent exact-byte B0/M0/m0 approval from
`/root/retry26_authority_audit`, `/root/retry26_mechanics_audit`, and
`/root/retry26_ledger_audit`, each binding 13,094 bytes, 228 LF, zero CRLF,
terminal LF, and no BOM.

The parent's finalized Master Governance v1.0.9, amendment, A02 anchor,
Overseer doctrine/status, plane separation, protected evidence, downstream
sequence, and Gate 6 ceiling continue unchanged. Any absent, draft, ambiguous,
inconsistent, superseded, or conflicting binding fails closed.

This grants no push, tag, install, catalog/cache mutation, archive/package
creation, shipment, consumer attestation, research mutation, host
qualification, promotion, canon change, or activation. Gate 6 remains closed
absent independently governed v0.43 consumer evidence.

## Registered static-validation attempt 2 failure

The frozen assembly sibling remained exactly:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V2.ps1.assembly.tmp`

at 404,288 bytes, SHA-256
`a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`,
4,754 LF, zero CR, terminal LF, and no BOM. Final V2 remained absent.

Static-validation attempt 2 ran under the parent's exact nine-slot,
600,000-millisecond-per-slot contract. Slots 1 through 6 returned ordinary
exit-zero PASS results:

1. exact format/parser/text closure: both parser entry points reported 63,408
   tokens and zero errors; retired active labels, canonical literals, UUID-v4
   manifest grammar, authority literals, transformation counts, and the sole
   guarded finalizer closed;
2. exact assembly-static helper closure: 124 isolated helper definitions, 21
   base names, six protected preimages, base21 at 23,518,448 bytes, and C2 at
   22 files/23,922,736 bytes with mode/capsule null;
3. original failed-capture binder PASS with three preserved RED files and zero
   validator errors;
4. retry-22 binder PASS for its exact target and publication preimage with zero
   validator errors;
5. retry-24 binder PASS for its exact target/preimage, target canonical
   `083583d2264ed2d9da0a58551ea0d038a31f168bb1dfa3b7bb8efad8ef4c9962`,
   normalized/preimage canonical
   `a5babe17da8b26c2d72ce8a3f179858c26ea3f45db8318f6ab6c84362efad048`,
   and zero validator errors; and
6. retry-26 binder PASS for its exact target/preimage, target canonical
   `db4b4e8a7185ebc82f0b9a9285a8a9d9b96d444fe61734110802d3e90088e557`,
   normalized/preimage canonical
   `311823f6562eead196ddb55535869ed52865a81b25c465c06cbb3a7a15a80ead`,
   exact RED/error vector, and zero validator errors.

Those results are preserved partial evidence only. The parent expressly denied
publication authority from partial phase verdicts, so none may be reused to
skip a fresh slot.

Slot 7 returned ordinary exit 1 after 53.2 seconds with
`IndexOutOfRangeException` at the retry-24 error-order adversary. The checker
deep-cloned the retry-24 target, whose `errors` array has exactly one entry
(`UNHANDLED`), then attempted to swap indexes zero and one. The publication
preimage, not the target, has the two-entry ordered vector `UNHANDLED`,
`CAPSULE-PUBLICATION-PENDING`. Slot 7 emitted no PASS result. Any internal
progress before the exception is non-reusable. This is a static-checker
failure/evidence-incomplete result, not a V2 rejection, operational-mode RED,
qualification result, source pass, or terminal success.

Attempt 2 is consumed and may not be resumed. Slots 8 and 9 did not start. No
mode, retry removal/creation, controller, supervised target, qualification,
manifest write, cleanup, publication, or downstream action occurred.

Immediate post-failure reconciliation found zero related surviving process,
the sibling unchanged at the exact binding above, final V2 absent, C2 exactly
22 files/zero directories/23,922,736 bytes, and clean main at the parent
commit/tree. Retry-26 remains present and untouched. The candidate-excluded
primary input-root preimage is exactly 1,888 identities: 315 directories,
1,573 files, zero links/other, 127,165,690 file bytes, 218,675 inventory bytes,
and SHA-256
`c0530ab1b5620b87a18649e9ac822f2e10fca2b84907744612cb918c238f3f44`.

## Exact attempt 3 authority

The complete sibling remains frozen evidence. Do not edit, append, truncate,
rewrite, rebuild, replace, delete, clean, resume attempt 2, or create an
alternate candidate. Authorize exactly one fresh static-validation attempt 3
over those same bytes. It executes no script main and no operational mode.

Attempt 3 repeats all nine parent slots from zero, in the same order, with the
same exact 600,000-millisecond caller wall-clock ceiling per process and the
same timeout termination, survivor census, evidence preservation, and
attempt-consumption semantics. No attempt-2 PASS substitutes for an attempt-3
result.

Slots 1 through 6 are byte-for-byte-equivalent fresh checks of the parent
contracts. Slot 7 uses only these corrected in-memory retry-24 adversaries:

1. first assert the target error vector has exactly one entry with code
   `UNHANDLED` and the publication-preimage vector has exactly two entries in
   order `UNHANDLED`, `CAPSULE-PUBLICATION-PENDING`;
2. mutate target error detail and require target canonical mismatch;
3. deep-clone the publication preimage, swap only its two validated error
   entries, and require normalized/publication canonical mismatch;
4. mutate target `ended_utc` and require target canonical mismatch;
5. mutate canonical serialization bytes and require raw/canonical byte-binding
   mismatch; and
6. remove only the target's terminal LF in memory and require raw-binding
   mismatch.

Slot 7 also repeats rejection of every field in the closed retry-26 preserved
schema. It must emit one concise PASS result reporting fifteen retry-26 and
five retry-24 rejections. Any unexpected shape, exception, missing rejection,
or missing result consumes attempt 3 and stops.

Slot 8 repeats the complete protected phase-5 rebind. Slot 9 freshly repeats
that full binding set, requires exact equality to slot 8, additionally proves
final-path absence, same-volume relation, and base21 equality, and permits no
intervening command except non-mutating result assertions. Only then may the
caller atomically publish with the non-overwriting two-argument
`[IO.File]::Move($assemblySibling, $finalV2)`.

After a successful move, re-read final V2, prove exact equality to the validated
404,288-byte binding and sibling absence, repeat both parsers, re-extract
helpers from final bytes, set only prospective `generation-structural`, and
require no-switch runtime closed-set equality for base21 plus final V2. Any
post-Move failure preserves final V2 and stops.

Then obtain three independent exact-byte B0/M0/m0 reviews of final V2. Any edit
invalidates them. No operational mode runs before unanimous approval and the
inherited full protected-evidence rebind.

## Review, commit, and writable ledger

Before this C0 becomes operative, three independent exact-byte B0/M0/m0 reviews
must bind its parent, attempt-2 evidence, corrected adversary, attempt-3 slots,
publication boundary, protected ledger, downstream sequence, and Gate 6.
Any edit invalidates all reviews.

After unanimous approval, commit only this file directly on `main`. The commit
must parent `6c7ec69678b6c921f2c115261db2b96f91512a71`, contain 761 tracked and
754 included paths, zero plugin/ZIP, six `scripts/analysis/` paths, one
manifest, and a 39,496-byte included-path payload at SHA-256
`45306469189bc8f97b587c3955180eca5e88423a4b11078d1c7077683ea4cfe6`.
Do not push or tag.

Only these paths may change:

1. this C0 path and primary Git objects/ref/log/index/`COMMIT_EDITMSG` for its
   isolated commit;
2. the exact assembly sibling only through a successful atomic move to exact
   final V2; its bytes may not change; and
3. after final-V2 unanimous review and the inherited full protected rebind,
   only the exact Git-managed removal of
   `B:\Agents\.coauthor-v043-c2-detached-retry-26` and its registered admin;
   use Git worktree management, never manual deletion, and prove both absent
   while every other protected binding remains unchanged; then only the
   still-pending inherited retry-30/31 and attempts 045/046
   recovery/qualification ledger.

Every other path remains read-only. Preserve all older RED/nonterminal/static
evidence, the immutable V1 script, retry-26 RED pair permanently, retry-26
worktree/admin through the exact review-and-rebind gate above, manifests, stale
lock, FIC/residue, runtime/supervisor, package/archive, cache/catalog, shipment,
consumer, research, host, and activation planes.

Any new timeout, crash, rejection, survivor, RED, missing receipt, or drift
stops and preserves evidence. Even a fully green source sequence stops at Gate
6 pending independently governed v0.43 consumer evidence.
