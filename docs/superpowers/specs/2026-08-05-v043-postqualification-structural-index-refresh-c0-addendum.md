# v0.43 postqualification structural-index refresh C0 addendum

**Date:** 2026-08-05  
**Status:** Approved C0 correction authority when these exact bytes receive
three independent B0/M0/m0 reviews; no
qualification, version, package, cache, consumer, shipment, host, research,
activation, acceptance, promotion, release, or canon claim  
**Run scope:** `adhoc_review`

## Parent authority and preserved stop

This correction is subordinate to the active Master Governance resolution and
the complete committed v0.43 C0 chain, especially:

- `docs/superpowers/specs/2026-08-05-v043-preexisting-temp-residue-correction-c0-addendum.md`.

Every parent single-branch, exact-byte, transaction, process, stream,
tested-population, residue, evidence, review, consumer, and no-claim boundary
remains binding except for the exact postqualification index correction below.

Attempt 028 at
`C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-028`
terminally succeeded at the exact manifest commit
`c11e59a71cfc85649775f6e30daeec2ca3f8ff48`. Before and immediately after
that transaction, clean retry-13's 95,071-byte index remained SHA-256
`ae88a7f7f8c32145bb2252052bf2c6080c2334aa9a2d8d541916dc8dc02050cb`.
The controller receipt is 368,044 bytes, SHA-256
`bc9b24c30a718912f10366edeb55f36d1c75aeddf99e64e2350ade217fa04c5a`.
It records state `succeeded`, diagnostic `null`, child exit `0`, canonical
intent SHA-256
`46c3a55ef1097b6f08a9c0aa1fd698ca00e98567b0a83117f61543ee18cd005c`,
and request SHA-256
`1180bd692102f643d0eea96159bf057bb0800ee9d69ae7b0c2cea5848cdd5787`.
Its 9,649-byte stdout has SHA-256
`ebcb3cad1b0ef1bf997b492e3470d1adf6eece2b23026a4e4ac9f798f508ce40`;
child stderr and both worker streams are zero bytes at SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
It executed the exact committed 93-suite / 97-case sequence, matched all 93
suite source hashes, used cache mode off with zero cache hits, preserved the
744-file canonical digest
`e54b157bee070d27e19ad5c5cd33d5f3f2f9d0902e3a315ce3b7df56c2249708`
and raw digest
`46e84f50992edf34e87f45009c9939abe98878c222e022935d077ced73094d14`,
kept all six explicit inputs and the primary input-root inventory stable,
created no output, and left no owned process alive.

The parent Gate 3 separately requires the 15 root structural commands. A
direct `-B` batch of those commands was therefore launched on retry-13 after
attempt 028. All 15 returned exit zero, but the first post-batch observation
found the detached index still 95,071 bytes and now SHA-256
`f65eacab4b3ec4dfd084ea839a0a95f3281e2688824f086489f632c399dd38b3`.
The worktree remained semantically clean, HEAD and tree remained
`c11e59a71cfc85649775f6e30daeec2ca3f8ff48` and
`44da33178f3718422061e38311b5e196cf2437c5`, and the manifest remained 54,260
bytes at SHA-256
`1f6eb7fd26f1b0417c6af946a5ddfcf4800e268097ace6831ed4ad62c44f9f2a`.

No per-command index observation was made inside that batch. The exact command
that refreshed administrative index bytes is therefore unknown and must not be
invented. The batch and attempt 028 are preserved as historical evidence under
their original authority, but together they do not close C2. The index rewrite
is a stop even though no product or manifest delta is observed. No C2 evidence
was updated.

## Frozen current state

- Repository: `B:\Agents\platform\co-author-harness`; branch: `main` only.
- HEAD: `c11e59a71cfc85649775f6e30daeec2ca3f8ff48`.
- HEAD tree: `44da33178f3718422061e38311b5e196cf2437c5`.
- Primary tracked worktree: clean.
- Primary index: 95,071 bytes, SHA-256
  `0df66ccbd1e8031174353fcd5f9e3e2525f517caae2a8a6924e0999f597ab6a7`,
  unchanged by the freezing no-optional-locks status observation.
- Retry-13: clean at the same HEAD/tree; index 95,071 bytes, SHA-256
  `f65eacab4b3ec4dfd084ea839a0a95f3281e2688824f086489f632c399dd38b3`.
- Retry-13 admin subtree:
  `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-13`.
- Attempt 028 and every earlier red, warning, diagnostic, interrupted, refused,
  historical-success, and successful transaction remain immutable.
- The one frozen Aug-3 residue remains 114 identities, 34 directories, 80
  files, zero links, and 45,541 file bytes; its 8,637-byte deterministic
  inventory remains SHA-256
  `3bd3e82ea9b0441ffb556aafa67d6a625dc376665889c6cc1bb18de8342e7f40`.
  It is still the sole `release-controller-smoke-*` directory.
- Worker PID 43852 and child PID 62320 from attempt 028 are dead.
- The shared runner lock is 66 bytes, SHA-256
  `e178159f0a844e1afbf4dff513f7ad31bf639bc33929cc592f572ef320d73cac`,
  containing exactly
  `pid=62320 worktree=B:\Agents\.coauthor-v043-c2-detached-retry-13` plus
  CRLF. The PID is dead.
- The candidate addendum, retry-14, retry-14 admin subtree, attempt 029,
  retry-15, retry-15 admin subtree, and attempt 030 paths were absent at
  freeze.

## Exact writable and transient ledger

This correction authorizes exactly these tracked changes in their ordered
gates:

| Action | Path | Preimage bytes | Preimage SHA-256 |
|---|---|---:|---|
| CREATE AND COMMIT ALONE | `docs/superpowers/specs/2026-08-05-v043-postqualification-structural-index-refresh-c0-addendum.md` | absent | not applicable |
| GENERATION OUTPUT; REPLACE AND COMMIT ALONE AFTER ATTEMPT 029 SUCCESS ONLY | `docs/analysis/generated/fixture_manifest.json` | 54,260 | `1f6eb7fd26f1b0417c6af946a5ddfcf4800e268097ace6831ed4ad62c44f9f2a` |

After exact-byte B0/M0/m0 review and an addendum-only commit on `main`, Git-managed
removal is authorized only for clean retry-13 and its exact admin subtree. The
attempt-028 transaction remains preserved.

Only these new transient/durable paths may then be created:

- detached worktree `B:\Agents\.coauthor-v043-c2-detached-retry-14`;
- admin subtree
  `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-14`;
- durable transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-029`.
- detached worktree `B:\Agents\.coauthor-v043-c2-detached-retry-15`;
- admin subtree
  `B:\Agents\platform\co-author-harness\.git\worktrees\-coauthor-v043-c2-detached-retry-15`;
- durable transaction
  `C:\Users\young\AppData\Local\Temp\coauthor-v043-qualification-20260805-attempt-030`.

Each path must be proved absent before creation. Any other retry, transaction,
admin identity, durable evidence file, or output path is a stop. The five C2
evidence paths remain writable only after all recovery gates below pass, under
their exact preimages and create/modify authority in the parent correction.
They remain ignored/local evidence under the governing main v0.43 C0 and must
not be staged, force-added, or committed. This corrects and supersedes the
lower parent-correction instruction to commit those five ignored paths.
Only the retry-14 and retry-15 admin `index` files may transition during their
respective structural batches. Git may create only the applicable transient
same-admin `index.lock` as the atomic mechanism for that index transition; it
must be absent before and after the batch. Any other admin transient or residue
is a stop. No other admin or repository byte may change during those batches.

Ordinary Git objects, the primary index, the `main` reflog, and
`refs/heads/main` may change only as Git-managed consequences of exactly two
commits: the addendum-only commit and the later manifest-only commit. Record each commit
SHA, tree, exact changed-path set, and primary-index pre/post digest. The C2
evidence update is local/ignored and has no Git-managed effect. No other commit
or Git mutation is authorized. No other product, manifest, version, package,
archive, cache, catalog, startup, host, consumer, research, shipment, or
activation byte is writable here.

## Recovery sequence and gates

1. Obtain three independent exact-byte B0/M0/m0 reviews of this addendum.
2. Rebind `main`, retry-13, attempt 028, runner lock, frozen residue, new-path
   absences, and the addendum bytes; commit this addendum alone on `main` and
   record commit SHA, tree, and exact changed-path set.
3. Rebind retry-13 clean status and its refreshed index, remove only retry-13
   and its admin subtree through Git worktree management, and prove both paths
   absent while preserving attempt 028.
4. Create retry-14 detached at the exact addendum commit. Bind HEAD, tree,
   canonical admin path, admin `HEAD`, `index`, `commondir`, and `gitdir`, clean
   status, environment, primary refs, lock, primary input root, frozen residue,
   and fresh-path absences.
5. On retry-14, run these exact 15 commands sequentially with interpreter
   `C:\Users\young\AppData\Local\Python\pythoncore-3.14-64\python.exe` and
   explicit `-B`, capturing each exit, stdout length/digest, and stderr
   length/digest in memory:
   `scripts/skill-check.py`, `scripts/schema_runtime_check.py`,
   `scripts/version-check.py`, `scripts/distribution-rights-check.py`,
   `scripts/catalog-check.py`, `scripts/command_surface_check.py`,
   `scripts/path-hygiene-check.py`, `scripts/snippet-check.py`,
   `scripts/output_economy_check.py`, `scripts/version-planes-check.py`,
   `scripts/commitment-interactions-check.py`,
   `scripts/phase_engagement_check.py`, `scripts/retirement-sweep-check.py`,
   `scripts/destination-coverage-check.py`, and
   `scripts/analysis/fixture_infrastructure_check.py`. Before and after the
   batch, bind a full retry-14 worktree inventory using the exact current
   controller canonical-inventory algorithm, including ignored files; bind
   HEAD/tree, all admin inputs, primary HEAD/main/index and full input-root
   inventory, worktree/admin topology, runner lock, manifest/source bytes,
   frozen residue, smoke-directory set, fresh-path set, and owned processes.
   Only retry-14's existing admin `index` may transition. Record its pre/post
   length and SHA-256. Its same-admin `index.lock` must be absent preflight and
   postflight; it may exist only transiently as Git's atomic index-refresh
   mechanism. Every other bound plane must be byte-identical, the worktree must
   remain clean, and no output or surviving owned process is permitted.
   Require 15/15 exit zero. Stderr must be zero bytes for the first 14 commands.
   Only `scripts/analysis/fixture_infrastructure_check.py` may
   produce the exact expected negative-test diagnostic stream: 805 bytes,
   SHA-256
   `9cd7690903b6b91f5a6183083883ffe9dadd4241bed165e6b2ca386416059ee2`.
   Any other stderr, warning, error outside that exact registered negative-test
   stream, or plane drift is red and stops.
6. Only after the structural batch and its postflight are complete, freeze the
   retry-14 index as the qualification preimage and rebind all six explicit
   inputs. Launch attempt 029 through
   the durable controller with exact interpreter `-B` for controller and child,
   no relevant Python environment variables, `--allow-user-site`, no ignored
   output, primary checkout as sole input root, the same six explicit inputs,
   retry-14 as sole watch root, and the retry-14 manifest as the sole allowed
   output. Its child is:

   ```text
   <python> -B <retry-14>\scripts\analysis\fixture_runner.py
     --tier full --cache-mode off
   ```

7. Attempt 029 must terminally succeed with child exit zero, diagnostic null,
   exact 93/93 suites and 97/97 registered cases, 93/93 suite-source hashes,
   zero warning or failure rows, zero cache hits, stable 745-file canonical and
   raw tested-input digests, stable six explicit inputs and primary input-root
   inventory, exactly one output change at the retry-14 manifest, index
   byte/digest equality to the post-structural preimage, a separately bound
   runner-lock transition, frozen residue equality, no other smoke residue,
   empty child stderr and worker streams, and zero surviving owned processes.
   Receipt and every stream must be bound by exact path, length, and SHA-256.
   Any mismatch, ambiguity, warning, other output, index rewrite, residue, or
   process survivor is red and stops.
8. After complete attempt-029 evidence capture, copy only its exact manifest to
   primary, prove byte equality, commit that manifest alone, and record the
   commit SHA/tree/path set and Git-managed effects. Force-remove dirty
   retry-14 and its exact admin subtree only after the manifest bytes are
   preserved and committed; prove both paths absent while preserving attempt
   029.
9. Create retry-15 detached at the exact manifest commit and bind the same
   preflight planes. Run the same 15-command structural batch under the exact
   step-5 output, stderr, index-transition, inventory, lock, residue, process,
   and no-drift rules. Only after its complete postflight may the retry-15
   index be frozen and all six explicit inputs rebound.
10. Launch attempt 030 through the durable controller with the same frozen
   interpreter, environment, input root, six explicit inputs, and retry-15
   watch root, but with no allowed output and child:

   ```text
   <python> -B <retry-15>\scripts\analysis\fixture_runner.py
     --tier full --cache-mode off --no-write
   ```

11. Attempt 030 must meet every non-output gate from step 7, must exactly match
   the committed attempt-029 manifest case order and 93/93 suite hashes, and
   must reproduce attempt 029's full 745-file canonical and raw tested-input
   digests. It must create or change no output, preserve the post-structural
   retry-15 index byte-for-byte, leave retry-15 clean, preserve all other
   planes, and leave no owned process alive. Any mismatch is red and stops.
12. Only after every gate passes may the five parent-registered C2 evidence
   paths be updated. They must preserve attempts 017, 019, 021, 022, 023, 024,
   027, 028, and 029 and this structural-index stop; attempt 030 is the current
   clean qualification evidence. Obtain three independent exact-byte B0/M0/m0
   reviews and bind every local pre/post byte length and SHA-256. Do not stage,
   force-add, or commit these ignored/local files. Then remove clean
   retry-15/admin and prove absence while preserving attempts 029 and 030.

Attempt-030 success, if reached, authorizes only completion of C2 and then the
next pre-existing v0.43 C0 gate. Gate 6 remains closed absent a separately
governed v0.43 consumer compatibility receipt. This task cannot author or
simulate that receipt. Package/archive/cache work, package clearance,
push/tag/assets, shipment, fresh-host qualification, research mutation, and
activation remain closed.
