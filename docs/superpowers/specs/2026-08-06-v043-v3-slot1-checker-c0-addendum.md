# v0.43 V3 slot-1 checker failure - C0 successor addendum

Status: proposed successor C0; non-operative until three independent exact-byte
B0/M0/m0 reviews and the isolated one-file commit required below.
Date: 2026-08-06 America/Toronto.
Run scope: `full_lifecycle`, limited to v0.43 source qualification and its
source-local evidence.

## Authority and claim boundary

This instrument is subordinate to the finalized research-governance chain and
the exact parent C0
`docs/superpowers/specs/2026-08-06-v043-v2-strictmode-empty-directory-c0-addendum.md`,
committed alone at `62e66c4ad9d9228f6dd8a0a88fcc83afc2227886`, tree
`07a53f62e7ab3f8869906e0ab46404baa11837f0`, parent
`257cc59db8469186f9fc2b40e771bfe901049298`, 12,346 bytes, SHA-256
`0634747a25c70caaa22ed1b508940180efed8be466e45c331e806194900ba1c9`.
That parent received independent exact-byte B0/M0/m0 approval from
`/root/retry26_authority_audit`, `/root/retry26_mechanics_audit`, and
`/root/retry26_ledger_audit`, each binding 12,346 bytes, 229 LF, zero CRLF,
terminal LF, and no BOM.

The finalized Master Governance v1.0.9, amendment, A02 anchor, Overseer
doctrine/status, all parent protected bindings, seven-preimage/base22 closure,
writable ledger, and Gate 6 ceiling continue unchanged except for the exact V4
derivation and 24/32/28 downstream C2 cardinality supersession below. Any
absent, draft, ambiguous, inconsistent, superseded, or conflicting binding
fails closed.

This grants no push, tag, install, catalog/cache mutation, archive/package
creation, shipment, consumer attestation, research mutation, host
qualification, promotion, canon change, or activation. Gate 6 remains closed
absent independently governed v0.43 consumer evidence.

## Registered V3 assembly and attempt-4 failure

The parent-authorized exact V3 derivation completed with all ten registered
replacement cardinalities, outside-span byte equality, create-new sequential
writes, durable flush, close, and independent readback. The now-frozen sibling
is:

`releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V3.ps1.assembly.tmp`

at 404,520 bytes, SHA-256
`ceb7c25389fdcab0bceabbeff475071902daf059bee5a1d20e3ad8f0dfbac704`,
4,756 LF, zero CR, terminal LF, and no BOM. Both Windows PowerShell 5.1 parser
entry points reported 63,422 tokens and zero errors. Final V3 remains absent.
Immutable V2 remains 404,288 bytes at SHA-256
`a808c39452ad9f7723f1b2d1d44f7a83d11822e227d18ca757d1f04106638acc`.
C2 is exactly 23 files, zero directories, and 24,327,256 bytes.

The complete pre-write protected rebind passed immediately before assembly:
finalized governance exact; clean main at the parent commit/tree; retry-26
clean/detached at `91ad31ae250dd9600ef85f330e8d4acaef36da8e`; primary
manifest 54,255 bytes at
`c0421d9b1e3dac989bebd8bc11f702f3260ac5b695e08ed90efd5311827f4950`;
stale lock 66 bytes at
`146809515519be1654ae43d9f21916d10a8558ab4c1005650dd84beb160986a0`,
PID 43052 dead and exclusive-open available; FIC inventory
`5d7242130166ab4adfa8743c9256264a2f0d9c108a50c76f117f5c097f5bab9d`;
frozen-residue inventory
`3bd3e82ea9b0441ffb556aafa67d6a625dc376665889c6cc1bb18de8342e7f40`;
and zero related process, registered transient, event subscriber, event, or
job.

Static-validation attempt 4 then launched only slot 1 in a fresh strict-mode
process. It rebound V3 before parsing, decoded strict UTF-8, confirmed both
parser passes, and began exact literal checks. The wrapper's local literal
counter was declared without required whitespace:

`function LC([string]$s){return[regex]::Matches(...)}`

Windows PowerShell therefore parsed `return[regex]::Matches` as a command name
when the helper was first invoked and returned ordinary exit 1 with
`CommandNotFoundException`. Slot 1 emitted no PASS result. This is a validator
wrapper failure/evidence-incomplete result, not a V3 rejection, operational
mode RED, qualification result, source pass, or terminal success.

Attempt 4 is consumed and may not be resumed. Slots 2 through 9 did not start.
No move, mode, retry removal/creation, controller, supervised target,
qualification, manifest write, cleanup, publication, or downstream action
occurred. Immediate reconciliation found zero related survivor, exact V2 and
V3 siblings unchanged, final V3 absent, exact 23-file C2 above, and clean main
at the parent commit/tree.

## Exact V4 derivation and static-validation attempt 5 authority

V2 and the complete V3 sibling are immutable evidence. V3 binds the parent
commit and therefore cannot pass authority/commit topology after this required
successor C0 commit advances `main`. Do not edit, append, truncate, rewrite,
rebuild, replace, delete, clean, move, or execute V2 or V3. They are not
operational candidates.

After this exact C0 is reviewed and committed alone, authorize creation of
exactly one complete sibling and eventual final path:

- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V4.ps1.assembly.tmp`;
- `releases/verification/v0.43.0/C2/C2_STRUCTURAL_CAPTURE_RETRY26_RECOVERY_V4.ps1`.

Before opening V4, rebind the complete inherited protected state, exact V2 and
V3, absent V4 leaves, clean main at this C0's isolated commit, C2 at exactly 23
files/zero directories/24,327,256 bytes, retry-26/admin, manifests, stale lock,
FIC/residue, runtime/supervisor, every fresh path, and zero process/event/job
state. Any mismatch stops pre-write.

Create V4 only from exact V3 bytes by a closed deterministic transformation
that may make only these semantic edits:

1. change recovery self-leaf literals from V3 to V4 for the assembly and final
   paths;
2. bind `AuthorityRel`, `AuthorityCommit`, `AuthoritySha256`, and
   `AuthorityBytes` to this committed C0's exact path, commit, bytes, and hash;
3. add immutable V3's assembly leaf exactly once to `BaseC2Names` and add its
   exact SHA-256
   `ceb7c25389fdcab0bceabbeff475071902daf059bee5a1d20e3ad8f0dfbac704`
   exactly once to `C2Preimages`, yielding base23 and eight individually
   hash-bound protected preimages; and
4. change only the closed-set base assertion from 22 files/23,922,736 bytes to
   23 files/24,327,256 bytes.

The two exact inserted LF-terminated rows are 134 bytes in `C2Preimages` and
66 bytes in `BaseC2Names`. The successor authority-relative-path assignment is
13 bytes shorter than V3's; every other replacement is length-neutral. The
derived V4 output is therefore exactly 404,707 bytes, 4,758 LF, zero CR,
terminal LF, and no BOM. Both parser entry points must return equal token counts
and zero errors, and the assembler must freeze the independently computed
on-disk SHA-256 before attempt 5.

No other semantic or byte change is authorized. Prove every source literal's
exact count, every replacement count, post-transform counts, no V3 active
self-leaf except its exact base/preimage evidence rows, and byte-for-byte
equality outside registered spans. Create V4 with create-new semantics,
sequential bounded writes, durable flush, close, and independent readback. A
partial or mismatched V4 sibling is immutable failure evidence and stops.

After successful V4 creation, authorize exactly one fresh static-validation
attempt 5 over frozen V4. It executes no script main and no operational mode.
Attempt 5 repeats all nine parent slots from zero, in order, in separate
Windows PowerShell 5.1 processes under `Set-StrictMode -Version Latest`, with
the same exact 600,000-millisecond caller wall-clock ceiling, timeout
termination, survivor census, evidence preservation, and attempt-consumption
semantics. No attempt-4 partial result may be reused.

Slot 1 uses only the corrected wrapper declaration:

`function LC([string]$s) { return [regex]::Matches(...) }`

It must freshly perform the full inherited exact byte/format/parser,
transformation, authority, label, canonical-literal, manifest UUID-v4,
sole-finalizer, immediate-return, and no-catch-republish checks and emit one
concise PASS result. Slot 2 must freshly extract 124 helpers, evaluate only the
exact `BaseC2Names` and `C2Preimages` assignments, keep mode/capsule null, and
prove strict-mode assembly-static closure for base23 plus V4, eight protected
preimages including exact V2 and V3 bindings, final absence, zero directories,
base23 at 24,327,256 bytes, total C2 at 24 files/24,731,963 bytes, and zero
helper errors.

Slots 3 through 6 freshly run the original-failed-capture, retry-22, retry-24,
and retry-26 binders separately. Slot 7 repeats the exact fifteen retry-26 and
five corrected retry-24 adversarial rejections. Slots 8 and 9 repeat the full
protected phase-5 binding; slot 9 must equal slot 8 exactly and additionally
prove final absence, same-volume relation, exact V2 and V3 path/bytes/SHA, and
base23 equality. Any exception, missing result, mismatch, timeout, survivor, or
drift consumes attempt 5 and stops.

No command may intervene between slot 9 PASS and publication except
non-mutating result assertions. Only then may the caller atomically move the
V4 sibling to exact final V4 with the non-overwriting two-argument
`[IO.File]::Move`. Post-move, freshly rebind exact final V4 and immutable V2/V3,
prove V4 sibling absence, repeat both parsers, re-extract helpers under strict
mode, capture a fresh complete protected state, set only prospective
`generation-structural`, require `Assert-FrozenBindings`, and require
no-switch runtime closure for base23 plus final V4.

Freeze final V4 only after all post-move checks pass. Then obtain three
independent exact-byte B0/M0/m0 reviews of final V4. Any edit invalidates them.
No operational mode runs before unanimous approval and another complete
protected-evidence rebind.

Because immutable V3 becomes one additional permanent base-evidence file,
this instrument expressly supersedes the parent's 23/31/27 C2 gates. The
post-V4-review/pre-generation protected rebind must contain exactly 24 files:
base23 plus final V4. Four successful downstream capsule/preimage pairs add
eight files, so every pre-cleanup rebind and review must bind exactly 32 files.
Cleanup may remove exactly four provisional publication-preimage files; final
rebind, summary, and all three final reviews must bind exactly 28 files. V2,
V3, final V4, older evidence, and all four terminal capsules remain.

## Review, commit, and writable ledger

Before this C0 becomes operative, three independent exact-byte B0/M0/m0
reviews must bind its parent, V3 assembly, attempt-4 failure, corrected slot-1
wrapper, exact V4 derivation, all nine attempt-5 slots, publication boundary,
protected ledger, 24/32/28 downstream topology, and Gate 6. Any edit invalidates
all reviews.

After unanimous approval, commit only this file directly on `main`, with exact
parent `62e66c4ad9d9228f6dd8a0a88fcc83afc2227886`. The prospective commit has
exactly 763 tracked paths, zero `.plugin`/`.zip`, six paths beneath
`scripts/analysis/`, one fixture manifest, and 756 governed included paths.
Their Python-Unicode-sorted UTF-8 LF payload without terminal LF is 39,651
bytes at SHA-256
`d6c3656c91bdb3aeea02644eba2589dceaac69f4b64088a6dd608e057ce0538f`.
Do not push or tag.

Only these paths may change:

1. this C0 path and primary Git objects/ref/log/index/`COMMIT_EDITMSG` for its
   isolated commit;
2. the exact V4 assembly sibling during its sole create transaction and then
   only through a successful atomic move to exact final V4; its bytes may not
   change after close; and
3. after final-V4 unanimous review and the inherited full protected rebind,
   only the exact Git-managed removal of
   `B:\Agents\.coauthor-v043-c2-detached-retry-26` and its registered admin;
   then only the inherited retry-30/31 and attempts 045/046 ledger.

Every other path remains read-only. Preserve all older RED/nonterminal/static
evidence, immutable V1, immutable V2, immutable V3, immutable V4 after close,
retry-26 RED pair permanently, retry-26 worktree/admin through the exact gate,
manifests, stale lock, FIC/residue, runtime/supervisor, package/archive,
cache/catalog, shipment, consumer, research, host, and activation planes.

Any new timeout, crash, rejection, survivor, RED, missing receipt, or drift
stops and preserves evidence. Even a fully green source sequence stops at Gate
6 pending independently governed v0.43 consumer evidence.
