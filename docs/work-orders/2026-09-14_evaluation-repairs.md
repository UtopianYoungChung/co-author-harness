# Evaluation repair work

Authorized by the user's instruction to proceed with the 14 September evaluation.
Source baseline: `c648221ca1636124a8f28da735be810239fc0ad3`, on `main`.
The workspace preflight validates Master Governance head 1.0.13. Its unrelated
wiki, research-package and Hermes configuration findings remain outside this
package repair. Model/provider selection remains a Desktop operation.

## Scope and completion evidence

1. Preserve and remove accidentally committed temporary workspaces; prevent
   their reintroduction through package enumeration and use an ignored fixture
   scratch directory. Rebuild and inspect the local distribution snapshots.
2. Review drifted contract members, refresh the kernel, reconcile the predicate
   map and earn new fixture evidence through the authoritative full registry.
3. Repair golden scoring and provide artifact-oriented benchmark inputs,
   explicit scoring contracts and an honest route to adjudicated baselines.
   No synthetic or self-authored score is a human-adjudicated baseline.
4. Align ordinary/governed onboarding and unavailable wiki descriptions.
5. Separate bibliographic resolution from passage-level attribution support in
   grounding instructions and the ordinary-task check boundary.
6. Produce exact source/archive/cache identities and synchronize authorized
   local installations after validation, preserving host loading as a distinct
   verification boundary.
7. Bound host-prefix reads, report role timing and check that later log growth
   does not enlarge pinned-prefix reads or weaken drift detection.
8. Add portable read-only archive verification without converting archives to
   live task completion or changing the original bindings.

## Verification sequence

Regression reproductions precede code repairs. Focused tests precede contract
rebinding and the full registry. The full corpus is mandatory because package
membership and registry inputs change. Generated records are written only from
successful runs. Build provenance and archive population are inspected after a
commit-bound build. Producer checks, installation, fresh-host verification,
research quality, human adjudication and research acceptance are separate states.

## Progress

- Initial investigation reconfirmed package-root fixture creation, unfiltered
  tracked-file packaging, substring scoring, and stale contract/census records.
- The generated trees and prior distribution bytes have a verified external
  forensic backup. Generated files are untracked and ignored; their originals
  remain on disk. Package enumeration now refuses transient tracked members.
- Focused regressions cover exact scoring/locators, baseline mismatch, blinded
  headers, source-passage binding, bounded prefix reads, portable archives and
  artifact assessment boundaries. Source/cache synchronization and full corpus
  evidence follow the source repair commit.
- Reviewed stale bindings reflect removal of the retired advisor command and
  restoration of the Claude host pack. Kernel and package compatibility-template
  hashes are rebound together; no consumer approval or authority is rewritten.
- The six-artifact benchmark has explicit inputs, dimensions and thresholds.
  A human-adjudicated reference set and repeated live runs remain outstanding;
  fixture success is not research-quality qualification. Fresh host loading and
  independent release/consumer authority remain separate from local repair.
- Full validation and installation receipts are recorded externally with the
  evaluation. This work order grants no release or research acceptance.
