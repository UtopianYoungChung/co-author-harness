# Harness CLAUDE.md — consolidation history

*Last updated: 2026-04-24 (repo root `co-author-harness/`, v0.8.5+; formerly `research-writing-harness/`). 2026-04-21. Option C″ consolidation: `paper-harness/` retired as a distinct workspace root; this file moved from `paper-harness/CLAUDE.md` to the harness root `CLAUDE.md`, rewritten to match the v0.7.2 substrate (then aligned to the shipped reference package for that minor). `§1 Package location` rewritten — package substrate now sits at the top level of this directory in four canonical subdirectories (`agents/`, `skills/`, `references/`, `scripts/`), replacing the former `.paper-package/` path. `§4 Lifecycle governance` rewritten to the Lifecycle-Stage Ladder (T1→T4). Updated 2026-04-22 to Lifecycle-Phase Ladder (Ph1→Ph4) per v0.7.4 rename. `§12 Related files` repointed to the v0.7.2 references paths. Added consolidation-history note after the scope statement and a final bullet in §9 for the v0.7.1 SD/SR opt-in gate. Previous: 2026-04-17 — release distribution surface `paper-harness/releases/` relocated to workspace sibling; Zotero governance relocated; folder `Research/` renamed to `paper-harness/`. Earlier: 2026-04-12 — added §§8–10; Package location updated to `.paper-package/` (Option B).*

The harness root substrate described by this page began at **v0.8.7+**; treat the page as **historical provenance** for the root instruction file, not the live version matrix (see `version.json` for current identity and `CHANGELOG.md` for release history).
