# Smoketest manuscript (PASS fixture)

This is a minimal manuscript stub for the Marshal smoketest. The Marshal
does not read manuscript content; it verifies only that the file is present
and that `manuscript_diff_001.patch` (round 001's diff artefact) exists
when the revision log claims edits.
