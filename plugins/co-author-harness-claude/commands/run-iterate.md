---
name: run-iterate
description: Public iterate stage — canonical post-draft iteration surface (profiles refine, structural, deep, stability); legacy /run-phase-2 and /run-phase-3-stability route here.
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/run-iterate/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority — and run-iterate is the canonical public surface for post-draft iteration (PR-3b.4); the workflow body lives in `skills/run-phase-3/SKILL.md`, and legacy `/run-phase-2` (profile=refine) and `/run-phase-3-stability` (profile=stability) route here.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/run-iterate/SKILL.md` from the workspace root the user opened.
