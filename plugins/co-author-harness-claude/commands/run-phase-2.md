---
name: run-phase-2
description: Legacy compatibility command for former Ph2; route to /run-iterate with profile=refine.
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/run-phase-2/SKILL.md` as a compatibility router, then follow `${CLAUDE_PLUGIN_ROOT}/skills/run-iterate/SKILL.md` with `profile: refine`.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/run-phase-2/SKILL.md` and `skills/run-iterate/SKILL.md` from the workspace root the user opened.
