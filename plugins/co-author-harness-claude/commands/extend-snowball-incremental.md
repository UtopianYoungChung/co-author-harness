---
name: extend-snowball-incremental
description: Ph2 in-loop snowball micro-iteration anchored on a single uncovered claim
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/extend-snowball-incremental/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, preconditions, anchor-selection rules, micro-iteration procedure, atomic-write contract, output artefacts, determinism guarantee, and failure-mode BLOCKER emission.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/extend-snowball-incremental/SKILL.md` from the workspace root the user opened.
