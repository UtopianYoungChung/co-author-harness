# Citation auditor fixture — known-bad

This fixture exercises the unresolved-citation path. Every line below
cites a GROUNDING_PROTOCOL rule that does not exist in the live anchor
set; the auditor must emit `CIT-001` `inviolable` findings for each.

## Prose form

The Reflector violated `GROUNDING_PROTOCOL.md §Rule 99` during the
audit. See also `GROUNDING_PROTOCOL.md §Rule 42b` for the side-band
clause.

## Short form

The architecture flagged `[GP §99]` as the consumer of these anchors,
and `[GP §42b]` is the sub-rule.

## Multi-rule list

Per `GROUNDING_PROTOCOL.md §§Rule 99, Rule 42b` the writer must
escalate.
