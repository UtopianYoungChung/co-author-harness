---
description: Classify paper type, P-stage, venue, and default final tier (T1–T4 / T3R) of the v0.7.0 Lifecycle-Stage Ladder
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/classify-manuscript/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, gates, finding format, and exit conditions.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/classify-manuscript/SKILL.md` from the workspace root the user opened.
