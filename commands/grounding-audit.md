---
name: grounding-audit
description: Run grounding protocol compliance audit
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/grounding-audit/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, gates, finding format, and exit conditions.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/grounding-audit/SKILL.md` from the workspace root the user opened.
