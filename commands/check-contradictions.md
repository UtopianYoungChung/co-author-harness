---
description: Run contradiction audit across co-invoked sources
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/check-contradictions/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, gates, finding format, and exit conditions.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/check-contradictions/SKILL.md` from the workspace root the user opened.
