---
name: run-iterate
description: Alias for /run-phase-3 — Ph3 Iterate & Converge under the v0.15.0-pre stage × profile vocabulary. Both names resolve to the same canonical workflow.
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/run-iterate/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority — and note that the alias delegates to `skills/run-phase-3/SKILL.md` for the canonical workflow.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/run-iterate/SKILL.md` from the workspace root the user opened.
