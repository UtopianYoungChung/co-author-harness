---
description: Run fast mechanical pre-flight checks
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/quick-deterministic/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, gates, finding format, and exit conditions.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/quick-deterministic/SKILL.md` from the workspace root the user opened.
