---
name: run-phase-3
description: Legacy compatibility route to public /run-iterate; preserves the full Ph3 implementation body.
---

Compatibility entrypoint: read `${CLAUDE_PLUGIN_ROOT}/skills/run-phase-3/SKILL.md`, follow its binding full-iterate implementation, and present `/run-iterate` as the public stage name.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/run-phase-3/SKILL.md` from the workspace root the user opened.
