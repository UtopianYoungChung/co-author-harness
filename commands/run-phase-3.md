---
description: T3 Iterate & Converge — unbounded loop with convergence_metric two-round stability test
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/run-phase-3/SKILL.md` and follow it as the binding instruction set for this invocation. Treat that file as the authority for trigger conditions, gates, finding format, and exit conditions.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, fall back to `skills/run-phase-3/SKILL.md` from the workspace root the user opened.
