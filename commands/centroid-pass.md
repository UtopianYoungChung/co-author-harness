---
name: centroid-pass
description: Public centroid-pass invocation is unavailable; it returns a deterministic read-only IMPLEMENTATION_MISSING envelope and performs no writes.
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/centroid-pass/SKILL.md` and follow it as the
binding instruction set. Return its exact machine-readable unavailable envelope.
Do not offer `write`, `revise`, `all`, `--apply`, milestone-fence bypass, or any
manuscript, lifecycle, review-artifact, graph, or Wiki mutation.

If `${CLAUDE_PLUGIN_ROOT}` does not resolve in this host, use
`skills/centroid-pass/SKILL.md` from the opened workspace root.
