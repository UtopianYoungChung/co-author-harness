---

## document_type: upgrade_architecture

filed_by: harness_session_agent
filed_on: 2026-04-22
revision: r21 (r20 + **§7.1** 2026-04-23: cross-reference to `proposals/check8_subcheck_g_consolidation_anchors_proposal.md` — Check 8 Sub-check G deferred; does not alter v0.8.0 ship scope)
author_of_record: Joseph Chung (user-directed)
target_version: v0.8.0 (single composed release — absorbs what would have been v0.7.5, v0.7.5 RC, v0.7.6; v0.7.4.1 shipped 2026-04-21 and serves as the canonical Phase 0 substrate via D-8)
status: READY-FOR-PHASE-3-BUILD
user_decisions_closed:

- "D-1 (no intermediate builds; ship only the final composed version) = confirmed 2026-04-22"
- "D-2 (endorse the composed release sequencing) = confirmed 2026-04-22, lifted into authoring order"
- "D-3 (measured-throughput calibration before external quoting of .plugin-efficiency.json) = confirmed 2026-04-22"
- "D-4 (Phase rename applied AFTER α-C1 Evaluator pointer-demotion) = confirmed 2026-04-22"
- "D-5 (INF3001H second-project replication before any v0.7.6-successor threshold retuning) = confirmed 2026-04-22"
- "D-8 (canonical Phase 0 build-surface = unpacked/research-writing-harness-claude-v0.8.0/ 'wip0' substrate, a copy of shipped v0.7.4.1 with deprecated-alias directories removed and α Tier A .plugin-efficiency.json landed in place) = confirmed 2026-04-22"
- "D-7 (version number for the composed release = v0.8.0, per the document's §5 recommendation) = confirmed 2026-04-22"
- "D-9 (Phase 0 residual R-P0-SCHEMA-3 — phase_state_schema.md / validator / fixture three-way inconsistency — DEFERRED to a dedicated P0.1a or Phase 5 substrate-cleanup session; R-P0-CAT-1 and R-P0-CAT-2 addressed in-session as Phase 1 closure hygiene) = confirmed 2026-04-22 per §9.1; P0.1a closure executed in a dedicated post-r6 session 2026-04-22 per §9.1 r7 amendment, closing D-9's deferral"
- "D-6 (A-GK-4: per-row disposition of §3.P-9.1 falsification table) = closed 2026-04-22 under user directive **Default continue** — rows 1–10 **retained in Ph4-deep** (stub sub-argument (2) accepted as holding); row 11 **retained in Ph4-deep with confessed coverage gap + v0.7.6 lightweight-proxy milestone logged** (row-11 constrained set; alternative *promoted to Ph3-refine via lightweight proxy* not selected); filed in `proposals/v0.7.5_phase3_refinement_loop_proposal.md` §3.P-9.1 + binding paragraph immediately under the table"
open_decisions: []
supersedes: revision r19 of this document (same path, dated 2026-04-22)
grounding_basis:
- research-writing-harness/proposals/v0.7.4_economic_efficiency_proposal.md
- research-writing-harness/proposals/v0.7.4_session_handoff.md
- research-writing-harness/proposals/v0.7.5_phase3_refinement_loop_proposal.md
- research-writing-harness/proposals/v0.7.5_phase3_refinement_loop_review_response.md
- research-writing-harness/proposals/v0.7.5_phase3_refinement_loop_advisor_consult.md
- research-writing-harness/proposals/v0.7.5_review_response_applied.md
- research-writing-harness/proposals/v0.7.6_calibrator_audit_optimization_proposal.md
- research-writing-harness/proposals/drafts/v0.7.6_hand_authored_route/README.md
- research-writing-harness/proposals/drafts/v0.7.6_hand_authored_route/plugin-efficiency.json.draft
- research-writing-harness/proposals/drafts/v0.7.6_hand_authored_route/evaluator.md.pointer-demotion.diff
- research-writing-harness/unpacked/v0.7.4.1_punch_list.md
- research-writing-harness/unpacked/writing_harness_quality_audit_v0.7.4.1.md
- research-writing-harness/unpacked/writing_harness_economics_audit_v0.7.4.1.md
- research-writing-harness/unpacked/writing_harness_speed_audit_v0.7.4.1.md
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4/.claude-plugin/plugin.json
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4/CHANGELOG.md
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4/references/MODEL_ALLOCATION.md
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4/skills/run-phase-3/SKILL.md
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md
- research-writing-harness/unpacked/research-writing-harness-claude-v0.7.4.1/.claude-plugin/plugin.json
- research-writing-harness/unpacked/research-writing-harness-claude-v0.8.0/.claude-plugin/plugin.json
- research-writing-harness/unpacked/research-writing-harness-claude-v0.8.0/.plugin-efficiency.json
- research-writing-harness/unpacked/plugin_calibrator_handoff_v0.7.4.1.md

# Upgrade Architecture — `research-writing-harness-claude` (v0.7.4 → v0.8.0, single composed release)

## 0. Framing

Per the user's 2026-04-22 decisions, the architecture collapses the remaining release slate from revision r1 of this document into a **single composed release, v0.8.0**, that absorbs every shipping-bucket contribution from the α and β tracks plus a verify-and-cleanup step against the γ work already shipped at v0.7.4.1 (`unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md`, 2026-04-21). γ is therefore no longer an authoring phase of v0.8.0 but a verification-and-cleanup step against the v0.7.4.1 tree; the architecture's canonical Phase 0 substrate is the v0.7.4.1 tree — specifically the `unpacked/research-writing-harness-claude-v0.8.0/` "wip0" derivative per decision D-8 — not the v0.7.4 tree. No further intermediate build is cut; no v0.7.5, no v0.7.5 RC, no v0.7.6. The deferred forward slate — P-E1 Reflector/Planner prompt shrink, P-E2 batch-dispatchable orchestrator tag — remains deferred and carries into a v0.8.x or later successor. P-14 paragraph-hash verdict carryover cache is pulled forward into v0.8.0.

Four constraints govern the composition:

1. **Single composed release.** α (calibrator-audit optimisation Tiers A–D) and β (Ph3 refinement-loop P-9 through P-16 including P-14) ship together in one zip at v0.8.0, starting from the v0.7.4.1 canonical substrate (γ punch-list hygiene already closed at v0.7.4.1 ship on 2026-04-21; Phase 0 reduces to verify-and-cleanup against that closure).
2. **Sequencing survives as authoring order.** The intra-track and cross-track dependencies that would have governed a five-cut release sequence survive as binding intra-release authoring order. The draft artefacts in `proposals/drafts/v0.7.6_hand_authored_route/` apply at specific points in that authoring order.
3. **Measured-throughput calibration precedes any external cost quote.** The `.plugin-efficiency.json` ships with measured throughput (not the 2,000 tok/s placeholder) before the release CHANGELOG quotes any wall-clock or per-round cost figure.
4. **Phase rename applied AFTER Evaluator pointer-demotion (α-C1).** The hand-authored `evaluator.md.pointer-demotion.diff` targets v0.7.0 Tier vocabulary on purpose; applying the Phase rename first would drift the diff's line numbers and multiply churn. The rename closes residual v0.7.0 Tier vocabulary inside `agents/evaluator.md` only after the pointer-demotion lands.

**Authoring discipline.** This is an architecture document, not a binding change. Every structural claim is grounded in the proposal corpus, the v0.7.4.1 calibrator audits, and the hand-authored draft route in `proposals/drafts/`. Where the document infers composition ordering, release-number recommendation, or risk prioritisation, it marks the inference as such. The four-agent contract, the Grounding Protocol, the Lifecycle-Phase Ladder rung semantics, the `SectionStateObject`, the 30-trigger enum (widening to 30+ inside v0.8.0 via β), and the I-SubAgent-1/2/3 invariants are preserved throughout. The `E-MA-CAPABILITY-INVERSION` refusal carries forward unchanged.

---

## 1. Current state — what v0.7.4 / v0.7.4.1 actually ships

### 1.1. Architectural invariants at v0.7.4

The shipped plugin (`research-writing-harness-claude-v0.7.4.zip`; `plugin.json` `version: 0.7.4`) codifies:

- **Four-agent AORE harness.** Planner (sole writer of `reviews/phase_state.json`), Evaluator (sole reviewer), Generator (sole writer of prose), Reflector (audits all, two dispatch modes).
- **Lifecycle-Phase Ladder.** Ph1 Plan & Draft → Ph2 Review & Revise → Ph3 Iterate & Converge → Ph4 Finalize & Close. Per-section advancement arbitrated by a 15-field `SectionStateObject`; 7-field `PhaseEntryLogRow`; 30-trigger enum.
- **Six artefact frontmatter families F1–F6.** Validator at `scripts/artefact_frontmatter_validate.py`.
- **Phase-conditioned model allocation.** Opus 4.7 floor at Evaluator-Ph2/Ph3/Ph4 and Reflector-full-Ph4; Sonnet 4.6 at Planner/Generator; Haiku 4.5 at Reflector-lightweight on H-MA-2 pilot.
- **Ph3 stability sub-mode** and **MCR admission gate.**
- **I-SubAgent-1/2/3 invariants.** Subagent-returned verdicts are authoritative-as-read.

### 1.2. Calibrator three-axis audit readings (v0.7.4.1)

Deterministic `pass` across three axes with four MINORs, zero MAJOR, zero BLOCKER:


| Axis      | Headline                            | Value   | Threshold | Verdict                                                 |
| --------- | ----------------------------------- | ------- | --------- | ------------------------------------------------------- |
| Quality   | `max_cyclomatic_complexity`         | 48      | ≤ 15      | MINOR (one outlier: `scripts/sk20_overlay_run.py`)      |
| Quality   | `prompt_clarity_score`              | 0.229   | ≥ 0.50    | MINOR (vocabulary mismatch)                             |
| Economics | `projected_cost_per_invocation_usd` | $1.8908 | —         | note (`assumed_rates_verified: false`)                  |
| Economics | `prompt_tokens_estimate`            | 131,437 | —         | four agent prompts = 37.6 %; top-ten artefacts = 59.3 % |
| Economics | `subagent_dispatch_multiplier`      | 10.0    | —         | risk signal (dispatch-plan hygiene)                     |
| Speed     | `max_subagent_chain_depth`          | 19      | ≤ 6       | MINOR (dual-surface `run-tier-*` / `run-phase-*` state) |
| Speed     | `parallelisable_fraction`           | 0.304   | —         | 7/23 executors free                                     |
| Config    | `efficiency_config_missing`         | present | —         | MINOR (three of four MINORs downstream)                 |


### 1.3. v0.7.4.1 punch-list residuals — closed at v0.7.4.1 ship

17 residuals in six classes, **closed at v0.7.4.1 ship on 2026-04-21** per `unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md` §Scope: (A) `tier_state_schema.md → phase_state_schema.md` rename-miss × 7 (closed via `git mv`); (B) four deprecated-alias relative-path bugs (closed via plugin-root-anchored link rewrite across `skills/run-tier-{1,2,3,4}/SKILL.md`); (C) legacy-tree README refs; (D) retired-skill refs in README (Classes C/D closed via prose rewrite removing retired-tree file paths while retaining narrative); (E) README skills-catalog parity (26 claimed vs. 31 on FS; closed via skills-table rewrite adding five phase-named rows and demoting four tier-named rows to deprecated-alias entries); (F) missing `## Output Contract` heading × 4 (closed via heading addition in each of the four `agents/*.md` prompts). All prose / file rename / heading additions with zero schema or contract change; expected post-fix severity profile BLOCKER 0 / MAJOR 0 / MINOR 0 under the curated `.plugin-calibrator.json`. §3.2 Phase 0 reduces to verify-and-cleanup against this shipped closure, operating on the wip0 substrate per decision D-8.

---

## 2. Evaluation of the three proposal tracks

Evaluation preserved from r1 §2 substantively. The composition decision at §3 has changed; the evaluation has not.

### 2.1. Track α — Calibrator-Audit Optimisation

Five tiers A–E surgical against the calibrator audit. A (`.plugin-efficiency.json` pinning), B (`run-tier-*` alias removal + chain-depth regression gate), C1 (Evaluator prompt pointer-demotion), C2 (three-runner envelope factorisation), D (`sk20_overlay_run.py` refactor), E (deferred). Draft route at `drafts/v0.7.6_hand_authored_route/` produces hand-authored A and C1 artefacts ready for paired review.

### 2.2. Track β — Ph3 Refinement-Loop

Eight proposals P-9 through P-16 redesigning the Ph3/Ph4 boundary. 25 Actions applied through the user review + Opus-4.6 advisor consult. Per-check falsification table (P-9.1) at RC-freeze gate (A-GK-4) is the load-bearing soundness discipline. Pre-MCR Ph3-deep pass (P-9a) is the safety net, not the primary guarantee. P-14 carryover cache was deferred to v0.7.6 in the v2 revision and is now pulled forward into v0.8.0 under this composed release.

### 2.3. Track γ — v0.7.4.1 Punch List (shipped)

Six classes of mechanical fix against 17 residuals. **Shipped as the v0.7.4.1 point release on 2026-04-21** (`unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md`; Classes A–F closed with expected post-fix severity profile BLOCKER 0 / MAJOR 0 / MINOR 0 under the curated `.plugin-calibrator.json`). Under the composed v0.8.0 plan, γ reduces from an authoring phase to a verify-and-cleanup step at §3.2 Phase 0, operating against the shipped v0.7.4.1 tree as canonical substrate per decision D-8.

### 2.4. Cross-track touch-points (unchanged from r1)

Three files carry edits from two or three tracks: `agents/evaluator.md` (γ-F + α-C1 + β-P-9/10/11/13), `references/PHASE_PROTOCOL.md` (γ-A + α-C2 + β-P-9/9a/10/12), and the new `.plugin-efficiency.json` at harness root (α-A authors; β instrumentation reads). These drive the authoring order at §3.2.

---

## 3. Proposed architecture — single composed release v0.8.0

### 3.1. Architectural layers preserved

The five layers from r1 remain the right way to read the work; only the release vehicle collapses.


| Layer               | Concern                                                                              | Release vehicle                                               | Change class                                            |
| ------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------- | ------------------------------------------------------- |
| L0 Hygiene          | γ six classes (shipped at v0.7.4.1)                                                  | v0.8.0 Phase 0 (verify-and-cleanup against the v0.7.4.1 tree) | Verification + deprecated-alias removal                 |
| L1 Instrumentation  | α-A pinning; α-B alias removal + regression gate                                     | v0.8.0 Phase 1                                                | Configuration + regression gate                         |
| L3 Ph3/Ph4 redesign | β core (P-9, P-9.1, P-9a, P-10, P-11, P-12, P-13, P-15, P-16) + register-audit patch | v0.8.0 Phase 2                                                | Schema + skill + agent-prompt rewrite                   |
| L2 Token pruning    | α-C1 Evaluator pointer-demotion; α-C2 envelope factorisation; α-D script refactor    | v0.8.0 Phase 3                                                | Pointer-demotion, envelope factorisation, code refactor |
| L4 Pulled-forward   | β P-14 paragraph-hash verdict carryover cache                                        | v0.8.0 Phase 4                                                | Schema + cache artefact class                           |
| L5 Deferred         | P-E1 Reflector/Planner prompt shrink; P-E2 batch-dispatchable tag                    | v0.8.x or later                                               | n/a                                                     |


The layer ordering L0 → L1 → L3 → L2 → L4 is the intra-release authoring order (§3.2). It differs from the layer-number order because L2 (token pruning) applies *after* L3 (Ph3/Ph4 redesign) — the Evaluator pointer-demotion diff and the three-runner envelope factorisation need β's new structure present before they can land without drift.

### 3.2. Intra-release authoring order for v0.8.0

Eight authoring phases. Each phase is self-contained; each ends with a verification step before the next begins. The phases are binding; the step-level detail inside each phase is illustrative.

#### Phase 0 — γ verify-and-cleanup against v0.7.4.1

γ's six bug-class closures landed at the v0.7.4.1 point release on 2026-04-21 (`unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md` §Scope). Phase 0 of v0.8.0 therefore reduces to (a) verifying each of the six closures still holds against the canonical Phase 0 substrate, and (b) completing the deprecated-alias retirement that v0.7.4.1 left in place for back-compatibility.

**Canonical Phase 0 substrate (D-8).** The Phase 0 substrate is `unpacked/research-writing-harness-claude-v0.8.0/` — the "wip0" authoring tree. This tree is a copy of the shipped v0.7.4.1 tree with two prior edits already landed: (i) the four `skills/run-tier-{1,2,3,4}/` deprecated-alias directories have been removed (what the r1 ordering scheduled as α Tier B Phase 1.2), and (ii) the α Tier A `.plugin-efficiency.json` placeholder has been authored in place with the `_comment` field citing this architecture's §3.2. The wip0 `.claude-plugin/plugin.json` description field already reads "γ hygiene (already closed at v0.7.4.1)" — this framing is retained and the Phase 0 authoring does not contradict it. Predecessor documentary evidence: `unpacked/plugin_calibrator_handoff_v0.7.4.1.md` documents the v0.7.4 → v0.7.4.1 calibrator cut; `unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md` names the six closed bug classes.

- **P0.1 (verify and cleanup Class A).** Confirm `references/phase_state_schema.md` exists in the wip0 tree and no reference to `tier_state_schema.md` remains in any `.md` file under `references/`, `agents/`, `skills/`, or `README.md`. *Empirical finding (r5):* the v0.7.4.1 physical rename left 7 stale body-prose citations to `tier_state_schema.md` inside `references/AGENT_ORCHESTRATION.md` (§§ 8.2a and 8.2b body prose, not historical provenance). Phase 0 closes these by flipping them to `phase_state_schema.md` — the body-section numbers (§ 3.1, § 6, § 6.2, § 6.3a) are preserved under the physical-file rename. The 2 remaining in-file references inside `references/phase_state_schema.md` itself (the file-rename note on line 3 and the v0.6.0 predecessor reference on line 9) are intentional provenance and are the named exception to the grep-returns-0 target in the Verification paragraph below.
- **P0.2 (verify and cleanup Class B + complete alias retirement).** Confirm the four deprecated `skills/run-tier-{1,2,3,4}/` directories are no longer present in the wip0 tree. This closes what the r1 ordering scheduled as α Tier B Phase 1.2. *Empirical finding (r5):* the directory retirement was structurally complete in wip0, but two narrative surfaces carried stale `run-tier-[1-4]` strings that would break post-retirement. Phase 0 flips the 4 `/run-tier-N` command-catalog rows in `skills/plugin-commands/SKILL.md` to `/run-phase-N` (command names only; the description-cell Tier vocabulary is preserved pending D-4 Phase 3.4 vocab sweep). `skills/run-phase-4/SKILL.md` line 187's rename-chain row is also updated from "removed at v0.7.5 RC" to "removed at v0.8.0 (the v0.7.5 RC removal target was absorbed into the v0.8.0 composed release per the v0.8.0 upgrade architecture D-1/D-2)" — a factual update, not a history rewrite.
- **P0.3 (verify Classes C/D).** Confirm the legacy-tree and retired-skill README path references removed at v0.7.4.1 are still absent in the wip0 `README.md`. *Status after r5 Phase 0 in-session expansion:* not fully audited in the r5 session; recommended as a targeted pre-Phase-1 verification against the v0.7.4.1 `RELEASE_NOTES` § Class C/D item list before Phase 1.1 begins. *Audit result (r6):* clean. The primary Class C/D target strings — `legacy/marshal-f1-retired/` and `legacy/rule-digest-v060-retired/` (the two retired-tree path prefixes named in `CHANGELOG.md §v0.7.4.1 Classes C and D`) — return zero matches against the wip0 `README.md`. The residual matches on `run-full-review`, `run-tier-standard`, `run-tier-submission`, and `run-tier-reflex` (README lines 102, 139, 148, 168, 175, 178, 180, 181, 183, 188, 192, 194, 204, 208, 216, 217, 222, 334, 362) all sit inside historical `§Version` narrative describing v0.5.x / v0.6.0 tree state — the carve-out the v0.7.4.1 CHANGELOG explicitly names ("narrative reference to the then-live skill name is not a dead-reference claim against the shipped v0.7.4.1 skill catalogue"), which is already folded into the Verification paragraph's carve-out (c) `README.md §Version` narrative. No fourth carve-out category surfaced; the Verification paragraph's three named carve-outs remain sufficient. P0.3 closes.
- **P0.4 (reconcile Class E skills-catalog count).** v0.7.4.1 `README.md` shipped with 31 skills including the four deprecated aliases. After P0.2's alias retirement, the canonical count is 27. Update the `### Skills (N)` heading and the skills table to reflect 27 skills, removing the four `**Deprecated alias**` rows. This is the only non-trivial Phase 0 edit.
- **P0.5 (verify Class F).** Confirm the `## Output Contract` heading is present in each of `agents/{planner,evaluator,generator,reflector}.md`.

*Verification.* `grep -c "tier_state_schema.md"` in every `.md` file under `references/`, `agents/`, `skills/`, `README.md` returns 0 **except for the two intentional-provenance matches inside `references/phase_state_schema.md` itself** (the v0.7.4.1 file-rename note on line 3 and the v0.6.0 predecessor reference on line 9 — both document the Tier → Phase rename and cannot be erased without rewriting provenance). A grep for `run-tier-[1-4]` under `skills/` returns nothing **except for two historical-provenance matches inside a rename-chain table in `skills/run-phase-4/SKILL.md` (lines 178 and 187)**; matches elsewhere are narrative references inside `README.md §Version` describing the v0.7.4 retired aliases as history. `scripts/release-gate.sh` passes. Target severity profile at this phase: BLOCKER 0 / MAJOR 0 / MINOR 4 (the α Tier A measured-throughput amendment at P1.1 has not yet replaced the 2,000 tok/s placeholder).

**Note on Phase 1 consequences.** Because α Tier A's draft-file landing is already present in the wip0 substrate, Phase 1.1 below narrows from "copy the draft to harness root, then amend" to "apply the two binding D-3 amendments in place" — replace the 2,000 tok/s placeholder with measured throughput against a warm Opus 4.7 connection, and re-confirm the `role_overrides` table after P0.2's alias retirement. Phase 1.2 (α Tier B alias removal) is folded into P0.2; Phase 1.3 (α Tier B regression gate) remains unchanged.

#### Phase 1 — α instrumentation (Tier A + Tier B)

Pin calibrator assumptions, remove deprecated aliases, add chain-depth regression gate.

- **P1.1 (α-A).** Copy `proposals/drafts/v0.7.6_hand_authored_route/plugin-efficiency.json.draft` to `research-writing-harness/.plugin-efficiency.json`. Apply the two binding amendments from D-3: replace `input_tokens_per_second: 2000` with a measured throughput figure against a warm Opus 4.7 connection, and confirm the `role_overrides` table still maps every shipped SKILL.md path after the Phase 0 edits.
- **P1.2 (α-B).** Remove `skills/run-tier-{1,2,3,4}/` directories per `CLAUDE.md §12.8`. Chain depth drops from 19 to the 12–15 range.
- **P1.3 (α-B regression gate).** Extend `scripts/release-gate.sh` with a chain-depth check against `audit-package-speed` at `max_chain_depth <= 15` — matching the `.plugin-efficiency.json` threshold. If measured chain depth exceeds 15, the regression gate fails closed and the `.plugin-efficiency.json` threshold is brought back into line with the measured floor (or the cause is architectural and the ceiling re-negotiated with the user).

Verification: `.plugin-efficiency.json` validates against the calibrator schema. `audit-package-economics` flips `assumed_rates_verified: false → true` on its next probe. `audit-package-speed` reports `max_chain_depth <= 15`. Severity profile after this phase: BLOCKER 0 / MAJOR 0 / MINOR 1 (the prompt-clarity-score MINOR remains — it is a structural Path A/B question the slate declines to tune).

**External quoting is STILL refused** after this phase until the measured-throughput amendment at P1.1 confirms the wall-clock / $ figures are honest.

#### Phase 2 — β core (Ph3/Ph4 redesign)

The architectural core of v0.8.0. Eight proposals compose into a substrate, agent-prompt, skill, and reference edit set. Authored in sub-phases to preserve invariant audit-ability.

- **P2.1. Substrate.** Widen `SectionStateObject` to 16 fields (`pre_mcr_deep_pass_completed: bool`). Extend F1/F6 frontmatter: F1 gains `adversarial_register: refinement | certification`, `routing_rationale: "primary_evidence=<check_id>"`; F6 gains `check_profile: refine | structural | deep`, `structural_delta_flag: bool`, `parallel_dispatch: bool`, `threshold_version: v0.7.5-provisional`. Extend F4 with `demoted_check_advisories` block. Author `scripts/paragraph_hash_map.py` producing a paragraph-level hash map over `manuscript_hash` artefacts. Author `scripts/migrate_convergence_journal_v075.py` backfilling prior scalar `convergence_metric` rows to the four-component shape where reconstructible, marking un-reconstructible rows `[v0.8.0-BACKFILL-N/A]`. Extend `scripts/phase_state_validate.py` and `scripts/artefact_frontmatter_validate.py` to the 16-field / extended-frontmatter shapes.
- **P2.2. `references/PHASE_PROTOCOL.md` rewrites.** §3.3 Ph3 contract — enumerate the three profiles (refine / structural / deep) and pin the per-check `halo_scope: paragraph | immediate_neighbour | containing_section` declarations from `references/DETERMINISTIC_CHECKS.md` and `references/SAFEGUARD_LAYER.md`. §3.3.2 convergence contract — multi-signal 4-vector (grounding_clean, check8_aggregate, findings_count_delta-excluding-Check-8, line_delta) over a 3-round window. §3.3.4 manuscript-hash contract — extends to paragraph level via the new hash map. §3.4 MCR admission contract — add `E-MCR-PRE-DEEP-PASS-REQUIRED` and the pre-MCR Ph3-deep-pass precondition.
- **P2.3. `skills/run-phase-{3,3-stability,4}/SKILL.md` rewrites.** Rewrite the Phase 3 (Evaluator Engagement) block of each skill to dispatch against the `check_profile` field; preserve the Ph3 stability sub-mode envelope (grounding audit + Check 8 only) under trigger 30 escalation. Note the three skills still carry shared envelope prose at this phase — it collapses at Phase 3.
- **P2.4. `agents/evaluator.md` additions.** Add §3.0 Parallel subagent dispatch (P-13) with A-OT-1/2/3 framing — "wall-clock per iteration" optimization target, `parallel_dispatch_cost_multiplier` logging, `parallel_dispatch: false` opt-out. Add §Adversarial register (P-9 / register-audit patch) naming the refinement ↔ certification register split. Add the diff-scope + halo preconditions (P-10). Add demoted-check advisory routing (P-15).
- **P2.5. `agents/reflector.md` additions.** Extend Phase 2f with `R-Refl-RG-1 register_mismatch` (MAJOR) and Phase 2g with `§2g.3 Demoted-check recurrence`. Add `R-Refl-RT-1 routing_ambiguity` as a new finding class.
- **P2.6. `agents/planner.md` additions.** Extend dispatch-plan authorship with per-round `check_profile` selection and ceiling-aware iteration budget (P-16) emitting `/ph3-terminate` suggestion under ceiling-lock.
- **P2.7. `references/AGENT_CONTRACTS.md` updates.** Evaluator contract gains parallel-dispatch preconditions. Planner contract gains ceiling-aware budget responsibility.
- **P2.8. RC-equivalent scope-freeze gate (A-GK-4).** **CLOSED r19** — disposition column of the §3.P-9.1 eleven-row falsification table populated under user directive **Default continue** (D-6); normative copy in `proposals/v0.7.5_phase3_refinement_loop_proposal.md` §3.P-9.1 + v0.8.0 P2.8 scope-freeze paragraph. Row 11: `**retained in Ph4-deep with confessed coverage gap + v0.7.6 lightweight-proxy milestone logged**`. Rows 1–10: `**retained in Ph4-deep**`. Phase 3 entry unblocked per `status: READY-FOR-PHASE-3-BUILD`.

Verification: `scripts/release-gate.sh` passes against the 16-field / extended-frontmatter shape. `scripts/migrate_convergence_journal_v075.py` dry-runs cleanly against any convergence-journal instance in the test corpus. The §3.P-9.1 disposition column is fully populated. All seven β §7 falsification metrics have populated their first data point at Phase 2 ship.

#### Phase 3 — α token pruning (Tier C1 + Tier C2 + Tier D)

Token-surface reductions on the highest-concentration agent prompt and skill envelopes. Applied *after* β core so the pointer-demotion and envelope-factorisation operate on the final post-β surface.

- **P3.1 (α-C1).** **Pointer demotion (token surface).** The hand-authored unified diff `proposals/drafts/v0.7.6_hand_authored_route/evaluator.md.pointer-demotion.diff` targets the **v0.7.4.1** Evaluator narrative with **v0.7.0 Tier** tokens and `TIER_PROTOCOL.md` pointers. On the **v0.8.0 wip0** tree (post-β P2.4), apply an **equivalent demotion** that preserves **Ph** vocabulary, `PHASE_PROTOCOL.md`, `phase_state.json`, and β **Ph3 / Ph4 v0.8.0** prose — do **not** regress Tier naming by patching the diff verbatim. **r20 (partial):** the long **Tier-conditioned** / gate-set exposition on wip0 was replaced by a **Phase-conditioned pointer table**, four binding call-outs, a one-line **Gate set** pointer to `**PHASE_PROTOCOL.md` §7** (correcting the pre-r20 §4 mis-cite), and a `What you read` cross-ref fix. **Remaining P3.1 gates:** draft README post-apply checklist items **2–4** (`audit-package-economics` token probe; SAFEGUARD Check 1 on first live round; paired review sign-off). **P3.4** (D-4 residual `T1`/`T2`/… cleanup) still follows after paired review confirms the v0.7.0 documentary block boundaries.
- **P3.2 (α-C2).** Author `references/PHASE3_PHASE4_COMMON_ENVELOPE.md` containing the shared envelope language (seven-step judgment-pass structure, SAFEGUARD layer invocation, Check 8 aggregation rule, convergence-metric declaration/reading contract, Coupling E.2 overlay dispatch, Reflector Phase 2f/2g audit hooks). Edit `skills/run-phase-{3,3-stability,4}/SKILL.md` to replace their copies of the shared envelope with a single pointer line plus SKILL-specific differentiators only. Paired review of the reference fragment in isolation (per Track α §4 P-C2 MEDIUM-HIGH sensitivity), then a single-session pass for the three SKILL edits.
- **P3.3 (α-D).** Refactor `scripts/sk20_overlay_run.py` from CC=48 / MI-proxy=0.0 to 3–4 cohesive functions at CC ≤ 15 per function and module MI-proxy ≥ 30. Preserve the I/O contract. Add unit-test scaffolding if none exists. Note the draft route explicitly does not cover α-D; the refactor authors fresh against the current script.
- **P3.4 (post-C1 Tier → Phase residual cleanup).** Per decision D-4, after the pointer-demotion diff lands, walk `agents/evaluator.md` for remaining `T1`/`T2`/`T3`/`T4` / `tier_state.json` / `t3_last_activity_at` strings. Each is either (a) inside a v0.7.0 vocabulary section documentary block the diff explicitly preserved — leave as is; or (b) outside such a block and residual — rewrite to Phase vocabulary. The residual cleanup is typically <10 string substitutions; diff-line-number churn from prior phases has already settled.

Verification: `audit-package-economics` probe returns a token-count reduction of ~3.2–3.4k on `agents/evaluator.md` and ~5–7k aggregate across the three Ph3/Ph4 SKILL files. `reviews/safeguard_layer_results.md` Check 1 (Regression Guard) reports CLEAN on the first post-pointer-demotion round. `scripts/sk20_overlay_run.py` passes its test scaffolding. Severity profile after this phase: BLOCKER 0 / MAJOR 0 / MINOR 1 (structural prompt-clarity-score remains).

#### Phase 4 — β P-14 pulled-forward (paragraph-hash verdict carryover cache)

The deferred v0.7.6 proposal pulled into v0.8.0 because the composed release absorbs it.

- **P4.1.** Per-check-class cache key specification. Paragraph-local checks use `(paragraph_hash, check_id, model_used)`. Cross-paragraph checks use `(paragraph_hash, check_id, model_used, diff_scope_fingerprint)` where `diff_scope_fingerprint = SHA-256(sorted(changed_paragraph_id_set))`. The check-class tag is derived from the `halo_scope` field shipped in Phase 2.2.
- **P4.2.** Cache is Evaluator-owned and lives at `reviews/.evaluator_verdict_cache/<project>.json`. Not a ledger artefact; the Planner does not read it. Cache is refused across a model-family change on the family ordering `{Haiku 4.5} ≺ {Sonnet 4.6} ≺ {Opus 4.7}` — capability-inversion protection extends to the cache layer.
- **P4.3.** Threshold retuning. The v0.7.5-provisional thresholds on P-12 and P-16 upgrade to `v0.8.0-provisional` at ship and to `v0.8.0-retuned` only when INF3001H replication produces sufficient data (per decision D-5). If retuning is under-powered at ship time, thresholds remain provisional with the retuning milestone logged at `proposals/v0.8.0_threshold_derivation.md`.

Verification: cache hit-rate instrumentation populates `reviews/reflection_report.md` on every post-cache-land round. Cache miss on model-family change traces correctly in `reviews/safeguard_layer_results.md`. No Ph4-admission round admits on a cache-supplied verdict without a concurrent re-read confirmation on the round's own fresh dispatch.

#### Phase 5 — release-gate composition and CHANGELOG authoring

- **P5.1.** Compose the v0.8.0 CHANGELOG covering all four phases. Carry forward the v0.7.4 "ship-on-iter-7-only (n=1)" evidentiary caveat, widen it to "structural-plus-γ-hygiene at v0.8.0; empirical backfill at n=2 deferred to INF3001H first Ph3 round" per decision D-5.
- **P5.2.** Bump `.claude-plugin/plugin.json` from `"version": "0.7.4"` to `"version": "0.8.0"` (per D-7 recommendation; see §5 below). Update the plugin description to reflect the Ph3/Ph4 redesign: "four-agent pipeline over a Lifecycle-Phase Ladder, with Ph3 iteration-shaped refinement profiles (refine / structural / deep), diff-scoped Evaluator with context halo, paragraph-hash verdict carryover cache, and multi-signal convergence vector."
- **P5.3.** Rebuild the zip via `scripts/build-release-zip.sh` (preserving the existing `__pycache`__ / `.DS_Store` / `setup/` exclusion recipe).
- **P5.4.** Run the full calibrator three-axis probe (`audit-package-quality`, `audit-package-economics`, `audit-package-speed`) against the built zip. Verify: `max_cyclomatic_complexity <= 20`, `max_chain_depth <= 15`, `assumed_rates_verified: true`, `prompt_tokens_estimate` down from 131,437 to a projected ~120–122k (3.2–3.4k + 5–7k reductions; conservative claim at 5.5–8k aggregate). The `prompt_clarity_score` remains below 0.50 — expected, structural, not a blocker.
- **P5.5.** File the final release notes; reset the `proposals/` folder to mark the three source proposals as LANDED (alongside a boundary marker `proposals/v0.8.0_release_boundary.md` referencing this architecture document).

### 3.3. v0.8.0 release-gate (single gate)

Replaces the three scope-freeze gates at r1 §3.3. The single gate passes when — and only when — all of the following hold:

1. `scripts/release-gate.sh` returns exit code 0 against the post-Phase-4 substrate.
2. The curated `.plugin-calibrator.json` allow-list ships as authoritative (no residual BLOCKER, no residual MAJOR, `prompt_clarity_score` MINOR declared structural).
3. The §3.P-9.1 falsification-table disposition column is fully populated under user review (A-GK-4 closed).
4. Measured throughput (not the 2,000 tok/s placeholder) lives in `.plugin-efficiency.json`. Without this amendment, the release-gate refuses to pass and the CHANGELOG cannot quote any wall-clock or per-round cost figure.
5. The Evaluator-prompt pointer-demotion binding-rule preservation check has been performed under paired review.
6. The three-runner envelope factorisation preserves each SKILL's distinct invariants under paired review.
7. `scripts/sk20_overlay_run.py` passes its test scaffolding at CC ≤ 15 per function.
8. The P-14 cache passes a single warm-round smoke test: hit on unchanged paragraphs, miss on changed paragraphs, miss on model-family change.
9. γ Phase 0 verification `grep` returns 0 on every expected-absent string (including both `tier_state_schema.md` and `run-tier-[1-4]` references outside historical narrative blocks in `README.md §Version`).
10. `audit-package-economics` probe returns `assumed_rates_verified: true` and a non-null `projected_cost_per_invocation_usd` within ±10 % of the pre-release estimate (else the estimate is revised before quote).

---

## 4. Hand-authored draft route — how it applies inside v0.8.0

The `proposals/drafts/v0.7.6_hand_authored_route/` folder produced three first-class artefacts for the composed release:


| Artefact                             | Applied at                                                                   | Amendment required at application                                                                                                                                |
| ------------------------------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `plugin-efficiency.json.draft`       | Phase 1.1 (α-A) — copy to `research-writing-harness/.plugin-efficiency.json` | Replace the 2,000 tok/s placeholder with measured throughput (D-3); re-confirm the 22-entry `role_overrides` path list against the post-Phase-0 skills directory |
| `evaluator.md.pointer-demotion.diff` | Phase 3.1 (α-C1) — apply to `agents/evaluator.md` after β core lands         | Verify diff-target context has not drifted under Phase 2.4's additions; run the README §Binding-rule preservation check; apply under paired review               |
| `README.md`                          | Referenced throughout                                                        | Line-by-line review guidance is binding at both application points                                                                                               |


The README's §Honesty notes remain binding across the composed release:

- **Throughput placeholder:** the 2,000 tok/s figure is not measured. v0.8.0 Phase 5.1 refuses to quote any wall-clock figure externally before measured throughput lands; the `_comment` field of the JSON carries the honesty note into the shipped file.
- **v0.7.0 vocabulary preservation:** the diff targets v0.7.0 Tier vocabulary in `agents/evaluator.md`'s Tier-conditioned-engagement narrative. Decision D-4 ensures the Phase rename lands after the pointer-demotion (Phase 3.4 in this architecture) — the diff-line-number drift hazard is thereby closed.
- **Haiku 4.5 representation gap:** the calibrator's binary executor/orchestrator split cannot distinguish Haiku 4.5 at Reflector-lightweight from Sonnet 4.6 at the executor tier. Not a plugin limitation; documented in `_comment` and `reviews/reflection_report.md`'s `model_dispatch_audit` block.
- **Ship-on-iter-7 caveat:** carries into v0.8.0 until INF3001H's first Ph3 round tightens the n=1 → n=2 framing (decision D-5).

Each draft artefact should be read line-by-line at its application phase per the README's existing guidance. The README's four mechanical verification checks on the pointer-demotion (`grep -c "^###"` header count; `audit-package-economics` re-probe; Check 1 Regression Guard; paired review) all surface in v0.8.0 Phase 3.1 and Phase 5.4.

---

## 5. Open decisions

Six decisions closed (D-1 through D-5 except D-4 which is closed-on-recommendation and remains inspectable at Phase 3.4 authoring time; D-8 closed in r4 adopting the wip0 substrate as canonical Phase 0 tree; D-7 closed in r6 with the composed release pinned at v0.8.0 per the §5 recommendation below). Revision r5 did not close any new user decision. Revision r6 closes D-7 by user confirmation 2026-04-22 — the release ships as v0.8.0, matching the document's own recommendation — and records the clean P0.3 Classes C/D audit as a §3.2 closure-marker. One decision remains open:


| #   | Decision                                                                  | Default if skipped                               | When it closes                                |
| --- | ------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------- |
| D-6 | Per-row disposition of the §3.P-9.1 falsification table under user review | Full user review required; no defensible default | v0.8.0 Phase 2.8 (intra-release scope-freeze) |


**D-7 rationale.** The composed release widens `SectionStateObject` from 15 → 16 fields, changes the shape of `convergence_metric` from scalar to 4-vector, adds a new root config file (`.plugin-efficiency.json`), authors a new reference (`PHASE3_PHASE4_COMMON_ENVELOPE.md`), redraws the Ph3/Ph4 softgoal boundary, and introduces a new artefact class (paragraph-hash verdict carryover cache). Under a strict semver reading this is a minor-version bump — v0.7 → v0.8. Under the plugin's observed versioning practice (every v0.7.x has been a substantive change), v0.7.5 is also defensible. This document recommends v0.8.0 because honest versioning helps the user and any downstream reader understand that the Ph3 contract has *changed*, not been refined. The v0.7.5 alternative is acceptable if the user prefers to reserve v0.8.0 for a later contract-renegotiation. **Closed at r6 (2026-04-22):** user confirmed v0.8.0 per this recommendation; D-7 moves to `user_decisions_closed:` in the frontmatter. This closure binds Phase 5.2 CHANGELOG authoring (composed-release entry is authored under the `v0.8.0` heading) and Phase 5.4 release-notes authoring (ship-version string reads `v0.8.0`).

**D-8 rationale (closed in r4, 2026-04-22).** Four candidate Phase 0 trees exist on disk: the working-tree root at `research-writing-harness/` (stale at `.claude-plugin/plugin.json` `version: 0.7.2`, with a partial phase-skill copy-in but tier-only references and scripts — structurally regressed from the shipped baseline); the v0.7.4 shipped baseline (tier-only, structurally behind γ closure); the v0.7.4.1 shipped point release (tier + phase dual-surface with `phase_state_schema.md`, `run-phase-1..4` + `run-phase-3-stability`, plus four `run-tier-*` deprecated aliases); and the "wip0" authoring substrate at `unpacked/research-writing-harness-claude-v0.8.0/` (`plugin.json` version `0.8.0-wip0`), created on 2026-04-22 as a copy of v0.7.4.1 with the four `run-tier-{1,2,3,4}/` deprecated-alias directories already removed and the α Tier A `.plugin-efficiency.json` placeholder authored in place (its `_comment` field citing this architecture's §3.2). D-8 closes on the wip0 substrate so that (a) the two already-landed substrate edits are not redone elsewhere, (b) subsequent Phase 0–4 edits operate on a single lineage, and (c) the wip0 `plugin.json` description's framing — "γ hygiene (already closed at v0.7.4.1)" — is preserved rather than contradicted. The prior `unpacked/plugin_calibrator_handoff_v0.7.4.1.md` (28 KB) and `RELEASE_NOTES_v0.7.4.1.md` (5 KB) are the documentary predecessors linking the v0.7.4 → v0.7.4.1 → wip0 lineage.

---

## 6. Risk register and soundness gates (unchanged from r1)

### 6.1. Risk matrix

Preserved verbatim from r1 §4.1 except:

- R-9 (chain-depth regression gate ceiling too tight) — mitigation moves from "ceiling raised if architectural floor proves higher" to "Phase 1.3 regression gate fails closed and `.plugin-efficiency.json` threshold is brought back into line." Under a single composed release, there is no intermediate-version fallback; the release must either pass the gate or renegotiate the ceiling before ship.
- R-10 (v0.7.5 RC ships on n=1 evidence) — renames to "v0.8.0 ships on structural-plus-γ-hygiene evidence; β §7 falsification metrics populate from the first v0.8.0 Ph3 round in the INF3001H replication, not at ship". The mitigation is per-metric rollback protocol (Track β §7) plus a v0.8.1 post-replication review window.
- R-11 (NEW) — intra-release ordering drift. A single composed release with an eight-phase authoring sequence has no intermediate release-gates; a mis-sequenced edit can require unwinding across two or three phases. Mitigation: each phase's verification step at §3.2 is binding before the next phase begins; Phases 2.1, 2.8, 3.1, 4.1 specifically carry paired-review gates.

### 6.2. Soundness gates (architectural) — preserved

SG-1 Grounding Protocol Rule 1, SG-2 four-agent contract, SG-3 `E-MA-CAPABILITY-INVERSION` refusal, SG-4 single-writer contract on `reviews/phase_state.json` — all preserved under v0.8.0. P-14's cache (Phase 4) extends SG-3 to the cache layer; all other SGs carry forward unchanged.

### 6.3. Evidence-adequacy note — tightened

Decision D-5 binds the framing. v0.8.0 ships with:

- Track α's token-surface reduction claims (~3.2–3.4k on `agents/evaluator.md`, ~5–7k aggregate across the three Ph3/Ph4 SKILL files) falsifiable at Phase 5.4 via calibrator re-probe.
- Track β's seven §7 falsification metrics with first data point at Phase 5.4 (structural falsification); empirical falsification deferred to INF3001H first Ph3 round.
- Track γ's severity-delta claims (MAJOR 32 → 0 / MINOR 5 → 0 under curated allow-list) falsifiable at Phase 0 verification.

The v0.8.0 CHANGELOG steps the n=1 evidentiary caveat forward from the v0.7.4 framing to: *"structural hygiene confirmed at release; economic-efficiency claims remain n=1 pending INF3001H replication; threshold retuning for P-12 / P-16 deferred to v0.8.1 post-replication."*

---

## 7. Non-goals (unchanged from r1 except L5 boundary)

This architecture does not propose:

- Compression of the Reflector or Planner prompts (Track α P-E1 — L5 deferred; contract-surface-bearing).
- Introduction of a `batch-dispatchable` orchestrator tag (Track α P-E2 — L5 deferred; wait-on-trigger).
- Modification of the Grounding Protocol, the four-agent contract, the Lifecycle-Phase Ladder rung semantics, the `SectionStateObject` cardinality beyond the 15 → 16 widening under β-P-9a, or the trigger enum ceiling (30 + any β additions as documented in Phase 2.1).
- Any change to `PHASE_PROTOCOL.md §3.3.3` (the Check 8 accessibility gate at Ph3 TerminalSignoffRow).
- Any change to the MCR admission contract other than the pre-MCR-deep-pass requirement (β-A-MCR-2).
- Re-litigation of the v0.7.4 P-1 through P-8 package.

### 7.1. Deferred: Check 8 Sub-check G (consolidation anchors / cumulative load)

A follow-on proposal **Check 8 — Sub-check G (Consolidation Anchors)** is drafted at `proposals/check8_subcheck_g_consolidation_anchors_proposal.md`. It extends the six local Sub-checks A–F to a seventh, **manuscript-cumulative** check, and **would require** edits to `PHASE_PROTOCOL.md §3.3.3` (at minimum Sub-check enumeration in the terminal gate). That conflicts with the §7 non-goal bullet immediately above; the proposal recommends **v0.8.1** (or a formal user decision to lift the non-goal for a late v0.8.0 slot). **v0.8.0** authoring sessions should not implement G unless §7 is amended and paired review re-runs on gate semantics.

---

## 8. External measurement and hygiene adjuncts (out of release scope)

Nothing in this section ships inside the v0.8.0 zip. Nothing here alters the four-agent contract, the Grounding Protocol, the Lifecycle-Phase Ladder rung semantics, SG-1 through SG-4, the `.claude-plugin/plugin.json` manifest, or any artefact class defined at §§ 2–3. The subsection documents three external tools whose output can be consumed as inputs by the v0.8.0 release-gate at § 3.3, so that gate items 4 and 10 — and the framing of decisions D-3 and D-5 — are serviced by measured data rather than by placeholder values. Each adjunct is grounded to a specific § of this architecture; none is load-bearing. This document does not make any of them a prerequisite of ship.

### 8.1. Per-turn cost and subagent-attribution measurement — `alexgreensh/token-optimizer`

**Feeds.** D-3 (measured-throughput calibration before external quoting of `.plugin-efficiency.json`); § 1.2 `subagent_dispatch_multiplier: 10.0` risk signal; § 3.3 gate item 10 (`audit-package-economics` probe returns `projected_cost_per_invocation_usd` within ±10 % of the pre-release estimate).

Per the tool's README, it records per-turn input / output / cache-read / cache-write to a local SQLite file at `~/.claude/_backups/token-optimizer/trends.db`, with per-session subagent attribution and cost rendered under four pricing tiers (Anthropic API, Vertex Global, Vertex Regional, AWS Bedrock). Two of its surfaced outputs map directly onto already-binding v0.8.0 obligations:

- The measured throughput figure the Phase 1.1 amendment requires in place of the `input_tokens_per_second: 2000` placeholder in `proposals/drafts/v0.7.6_hand_authored_route/plugin-efficiency.json.draft`.
- A per-invocation dollar figure that the Phase 5.4 calibrator probe can compare against the `.plugin-efficiency.json` estimate for the ±10 % check at § 3.3 gate item 10.

The tool runs as an external process and, per its README, adds no MCP or prompt surface to Claude's session. This matters for § 7 Non-goals, which decline to widen the four-agent contract. The tool supplies numbers; the harness' A-GK and A-CP dispositions, paired-review bindings, and A-GK-4 scope-freeze gate remain the binding authoring steps — Phase 1.1's measurement must still be transcribed into `.plugin-efficiency.json` under paired review.

Repo: `https://github.com/alexgreensh/token-optimizer`.

### 8.2. Terminal-output compression during authoring-phase verification — `rtk-ai/rtk`

**Feeds.** § 3.2 Phase 0 verification (bulk `grep -c` across `.md` files under `references/`, `agents/`, `skills/`, and `README.md`); Phase 3.3 test scaffolding on `scripts/sk20_overlay_run.py`; Phase 5.3 zip rebuild; Phase 5.4 full three-axis calibrator probe.

These authoring-phase verification steps run Bash-dispatched commands whose full stdout re-enters Claude's context on each invocation. Per `rtk-ai/rtk`'s README, the tool is a Rust proxy that rewrites common dev-tool commands (`git`, `grep/rg`, `pytest`, `cargo test`, `ruff check`, `docker`, and others) into compact output before execution, with reported reductions of −80 % on `grep/rg`, −80 % on `git status`, −90 % on test runners, and −80 % on `ruff check` against the repo's own corpus. The command classes overlap with the v0.8.0 verification vocabulary.

**Binding caveat from the repo's own README.** On native Windows without WSL, the auto-rewrite hook falls back to CLAUDE.md injection mode and does not rewrite Bash commands automatically; full hook behaviour requires WSL or an explicit `rtk <cmd>` invocation per command. This architecture does not mandate RTK use; where an authoring session elects to use it, the WSL / explicit-invocation caveat is a documented limitation rather than an architectural deficit.

Repo: `https://github.com/rtk-ai/rtk`.

### 8.3. Consumer-side context hygiene for INF3001H replication — `nadimtuhin/claude-token-optimizer`

**Feeds.** Decision D-5 (INF3001H second-project replication before any v0.7.6-successor threshold retuning); § 6.3 evidence-adequacy note ("empirical falsification deferred to INF3001H first Ph3 round").

The tool is a setup-time convention applied to a **consumer project** (the workspace where a paper is written using this harness), not to the plugin itself. Per its README, the convention writes a small `CLAUDE.md`, a `.claudeignore`, and `.claude/{completions,sessions}/` directories so that completed and archived work stays out of Claude Code's session-start load. The relevance to v0.8.0 is indirect: a long-horizon INF3001H Ph3/Ph4 session accumulates `reviews/`, `drafts/`, and prior-session artefacts that re-enter context on every Claude Code startup, and the convention structures those paths to 0-token cost until explicitly requested.

**Scope constraint.** This convention cannot selectively un-load the plugin's own shipped `agents/*.md`, `skills/*.md`, or `references/*.md` — those load because the plugin installs them. The plugin-surface token-pruning obligation stays with Phase 3 (α-C1 Evaluator pointer-demotion; α-C2 envelope factorisation). This adjunct is complementary, not substitutive. It is appropriate for the replication workspace, not for the plugin tree.

Repo: `https://github.com/nadimtuhin/claude-token-optimizer`.

### 8.4. Candidate pool evaluated and not selected

Seven further repos in the ten-repo candidate pool were evaluated and not selected for the following grounded reasons. Each rejection is documentary, not binding against future re-evaluation should evidence change.

- `**mksglu/context-mode`.** Provides MCP sandbox tools and session continuity. The v0.7.4 invariants at § 1.1 list no MCP dependencies; introducing one would clash with SG-2 (four-agent contract) and SG-4 (single-writer on `reviews/phase_state.json`).
- `**tirth8205/code-review-graph`, `Mibayy/token-savior`, `zilliztech/claude-context`.** All three build AST, symbol-graph, or vector indices over source code. The plugin's shipping surface is prompt markdown; there is no call graph to traverse, and `scripts/sk20_overlay_run.py` — the only non-markdown substantive file — is being refactored to CC ≤ 15 under Phase 3.3 in-tree.
- `**JuliusBrussee/caveman`.** Compresses Claude's output prose to caveman-speak. The harness' output is prose destined for an academic paper under a clear-formal-academic-English authoring convention; the style is a category mismatch with the artefact class.
- `**drona23/claude-token-efficient`.** The repo's own README documents fit for "automation pipelines with high output volume" and explicit non-fit for "architectural drift" / deep failure modes. Ph3/Ph4 structured-verdict iteration falls on the wrong side of that boundary.
- `**ooples/token-optimizer-mcp`.** Evidence in this session covered CI/CD workflow documentation only; the tool-surface description was not available for grounded selection. Rejected on insufficient evidence, not on merits.

---

## 9. What the next authoring session should do in its first three moves

*Updated in r20.* **P3.1 (partial):** `unpacked/research-writing-harness-claude-v0.8.0/agents/evaluator.md` landed the **phase-conditioned pointer demotion** (α-C1 intent, Ph-native adaptation) per §3.2 P3.1 r20 amendment — `python scripts/skill-check.py` on wip0 reports **0 blockers**. Post-apply economics probe + Regression Guard + paired review remain **open** before declaring P3.1 fully closed.

*Updated in r19.* Phase 0 closed on wip0 on 2026-04-22 (all five P0.* steps verified or landed: P0.1, P0.2, P0.4 in r5's in-session expansion; P0.3 in r6; P0.5 on a single grep). Phase 1 closed same day on wip0 — P1.1 measured-throughput amendment landed at `.plugin-efficiency.json` `input_tokens_per_second: 16897.1`; P1.2 (α-B alias removal) folded into P0.2; P1.3 (α-B chain-depth regression gate) wired into `scripts/release-gate.sh` at `max_chain_depth <= 15`. P0.1a substrate-cleanup closed 2026-04-22 in a dedicated post-r6 session per §9.1 r7 amendment. P2.1a through P2.1d and P2.1a.1 are **CLOSED** (r8–r12). **P2.2 closed 2026-04-22** (r13) — `PHASE_PROTOCOL.md` on wip0. **P2.3 closed 2026-04-22** (r14) — `skills/run-phase-3/SKILL.md`, `skills/run-phase-3-stability/SKILL.md`, `skills/run-phase-4/SKILL.md` aligned to `check_profile`, P-12 / `[CONVERGENCE-STABLE]`, and MCR `pre_mcr_deep_pass_completed` / `E-MCR-PRE-DEEP-PASS-REQUIRED`. **P2.4 closed 2026-04-22** (r15) — `agents/evaluator.md` Ph3/Ph4 v0.8.0 envelope. **P2.5 closed 2026-04-22** (r16) — `agents/reflector.md` on wip0 extended per §3.2. **P2.6 closed 2026-04-22** (r17) — `agents/planner.md` on wip0. **P2.7 closed 2026-04-22** (r18) — `references/AGENT_CONTRACTS.md` on wip0. **P2.8 closed 2026-04-22** (r19) — A-GK-4 / D-6: user directive **Default continue**; §3.P-9.1 disposition column populated in `proposals/v0.7.5_phase3_refinement_loop_proposal.md` (rows 1–10 **retained in Ph4-deep**; row 11 **retained in Ph4-deep with confessed coverage gap + v0.7.6 lightweight-proxy milestone logged**). **Phase 2 (β core) is CLOSED** for scope-freeze purposes. Release-gate BLOCKER count unchanged at 1 (Phase 5.2 version-bump scope). D-6 closed at r19; D-7 closed in r6 at v0.8.0; D-9's deferral closed in r7 by execution. The next session's entry point is **Phase 3** (α token pruning: §3.2 P3.1–P3.4) per `status: READY-FOR-PHASE-3-BUILD`.

1. **Read this architecture (r20), then `proposals/drafts/v0.7.6_hand_authored_route/README.md` line-by-line** — complete the **P3.1** post-apply checklist (economics probe; Check 1 Regression Guard; paired review). Phase 3.2 (α-C2 envelope factorisation) depends on the same README honesty notes. P2.8's §3.P-9.1 table remains authoritative in `proposals/v0.7.5_phase3_refinement_loop_proposal.md`. Phases 0–2 do not need re-running; wip0 is the live build surface (release-gate **1** blocker, Phase 5.2).
2. **Close P3.1 verification** — run `audit-package-economics` on the harness root against the post-demotion `agents/evaluator.md`; capture SAFEGUARD Check 1 CLEAN on the first post-demotion Evaluator round; obtain **paired review** sign-off that the pointer table + four call-outs preserve the draft README's binding rules (Ph-native adaptation documented at §3.2 P3.1 r20).
3. **Continue Phase 3** per §3.2 — P3.2 (`PHASE3_PHASE4_COMMON_ENVELOPE.md` + three SKILL pointers), P3.3 (`sk20_overlay_run.py`), P3.4 (D-4 residual Tier→Phase cleanup in `agents/evaluator.md` **after** P3.1 sign-off). Preserve §9 per-phase discipline (r6 through **r20**).

### 9.1 Phase 0 residuals identified during Phase 1.1 closure (2026-04-22)

During Phase 1.1 closure verification, three `scripts/release-gate.sh` BLOCKERs surfaced on the wip0 substrate that were not enumerated by any Phase 0 move in §3.2. Classified here to preserve the Phase 0 closure record on wip0 and schedule triage.

1. **R-P0-CAT-1 (catalog parity, commands).** `skills/plugin-commands/SKILL.md` does not list `accessibility-overlay` or `run-phase-3-stability` in its command catalog, but both live under `commands/`. Scope: single-file catalog append. In-session triage is safe (pure discoverability edit, no contract or prompt change). Scheduled for immediate Phase 1 closure hygiene.
2. **R-P0-CAT-2 (catalog parity, skill registry).** `references/SKILL_REGISTRY.md` retains pre-rename `run-tier-{1,2,3,4}` rows and is missing the post-rename `run-phase-{1,2,3,4}` rows. This is a residual of the v0.7.4 tier→phase vocabulary rename that D-4's lint target (`agents/evaluator.md`) did not cover. Scope: registry table edit. In-session triage is safe (registry is a discovery artefact, not a contract). Scheduled for immediate Phase 1 closure hygiene.
3. **R-P0-SCHEMA-3 (three-way schema/validator/fixture inconsistency).** A substrate inconsistency was discovered between the renamed-but-not-content-updated `references/phase_state_schema.md` (describes v0.7.0 shape: `sections` as array, T-vocabulary, 15-field `SectionStateObject`), the wip0 validator `scripts/phase_state_validate.py` (enforces v0.7.4 shape: `sections` as dict, Ph-vocabulary, 7-field log rows, exit-4 blocker convention), and the `scripts/fixtures/tier_state_smoketest/` fixtures (follow the v0.7.0 schema doc, so the PASS fixture fails validator and the BLOCK fixture returns exit 4 where release-gate expects exit 2). The P0.1 move in §3.2 renamed the filename only; a content-update of the schema reference document to v0.7.4 shape was not part of any Phase 0 item and was not discovered until Phase 1.1 closure. **Disposition (D-9, user-confirmed 2026-04-22):** DEFER. Opening this residual requires a content rewrite of `phase_state_schema.md` §§1, 1.1, 2, 3, 6.1, 7, 8, coordinated updates to the smoketest fixtures and their README, and an exit-code expectation update in `scripts/release-gate.sh` lines 337–341. That scope is not a closure-hygiene edit; it is a dedicated substrate-reconciliation phase and should be authored in its own session — provisional label P0.1a (phase_state_schema content update) or Phase 5 substrate cleanup — with its own verification paragraph. The current release-gate output on wip0 will therefore show two BLOCKERs against `tier_state_validate.py PASS`/`BLOCK` fixtures until R-P0-SCHEMA-3 is scheduled and closed; Phase 1.1's verification paragraph (§3.2) is satisfied on the efficiency-config + chain-depth gate alone, which is the condition §3.3 actually binds.
  **Closure (r7 amendment, 2026-04-22, dedicated post-r6 session).** R-P0-SCHEMA-3 closed under P0.1a per the r6 provisional label. Clean-rewrite policy applied per user direction: preserve only the two provenance carve-outs (v0.7.4.1 file-rename note; v0.6.0 predecessor reference) and rewrite the remainder to v0.7.4 shape. Closure scope:
  - `references/phase_state_schema.md` rewritten end-to-end: `sections` as object (validator requirement); 15-field `SectionStateObject` with the two validator-enforced required fields (`current_phase`, `phase_entry_log`) explicitly flagged versus the 13 contract-required fields validated by `scripts/pre_phase_advance_check.py` clauses (a)–(h); 7-field `PhaseEntryLogRow` including `model_used` with absent-means-null semantics; 30-trigger enum matching `PHASE_PROTOCOL.md §6.3` and `§6.3` additions 29–30; authoritative v0.7.4 validator finding-code table in §6.1 with the contracts-enforced-elsewhere sub-list; migration §7 redirected to the v0.7.3 → v0.7.4 path via `scripts/migrate_v073_to_v074_tier_to_phase.py`; canonical seed template §8 rewritten to the v0.7.4 two-section shape. One in-rewrite correction: `new_phase: null` is **not** validator-legal (fires `LOG_ROW_BAD_NEW_PHASE` BLOCKER); non-phase-changing rows repeat the prior phase in `new_phase` to satisfy the monotonicity check.
  - `scripts/fixtures/tier_state_smoketest/` renamed to `scripts/fixtures/phase_state_smoketest/`; `{pass,block}/reviews/tier_state.json` renamed to `phase_state.json` with v0.7.4-shape content; the BLOCK fixture's single deliberate failure is a `MONOTONICITY_VIOLATION` under a non-exempt `user_rejection` trigger. Fixture `README.md` rewritten to document the v0.7.4 exit-code convention `{0 PASS, 1 usage, 2 I/O, 3 MINOR/MAJOR, 4 BLOCKER}` and record the v0.8.0 rename provenance.
  - `scripts/release-gate.sh` Phase 0.67 block rewritten: call `phase_state_validate.py` directly (no shim detour); target the renamed fixtures; assert exit 0 on PASS and exit 4 on BLOCK; header comment updated to cite `phase_state_schema.md §§3a, 6.1` as authoritative surfaces.
  - `README.md` line 85 fixture-path reference updated from `tier_state_smoketest/` to `phase_state_smoketest/` (coordinated with the directory rename).
   **Verification evidence (grounded).** Live validator against the new fixtures: `PASS exit=0`, `BLOCK exit=4` with finding `[BLOCKER] MONOTONICITY_VIOLATION @ sections['1. Introduction'].phase_entry_log[1]: downward transition 'Ph2' -> 'Ph1' is not licensed by trigger 'user_rejection'`. Release-gate smoketest block reports `[OK] phase_state_validate.py PASS fixture returned exit 0` and `[OK] phase_state_validate.py BLOCK fixture returned exit 4 (MONOTONICITY_VIOLATION BLOCKER)`. Full release-gate verdict on post-closure wip0: **1 blocker, 8 warnings** (down from 3 blockers pre-closure). The remaining blocker is `scripts/version-check.py` reporting README latest-version `0.7.4` and CHANGELOG top-version `0.7.4` vs manifest `0.8.0-wip0` — explicit Phase 5.2 release-packaging scope per §3.2, not within R-P0-SCHEMA-3.
   **Residuals surfaced during closure (out-of-scope, flagged for later).** (i) `scripts/migrate_v073_to_v074_tier_to_phase.py` omits renaming `last_approved_tier → last_approved_phase` while renaming the other tier-prefixed fields; `phase_state_schema.md` and `PHASE_PROTOCOL.md` both use `last_approved_phase`. Patch before any v0.7.3 → v0.7.4 migration is actually run in the field. (ii) `README.md` lines 77–78 describe `tier_state_validate.py` / `tier_state_canonicalize.py` with a v0.7.0 exit-code map (`1 schema error, 2 consistency error, 3 migration gate, 4 bad invocation`); the v0.7.4 validator's actual map is `{0 PASS, 1 usage, 2 I/O, 3 MINOR/MAJOR, 4 BLOCKER}`. Documentation-currency issue; not a functional break. Defer both to a later README / migration-script hygiene pass.

R-P0-CAT-1 and R-P0-CAT-2 are in-session Phase 1 closure items. R-P0-SCHEMA-3 was deferred at r6 and closed at r7 in a dedicated P0.1a session (2026-04-22) per the closure note above.

*Optional pre-Phase-1 step (still available).* Before the Phase 1.1 measurement is captured, an authoring session may install the adjunct at § 8.1 (`alexgreensh/token-optimizer`) against the workspace so that the measured-throughput amendment has a real per-turn data source rather than requiring a single ad-hoc measurement. This step is not a prerequisite to Phase 1.1 and is explicitly out of release scope per § 8.

### 9.2 Phase 2 sub-phase closure records

The r1-through-r7 framing of §3.2 treats Phase 2 P2.1 as a single substrate-widening bullet. During r8's authoring session it became apparent that P2.1 decomposes cleanly into four independently-verifiable sub-phases, each substrate-pure with its own verification surface. The post-hoc split preserves the r6 per-phase-verification discipline at a finer grain and mirrors the r7 P0.1/P0.1a precedent for intra-phase audit-separation.


| Sub-phase       | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Verification surface                                                                                                                                                                                                           | Status at r20                                                                                 |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| **P2.1a**       | `SectionStateObject` 15 → 16 fields (`pre_mcr_deep_pass_completed: bool`); `scripts/phase_state_validate.py` type-check; `scripts/fixtures/phase_state_smoketest/` fixtures                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `phase_state_validate.py` smoketest against PASS/BLOCK fixtures; `scripts/release-gate.sh` Phase 0.67 block                                                                                                                    | **CLOSED** 2026-04-22 (r8 session)                                                            |
| **P2.1a.1**     | R-P2-VALIDATOR-F6 substrate reconciliation: author the F6 `planner_dispatch_plan` family schema in `scripts/artefact_frontmatter_validate.py` `FAMILY_SCHEMAS` to match the existing `references/ARTEFACT_FRONTMATTER_SCHEMA.md §7a` contract; extend the schema DSL's `_check_nested` with `("list", element_schema)` handling; add F6 cross-field must-be-true rules for `user_approval_required` and `subagent_envelope[].verdict_authoritative_as_read`; create the `scripts/fixtures/artefact_frontmatter_smoketest/` fixture harness (F6 PASS + F6 BLOCK + README); extend `release-gate.sh` with Phase 0.68 F6 smoketest block and py_compile on `phase_state_validate.py` + `artefact_frontmatter_validate.py` | `artefact_frontmatter_validate.py` direct runs against F6 fixtures (exit 0 on PASS, exit 3 on BLOCK); `scripts/release-gate.sh` Phase 0.68 block reports both fixtures `[OK]`; full release-gate BLOCKER count unchanged at 1  | **CLOSED** 2026-04-22 (r9 session; nested under P2.1 parallel to the r7 P0.1/P0.1a precedent) |
| **P2.1b**       | F1 frontmatter (`adversarial_register`, `routing_rationale`); F6 frontmatter (`check_profile`, `structural_delta_flag`, `parallel_dispatch`, `threshold_version`); F4 frontmatter (`demoted_check_advisories` block); `scripts/artefact_frontmatter_validate.py` extension; `R-Refl-FM-2` on enum/shape violations; F1 + F4 + F6 PASS/BLOCK fixture extensions under the harness from P2.1a.1; `references/templates/F1_evaluator_findings.md`                                                                                                                                                                                                                                                                         | Per `artefact_frontmatter_smoketest/README.md`: 3× PASS exit 0, 4× BLOCK exit 3 (MAJOR R-Refl-FM-2; not exit 4 — no in-file BLOCKER in the v0.8.0 R-Refl-FM-1..6 map on these paths); `release-gate.sh` Phase 0.68 array loops | **CLOSED** 2026-04-22 (r10 session)                                                           |
| **P2.1c**       | `scripts/paragraph_hash_map.py` (paragraph-level hash map over `manuscript_hash` artefacts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Script dry-run against any manuscript-hash instance in the test corpus; hash-determinism property-test                                                                                                                         | **CLOSED** 2026-04-22 (r11 session)                                                           |
| **P2.1d**       | `scripts/migrate_convergence_journal_v075.py` (backfills prior scalar `convergence_metric` rows to the four-component shape, marks un-reconstructible rows `[v0.8.0-BACKFILL-N/A]`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Dry-run against any convergence-journal instance in the test corpus; idempotency re-run check                                                                                                                                  | **CLOSED** 2026-04-22 (r12 session)                                                           |
| **P2.2**        | `references/PHASE_PROTOCOL.md` — Ph3 `check_profile` / `halo_scope`; §3.3.1a P-12 object + three-round `[CONVERGENCE-STABLE]`; §3.3.4 journal optional keys + `convergence_metric` widening; Ph4 pre-MCR deep-pass + `E-MCR-PRE-DEEP-PASS-REQUIRED`; sixteen-field cross-refs                                                                                                                                                                                                                                                                                                                                                                                                                                          | Manual read-sweep + consistency grep on wip0; doc-only — no new release-gate phase                                                                                                                                             | **CLOSED** 2026-04-22 (r13 session)                                                           |
| **P2.3**        | `skills/run-phase-3/SKILL.md`, `skills/run-phase-3-stability/SKILL.md`, `skills/run-phase-4/SKILL.md` — `check_profile` router; P-12 / journal / `[CONVERGENCE-STABLE]`; `pre_mcr_deep_pass_completed`; MCR gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                                                                                                                | **CLOSED** 2026-04-22 (r14 session)                                                           |
| **P2.4**        | `agents/evaluator.md` — Ph3 / Ph4 v0.8.0: P-13 parallel dispatch + `parallel_dispatch_cost_multiplier`; P-9 `adversarial_register`; P-10 diff-scope + `halo_scope` + `paragraph_hash_map` + `containing_section` fallback; P-15 `demoted_check_advisories` + `routing_rationale`; F6 `check_profile` in Ph3 procedure; Step 8 + sixteen-field read list + YAML frontmatter                                                                                                                                                                                                                                                                                                                                             | `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                                                                                                                | **CLOSED** 2026-04-22 (r15 session)                                                           |
| **P2.5**        | `agents/reflector.md` — Phase 2f §6.10 `R-Refl-RG-1` + `R-Refl-RT-1`; Phase 2g §2g.3 + `R-Refl-DC-1`; §10d/§10e + Phase 2.5.1 + Phase 6; read list; v0.8.0 YAML + title + rules                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                                                                                                                | **CLOSED** 2026-04-22 (r16 session)                                                           |
| **P2.6**        | `agents/planner.md` — Phase 0.6 F6 §7a.2 (`check_profile` / `structural_delta_flag` / `parallel_dispatch` / `threshold_version`) + P-16 budget surfacing + `/ph3-terminate` + F5 `menu_items_presented` under P-8; Phase 5.5 `pre_mcr_deep_pass_completed` flip on `deep`; Phase 6 MCR β-P-9a list; sixteen-field read/output + grounding trail                                                                                                                                                                                                                                                                                                                                                                        | `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                                                                                                                | **CLOSED** 2026-04-22 (r17 session)                                                           |
| **P2.7**        | `references/AGENT_CONTRACTS.md` — **I-Eval-8** (P-13 F6 `parallel_dispatch` + multiplier); **I-Planner-11** (P-16 + MCR β-P-9a hook); **I-Planner-10** F6 quartet; **I-Planner-9** scalar + menu; **I-Planner-5** `phase_state.json` path; §6 table + §4.5 pointer                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                                                                                                                | **CLOSED** 2026-04-22 (r18 session)                                                           |
| **P2.8**        | A-GK-4 / D-6 — §3.P-9.1 eleven-row falsification disposition column under user review; normative copy in `proposals/v0.7.5_phase3_refinement_loop_proposal.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Table populated; D-6 recorded in this document's frontmatter `user_decisions_closed:`; `scripts/skill-check.py` (0 blockers); WSL `bash scripts/release-gate.sh` (unchanged 1 blocker)                                         | **CLOSED** 2026-04-22 (r19 session)                                                           |
| **P3.1 (α-C1)** | `agents/evaluator.md` — phase-conditioned pointer table + gate-set §7 pointer + `What you read` fix; Ph-native adaptation of draft diff (not raw `patch`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `scripts/skill-check.py` (0 blockers); **open:** `audit-package-economics`; SAFEGUARD Check 1 post-demotion; paired review                                                                                                     | **IN PROGRESS** 2026-04-22 (r20 session — pointer land only)                                  |


**P2.1a closure (r8 amendment, 2026-04-22, dedicated post-r7 session).** P2.1a closed under the user's "continue cadence" pace-selection after r7 filing. Scope applied:

- `references/phase_state_schema.md` §§2, 2.1, 6.1, 8 amended in place under the clean-rewrite-policy's amendment lane (additive edit, does not re-open the r7 P0.1a rewrite's scope). §2 header: "fifteen fields" → "sixteen fields". §2 JSON block: `pre_mcr_deep_pass_completed: false` inserted between `ph3_last_activity_at` and `phase_entry_log`. §2.1: new field-semantics paragraph for `pre_mcr_deep_pass_completed` documenting the β-P-9a safety-net role per `proposals/v0.7.5_phase3_refinement_loop_proposal.md §3.P-9a`, default `false`, writer (Planner at Phase 5.5 on a Ph3 iteration closed with `check_profile: deep`), MCR admission refusal under `E-MCR-PRE-DEEP-PASS-REQUIRED`, ledger interaction under `ceiling_raised`/`fingerprint_reset`/`retraction`, and tolerant-absence semantics. §6.1 table: `SECTION_BAD_PRE_MCR_DEEP_PASS_TYPE` (MINOR) added. §6.1 "Contracts enforced elsewhere" paragraph: `E-MCR-PRE-DEEP-PASS-REQUIRED` enumerated with provenance. §8 seed template: both example sections carry `"pre_mcr_deep_pass_completed": false`. The v0.6.0 predecessor provenance note in the file's header block extended with the v0.8.0 15 → 16 widening citation. Footer `Last updated:` line bumped.
- `scripts/phase_state_validate.py` extended: new conditional bool-type check in `_validate_section` firing `SECTION_BAD_PRE_MCR_DEEP_PASS_TYPE` (MINOR) on non-boolean values; absent-tolerance preserved so ledgers without the field remain shape-valid (the MCR admission gate at `pre_phase_advance_check.py` clause (f) will treat absent-as-false at P2.6). Module-docstring validation-surface list extended with the new item; `MONOTONICITY_EXEMPTIONS` and all other existing checks unchanged.
- `scripts/fixtures/phase_state_smoketest/{pass,block}/reviews/phase_state.json` extended with `"pre_mcr_deep_pass_completed": false` on each section. Fixture-README bumped: PASS fixture bullet flips "fifteen fields" to "sixteen fields" with the new field explicitly enumerated and its default rationale; BLOCK fixture shell description updated to "v0.7.4+P2.1a shell"; directory-rename provenance paragraph extended with a v0.8.0 P2.1a note.

**Verification evidence (grounded).** Direct validator runs against the extended fixtures: `PASS-fixture exit=0` (zero findings), `BLOCK-fixture exit=4` with sole finding `[BLOCKER] MONOTONICITY_VIOLATION @ sections['1. Introduction'].phase_entry_log[1]` (the pre-existing monotonicity violation, not a regression). Ad-hoc sanity probe against the new check: a PASS fixture with `pre_mcr_deep_pass_completed` mutated to the string `"oops-string-not-bool"` returned `exit=3` with sole finding `[MINOR] SECTION_BAD_PRE_MCR_DEEP_PASS_TYPE @ sections['1. Introduction'].pre_mcr_deep_pass_completed: pre_mcr_deep_pass_completed must be a boolean (got str)` — confirms the new code path is wired and the severity map is correct. Ad-hoc absence probe: a PASS fixture with the field deleted returned `exit=0` — confirms tolerant-absence. Full `scripts/release-gate.sh` run reports `[OK] phase_state_validate.py PASS fixture returned exit 0` and `[OK] phase_state_validate.py BLOCK fixture returned exit 4 (MONOTONICITY_VIOLATION BLOCKER)`; final verdict remains at 1 blocker, 8 warnings (same as post-r7; the sole blocker is `scripts/version-check.py` reporting README latest-version `0.7.4` and CHANGELOG top-version `0.7.4` vs manifest `0.8.0-wip0`, which is explicit Phase 5.2 scope). No new BLOCKER or MAJOR introduced by P2.1a.

**Residuals surfaced during closure (in-scope for later P2.1 sub-phases, flagged for continuity).** (i) **Executable MCR admission gate `E-MCR-PRE-DEEP-PASS-REQUIRED` (substrate-deferred).** The normative home remains `scripts/pre_phase_advance_check.py` clause (f) per `phase_state_schema.md` / `PHASE_PROTOCOL.md` *(later hygiene: a byte-identical `pre_tier_advance_check.py` duplicate was removed from the harness tree; only the phase-named path remains.)* **P2.6 (r17) landed the Planner narrative:** `agents/planner.md` Phase 6 step 4 lists Ph4-ceiling sections with `pre_mcr_deep_pass_completed != true` and prescribes the `deep` F6 remediation path. Schedule executable clause (f) extension with Phase 5 packaging / script rename when the phase-named guardrail replaces the legacy shim. (ii) **Discharged at P2.6 (r17):** the writer-side flip `pre_mcr_deep_pass_completed: false → true` on a Ph3 iteration closed under an approved F6 with `check_profile: deep` is authored in `agents/planner.md` Phase 5.5 (Ph3 approve bullet). (iii) **Discharged at P2.8 (r19):** §3.P-9.1 dispositions bind (rows 1–10 **retained in Ph4-deep**; row 11 **retained in Ph4-deep with confessed coverage gap + v0.7.6 lightweight-proxy milestone logged** per user directive **Default continue**); the Planner's `deep` scheduling prose in `agents/planner.md` must stay consistent with that partition — no Ph3-refine profile widening from this closure.

**P2.1a.1 closure (r9 amendment, 2026-04-22, dedicated post-r8 session).** P2.1a.1 closed under the same "continue cadence" pace-selection that carried P2.1a, after a P2.1b pre-flight grounding audit surfaced a pre-existing v0.7.4 substrate inconsistency (R-P2-VALIDATOR-F6 — see §9.3 for discovery provenance). The remedy nested a dedicated reconciliation sub-phase inside P2.1 before P2.1b's additive field-adds touched the same surface, pattern-matched to r7's handling of R-P0-SCHEMA-3 via P0.1a. Scope applied:

- `scripts/artefact_frontmatter_validate.py` `FAMILY_SCHEMAS` gained a `planner_dispatch_plan` entry with `strict: True` (matching `ARTEFACT_FRONTMATTER_SCHEMA.md §8` rule 3), six required fields per §7a.1 (`round_id: str`, `sections_in_scope: list`, `dispatched_agents` as `("list", {agent: enum VALID_ACTORS, phase: enum VALID_PHASES, model_allocation: enum VALID_MODELS, scope: enum VALID_DISPATCH_SCOPES, purpose: str})`, `checks_scheduled: list`, `user_approval_required: bool`), and six optional fields per §7a.2 (`subagent_envelope` as list-of-objects with `verdict_authoritative_as_read: bool`, `stability_sub_mode_anticipated: bool`, `ceiling_lock_anticipated: bool`, `mcr_admission_anticipated: bool`, `notes: str`, `user_approval_signature` as nested dict with `approved_at: str`, `approved_by: str`, `modifications_recorded: bool`). A new module-level `VALID_DISPATCH_SCOPES = {"per_section", "manuscript_level", "cycle_level"}` constant was added alongside the existing `VALID_PHASES` / `VALID_MODELS` / `VALID_ACTORS` constants.
- `_check_nested` extended with a new branch handling `("list", element_schema)` tuples — the DSL's list-of-schemas shape was documented in the header comment at line 114 but had no implementation; v0.8.0 P2.1a.1 landed it as a type-dispatched recursive walk over list elements, which is the minimum machinery F6's `dispatched_agents` and `subagent_envelope` required. The existing type-leaf, enum-tuple, and dict-node branches are unchanged, so all F1/F2/F3/F4/F5 validation paths are preserved bit-for-bit.
- `_check_cross_field` extended with two F6-specific must-be-true rules: (a) `user_approval_required == true` (§8 rule 7; R-Refl-FM-2 MAJOR on violation with the schema-doc citation embedded in the finding message), (b) `subagent_envelope[].verdict_authoritative_as_read == true` under I-SubAgent-1 (R-Refl-FM-2 MAJOR per element on violation). Both rules are enforced here rather than via `("enum", {True})` literals in `FAMILY_SCHEMAS` because YAML parses `1` as int and Python's `1 == True` would spuriously pass a `{True}`-set via `in`-membership; the schema-dict approach keeps the bool-type check clean and delegates the must-be-true semantic to cross-field.
- Module docstring extended with a P2.1a.1 paragraph framing the R-P2-VALIDATOR-F6 reconciliation, an explicit enumeration of the in-file F6 checks now enforced (required/optional fields, types, strict-family unknown-field rejection, the two must-be-true rules), and an explicit note that the three cross-artefact DP finding classes (`R-Refl-DP-1` plan-drift, `R-Refl-DP-2` missing-plan, `R-Refl-DP-3` unsigned-plan) require joining F6 against `reviews/phase_state.json` + sibling artefacts and are therefore out of scope for this single-file validator; they were scheduled against P2.8 as the scope-freeze authoring locus; the §3.P-9.1 table is populated at r19, but **machine enforcement** of R-Refl-DP-* remains deferred (cross-artefact check not yet in `release-gate.sh`). The "Finding classes" list in the docstring gained an `F6-specific cross-artefact finding classes (scheduled, not enforced here)` sub-list to record the deferral surface.
- `scripts/fixtures/artefact_frontmatter_smoketest/` created fresh (no prior fixture harness existed for `artefact_frontmatter_validate.py`). Layout mirrors `phase_state_smoketest/`: `pass/reviews/dispatch_plan_C0001.md` is a well-formed F6 artefact exercising every §7a.1 required field plus `subagent_envelope`, `stability_sub_mode_anticipated: false`, `ceiling_lock_anticipated: false`, `mcr_admission_anticipated: false`, `notes`, and a populated `user_approval_signature`; `block/reviews/dispatch_plan_C0002.md` carries the minimum §7a.1 substrate with `user_approval_required: false` as the single intended malformation. The fixture README documents the single violation, the §8 rule 7 citation, the resulting R-Refl-FM-2 MAJOR finding, and the exit-3 expectation (no in-file F6 finding class is BLOCKER severity at this sub-phase; the BLOCK fixture surfaces exactly one MAJOR, so exit 3 is the correct expectation rather than the phase_state smoketest's exit 4), plus the rebuilding contract if a future sub-phase promotes an in-file F6 class to BLOCKER.
- `scripts/release-gate.sh` gained a Phase 0.68 block after the Phase 0.67 phase_state smoketest block and before the Phase 0.7 py_compile block. The Phase 0.68 block calls `artefact_frontmatter_validate.py` directly on the two fixtures (no shim, no `--project-root` flag — the artefact validator takes a file path as positional arg), asserts exit 0 on the PASS fixture and exit 3 on the BLOCK fixture, and reports `[BLOCKER] artefact_frontmatter_validate.py ... returned exit $RC (expected 0|3)` on divergence. The Phase 0.7 py_compile list was extended with `scripts/phase_state_validate.py` (a back-fill — it was missing from the pre-r9 list despite being the subject of Phase 0.67) and `scripts/artefact_frontmatter_validate.py` (new), so any future syntax regression in either validator trips the release gate even before the smoketest runs.

**Verification evidence (grounded).** Direct validator runs against the two F6 fixtures: `PASS-fixture exit=0` (zero findings); `BLOCK-fixture exit=3` with the sole finding `[MAJOR] R-Refl-FM-2  user_approval_required: user_approval_required must be true at v0.7.4 (see references/ARTEFACT_FRONTMATTER_SCHEMA.md §8 rule 7)`. Full `scripts/release-gate.sh` run reports `[OK] artefact_frontmatter_validate.py PASS fixture (F6) returned exit 0` and `[OK] artefact_frontmatter_validate.py BLOCK fixture (F6) returned exit 3 (R-Refl-FM-2 MAJOR, user_approval_required)` in the Phase 0.68 block, and `[OK] phase_state_validate.py` + `[OK] artefact_frontmatter_validate.py` in the Phase 0.7 py_compile block. Final verdict remains at 1 blocker, 8 warnings (same as post-r8; the sole blocker is `scripts/version-check.py` reporting README latest-version `0.7.4` and CHANGELOG top-version `0.7.4` vs manifest `0.8.0-wip0`, which is explicit Phase 5.2 scope). No new BLOCKER or MAJOR introduced by P2.1a.1.

**Residuals surfaced during P2.1a.1 closure (out-of-scope, flagged for continuity).** (i) **R-Refl-DP-1 / R-Refl-DP-2 / R-Refl-DP-3 authoring.** The three F6 cross-artefact finding classes are documented in `ARTEFACT_FRONTMATTER_SCHEMA.md §8 rule 7` and are out of scope for the single-file artefact-frontmatter validator. Their natural home is a cross-artefact consistency check that reads F6 + `reviews/phase_state.json` + sibling `reviews/*.md` artefacts together. P2.8 (r19) closed the falsification-table disposition column; R-Refl-DP-* **machine enforcement** remains the scheduled next home — an interstitial Phase-2 hygiene sub-phase or Phase 5 cross-artefact gate if scope mismatches. (ii) **String-length contract checks.** `ARTEFACT_FRONTMATTER_SCHEMA.md §7a.1` specifies `purpose ≤ 140 chars` per `dispatched_agents` entry and `§7a.2` specifies `notes ≤ 500 chars`; the validator does not enforce any string-length contracts on any family today. Not an F6-specific gap — also unchecked for F1–F5 — but schedule a length-contract-pass for a later P2.x or Phase 5 hygiene sub-phase rather than surface it inline. (iii) **Fixture coverage for F1/F2/F3/F4/F5.** *Partially closed at P2.1b (r10):* the harness now carries dedicated F1 and F4 PASS/BLOCK files plus three F6 files (F6 P2.1a.1 pair + F6 P2.1b bad-`check_profile` BLOCK). F2, F3, and F5 still have no dedicated smoketest fixtures — scheduled as optional hardening, not a P2.1b gate.

**P2.1b closure (r10 amendment, 2026-04-22, post-r9 session).** P2.1b closed on the "continue, default" session directive. Scope applied:

- `references/ARTEFACT_FRONTMATTER_SCHEMA.md` — F1 §3.1/3.3: required `adversarial_register` + `routing_rationale` with Ph3 register semantics and Action A-RT-1 routing string; F4 §6.2/6.3: optional `demoted_check_advisories` append-only list with five keys per row; F6 §7a.2/7a.3: optional `check_profile` (`refine` / `structural` / `deep` per `proposals/v0.8.0_upgrade_architecture.md` §3.2, not the longer β `Ph3-*` tokens), `structural_delta_flag`, `parallel_dispatch`, `threshold_version` with documented absent-means-default semantics; §8: new rules 8 (F1 routing shape), 9 (F6 P2.1b enums), 10 (F4 demoted row shape and severity enum); footer `Last updated` bumped.
- `scripts/artefact_frontmatter_validate.py` — module docstring and enforcement surface for §8 rules 1–10; new constants; F1 `FAMILY_SCHEMAS` two new required fields; F4 one new optional `demoted_check_advisories` list-of-objects schema; F6 four new optional fields with enum constraints on `check_profile` and `threshold_version`; `ROUTING_RATIONALE_RE` and `_check_cross_field` checks on F1 `routing_rationale` and each F4 demoted row's `routing_rationale`.
- `references/templates/F1_evaluator_findings.md` — `schema_version: "1.1"` and the two new required field blocks with a worked `routing_rationale` example.
- `scripts/fixtures/artefact_frontmatter_smoketest/` — `pass/reviews/dispatch_plan_C0001.md` bumped to `schema_version: "1.1"` and four P2.1b F6 optionals; new F1/F4 PASS; three new BLOCKs (F6 bad `check_profile` uses `Ph3-refine` to assert short-token contract vs β naming); `README.md` rewritten for the seven-file layout and exit-3 convention.
- `scripts/release-gate.sh` — Phase 0.68: bash arrays of PASS and BLOCK paths, three PASS × exit 0 + four BLOCK × exit 3, header comment retitled to P2.1a.1 + P2.1b.

**Verification evidence (grounded).** Direct validator: all three PASS `exit=0`, all four BLOCK `exit=3` with single MAJOR `R-Refl-FM-2` per file on the intended field. Full `release-gate.sh`: Phase 0.68 reports seven `[OK]` lines; final verdict 1 blocker, 8 warnings (unchanged — `version-check.py` only). No new release-gate BLOCKER.

**P2.1b residual (out-of-scope).** F2, F3, F5: no smoketest files yet; optional follow-up, not blocking P2.6 entry.

**P2.1c closure (r11 amendment, 2026-04-22, post-r10 session).** P2.1c closed on the "continue, default" session directive without co-landing P2.1d. Scope applied:

- `scripts/paragraph_hash_map.py` — CLI `--project-root` (required), `--manuscript` default `manuscript/main.md` relative to root; reads UTF-8 manuscript; emits one JSON object per §3.2 P2.1 / §P-10 intent: `paragraph_hash_map` (`p-0000` … stable document order), `paragraph_count`, `manuscript_sha256` (raw bytes), `manuscript_path` (posix path relative to project root); canonicalization and paragraph-boundary rules are documented in the module header (blank-line split after CRLF→LF; per-line `rstrip` before paragraph digest).
- `scripts/fixtures/paragraph_hash_map_smoketest/` — `minimal_project/manuscript/main.md` three-paragraph corpus; `README.md` documents the release-gate contract.
- `scripts/release-gate.sh` — new Phase 0.69 after Phase 0.68: two `python3` invocations with `cmp -s` on stdout captures (byte-identical determinism); Phase 0.7 `py_compile` list extended with `paragraph_hash_map.py`.

**Verification evidence (grounded).** Two consecutive runs against `minimal_project` produce identical stdout (JSON keys sorted; paragraph digests stable). Full `scripts/release-gate.sh` on wip0 via WSL `bash` reports `[OK] paragraph_hash_map.py: two runs byte-identical (exit 0)` in Phase 0.69 and `[OK] paragraph_hash_map.py` in the Phase 0.7 `py_compile` block; final verdict remains at **1 blocker** on the Phase 5.2 `version-check.py` axis — unchanged from r10. No `convergence_journal.jsonl` schema field was added in this sub-phase; that surface waits on P2.1d / P2.2 per audit separation.

**P2.1c residual (out-of-scope, partially narrowed at P2.2).** `PHASE_PROTOCOL.md` §3.3.4 now documents the optional `paragraph_hash_map` journal key and downstream intent. **Still open:** Planner Phase 0.6 invocation wiring for `paragraph_hash_map.py`, automated drift checks against prose iterations, and any JSON-schema validator extension if a machine-readable journal schema is introduced — schedule with P-10 / P2.6 per audit separation, not blocking P2.6 entry (P2.5 closed without co-landing P2.6).

**P2.1d closure (r12 amendment, 2026-04-22, post-r11 session).** P2.1d closed on the user's "continue" directive without co-landing P2.2. Scope applied:

- `scripts/migrate_convergence_journal_v075.py` — reads `reviews/convergence_journal.jsonl` (override via `--journal`); journal-only reconstruction: scalar or null `convergence_metric` becomes an object with `_v080_p12: journal-scalar-backfill-1`, preserved `legacy_scalar`, `grounding_clean` always `[v0.8.0-BACKFILL-N/A]`, `check8_aggregate_ok` from `check8_aggregate`, `findings_count_delta` as sequential `new_findings_count` difference when both sides are int (else sentinel), `line_delta` from `delta_lines` when int (else sentinel); idempotent on objects already carrying the tag; non–dry-run writes atomically via `.tmp` rename and emits `reviews/migration_report_convergence_journal_v075.md`.
- `scripts/fixtures/convergence_journal_smoketest/minimal_project/reviews/convergence_journal.jsonl` — two scalar-metric rows (P-4 field set); `README.md` documents Phase 0.695 expectations.
- `scripts/release-gate.sh` — Phase 0.695: two `--dry-run` stdout captures `cmp` on the read-only fixture; temp-directory copy, two non–dry-run passes, SHA-256 of `convergence_journal.jsonl` identical after first and second pass; Phase 0.7 `py_compile` includes `migrate_convergence_journal_v075.py`.

**Verification evidence (grounded).** WSL `bash scripts/release-gate.sh` on wip0 reports `[OK] migrate_convergence_journal_v075.py: two dry-runs byte-identical`, `[OK] ... second write idempotent (SHA-256 stable)`, and `[OK] migrate_convergence_journal_v075.py` in the `py_compile` block; final verdict remains **1 blocker** (`version-check.py` / Phase 5.2).

**P2.1d residual (out-of-scope).** F1/F2 artefact crawl for true `grounding_clean` and Check-8-excluded findings deltas; tightening `convergence_metric` object schema to the final §3.3.2 field names now that `PHASE_PROTOCOL.md` §3.3.1a is landed — still open for a later P2.x / Phase 5 hygiene pass.

**P2.2 closure (r13 amendment, 2026-04-22, post-r12 session).** P2.2 closed on the user's "continue" directive without co-landing P2.3. Scope applied on wip0 at `unpacked/research-writing-harness-claude-v0.8.0/references/PHASE_PROTOCOL.md`:

- Document header and grounding basis aligned to v0.8.0 wip0 substrate; v0.8.0 β Ph3 supersession clause added after the v0.7.4 clause (§§3.3.0, 3.3.1a, 3.3.4 paragraph-hash intent, §3.4 pre-MCR gate).
- New **§3.3.0** — three `check_profile` values (`refine` / `structural` / `deep`); `halo_scope` enum; normative per-check `halo_scope` matrix **import-by-reference** to `references/DETERMINISTIC_CHECKS.md` + `references/SAFEGUARD_LAYER.md` (those files were not amended in this session — **P2.2 residual**, schedule a dedicated doc pass or fold into a later Phase 2 touch when those references are next amended; P2.4 evaluator prose documents interim `containing_section` fallback only).
- New **§3.3.1a** — P-12 four-component `convergence_metric` object, three-round `[CONVERGENCE-STABLE]` window for objects vs two-round for legacy scalars, provisional thresholds, legacy vs migrated semantics, `[v0.8.0-BACKFILL-N/A]` note; §3.3.1 closing paragraph aligned (Option B, sixteen fields, no persisted stale boolean).
- Ph3 table (§3.3) — convergence metric / warning-threshold rows updated for object vs scalar.
- **§3.3.4** — `convergence_metric` typed `float | null | object`; optional `paragraph_hash_map`; nine core keys + optional v0.8.0 keys; downstream consumers (P-10 / `paragraph_hash_map.py`); dual `[CONVERGENCE-STABLE]` rule; writer contract on unknown keys.
- **§3.4** — pre-MCR Ph3-deep pass, `pre_mcr_deep_pass_completed: true`, `E-MCR-PRE-DEEP-PASS-REQUIRED`, enforcement cross-refs to `phase_state_schema.md` and `pre_phase_advance_check.py` clause (f) (see later hygiene: legacy duplicate filename removed).
- **§3.3.6** — tension / distance-to-ceiling reads `legacy_scalar` when `convergence_metric` is an object.
- **§6.x** — `[Ph3-STALE]` Option A rationale: fifteen → sixteen fields.
- **§13** — success metric (2): `SectionStateObject` field-count bullet updated to sixteen at v0.8.0 (fifteen at v0.7.0–v0.7.4) with `phase_state_schema.md` §2 cross-ref.

**Verification evidence (grounded).** Doc-only sub-phase: no new `release-gate.sh` phase. Post–P2.2 consistency edit to `PHASE_PROTOCOL.md` §13 success-metric (sixteen-field invariant) is markdown-only. WSL `bash scripts/release-gate.sh` on wip0 after that edit reports `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)` with the sole release-gate blocker axis unchanged (`scripts/version-check.py`: README `0.7.4` + CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope); Phase 0.67 / 0.68 / 0.69 / 0.695 and Phase 0.7 `py_compile` paths all `[OK]`.

**P2.2 residual (out-of-scope).** Author the normative `halo_scope` ↔ check matrix inside `references/DETERMINISTIC_CHECKS.md` and `references/SAFEGUARD_LAYER.md` so §3.3.0's import-by-reference resolves to pinned tables — schedule the next touch of those files or a micro-sub-phase.

**P2.3 closure (r14 amendment, 2026-04-22, post-r13 session).** P2.3 closed on the user's "continue" directive without co-landing P2.4. Scope applied on wip0:

- `skills/run-phase-3/SKILL.md` — frontmatter `version: 0.8.0`; description + grounding basis for §3.3.0 / §3.3.1a / §3.3.4 / sixteen-field ledger; **§3.1–3.2** P-12 + three-round vs legacy two-round `[CONVERGENCE-STABLE]`; new **§4.5** `check_profile` dispatch table (`refine` / `structural` / `deep`); **§5** dispatch sequence renumbered with Phase 0.6 F6 read, branch on profile, F6-default `deep` when `check_profile` absent; journal + `pre_mcr_deep_pass_completed` on closed `deep` iteration per `phase_state_schema.md` §2.1; agent table + §1 intro updated.
- `skills/run-phase-3-stability/SKILL.md` — `version: 0.8.0`; grounding + §1 / §3 / footer: `stability_sub_mode_anticipated` overrides §4.5 for the reduced pass; trigger 30 hand-off typically chains `check_profile: deep`; stability does not flip `pre_mcr_deep_pass_completed`.
- `skills/run-phase-4/SKILL.md` — `version: 0.8.0`; MCR §2.1 **item 4** (`pre_mcr_deep_pass_completed`), climb-plan bullet, Phase 0 preflight triad, §4 Ph4 Evaluator cites `run-phase-3` §5 steps 4–12 deep envelope, §10 `E-MCR-PRE-DEEP-PASS-REQUIRED` escalation; MCR table column.

**Verification evidence (grounded).** `python3 scripts/skill-check.py` on wip0: **0 blockers, 0 warnings**. WSL `bash scripts/release-gate.sh`: `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)`; sole blocker axis unchanged (`version-check.py` README/CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`); Phase 0.67–0.695 and `py_compile` all `[OK]`.

**P2.3 residual (narrowed at P2.6 / r17).** §9.2 P2.1a residual (ii) **discharged** — `agents/planner.md` Phase 5.5 now authors the `pre_mcr_deep_pass_completed` flip when the approved F6 carried `check_profile: deep`. Optional: shorten run-phase SKILL `description:` YAML strings below 500 chars (release-gate WARN only — not a BLOCKER).

**P2.4 closure (r15 amendment, 2026-04-22, post-r14 session).** P2.4 closed on the user's "continue" directive without co-landing P2.5. Scope applied on wip0 at `unpacked/research-writing-harness-claude-v0.8.0/agents/evaluator.md`:

- YAML `description:` and title aligned to v0.8.0 β Ph3/Ph4 framing (F6 `check_profile`, `adversarial_register`, `parallel_dispatch`, P-10 `halo_scope`, P-15 `demoted_check_advisories`; cross-ref `skills/run-phase-3/SKILL.md` §4.5).
- New **## Ph3 / Ph4 v0.8.0 — β envelope extensions** block: §3.0 P-13 (A-OT-1 wall-clock target, A-OT-2 `parallel_dispatch_cost_multiplier`, A-OT-3 sequential-only when `parallel_dispatch: false`, >3.5× consecutive-round MINOR advisory); P-9 adversarial register (refinement vs certification; no mid-round self-toggle; cross-ref Reflector `**R-Refl-RG-1`** / `**R-Refl-RT-1`** / §2g.3 `**R-Refl-DC-1**` — **P2.5 / r16**); P-10 diff-scope / `halo_scope` / `paragraph_hash_map` / `deep` envelope per `run-phase-3` §4.5; `**containing_section` fallback** when a check id has no pinned matrix row yet (**P2.2 residual** unchanged); P-15 F4 `demoted_check_advisories` + `routing_rationale: "primary_evidence=<check_id>"` when demoted-class signal is incidental under a *different* Ph3-primary check (F1 when the scheduled primary check owns the finding).
- Ph3 procedure pass retitled / cross-ref'd to F6 `check_profile` vs narrowed schedules; convergence metric bullet for P-12 object vs legacy scalar and three-round `[CONVERGENCE-STABLE]` vs two-round legacy; Step 8 consolidated report cites phase, P-12 / `convergence_metric`, `adversarial_register`, `routing_rationale`, `parallel_dispatch_cost_multiplier` when parallel ran; Evaluator-specific rules sub-pass bullet for P-13; **What you read** lists sixteen-field `SectionStateObject` (`pre_mcr_deep_pass_completed`).

**Verification evidence (grounded).** WSL `bash scripts/release-gate.sh` on wip0: `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)`; sole blocker axis unchanged (`scripts/version-check.py`: README `0.7.4` + CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope); Phase 0.67–0.695 and `py_compile` all `[OK]`. `python3 scripts/skill-check.py` on wip0: **0 blockers, 0 warnings**.

**P2.4 residual (superseded at P2.5 / r16).** The deferral note for Reflector-side `R-Refl-RG-1` / §2g.3 / `R-Refl-RT-1` is **discharged** — see **P2.5 closure** below. **P2.2 residual** (normative per-check `halo_scope` matrix in `references/DETERMINISTIC_CHECKS.md` / `references/SAFEGUARD_LAYER.md`) unchanged — Evaluator + Reflector prose still rely on interim `containing_section` fallback until those files are amended.

**P2.5 closure (r16 amendment, 2026-04-22, post-r15 session).** P2.5 closed on the user's "continue" directive without co-landing P2.6. Scope applied on wip0 at `unpacked/research-writing-harness-claude-v0.8.0/agents/reflector.md`:

- YAML `description:` + H1 title aligned to v0.8.0 β framing; lightweight scope explicitly includes §6.10 when v0.8.0-shaped F1 artefacts exist; full-mode Phase 2g includes §2g.3.
- **Phase 2f §6.10** — `R-Refl-RG-1 register_mismatch` (MAJOR): F1 `adversarial_register` vs findings-body rhetorical frame + F6 `checks_scheduled` / `check_profile` coherence; `R-Refl-RT-1 routing_ambiguity` (MAJOR): parsed `primary_evidence=<check_id>` vs F6 `checks_scheduled[]` after `cycle_id` resolution (F5 `dispatch_plan_reference` / ledger / filename); F4 demoted row P-15 incidental-path contradiction. Orthogonal to `R-Refl-FM-2` syntactic validation.
- **Phase 2g §2g.3** — aggregate F4 `demoted_check_advisories` across `reviews/`; `**R-Refl-DC-1 demoted_check_recurrence` (MAJOR)** when same `check_id` ≥3 rows across ≥2 distinct `source_iteration`; §10e part (f); hygiene when block absent.
- **§10d / §10d template**, **Phase 2.5.1** severity-floor row for §6.10, **Phase 6** presentation bullet, **What you read** (`ARTEFACT_FRONTMATTER_SCHEMA`, F1/F6 paths, F4 demoted sources), **Reflector-specific rules** v0.8.0 triad (RG-1 vs RT-1 vs DC-1), `***Last updated***` footer.

**Verification evidence (grounded).** WSL `bash scripts/release-gate.sh` on wip0: `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)`; sole blocker axis unchanged (`scripts/version-check.py`: README `0.7.4` + CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope); Phase 0.67–0.695 and `py_compile` all `[OK]`. `python3 scripts/skill-check.py` on wip0: **0 blockers, 0 warnings**.

**P2.5 residual (out-of-scope).** Optional: extend `ARTEFACT_FRONTMATTER_SCHEMA.md` / `AGENT_CONTRACTS.md` with explicit prose for `R-Refl-DC-1` if a future hygiene pass wants validator-visible enumeration — not required for P2.5 prompt closure.

**P2.6 closure (r17 amendment, 2026-04-22, post-r16 session).** P2.6 closed on the user's "continue" directive without co-landing P2.7. Scope applied on wip0 at `unpacked/research-writing-harness-claude-v0.8.0/agents/planner.md`:

- YAML `description:` + H1 title aligned to v0.8.0; **Output Contract** + **What you read** + **What you write** bullets: sixteen-field `SectionStateObject`; `ARTEFACT_FRONTMATTER_SCHEMA.md §7a` read for F6 authoring.
- **Phase 0.6** — renumbered steps 6–10; new **step 5** authors F6 §7a.2 quartet (`check_profile`, `structural_delta_flag`, `parallel_dispatch`, `threshold_version`) with `run-phase-3` §4.5 / P2.8 disposition hook; new **step 7** (**P-16**): iteration telemetry, §8.4 five-rejection soft cap, §9.7 MCR reserve, `/ph3-terminate` + Option C contrast under `ceiling_lock_anticipated` / P-8 tension; **§3.3.6** `distance_to_ceiling` uses line-diff scalar per `PHASE_PROTOCOL.md`.
- **Phase 5.5** — `[CONVERGENCE-STABLE]` dual-window (scalar vs object); Ph3 approve: `pre_mcr_deep_pass_completed` writer flip; P-8 branch: F5 `menu_items_presented`, P-16 budget prose, `/ph3-terminate` when stable+tension overlap.
- **Phase 6** — MCR presentation lists β-P-9a blocked sections (`pre_mcr_deep_pass_completed != true` at Ph4 ceiling) with remediation (`deep` F6 + one Ph3 cycle).
- `***Last updated***` + grounding trail bump.

**Verification evidence (grounded).** `python scripts/skill-check.py` from `unpacked/research-writing-harness-claude-v0.8.0` on Windows: **0 blockers, 0 warnings**. WSL `bash scripts/release-gate.sh` on the same tree: `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)`; sole blocker axis unchanged (`scripts/version-check.py`: README `0.7.4` + CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope); Phase 0.67–0.695 and `py_compile` all `[OK]`.

**P2.6 residual (out-of-scope).** Executable `E-MCR-PRE-DEEP-PASS-REQUIRED` in `scripts/pre_phase_advance_check.py` clause (f) — *Superseding note: canonical tree now ships `pre_phase_advance_check.py` only;* see amended §9.2 P2.1a residual (i) paragraph. P2.1c **paragraph_hash_map** Phase 0.6 automation remains deferred.

**P2.7 closure (r18 amendment, 2026-04-22, post-r17 session).** P2.7 closed on the user's "continue default" directive without co-landing P2.8. Scope applied on wip0 at `unpacked/research-writing-harness-claude-v0.8.0/references/AGENT_CONTRACTS.md`:

- **§1 Planner — I-Planner-5:** `tier_state.json` → `reviews/phase_state.json` `phase_entry_log[].notes` for model dispatch / override audit paths.
- **§1 Planner — I-Planner-10:** F6 §7a.2 profile quartet when any `dispatched_agents[]` row is at `phase: Ph3`.
- **§1 Planner — I-Planner-9:** line-diff scalar (`legacy_scalar` when object-shaped) for tension + `distance_to_ceiling`; F5 `menu_items_presented` + `/ph3-terminate` cross-ref.
- **§1 Planner — I-Planner-11 (new):** P-16 ceiling-aware iteration budget surfacing + Phase 6 MCR `pre_mcr_deep_pass_completed` narrative; hook to `agents/planner.md` P2.6 and `pre_phase_advance_check.py` clause (f) deferral.
- **§2 Evaluator — I-Eval-8 (new):** P-13 — read approved F6 `parallel_dispatch`; sequential-only when `false`; parallel + `parallel_dispatch_cost_multiplier` when `true` and independent; no override without superseding F6.
- **§4.5:** shared-invariants pointer adds I-Eval-8 beside I-Eval-7.
- **§6 contract-verification table:** `current_phase` + `reviews/phase_state.json` paths on model rows; I-Planner-9 row — line-diff scalar; two P-13 rows (parallel violation; multiplier omission).

**Verification evidence (grounded).** `python scripts/skill-check.py` from wip0 on Windows: **0 blockers, 0 warnings**. WSL `bash scripts/release-gate.sh`: `VERDICT: BLOCKED — 1 blocker(s), 8 warning(s)`; sole blocker axis unchanged (`scripts/version-check.py` README/CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope).

**P2.7 residual (out-of-scope).** Optional prose extension for `R-Refl-DC-1` in this file — same note as P2.5 residual; not required for P2.7 closure.

**P2.8 closure (r19 amendment, 2026-04-22, post-r18 session).** P2.8 closed on the user's **Default continue** directive (binding interpretation recorded in frontmatter **D-6**). Scope applied:

- `proposals/v0.7.5_phase3_refinement_loop_proposal.md` §3.P-9.1 — disposition column populated: rows **1–10** `**retained in Ph4-deep**`; row **11** `**retained in Ph4-deep with confessed coverage gap + v0.7.6 lightweight-proxy milestone logged**`; new **v0.8.0 P2.8 scope-freeze** paragraph under the table documenting directive semantics, milestone cross-refs (`v0.7.5_threshold_derivation.md` / `v0.8.0_threshold_derivation.md`), and that *promoted to Ph3-refine via lightweight proxy* was not selected for row 11.
- `proposals/v0.8.0_upgrade_architecture.md` — frontmatter: **D-6** → `user_decisions_closed:`; `open_decisions: []`; `status:` `**READY-FOR-PHASE-3-BUILD**`; `revision` / `supersedes` r19 chain; §9 intro + three moves repointed at **Phase 3** entry; §9.2 table column `Status at r19` + **P2.8** row; §9.2 P2.1a residual (iii) discharged note; P2.1a.1 residual (i) split: table done vs R-Refl-DP-* enforcement still deferred.

**Verification evidence (grounded).** `python scripts/skill-check.py` from `unpacked/research-writing-harness-claude-v0.8.0` on Windows: **0 blockers, 0 warnings**. WSL `bash scripts/release-gate.sh` on the same tree: final verdict unchanged at **1 blocker** (`scripts/version-check.py` README/CHANGELOG `0.7.4` vs manifest `0.8.0-wip0`, Phase 5.2 scope).

**P2.8 residual (out-of-scope).** Row-11 **lightweight-proxy milestone** text is filed in the disposition string; consolidate into `proposals/v0.7.5_threshold_derivation.md` (or `v0.8.0_threshold_derivation.md` when authored per §3.2 Phase 4.3) on first edit of that artefact — not a P2.8 gate. R-Refl-DP-1/2/3 **executable** cross-artefact checks remain deferred per amended §9.2 P2.1a.1 residual (i).

**P3.1 partial land (r20 amendment, 2026-04-22, post-r19 session).** On the user's **Continue default** directive, the session landed the **α-C1 pointer demotion** on wip0 `agents/evaluator.md` as a **Ph-native adaptation** of `proposals/drafts/v0.7.6_hand_authored_route/evaluator.md.pointer-demotion.diff` (the unified diff was **not** applied with `patch -p1` — it targets Tier/`TIER_PROTOCOL` vocabulary incompatible with the post-β Evaluator). **Scope:** replace long phase-engagement prose with a **Phase-conditioned pointer table** + four binding call-outs; replace the duplicate gate markdown table with a one-line pointer to `**references/PHASE_PROTOCOL.md` §7**; fix `What you read` gate cross-ref from §4 → §7; fix Re-check Mode "same tier" → "same phase". **Verification (grounded this session):** `python scripts/skill-check.py` from wip0 — **0 blockers, 0 warnings**. **Open before P3.1 CLOSED:** draft README post-apply items 2–4 (economics probe; SAFEGUARD Check 1; paired review).

### 9.3 Phase 2 residuals identified during P2.1b pre-flight (2026-04-22)

During the post-r8 session's P2.1b pre-flight grounding audit — a read-sweep of `references/ARTEFACT_FRONTMATTER_SCHEMA.md` against `scripts/artefact_frontmatter_validate.py` before touching any F1/F6/F4 field-adds — one pre-existing v0.7.4 substrate inconsistency surfaced that was not enumerated by any Phase 2 move in §3.2. Classified here to preserve the Phase 2 entry-point provenance and record the in-session remedy.

1. **R-P2-VALIDATOR-F6 (F6 schema-doc ↔ validator dispatch-table inconsistency).** The F6 `planner_dispatch_plan` family is fully documented at `references/ARTEFACT_FRONTMATTER_SCHEMA.md §7a` — the §1 taxonomy table lists six families including F6 at row 6, §7a specifies F6's required (§7a.1) and optional (§7a.2) fields, §7a.3 documents absent-means-default semantics for the four boolean flags and the `user_approval_signature` block, §7a.4 records F6-not-inherited, and §8 rules 3 and 7 cite F6 explicitly (rule 3 enumerates F6 among the strict families; rule 7 binds `user_approval_required` to `true` at v0.7.4, documents R-Refl-DP-1/2/3, and routes R-Refl-DP-* to the dispatch-plan audit channel). At the validator, however, `scripts/artefact_frontmatter_validate.py`'s `FAMILY_SCHEMAS` dict contained entries only for F1 `evaluator_findings`, F2 `evaluator_deterministic`, F3 `reflector_lightweight_probe`, F4 `reflector_full_report`, and F5 `planner_consolidated_findings` — five families, not six. `validate_family` dispatches on `doc_type` via `FAMILY_SCHEMAS`; a `doc_type: planner_dispatch_plan` artefact hits the unknown-family branch and fires `R-Refl-FM-7` MAJOR ("unknown document_type; legal values: [...]") rather than being validated. The consequence: at v0.7.4 and at v0.7.4.1, every F6 artefact produced under proposal P-1's dispatch-plan-consent surface was unvalidated for required-field presence (§8 rule 1), type conformance (rule 2), strict-family unknown-field rejection (rule 3), enum conformance (rule 4), the `user_approval_required == true` consent rule (rule 7), and the I-SubAgent-1 `verdict_authoritative_as_read == true` rule (§7a.2) — the validator reported only that `document_type` was unknown and did not even attempt to check any of the F6-specific surface. No fixture harness existed for the validator either: `scripts/fixtures/` contained the `phase_state_smoketest/` directory and nothing else, so no F6 PASS/BLOCK cases had ever been exercised.
  **Shape parallel to R-P0-SCHEMA-3.** The inconsistency is the same shape as the r7 R-P0-SCHEMA-3: a schema-doc surface that is fully specified but not enforced by its corresponding validator, introduced at v0.7.4 because the P-1 landing record (per `ARTEFACT_FRONTMATTER_SCHEMA.md §11 "Last updated" line, 2026-04-21) extended §1 / §7a / §8 but did not extend the validator's` FAMILY_SCHEMAS`dispatch table in the same commit. The v0.7.4.1 punch-list audit did not catch this because the audit's focus was economic-efficiency, quality, and speed axes (per`unpacked/writing_harness_*_audit_v0.7.4.1.md`), not schema ↔ validator parity.  **Disposition (in-session, 2026-04-22).** The r7 precedent for R-P0-SCHEMA-3 was: defer at r6, close at r7 under a dedicated P0.1a sub-phase before any Phase 2 move. Pattern-matched at r9: close R-P2-VALIDATOR-F6 as a dedicated sub-phase nested inside P2.1 (label P2.1a.1, inheriting the P2.1a numeric prefix and a` .1`suffix to mark it as a substrate-reconciliation prerequisite for P2.1b rather than a peer of P2.1a through P2.1d). P2.1a.1 was authored in the same session that surfaced the discovery because the scope was substrate-reconciliation-only (no new fields beyond what §7a already documents, no new finding classes beyond what §8 already cites, no cross-artefact machinery), which satisfies the r6 per-phase-verification discipline and the "substrate-pure sub-phase should close on its own session" guidance from §9 move 2 — P2.1a.1 *is* its own session, a session nested inside the P2.1b entry-point session that never got to P2.1b. Full closure record at §9.2 P2.1a.1 block; verification evidence there confirms release-gate BLOCKER count unchanged at 1, so P2.1a.1 is non-regressive.  **Residuals of the discovery (out-of-scope, flagged).** (a) **Field-audit parity across families.** The R-P2-VALIDATOR-F6 shape — schema-doc surface fully specified but unenforced by the validator — is the kind of gap a cross-artefact parity audit would catch. Scheduled for a later Phase 5 hygiene pass: a script that reads`ARTEFACT_FRONTMATTER_SCHEMA.md §§3–7a`and`scripts/artefact_frontmatter_validate.py ``FAMILY_SCHEMAS`and asserts the families enumerated in the doc match the families in the dict. Not authored at r9; flagged here for continuity. (b)`**dispatch_plan_reference`referent-check.** F5`planner_consolidated_findings`'s optional` dispatch_plan_reference: str` field is not referent-checked against any F6 artefact today — the validator accepts any string. This is the R-Refl-DP-3 surface (unsigned-plan) in its P2.8 authoring form; it is both a cross-artefact concern and a direct consequence of F6's pre-r9 unvalidated state. Already flagged under §9.2 P2.1a.1 residual (i); noted here again so the §9.3 R-P2-VALIDATOR-F6 provenance paragraph carries its own cross-reference to the deferred cross-artefact machinery.

---

*Normative status.* Upgrade architecture document, revision r6 (same-day successor to r5, 2026-04-22). Not a binding change to any plugin surface. Composes the remaining α and β tracks plus a γ verify-and-cleanup step under a single composed release (v0.8.0) per user decisions D-1 through D-5 confirmed 2026-04-22, D-8 confirmed in r4, and D-7 confirmed in r6. r6 records three user-driven or verification-driven state transitions on top of the r5 substrate: (i) D-7 closed by user confirmation at v0.8.0, moving D-7 from `open_decisions:` to `user_decisions_closed:` in the frontmatter and annotating the §5 D-7 rationale paragraph with a closure-at-r6 note; (ii) P0.3 Classes C/D audit completed clean against `CHANGELOG.md §v0.7.4.1 Classes C and D` and `RELEASE_NOTES_v0.7.4.1.md §Scope` — zero matches for `legacy/marshal-f1-retired/` and `legacy/rule-digest-v060-retired/` in the wip0 README, with residual retired-skill name matches folded into Verification carve-out (c), captured as an `*Audit result (r6):`* sub-paragraph on the §3.2 P0.3 bullet; (iii) §9 next-session moves rewritten to reflect Phase 0 closure on wip0 and Phase 1.1 as the new entry point, with step 2 (previously "Surface D-7") retired and step 3 (previously "Begin Phase 0") replaced with "Begin Phase 1.1". The document's `status:` field flips from `DRAFT-FOR-REVIEW` to `READY-FOR-PHASE-1-BUILD` — a new lifecycle token minted per the observed per-document status-vocabulary convention (`PHASE-1-CLOSED`, `READY-FOR-NEXT-SESSION-INGEST`, `REVISION-BOUNDARY-CLOSED`, etc., observed across `proposals/*.md`), signalling that Phase 0 has closed on the canonical substrate and the document is build-approved for Phase 1+ execution. r6 touches: frontmatter (revision, status, user_decisions_closed, open_decisions, supersedes), §3.2 (P0.3 bullet), §5 intro + open-decisions table + D-7 rationale paragraph, §9 (moves 1–3 rewritten), and this footer. §§ 0, 1.3, 2.3, 3.1, 3.3 (gate item 9), 3.4, 6, 7, and 8 are all unchanged in r6. r6 introduces no new framing beyond r5; the build surface, the four-candidate-tree analysis, the layer hierarchy, the risk register, and the soundness gates all stand as r4/r5 recorded them. Does not revise any of the three source proposals; does not re-litigate the v0.7.4 P-1 through P-8 package.

*Related.* Revisions r1, r2, r3, r4, and r5 of this document are superseded. The three source proposals (`v0.7.6_calibrator_audit_optimization_proposal.md`, `v0.7.5_phase3_refinement_loop_proposal.md`, `v0.7.4_economic_efficiency_proposal.md` + its session handoff) remain the binding contract-surface references for their respective changes. The v0.7.4.1 release notes (`unpacked/research-writing-harness-claude-v0.7.4.1/RELEASE_NOTES_v0.7.4.1.md`) and the v0.7.4.1 calibrator handoff (`unpacked/plugin_calibrator_handoff_v0.7.4.1.md`) are documentary predecessors for the r4 γ-closure framing; the v0.7.4.1 CHANGELOG entry (`CHANGELOG.md §v0.7.4.1 Classes C and D`) is the documentary reference for the r6 P0.3 audit target list. The hand-authored draft route at `proposals/drafts/v0.7.6_hand_authored_route/` is the authoring substrate for Phase 1.1 and Phase 3.1. The § 8 external adjuncts (`alexgreensh/token-optimizer`, `rtk-ai/rtk`, `nadimtuhin/claude-token-optimizer`) are documentary only — they ship nothing into v0.8.0.

*Normative status.* Upgrade architecture document, revision r7 (same-day successor to r6, 2026-04-22). Not a binding change to any plugin surface. Records three post-r6 state transitions on top of the r6 substrate: (i) P0.1a substrate-cleanup closure — `references/phase_state_schema.md` rewritten to v0.7.4 shape under the clean-rewrite policy, fixture directory `scripts/fixtures/tier_state_smoketest/` renamed to `phase_state_smoketest/` with fixture files renamed to `phase_state.json` and content rewritten to v0.7.4 shape, `scripts/release-gate.sh` Phase 0.67 block rewritten to call `phase_state_validate.py` directly with exit-code expectations `{PASS: 0, BLOCK: 4}` matching the v0.7.4 validator's severity map, and `README.md` line 85 fixture-path reference updated in coordination; release-gate BLOCKER count on wip0 falls from 3 to 1 with the remaining blocker scoped to Phase 5.2 version-bumps; §9.1 item 3 amended in place with closure evidence and two out-of-scope residuals flagged for a later hygiene pass (migration-script `last_approved_tier` rename omission; `README.md` lines 77–78 v0.7.0 exit-code-map staleness); §9.1 tail sentence flipped from "R-P0-SCHEMA-3 is deferred" to the post-closure reading. (ii) D-9 deferral executed — annotated in `user_decisions_closed:` to record execution rather than introducing a new decision token. (iii) §9 next-session moves rewritten to reflect Phase 1 closure (P1.1 measured throughput `input_tokens_per_second: 16897.1`; P1.2 folded into P0.2; P1.3 chain-depth gate `max_chain_depth <= 15` wired into `scripts/release-gate.sh`) plus P0.1a closure, with Phase 2 (β core, P2.1 substrate-first entry) as the new entry point per user pace-selection "continue cadence" (option 1 of the pace-trade-off triage surfaced at the r7 session close). The document's `status:` field flips from `READY-FOR-PHASE-1-BUILD` to `READY-FOR-PHASE-2-BUILD` under the per-status-token convention established in the r6 footer. r7 touches: frontmatter (revision, status, supersedes, D-9 annotation), §9 intro + three moves (all three rewritten to point at Phase 2), §9.1 item 3 (closure-paragraph + verification-evidence + residuals added in place), §9.1 tail sentence, and this footer paragraph + the *Filed on:* metadata line. §§ 0, 1.2, 1.3, 2, 3.1–3.3, 4, 5, 6, 7, and 8 are all unchanged in r7. r7 introduces no new framing, no new decision token, no new risk, and no change to any of SG-1 through SG-4 or the four-agent contract; the build surface, the layer hierarchy, the authoring order, and the architecture's core composition commitments all stand as r6 recorded them. Does not revise any of the three source proposals; does not re-litigate any of the v0.7.4 P-1 through P-8 package, the v0.7.6 α track, or the v0.7.5 β track.

*Normative status.* Upgrade architecture document, revision r9 (same-day successor to r8, 2026-04-22). Not a binding change to any plugin surface. Records three post-r8 state transitions on top of the r8 substrate: (i) **P2.1a.1 substrate-reconciliation sub-phase closure** — a P2.1b pre-flight grounding audit surfaced R-P2-VALIDATOR-F6, a pre-existing v0.7.4 inconsistency where the F6 `planner_dispatch_plan` family documented at `references/ARTEFACT_FRONTMATTER_SCHEMA.md §7a` had no entry in `scripts/artefact_frontmatter_validate.py`'s `FAMILY_SCHEMAS` dispatch table, leaving §8 rules 3 and 7 unenforced for F6. Pattern-matched to r7's P0.1a closure of R-P0-SCHEMA-3: close the inconsistency as a dedicated nested sub-phase (label P2.1a.1, inheriting the P2.1a prefix and a `.1` suffix) before P2.1b's additive field-adds touch the same surface. The validator was extended with an F6 `FAMILY_SCHEMAS` entry carrying the six required fields per §7a.1 (including `dispatched_agents` as list-of-objects), the six optional fields per §7a.2, and two cross-field must-be-true rules in `_check_cross_field` (`user_approval_required == true` per §8 rule 7, `subagent_envelope[].verdict_authoritative_as_read == true` under I-SubAgent-1); `_check_nested` gained `("list", element_schema)` handling to support F6's list-of-objects shape (the DSL's list tuple was documented at line 114 but previously unimplemented); the module docstring gained a P2.1a.1 paragraph framing the reconciliation and an explicit deferral note for the three cross-artefact DP finding classes (R-Refl-DP-1 plan-drift, R-Refl-DP-2 missing-plan, R-Refl-DP-3 unsigned-plan) to P2.8; a new `scripts/fixtures/artefact_frontmatter_smoketest/` harness was created with F6 PASS and F6 BLOCK fixtures plus a README documenting the exit-3 expectation for the BLOCK case (R-Refl-FM-2 MAJOR on `user_approval_required: false`); `scripts/release-gate.sh` gained a Phase 0.68 block exercising the F6 fixtures and two new py_compile entries covering both validators. Release-gate BLOCKER count on wip0 unchanged at 1 (Phase 5.2 version-bump scope), confirming P2.1a.1 introduced no regression. (ii) **§9.2 sub-phase closure table extended** with a P2.1a.1 row marked CLOSED, between P2.1a and P2.1b; the P2.1a closure block is preserved bit-for-bit from r8 and a P2.1a.1 closure block was appended after it, paralleling r8's treatment of P2.1a. (iii) **§9.3 added** to carry the R-P2-VALIDATOR-F6 discovery provenance paragraph, structured in parallel to §9.1's treatment of R-P0-SCHEMA-3: discovery circumstances (P2.1b pre-flight grounding audit), shape (schema-doc fully specified but not enforced by its corresponding validator, same shape as R-P0-SCHEMA-3), pattern-matched disposition (close as a dedicated nested sub-phase, P2.1a.1 relative to P2.1 just as P0.1a was relative to P0.1), and residuals of the discovery (cross-family parity audit; F5 `dispatch_plan_reference` referent-check via P2.8). §9 next-session moves rewritten: move 1 extended with the r9 discovery pattern and a methodological note that Phase 2 sub-phase pre-flight audits under "continue cadence" may surface pre-existing inconsistencies of the r7 R-P0-SCHEMA-3 shape, with the remedy to nest a dedicated sub-phase for reconciliation (pattern P2.1.x / P0.1.x) before the additive work touches the same surface. The document's `status:` field is unchanged at `READY-FOR-PHASE-2-P2.1B` — P2.1b remains the next logical entry, now on a surface the validator actually enforces, so every P2.1b F6 field-add is additive against a real enforcement baseline rather than compounding a silent gap. r9 touches: frontmatter (revision, supersedes), §9 intro + move 1, §9.2 table + new P2.1a.1 closure block, new §9.3 R-P2-VALIDATOR-F6 discovery section, and this footer paragraph + the *Filed on:* metadata line. §§ 0, 1.2, 1.3, 2, 3.1–3.3, 4, 5, 6, 7, 8, 9.1 are all unchanged in r9. r9 introduces no new framing, no new decision token, no new risk, and no change to any of SG-1 through SG-4 or the four-agent contract; the build surface, the layer hierarchy, the authoring order, and the architecture's core composition commitments all stand as r6 / r7 / r8 recorded them. Does not revise any of the three source proposals; does not re-litigate any of the v0.7.4 P-1 through P-8 package, the v0.7.6 α track, or the v0.7.5 β track.

*Normative status (superseded by r9).* Upgrade architecture document, revision r8 (same-day successor to r7, 2026-04-22). Not a binding change to any plugin surface. Records three post-r7 state transitions on top of the r7 substrate: (i) **P2.1a sub-phase closure** — `SectionStateObject` widened 15 → 16 fields with the additive boolean `pre_mcr_deep_pass_completed` per β-P-9a safety-net semantics from `proposals/v0.7.5_phase3_refinement_loop_proposal.md §3.P-9a`; `scripts/phase_state_validate.py` extended with a soft bool-type check firing `SECTION_BAD_PRE_MCR_DEEP_PASS_TYPE` (MINOR) on non-boolean values, tolerant-absence honoured so ledgers omitting the field remain shape-valid and the MCR admission gate at `pre_phase_advance_check.py` clause (f) will treat absent-as-false when P2.6 authors that gate; `scripts/fixtures/phase_state_smoketest/{pass,block}/reviews/phase_state.json` + fixture README extended to carry the 16th field at its default `false`; `references/phase_state_schema.md` §§2, 2.1, 6.1, 8 amended in place under the clean-rewrite-policy's amendment lane (additive edit, does not re-open r7's P0.1a rewrite's scope); release-gate BLOCKER count on wip0 unchanged at 1 (confirming P2.1a introduced no regression; the sole blocker remains `scripts/version-check.py` on the Phase 5.2 version-bump axis). (ii) **P2.1 post-hoc decomposition** — §3.2's P2.1 single bullet split into four independently-verifiable sub-phases P2.1a (substrate-ledger widening + validator + smoketest, closed at r8), P2.1b (F1/F6/F4 frontmatter extensions + `artefact_frontmatter_validate.py`, open), P2.1c (`paragraph_hash_map.py`, open), P2.1d (`migrate_convergence_journal_v075.py`, open). The split mirrors the r7 P0.1/P0.1a precedent and preserves the r6 per-phase-verification discipline at a finer grain. §9.2 added to carry the sub-phase closure table and the P2.1a closure note. (iii) §9 next-session moves rewritten to reflect P2.1a closure and point at P2.1b (F1/F6/F4 frontmatter extensions + `artefact_frontmatter_validate.py` extension) as the next entry; the P2.1 substrate-pure "should close on its own session" discipline is preserved by making P2.1b likewise substrate-pure and by explicitly refusing to co-land P2.1c or P2.1d with P2.1b. The document's `status:` field flips from `READY-FOR-PHASE-2-BUILD` to `READY-FOR-PHASE-2-P2.1B` under the per-status-token convention established in the r6 footer. r8 touches: frontmatter (revision, status, supersedes), §9 intro + three moves (all three rewritten around the P2.1a/P2.1b split), new §9.2 sub-phase closure section, and this footer paragraph + the *Filed on:* metadata line. §§ 0, 1.2, 1.3, 2, 3.1–3.3, 4, 5, 6, 7, 8, 9.1 are all unchanged in r8. r8 introduces no new framing, no new decision token, no new risk, and no change to any of SG-1 through SG-4 or the four-agent contract; the build surface, the layer hierarchy, the authoring order, and the architecture's core composition commitments all stand as r6 / r7 recorded them. Does not revise any of the three source proposals; does not re-litigate any of the v0.7.4 P-1 through P-8 package, the v0.7.6 α track, or the v0.7.5 β track.

*Normative status.* Upgrade architecture document, revision r10 (same-day successor to r9, 2026-04-22). Not a binding change to any plugin surface. Records the post-r9 state transition: **P2.1b sub-phase closure** — v0.8.0 F1/F4/F6 frontmatter field-adds per §3.2 / §9 move 2, schema-doc §§3/6/7a/8, validator `FAMILY_SCHEMAS` + cross-field routing checks, F1 template, seven-file `artefact_frontmatter_smoketest/` harness, Phase 0.68 array-driven release-gate; `status:` `READY-FOR-PHASE-2-P2.1C`; next entry P2.1c (`paragraph_hash_map.py`). Release-gate BLOCKER count unchanged at 1 (Phase 5.2). r10 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table (P2.1b row CLOSED + closure block + residual note), new r10 footer. §§3.1–3.3 core Phase 2 plan unchanged. Does not revise the three source proposals.

*Normative status.* Upgrade architecture document, revision r11 (same-day successor to r10, 2026-04-22). Not a binding change to any plugin surface. Records the post-r10 state transition: **P2.1c sub-phase closure** — `scripts/paragraph_hash_map.py` with documented canonicalization; `scripts/fixtures/paragraph_hash_map_smoketest/` minimal corpus; `release-gate.sh` Phase 0.69 determinism gate + `py_compile` on the new script; §9.2 P2.1c row CLOSED; `status:` `READY-FOR-PHASE-2-P2.1D`; next entry P2.1d (`migrate_convergence_journal_v075.py`). P2.1d not co-landed. Release-gate BLOCKER count unchanged at 1 (Phase 5.2). r11 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.1c closure block + P2.1c residual, this footer. §§3.1–3.3 core Phase 2 plan unchanged.

*Normative status.* Upgrade architecture document, revision r12 (same-day successor to r11, 2026-04-22). Not a binding change to any plugin surface. Records the post-r11 state transition: **P2.1d sub-phase closure** — `scripts/migrate_convergence_journal_v075.py` (P-12 journal-only scalar→object backfill with `_v080_p12` tag); `scripts/fixtures/convergence_journal_smoketest/`; `release-gate.sh` Phase 0.695 + `py_compile`; §9.2 P2.1d row CLOSED; all four P2.1 sub-phases CLOSED; `status:` `READY-FOR-PHASE-2-P2.2`; next entry P2.2 (`PHASE_PROTOCOL.md`). P2.2 not co-landed. Release-gate BLOCKER count unchanged at 1 (Phase 5.2). r12 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.1d closure block + P2.1d residual + P2.1c residual tail, this footer.

*Normative status.* Upgrade architecture document, revision r13 (same-day successor to r12, 2026-04-22). Not a binding change to any plugin surface. Records the post-r12 state transition: **P2.2 sub-phase closure** — `unpacked/research-writing-harness-claude-v0.8.0/references/PHASE_PROTOCOL.md` rewrites per §3.2 (§3.3.0 profiles + `halo_scope` import-by-reference; §3.3.1a P-12 object + three-round `[CONVERGENCE-STABLE]`; §3.3.4 optional `paragraph_hash_map` + `convergence_metric` widening + writer contract; Ph4 `E-MCR-PRE-DEEP-PASS-REQUIRED`; §3.3.6 `legacy_scalar`; sixteen-field cross-refs); **§13 success-metric** invariant bullet aligned fifteen → sixteen fields (v0.8.0 vs v0.7.x); §9.2 P2.2 row CLOSED + closure block + P2.2 residual (`halo_scope` matrix still to pin in `DETERMINISTIC_CHECKS.md` / `SAFEGUARD_LAYER.md`); P2.1c residual narrowed; P2.1d residual wording post-`PHASE_PROTOCOL` landing; `status:` `READY-FOR-PHASE-2-P2.3`; next entry P2.3 (three `run-phase-`* SKILL rewrites). P2.3 not co-landed. WSL `release-gate.sh` after the §13 edit — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r13 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.2 row + P2.2 closure + residuals + P2.1c residual edit + P2.1d residual edit + P2.1b residual tail, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r14 (same-day successor to r13, 2026-04-22). Not a binding change to any plugin surface. Records the post-r13 state transition: **P2.3 sub-phase closure** — `skills/run-phase-3/SKILL.md`, `skills/run-phase-3-stability/SKILL.md`, and `skills/run-phase-4/SKILL.md` on wip0 rewritten per §3.2 P2.3 (`check_profile` dispatch router §4.5 in run-phase-3; P-12 journal + dual `[CONVERGENCE-STABLE]` window; `pre_mcr_deep_pass_completed` semantics; stability override + escalation; MCR fourth precondition + table column + §10 escalation); frontmatter `version: 0.8.0` on the three skills; §9.2 P2.3 row CLOSED + closure block + P2.3 residual (Planner.md executable flip remains P2.6); P2.1b / P2.1c residual tails pointed at P2.4 entry; `status:` `READY-FOR-PHASE-2-P2.4`; next entry P2.4 (`agents/evaluator.md`). P2.4 not co-landed. `skill-check.py` + WSL `release-gate.sh` — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r14 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.3 row + P2.3 closure + residual + residual-tail edits, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r15 (same-day successor to r14, 2026-04-22). Not a binding change to any plugin surface. Records the post-r14 state transition: **P2.4 sub-phase closure** — `unpacked/research-writing-harness-claude-v0.8.0/agents/evaluator.md` extended per §3.2 P2.4 (Ph3 / Ph4 v0.8.0 envelope: P-13 parallel dispatch + `parallel_dispatch_cost_multiplier`; P-9 `adversarial_register`; P-10 diff-scope + `halo_scope` + `paragraph_hash_map` + `containing_section` fallback when matrix rows absent; P-15 demoted-check advisory routing; Ph3 section F6 `check_profile`; Step 8 consolidated-report field citations; sixteen-field `phase_state_schema` read list; YAML frontmatter); §9.2 P2.4 row CLOSED + closure block + P2.4 residual (`R-Refl-RG-1` / §2g.3 / `R-Refl-RT-1` remain P2.5; P2.2 `halo_scope` matrix residual unchanged); P2.1b / P2.1c residual tails pointed at P2.5 entry; `status:` `READY-FOR-PHASE-2-P2.5`; next entry P2.5 (`agents/reflector.md`). P2.5 not co-landed. `skill-check.py` + WSL `release-gate.sh` on wip0 — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r15 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.4 row + P2.4 closure + residuals + residual-tail edits, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r16 (same-day successor to r15, 2026-04-22). Not a binding change to any plugin surface. Records the post-r15 state transition: **P2.5 sub-phase closure** — `unpacked/research-writing-harness-claude-v0.8.0/agents/reflector.md` extended per §3.2 P2.5 (Phase 2f §6.10 `R-Refl-RG-1` + `R-Refl-RT-1`; Phase 2g §2g.3 `R-Refl-DC-1`; §10d/§10e template + Phase 2.5.1 table + Phase 6 bullets; read-list + Reflector-specific rules + footer); §9.2 P2.5 row CLOSED + P2.5 closure + P2.5 residual (optional schema prose for `R-Refl-DC-1`); P2.4 residual superseded note; P2.1b / P2.1c residual tails pointed at P2.6 entry; `status:` `READY-FOR-PHASE-2-P2.6`; next entry P2.6 (`agents/planner.md`). P2.6 not co-landed. `skill-check.py` + WSL `release-gate.sh` on wip0 — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r16 touches: frontmatter (revision, status, supersedes), §9 intro + three moves, §9.2 table + P2.5 row + P2.5 closure + residuals + superseded P2.4 residual + residual-tail edits, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r17 (same-day successor to r16, 2026-04-22). Not a binding change to any plugin surface beyond the documented prompt edits on wip0. Records the post-r16 state transition: **P2.6 sub-phase closure** — `unpacked/research-writing-harness-claude-v0.8.0/agents/planner.md` extended per §3.2 P2.6 (Phase 0.6 F6 §7a.2 quartet + P-16; Phase 5.5 `pre_mcr_deep_pass_completed` + P-8 `menu_items_presented` / `/ph3-terminate`; Phase 6 β-P-9a MCR list; sixteen-field contract); §9 intro + three moves rewritten for P2.7 entry; §9.2 table `Status at r17` + P2.6 row + amended P2.1a residual (i)(ii)(iii) + P2.3 residual narrowed + **P2.6 closure** + **P2.6 residual**; `status:` `READY-FOR-PHASE-2-P2.7`; P2.7 (`AGENT_CONTRACTS.md`) not co-landed. `skill-check.py` + WSL `release-gate.sh` — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r17 touches: frontmatter (revision, status, supersedes), §9, §9.2 P2.1a residual paragraph, P2.3 residual line, new P2.6 closure block, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r19 (same-day successor to r18, 2026-04-22). Not a binding change to any plugin surface beyond proposal-table prose in-repo. Records the post-r18 state transition: **P2.8 sub-phase closure (A-GK-4 / D-6)** — user directive **Default continue**; §3.P-9.1 disposition column populated in `proposals/v0.7.5_phase3_refinement_loop_proposal.md`; **D-6** closed in frontmatter; `open_decisions: []`; `status:` `**READY-FOR-PHASE-3-BUILD`**; §9 intro + three moves repointed at **Phase 3**; §9.2 table `Status at r19` + **P2.8** row + **P2.8 closure** + **P2.8 residual** + P2.1a residual (iii) discharge + P2.1a.1 residual (i) amendment. `skill-check.py` + WSL `release-gate.sh` — release-gate BLOCKER count unchanged at 1 (Phase 5.2). r19 touches: frontmatter, §9, §9.2, *Related* supersession line, this footer + *Filed on* metadata.

*Normative status.* Upgrade architecture document, revision r20 (same-day successor to r19, 2026-04-22). Not a binding change beyond wip0 `agents/evaluator.md` prompt prose + §3.2 P3.1 / §9 / §9.2 documentation in this file. Records the post-r19 state transition: **P3.1 partial (α-C1 pointer demotion)** — Ph-native pointer table + `PHASE_PROTOCOL.md` §7 gate pointer + read-list cross-ref fix on `unpacked/research-writing-harness-claude-v0.8.0/agents/evaluator.md`; §3.2 P3.1 bullet amended to forbid raw Tier-diff apply on wip0; §9 `*Updated in r20.`* lead + three moves rewritten for **P3.1 verification** carry-forward; §9.2 column `Status at r20` + **P3.1** table row **IN PROGRESS** + **P3.1 partial land** closure block; `status:` unchanged `**READY-FOR-PHASE-3-BUILD`**. `skill-check.py` on wip0 — **0 blockers**; full `release-gate.sh` not re-run this session. r20 touches: frontmatter (revision, supersedes), §3.2 P3.1 bullet, §9, §9.2, *Related*, this footer + *Filed on*.

*Normative status.* Upgrade architecture document, revision r21 (successor to r20, 2026-04-23). **No change to v0.8.0 ship scope or non-goals** beyond documentation: adds **§7.1** — cross-reference to `proposals/check8_subcheck_g_consolidation_anchors_proposal.md` (Check 8 Sub-check G / consolidation anchors; **deferred** — would require `PHASE_PROTOCOL.md` §3.3.3 edits, conflicting with §7 until a follow-on release or explicit non-goal lift). r21 touches: frontmatter (revision, supersedes), new §7.1, this footer + *Related* + *Filed on*.

*Related.* Revisions r1 through r19 of this document are superseded under r20; r21 supersedes r20 for §7.1 and metadata only.

*File path:* `research-writing-harness/proposals/v0.8.0_upgrade_architecture.md`
*Filed on:* 2026-04-23 (revision r21 amends 2026-04-22 r20; r19 and earlier same-day chain as prior filings)
*Filed by:* harness_session_agent under user direction (Joseph Chung)